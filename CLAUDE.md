# שכר לימוד — CLAUDE.md

עודכן לאחרונה: סבב 30 · 2026-08-14

## סביבת עבודה
- **ריפו:** `ygtotlrl-lab/schar-limud`
- **Pages:** `https://ygtotlrl-lab.github.io/schar-limud/`
- **קובץ ראשי:** `index.html`
- **Supabase:** `kxbtskqobynewvnckaaz`
- **אופן העבודה (נכון ליולי 2026):** העבודה מתנהלת בסשני ענן (Claude Code cloud) — הריפו משוכפל טרי בתחילת כל סשן, העבודה נעשית על ענף ייעודי, והדחיפה בסוף הסשן. **אין עותקים מקומיים קבועים.**
- **עבודה מקומית במחשב (אם בכל זאת):** שכפול **אך ורק** לתיקייה יציבה כמו `C:\Users\F\Documents\repos\` — **לעולם לא ל-Temp** (מנקה Windows / Storage Sense מוחק משם קבצים באמצע עבודה; זה גרם ל"מחיקות רפאים" חוזרות). טוקן: מאוחסן ב-Windows Credential Manager (host `github.com`) — לעולם לא בקובץ; `git push`/`clone` מושכים אותו אוטומטית דרך GCM.

## גישת Supabase
כשזמין ה-Supabase MCP, נהג לפי הכללים הבאים — ללא יוצאים מן הכלל:
- **שינויי סכימה** (יצירה/שינוי/מחיקת טבלאות, עמודות, פוליסות, הרשאות) — **אך ורק דרך `apply_migration`** עם שם ברור ותיאורי (למשל `add_sl_transactions_soft_delete`). לא דרך `execute_sql`.
- **שאילתות אבחון וקריאה** (SELECT, בדיקת מבנה, ספירות, `list_tables` וכו') — **חופשיות**, ללא אישור.
- **עדכון או מחיקת נתונים בטבלאות `sl_*`** (`sl_students`, `sl_transactions`, `sl_settings`, `sl_lists`, `sl_users`) — **מחייבים אישור מפורש מהמשתמש לפני הרצה**. אין להריץ `UPDATE`/`DELETE`/`upsert` על נתונים בלי אישור. (בפרט: `sl_transactions` הוא כספים — מחיקה היא soft-delete בלבד, `deleted=true`.)

<!-- SHARED:start id="branch-rules" -->
## כללי ענפים
- כל עבודה נעשית על **ענף ייעודי** — לא ישירות על `main`.
- **מיזוג ל-`main` רק לאחר אישור מפורש מהמשתמש**, אחרי שבדק את השינוי בבדיקה חיצונית.
- **מחיקת ענפים מרוחקים חסומה בסביבת הענן** — אין לנסות למחוק; לדלג ולדווח למשתמש שהענף נותר.
- **ענפים ממוזגים נמחקים אוטומטית אחרי מיזוג** (`.github/workflows/cleanup-merged-branches.yml`, רץ ב-push ל-`main`); ההגנה — prefix `claude/` + בדיקת `merge-base --is-ancestor`.

### ⚠️ שני כללי תפעול (סבב 21) — שניהם נלמדו מכשל אמיתי בסבב 20

**1. לפני כל מיזוג — מרעננים את `main` המקומי, ורק אז ממזגים.**
```bash
git -C <path> fetch origin
git -C <path> checkout main
git -C <path> merge --ff-only origin/main   # חייב להצליח
git -C <path> merge <ענף-העבודה>
```
⛔ **אין למזג מעל `main` שאינו זהה ל-`origin/main`.** סשן ענן מחזיק עותק
שנוצר ברגע פתיחתו והוא **קופא שם**: כל מה שנדחף ל-`origin/main` מאז — מסבב
מקביל, מסשן אחר, או מהמשתמש — פשוט אינו קיים בעותק הזה. בסבב 20 נמצא
`main` מקומי שפיגר **בשני סבבים שלמים**. `--ff-only` הוא מה שהופך את זה
לגלוי: הוא **נכשל** במקום ליצור קומיט מיזוג שקט שמחזיר את הקוד אחורה.

**2. בעבודה רב-ריפו — כל פקודת git מציינת את הריפו במפורש: `git -C <path> …`.**
בסבב 20 פקודת מיזוג רצה בתיקייה של ריפו **אחר**, בגלל משתנה לולאה שדלף
מבלוק קודם. שם היא הייתה no-op **במקרה** — הענף לא היה קיים באותו ריפו.
⚠️ **וזה בדיוק מה שלא ניתן לסמוך עליו כאן:** שם ענף העבודה **זהה בארבעת
הריפו** (אותה תבנית `claude/<נושא>-<סיומת>`), ולכן פקודה שרצה בתיקייה
הלא-נכונה תמצא שם ענף תקף ותמזג אותו — **מיזוג שקט בריפו הלא נכון**, בלי
שגיאה ובלי סימן. ⛔ **אין להסתמך על `cd` ואין להסתמך על תיקיית העבודה
הנוכחית** — הנתיב נכתב בפקודה עצמה.
<!-- SHARED:end -->

## לפני כל push — חובה: בדיקת תחביר עם node
**כל שינוי בקוד (`index.html` או `sw.js`) חייב לעבור את הבדיקה הזו לפני דחיפה. דחיפה ללא הבדיקה — אסורה.**
בדיקת איזון-סוגריים בלבד אינה מספיקה (עיוורת לשגיאות בתוך מחרוזות). `node --check` = parse מלא אמיתי ב-V8. (node זמין בסביבת הענן; הנוהל הישן עם Chrome headless היה נכון לסביבה המקומית שבה node לא היה מותקן.)
```bash
# 1) חלץ את כל ה-JS המוטבע מ-index.html לקובץ
python3 -c "
import io, re
s = io.open('index.html', encoding='utf-8').read()
js = chr(10).join(re.findall(r'<script(?![^>]*src)[^>]*>(.*?)</script>', s, re.DOTALL))
io.open('_app.js', 'w', encoding='utf-8').write(js)
"
# 2) בדיקת parse — חובה ששתי הפקודות יעברו בלי שגיאה
node --check _app.js
node --check sw.js
rm -f _app.js
# 3) אחידות "אזור מצב" (סבב 17, כלל ברזל 7) — קוד זהה + מיקום זהה + חיווט חי
node tools/check-status-area.mjs
# 4) אחידות התיעוד (סבב 18, כלל ברזל 8) — סימוני SHARED, חתימות, שורת העדכון
node tools/check-docs.mjs
# 5) תקן ההערות בקוד (סבב 28, כלל ברזל 11) — כותרות בלוק, ⛔ עם סבב וסיבה,
#    שלושה סימנים בלבד וטרמינולוגיה אחידה
node tools/check-comments.mjs
# 6) אחידות היכולות המשותפות (סבב 30 השלמה, כלל ברזל 12) — ליבה זהה,
#    נקודת הפעלה זהה, ורישום תואם במטריצת היכולות
node tools/check-capabilities.mjs
# 7) בדיקות הסבבים — כלל ברזל 8 סעיף 6 (בדיקות נשמרות כקובץ ורצות שוב)
node tools/test_round24.mjs    # כניסה אופליין + `password` שאינו נוגע בדיסק
node tools/test_round26.mjs    # הרשאות לפי תפקיד + ניקוי שער הסיסמה
node tools/test_round30_stage_a.mjs   # גיבוי יומי ויומן פעולות (מודול משותף)
```
אם `node --check` מדווח שגיאה — אסור לדחוף עד שהיא מתוקנת.
⚠️ **אין יותר `sync-setup-sql.py`** — הוא הוסר בהשלמת סבב 24 יחד עם העותק
המוטבע של הסכימה. אין מה לסנכרן: הסכימה יושבת ב-`migrations/000_initial_schema.sql`
בלבד, ומסך ההגדרה מושך אותה ב-`fetch`. ⛔ אין להחזיר עותק מוטבע.
אם `check-status-area.mjs` נכשל — ר' הפרק «כלל ברזל 7» למטה. שינוי בבלוק
משותף מחייב עדכון בארבע האפליקציות ובארבעת עותקי קובץ הבדיקה, באותו סבב.
אם `check-docs.mjs` נכשל — ר' הפרק «כלל ברזל 8» למטה. אותו כלל חל על פרק
תיעוד מסומן `SHARED`: משנים בארבעתם ובארבעת עותקי הבדיקה, באותו סבב.
אם `check-comments.mjs` נכשל — ר' הפרק «כלל ברזל 11» למטה. ⛔ התקן חל על
הקוד הפרטי בלבד; הבלוקים המשותפים והמודולים הקפואים מוחרגים ממנו במפורש.
אם `check-capabilities.mjs` נכשל — ר' הפרק «כלל ברזל 12» למטה. ⛔ נקודת
ההפעלה של יכולות העלייה היא **פונקציית העלייה של האפליקציה** — זו שקוראת
גם ל-`lsBoot()` וגם ל-`pendBoot()` — ואין להזיז אותה למסלול אחר.

## עדכוני Service Worker
- **כל שינוי בקוד מחייב קידום `CACHE_NAME` ב-`sw.js` לגרסה הבאה** — בלי זה המשתמשים לא יקבלו את העדכון.
- מנגנון באנר "גרסה חדשה זמינה" קיים באפליקציה — המשתמשים מקבלים את העדכון בלחיצה על הבאנר.

## Push
```bash
git add . && git commit -m "תיאור השינוי"
git push -u origin <שם-הענף>   # דחיפה לענף העבודה — לא ל-main
```

## סיום משימה
בסיום כל משימה משמעותית — **עדכן קובץ זה בתמצית** לפני סיום הסשן: מה שונה, מה הוחלט. כך הסשן הבא מתחיל עם תמונת מצב עדכנית.

## כללים קריטיים
1. **בדיקת תחביר עם `node --check` לפני כל push** (הסעיף למעלה) — חובה מוחלטת. כל שינוי ב-`index.html` חייב לעבור חילוץ-JS + `node --check` (וגם `sw.js`). דחיפה בלי זה — אסורה.
2. **קידום `CACHE_NAME` ב-`sw.js` בכל שינוי קוד** (הסעיף למעלה) — בלי זה העדכון לא מגיע למשתמשים.
3. Supabase: כל טבלה חדשה — GRANT מפורש ל-anon, authenticated, service_role + RLS
4. **מקור אמת יחיד = `index.html`** — זה הקובץ ש-Pages מגיש ושאליו מצביע `start_url` במניפסט. כל עדכון קוד נכנס לכאן בלבד. אסור ליצור קבצי HTML כפולים של האפליקציה. (אין כאן מנגנון אוטו-אפדייט פנימי — בניגוד ל-yoman-avoda. אין יותר sl_p1/p2/p3.txt + gen.py — index.html נערך ישירות. הרענון הוא דרך ה-Service Worker.)
5. **`sl_transactions` = כספים** — מחיקה היא soft-delete (`deleted=true`+`deleted_at`+`deleted_by`), לעולם לא מחיקה פיזית. כל שליפה/סיכום מסננת `deleted=false`. הוספה ממלאת `created_by`.
6. **`sl_students` = גם soft-delete** (יולי 2026, `migrations/002`) — `deleteStudent` הריץ `DELETE` פיזי, ועל `sl_transactions` מוגדר `ON DELETE CASCADE`: מחיקת תלמיד השמידה פיזית את **כל היסטוריית הכספים שלו**, בעקיפה של כלל 5. מעכשיו התלמיד מסומן `deleted=true` והתנועות נשארות. הסינון נעשה בנקודה אחת — ב-`syncAll`, בדיוק כמו `TRANSACTIONS` — ולכן כל 11 הקוראים של `STUDENTS` מקבלים אותו אוטומטית. **אין להחזיר `.delete()` על אף אחת מהטבלאות האלה.**
7. **מקור אמת יחיד לסכימה = `migrations/000_initial_schema.sql`** (יולי 2026, סבב 6; העותק המוטבע הוסר בהשלמת סבב 24). `supabase-setup.sql` הוא קובץ הפניה בלבד, ומסך ההגדרה ב-`index.html` **מושך** את קובץ המיגרציה ב-`fetch` ומציג אותו. **⛔ אין סכימה מוטבעת ב-`index.html`, ואין להחזיר אותה** — כשל משיכה מציג הודעה עם קישור ישיר לקובץ ב-GitHub. **אין להחזיר סכימה כתובה-ביד לאף אחד מהשניים.**
8. **בריחת HTML** — כל ערך שמקורו בנתוני משתמש ונכנס ל-`innerHTML` חייב לעבור `esc()` (סבב 6).
9. **גרסאות CDN נעוצות במדויק** — אין `@2`/`@4` צפים (סבב 6).
10. **`client_id` — מפתח זהות שנוצר במכשיר** (אוגוסט 2026, `migrations/006`).
    `sl_transactions.id` ו-`sl_students.id` הם SERIAL, כלומר המסד מקצה אותם.
    **כל רשומה חדשה של תנועה או תלמיד חייבת לקבל `client_id`** מ-`newClientId()`,
    והכתיבה לענן היא **`upsert` עם `{onConflict:'client_id'}` — לא `insert`**.
    זה התנאי לעבודה אופליין בטוחה. **אין להחזיר `.insert()` על שתי הטבלאות האלה.**

## הגדרת Supabase — פעם ראשונה בלבד
הרץ **`migrations/000_initial_schema.sql`** ב-Supabase SQL Editor:
https://supabase.com/dashboard/project/kxbtskqobynewvnckaaz/sql
(אידמפוטנטי, כולל כבר את 001–009. `supabase-setup.sql` הוא קובץ הפניה בלבד.)

### מצב המיגרציות במסד הייצור (אוגוסט 2026)

| | מה היא עושה | מצב |
|---|---|---|
| 001 | soft-delete ל-`sl_transactions` | ✅ הורצה ואומתה |
| 002 | soft-delete ל-`sl_students` | ✅ הורצה ואומתה |
| 003 | `ON DELETE RESTRICT` על `sl_transactions` | ✅ הורצה ואומתה |
| 004 | `start_month`/`end_month`, הסרת `handled_months` | ✅ הורצה ואומתה |
| 005 | זריעת סעיף «זוכה על חשבון יתרת זכות» | ⏸️ **לא תורץ בכוונה** — האפליקציה זורעת בעצמה |
| 006 | `client_id` בשתי טבלאות הליבה | ✅ הורצה |
| 007 | החלפת האינדקסים החלקיים באינדקסים **מלאים** | ✅ **הורצה ואומתה** |
| 008 | `updated_at` + טריגר בשתי טבלאות הליבה | ✅ הורצה ואומתה |
| 009 | `updated_at`+`client_id`+tombstones ל-`sl_settings`/`sl_lists` | ✅ הורצה ואומתה |
| 010 | `pass_salt`/`pass_fp` ל-`sl_users` — כניסה אופליין | ✅ **הורצה ואומתה** (סבב 26) |
| 011 | `role` ל-`sl_users` — הרשאות | ✅ **הורצה ואומתה** (סבב 26, השלמה) |
| 012 | צמצום `anon`/`authenticated` — `REVOKE DELETE/TRUNCATE` | ✅ **הורצה ואומתה** (סבב 29) |

**007 — אומתה במסד:** קיימים בדיוק שני אינדקסים ייחודיים על `client_id`,
`sl_transactions_client_id_key` ו-`sl_students_client_id_key`, **שניהם מלאים
ואפס חלקיים** (אין `WHERE` ב-`indexdef`, ו-`_uidx` נמחקו). כלומר `upsert` על
`client_id` עובד, ושמירת תשלום ותלמיד חדש תקינה.
(תיעוד קודם כאן אמר ש-007 «מחייבת וממתינה להרצה» — זה כבר לא נכון והוסר.)

**009 — אומתה במסד** (אוגוסט 2026): `updated_at` והטריגרים קיימים בשתי
טבלאות ההגדרות, `client_id` קיימת עם **ארבעה** אינדקסים ייחודיים מלאים
ואפס חלקיים, ה-tombstones קיימות ב-`sl_lists`, ו-`EXPLAIN` על ה-UPSERT
מתכנן עם הבורר הנכון. הנתונים שלמים. ר' הפרק «סבב 13» למטה.

**010 — ✅ הורצה ואומתה** (אומת מול המסד בסבב 26, לא הוצהר): שתי העמודות
קיימות ב-`sl_users`, שתיהן `text`; **`password` שלם ולא נגע** (6 תווים,
כפי שהיה); ולמשתמש הקיים **נוצרה טביעה בפועל** — `pass_salt` מלא
ו-`pass_fp` באורך 64 תווים hex, כלומר PBKDF2 רץ והכניסה האופליין פעילה.
(תיעוד קודם כאן אמר «⏳ ממתינה להרצה» ו«כניסה אופליין לא תעבוד» — שניהם
כבר לא נכונים והוסרו.)

**011 — ✅ הורצה ואומתה** (אומת מול המסד בהשלמת סבב 26, לא הוצהר):
`role` ל-`sl_users` — `text`, `NOT NULL`, **`column_default = NULL`**,
והמשתמש היחיד מחזיק `'admin'`. ⛔ אינה נוגעת ב-`password`, ב-`pass_fp`
ובשום נתון קיים פרט לשורות שקדמו לעמודה (`UPDATE … WHERE role IS NULL`).
⚠️ **אין `DEFAULT` כלל** — הניסוח הראשון היה `DEFAULT 'admin'`, והוא
הוסר בהשלמה: ברירת מחדל ש**מעניקה** הרשאה היא בדיוק הכשל שסבב 26 בא
לסגור. **יצירת משתמש מחייבת `role` מפורש** (`'admin'` או `'user'`),
ו-INSERT בלעדיו נכשל במסד. ר' פרק «סבב 26 — השלמה» למטה.
(תיעוד קודם כאן אמר «⏳ ממתינה להרצה» ו«עד שתורץ מסך ההגדרות אינו נפתח
לאיש» — שניהם כבר לא נכונים והוסרו. מסלול ה-`'no-role'` בקוד **נשאר**
כרשת ביטחון להתקנה שטרם שודרגה.)
**005 היא היחידה שלא תורץ, ובכוונה.**

טבלאות: sl_users, sl_students, sl_transactions, sl_settings, sl_lists

⛔ **אין משתמש ברירת מחדל, ואין ליצור אחד** (סבב 24). עד אז זרעה הסכימה
`admin`/`admin`, כלומר כל התקנה טרייה נולדה עם חשבון שסיסמתו ידועה לכל
העולם, על מסד שה-RLS שלו `USING (true)` וכתובתו בקוד המקור. המשתמש הראשון
נוצר ידנית ב-SQL Editor — **מסבב 26 עם `role` במפורש**:
`INSERT INTO public.sl_users (username, password, role) VALUES ('<שם>', '<שש ספרות>', 'admin');`
והאפליקציה מציגה את ההנחיה מעצמה כשהטבלה ריקה.
⚠️ **`sl_settings.admin_pass` — בוטלה בסבב 26.** היא הייתה סיסמת **שער
מסך ההגדרות**, שכבת הרשאה שנייה שהתקיימה כאן ולא בשלוש האחיות. ההרשאה
נגזרת עכשיו מ-`sl_users.role`, הזריעה הוסרה מהסכימה, ו⛔ **אין להחזיר
אותה** — ר' פרק סבב 26.

## חתימת APK — מפתח קבוע (לעולם לא משתנה!)

| | |
|---|---|
| **קובץ** | `signing/schar.keystore` (PKCS12, RSA 2048) |
| **alias** | `schar` |
| **storepass** | `schar123` |
| **keypass** | `schar123` (זהה ל-storepass) |
| **תוקף** | 10,000 יום — 07.08.2026 עד 23.12.2053 |
| **SHA256** | `29:32:D9:B5:94:69:D4:E4:53:EF:C7:EE:3B:10:55:C9:CE:4B:EE:D6:9B:BB:78:EC:EE:18:BD:C6:BE:2D:0F:87` |
| **SHA1** | `F5:BD:6A:6E:BE:EF:B5:85:78:9F:70:B1:19:60:8F:1B:DE:90:1B:D4` |
| **DN** | `CN=schar, OU=Yeshiva, O=Yeshiva, L=Rishon LeZion, ST=Israel, C=IL` |

### ⛔ אזהרה — אין להחליף את המפתח לעולם

אנדרואיד מזהה אפליקציה מותקנת לפי **חתימת המפתח**, לא לפי שם הקובץ או מספר הגרסה.
APK שנחתם במפתח אחר נחשב אפליקציה **זרה**, וההתקנה מעל הקיימת נכשלת בשגיאת
`INSTALL_FAILED_UPDATE_INCOMPATIBLE`.

**המשמעות המעשית של החלפת מפתח:** כל משתמש שכבר התקין יצטרך **להסיר את האפליקציה
ולהתקין מחדש** — ואין דרך לעקוף את זה. אין שחזור, אין מיגרציה, אין "חתימה מחדש
במפתח הישן" אחרי שהוא אבד.

לכן:
1. **לעולם לא להריץ `keytool -genkeypair` שוב** עבור הפרויקט הזה. הריצה הראשונה
   כבר בוצעה, והקובץ בריפו הוא התוצאה שלה.
2. **לעולם לא למחוק, לדרוס או "לרענן" את `signing/schar.keystore`.** הוא חלק מהריפו
   בדיוק כדי שלא יאבד — כמו שקרה ב-yoman-avoda כשהמפתח ישב ב-`/tmp`.
3. **כל APK חדש נחתם אך ורק במפתח הזה**, גם אם נבנה בכלי אחר (PWABuilder, Bubblewrap,
   Android Studio).
4. אחרי חתימה — לאמת שה-SHA256 תואם לטבלה למעלה.

### חתימה

```bash
./signing/sign-apk.sh app-unsigned.apk schar.apk
```

הסקריפט מריץ `zipalign` ואז `apksigner`, ובסוף `apksigner verify --print-certs`.
דורש Android build-tools ב-PATH.

חלופה ידנית (apksigner):
```bash
apksigner sign --ks signing/schar.keystore --ks-key-alias schar \
  --ks-pass pass:schar123 --key-pass pass:schar123 app.apk
```

חלופה ידנית (jarsigner, אם אין apksigner):
```bash
jarsigner -keystore signing/schar.keystore -storepass schar123 -keypass schar123 app.apk schar
```

אימות טביעת האצבע:
```bash
keytool -list -v -keystore signing/schar.keystore -storepass schar123
```

## APK — מעטפת WebView מקורית שטוענת מהרשת (⛔ לא TWA) — אוגוסט 2026
שכר לימוד עברה לדפוס המעטפת-מהרשת של yoman-avoda (סבב 13 שם): תיקיית
**`android/`** מכילה מעטפת WebView מקורית שטוענת את
`https://ygtotlrl-lab.github.io/schar-limud/` **מהרשת** — לא מנכסים מוטבעים.
פרטים מלאים ב-`android/README.md`. עיקרי הדברים:

- **לעולם לא TWA ולא PWABuilder** — TWA מריץ את האתר בתוך כרום, וסינון התוכן
  במכשירי המשתמשים חוסם את כרום (נמדד בפועל ב-gius). WebView מרנדר בתוך
  התהליך ולא נחסם.
- **package `com.schar.limud`, versionCode 1** — המעטפת הראשונה כאן, טוענת
  מהרשת מהיום הראשון (בניגוד ליומן, לא היה כאן שלב `file://`).
  `usesCleartextTraffic=false`, minSdk 21 / targetSdk 34.
- ⛔ **אין נכסים מוטבעים, ואין להוסיף** — עותק `file://` הוא origin אחסון
  נפרד: תשלום שנרשם אליו אופליין לא היה נראה לאפליקציה האמיתית לעולם, וזה
  כסף. בנוסף הוא מקור אמת שני (כלל קריטי 4). ה-service worker + שכבת
  ה-MIRROR נותנים אופליין אחרי עלייה מוצלחת אחת.
- ⛔ **אין גשר שיתוף — וזה ההבדל היחיד מהתבנית של יומן.** בקוד של שכר לימוד
  אין `navigator.share`, ולכן `AndroidShareBridge` הושמט כליל — Java, manifest
  (אין FileProvider/queries) ותלויות (אין androidx). אם אי-פעם יידרש גשר —
  מעתיקים את הדפוס הכפול-נעילה של יומן; לעולם לא `addJavascriptInterface` חשוף
  על דף שנטען מהרשת.
- **`http`/`https` נשארים בתוך המעטפת** (מסירה לדפדפן = כרום = חסום);
  `tel:`/`mailto:`/`whatsapp:` וכו' נמסרים למערכת.
- **עדכוני קוד web לא מצריכים APK חדש** — מגיעים דרך ה-service worker ובאנר
  העדכון, כמו בדפדפן. APK חדש נדרש רק לשינוי במעטפת עצמה.
- **צנרת בנייה:** `.github/workflows/build-apk.yml` — Actions → **Build Signed
  APK** → Run workflow (temurin 17, בנייה, חתימה עם `signing/schar.keystore`
  הקבוע alias `schar`, אימות `apksigner verify`). ה-artifact:
  **`schar-limud-signed-apk`**.

### ⚠️ אזהרת מעבר-origin חד-פעמי (אותו עיקרון כמו ביומן)
ה-WebView של ה-APK מחזיק **מחיצת אחסון נפרדת** מזו של הדפדפן באותו מכשיר.
מי שעבד בדפדפן ועובר ל-APK מתחיל עם localStorage ריק — כניסה מחדש, וה-MIRROR
נטען מהענן (מקור האמת). ⛔ **מה שכן יכול ללכת לאיבוד: רשומה שסומנה ⏳ בדפדפן
וטרם עלתה לענן.** לכן לפני מעבר מכשיר ל-APK — לוודא בדפדפן שההגדרות ←
«⏳ ממתין לסנכרון» מציג **0**.

## סבב תיקונים 6 (יולי 2026, `sw.js` v6)
- **סכימת SQL — מקור אמת יחיד.** הסכימה ישבה בשלושה עותקים לא מסונכרנים (ה-SQL המוטבע
  במסך ההגדרה, `supabase-setup.sql`, ו-`migrations/`), ושניים מהם התיישנו בשקט: חסרו בהם
  `deleted`/`deleted_at`/`deleted_by`/`created_by`, ולכן **התקנה טרייה לפי מסך האפליקציה
  לא יכלה לשמור או למחוק תשלום**. מעכשיו:
  - **`migrations/000_initial_schema.sql` = מקור האמת** (כל 5 הטבלאות + GRANT + RLS +
    אינדקסים + seed, כולל עמודות ה-soft-delete). 001/002 נשארות כמיגרציות שדרוג.
  - `supabase-setup.sql` = **קובץ הפניה בלבד**, בלי סכימה. אין להחזיר אליו סכימה.
  - `index.html` **מושך** את קובץ המיגרציה ב-`fetch` (`SETUP_SQL_URL`) ומציג אותו;
    `SETUP_SQL_FALLBACK` הוא עותק **מיוצר אוטומטית** לשעת כשל fetch בלבד, ומסומן
    באזהרה בראשו. **אין לערוך אותו ביד.**
  - **`python3 tools/sync-setup-sql.py --check` — בדיקת חובה לפני push**, לצד
    `node --check`. שינית סכימה? הרץ `python3 tools/sync-setup-sql.py` ואז קומיט.
- **בריחת HTML — `esc()`.** לא הייתה בריחה בכלל: שמות תלמידים, הערות וערכי רשימות נכנסו
  ל-`innerHTML` גולמיים. **כל הזרקה של ערך מנתוני משתמש ל-HTML חייבת לעבור דרך `esc()`.**
  `sdRenderList` קיבל טיפול מיוחד: ה-label עבר דרך `replace(/'/g,"\\'")` לתוך מחרוזת JS
  בתוך מאפיין `onmousedown` — הבנייה שנשברת מכל מרכאה כפולה. במקומה `data-k`/`data-id`/
  `data-label` + `sdSelectEl(this)`. **אין להחזיר קוד JS מורכב לתוך מאפייני HTML.**
- **`syncAll` בודק שגיאות באמת.** `supabase-js` לא זורק, ולכן ה-`catch` היה קוד מת וכשל
  השאיר נתונים ישנים על המסך בלי משוב. עכשיו `rs[i].error` נבדק פר-קטגוריה, רק תוצאות
  תקינות נכתבות (עדיף ישן מרוקן), והמשתמש מקבל טוסט **פעם אחת** (`_syncWarned`) + טוסט
  התאוששות.
- **`_syncBusy` נגד חפיפה** — `setInterval(syncAll,3000)` הערים קריאות ברשת איטית.
  **חובה לשחרר ב-`finally`** ולא בסוף ה-`try`: דגל שנתקע דלוק מקפיא את הסנכרון לתמיד
  (הבאג של `_ysSyncing` ב-hanhala).
- **דגל הגיבוי היומי נכתב אחרי הצלחה** — `sl_last_backup` נכתב ראשון, ולכן גיבוי שנכשל
  דילג בשקט על יממה. נוסף `_slBackupRunning` כנעילת ריצה במקום הכתיבה-מראש, ובדיקת
  `error` גם על ה-`insert` ל-`kv_backup`. **אין להחזיר את הכתיבה-מראש.**
- **גרסאות CDN נעוצות** — supabase-js (מאז סבב 8: `@2.111.0/dist/umd/supabase.js`, זהה
  לשלושת הפרויקטים) ו-`chart.js@4.4.1`. **אין לחזור ל-`@2`/`@4` צפים**: שחרור בצד הספק שובר את
  האפליקציה בלי שינוי קוד, וזה בלתי ניתן לשחזור מהריפו.

## סבב תיקונים 7 (יולי 2026, `sw.js` v7)
- **`isSupabaseRequest()` ב-`sw.js`** (הפריט שנותר פתוח מסבב 6) — ה-fetch handler שמר
  במטמון **כל** תגובת GET, כולל קריאות PostgREST. עקיפת אימות לא הייתה כאן (ההתאמה
  לפי URL מלא, בלי `ignoreSearch`), אבל **נתוני כספים ישנים הוגשו כטריים** אחרי ניתוק:
  יתרה שכבר שולמה, תנועה שנמחקה. מעכשיו כל בקשה ל-`.supabase.co` **לא עוברת דרך ה-SW
  בכלל** — לא מיוירטת, לא נשמרת, לא מוגשת. באופליין היא נכשלת באמת, וזה הרצוי: `syncAll`
  מזהה `error` ומציג טוסט. **אין להחזיר תגובות API למטמון.** (זהה ל-hanhala v17.)
- **`ON DELETE RESTRICT` על `sl_transactions`** — הבלוק המוער ב-002 הופעל, כמיגרציה
  משלו: **`migrations/003_transactions_fk_restrict.sql`**. הקוד כבר לא מוחק פיזית
  (כלל 6), אבל `DELETE` ידני אחד מלוח הבקרה של Supabase עדיין השמיד את כל היסטוריית
  הכספים של התלמיד דרך ה-CASCADE. עכשיו המסד מסרב. המיגרציה בנויה בשלושה שלבים:
  בדיקת יתומים → החלפה ב-`DO` block (שבודק שוב, ומדלג אם האילוץ כבר RESTRICT) → אימות.
  שים לב ל-`student_id IS NOT NULL` בבדיקת היתומים — בלעדיו כל תנועה בלי תלמיד נספרת
  כיתומה. **`000_initial_schema.sql` עודכן גם הוא** (כלל 7 — מקור אמת יחיד): ה-FK הוא
  `RESTRICT`, ונוסף `DO` block שמתכנס גם בהתקנה קיימת אך **מדלג בשקט** אם יש יתומים,
  כדי שהרצה חוזרת של 000 לא תיכשל לעולם.
- ✅ **המיגרציה הורצה ואומתה במסד** (אוגוסט 2026): ה-FK `sl_transactions_student_id_fkey`
  הוא `ON DELETE RESTRICT`. מחיקה פיזית של תלמיד תיכשל ברמת המסד.

## חודש הצטרפות / עזיבה לתלמיד (אוגוסט 2026, `sw.js` v10, `migrations/004`)
עד כה כל תלמיד פעיל חויב אוטומטית ב-12 חודשי השנה, ולכן מי שהצטרף באמצע השנה הוצג
כחייב גם על חודשים שלא היה בהם. נוספו ל-`sl_students` שתי עמודות טקסט בפורמט `YYYY-MM`:
- **`start_month`** — החודש הראשון שבו התלמיד פעיל. **ריק = כל השנה** (התנהגות שלפני 004),
  כדי לא לשבור נתונים קיימים.
- **`end_month`** — החודש האחרון שבו היה פעיל. ריק = עדיין פעיל. (מי שהוגדר לו רק
  `end_month` עדיין נחסם אחרי העזיבה — היעדר `start_month` מסיר את הגבול התחתון בלבד.)

**גבולות שנת הלימודים לא נגעו:** `YEAR_MONTHS` ו-`acadYearOf` נשארו ספטמבר–אוגוסט.
הטווח כאן הוא חודשי לוח גרגוריאני, וההשוואה **לקסיקוגרפית** — `YYYY-MM` ממוין
כרונולוגית כמחרוזת, ולכן אין המרה לתאריך. הגשר בין השניים הוא `monthKey(y,m)`
(היפוך `acadYearOf`: אינדקסים 8–11 שייכים ל-`y`, השאר ל-`y+1`).

- **`calcDistribution`** מסמן חודש מחוץ לטווח כ-`{req:0,paid:0,off:true}` — לא מחויב,
  לא נספר כחוב, והתשלומים **מדלגים עליו** ונפרסים על חודשי הטווח בלבד. הדגל `off`
  מבדיל בתצוגה בין "לא היה בישיבה" לבין "חודש מחויב שלא שולם".
- **דשבורד:** נוספה עמודת **"פעילים"** — מצבת התלמידים בכל חודש (`countInMonth`);
  בשורת הסיכום — כמה היו פעילים אי-פעם באותה שנה. `showDashMonth` מציג רק תלמידים
  שהיו בטווח באותו חודש.
- **UI:** בוררי `input[type=month]` בכרטיס התלמיד ובטופס ההוספה (ברירת מחדל בהוספה =
  החודש הנוכחי). האימות ב-`readMonthRange` **חובה** — פיירפוקס לא תומך ב-type=month
  ונופל ל-input טקסט חופשי; המסד מגבה בשני CHECK (פורמט + `end >= start`).
- **`handled_months` הוסרה** (מ-000 ומ-004): לא נקראה ולא נכתבה באף מקום, וכל הרשומות
  החזיקו בה ערך ריק. **אין להחזיר אותה.**
- ✅ **`migrations/004` הורצה ואומתה במסד** (אוגוסט 2026, יחד עם 003):
  `start_month`/`end_month` קיימות, `handled_months` הוסרה בפועל.
  **63 התלמידים כבר מוגדרים עם חודש הצטרפות** — כלומר מסלול «`start_month` ריק =
  כל השנה» הוא כיום מסלול תאימות-לאחור בלבד ולא המצב הרווח בנתונים. אין להסיר אותו
  (הוא עדיין ההתנהגות הנכונה לרשומה חסרה), אבל בדיקה ידנית של הטווח צריכה להיעשות
  מול תלמיד אמיתי — לרובם הטווח מוגדר.


## יתרת זכות + סעיף «זוכה על חשבון יתרת זכות» (אוגוסט 2026, `sw.js` v11, `migrations/005`)
עודף תשלום נשמט עד כה בשקט: `calcDistribution` פרס את התשלומים עד גובה החיוב והשאר נעלם
מהתצוגה. עם טווח ההצטרפות/עזיבה (004) יש פחות חודשים לפרוס עליהם, והמצב נפוץ יותר.
**שני מושגים נפרדים — אין לערבב:**
- **יתרת זכות** = שולם בשנת לימודים מעבר לסך החיוב בטווח החודשים של התלמיד.
  מוחזרת מ-`calcDistribution` תחת המפתח **`credit`** (שאר המפתחות הם אינדקסי חודש 0–11,
  אין התנגשות), ונקראת דרך `distCredit(dist)` / `studentCredit(sid,year)`.
  **אינה חוב שלילי:** לא מקוזזת מסך החוב, לא נכנסת ל"סה"כ שולם" ולא לאף סיכום חוב —
  שדה נפרד, בצבע נפרד (כחול `--credit`, נבדל מירוק=שולם ומאדום=חוב).
  מוצגת ב-3 מקומות: כרטיס התלמיד (שורת `#sc-credit` מתחת לטבלה השנתית), רשימת
  התלמידים בהגדרות (תגית «💠 זכות ₪X» לשנה הנוכחית) והדשבורד (שורת «💠 יתרת זכות»).
- **זיכוי מיתרה** = תנועה שנרשמה עם `payment_method === CREDIT_METHOD`
  (`'זוכה על חשבון יתרת זכות'`). מקטינה את חוב התלמיד בדיוק כמו תשלום, אבל **אינה כסף
  שהתקבל** ולכן אינה נספרת בשום סיכום גבייה/הכנסה.

**למה אמצעי תשלום ולא רשימת ה-sections:** `payment_method` הוא השדה היחיד שנשמר **על
התנועה**. `sl_lists.sections` משמשת לשיוך תלמיד (`card_settings.section`) ואין לה ייצוג
ברמת התנועה — שימוש בה היה מחייב עמודה חדשה ב-`sl_transactions`, כלומר שינוי סכימה שאינו
נדרש — ובפרט מיגרציה נוספת שהפיצ'ר היה תלוי בהרצתה הידנית כדי לעבוד בכלל.

**`calcDistribution` עבר מ"בריכה" ל"תור תשלומים לפי תאריך"** — כל שקל שנפרס על חודש יודע
מאיזו תנועה בא, ולכן `result[m]` מחזיק גם `cash` (תקבול) ו-`cred` (זיכוי), כאשר
`paid === cash + cred`. **הסכומים זהים בדיוק לפריסה הישנה** (מאומת בבדיקה) — הפיצול הוא
סיווג בלבד. בלעדיו אי אפשר להוציא זיכויים מסיכומי הגבייה: בבריכה הם מתמזגים עם הכסף האמיתי.

**הבחירה בדשבורד — שורות נפרדות, לא הפחתה שקטה:** שורת «סה"כ» נשארת כיסוי החיוב כפי שהיה
(אין רגרסיה במספר המוכר), ומתחתיה נוספו **«↩ מזה זיכוי מיתרה»** ו**«גבייה בפועל»** —
מוצגות רק כשיש זיכויים בשנה. הגרף מציג גבייה בפועל, והזיכויים נערמים מעליו כ-dataset שני
(עם legend) רק כשקיימים. גם `showDashMonth` מפצל כך, ותאי חודש עם זיכוי נצבעים כחול ומסומנים ↩.

**עמידות הסעיף (חובה לשמר):**
1. **הסיווג לפי המחרוזת ששמורה על התנועה** (`isCreditTxn`), לא לפי קיום הפריט ב-`sl_lists` —
   מחיקת הפריט אינה משנה שום סיווג היסטורי בשקט.
2. `updateDropdowns` **מזריק את האפשרות לטופס תמיד** כשהיא חסרה מהרשימה.
3. `deleteListItem` **מסרב** למחוק אותה (טוסט מסביר), והיא מוצגת בהגדרות עם «🔒 סעיף מערכת»
   בלי כפתור מחיקה — הבדיקה לפי ערך, ולכן חלה על שתי הקטגוריות.
4. `ensureCreditMethod()` זורע את הפריט אחרי סנכרון תקין (`_lastSyncOk`, פעם אחת בסשן),
   כדי שיופיע בלי הרצת SQL ידנית. **`migrations/005` היא אופציונלית** (זריעת נתון בלבד,
   `WHERE NOT EXISTS` — ל-`sl_lists` אין אילוץ ייחודיות ולכן `ON CONFLICT DO NOTHING`
   אינו מונע כפילות שם). **הוחלט שלא תורץ** (אוגוסט 2026) — האפליקציה זורעת בעצמה,
   ולכן היא נשארת כתיעוד ולשימוש ידני בלבד. 003 ו-004, לעומתה, כן הורצו ואומתו.

## מוסכמת הודעות קומיט (סבב 8 — משותפת לשלושת הפרויקטים)
**עברית חופשית**, לא conventional-commits. זה הדפוס שהתגבש בפועל בשלושת הריפו,
ומעכשיו הוא ההחלטה המוצהרת:
- שורה ראשונה: תיאור תמציתי בעברית של מה השתנה (עד ~72 תווים).
- עבודה מרוכזת מקבלת קידומת `סבב N:` (למשל `סבב 8: אחידות בין שלושת הפרויקטים`).
- בלי קידומות `feat:`/`fix:` באנגלית.

## ⚠️ התנגשות שמות: הקידומת `sl` (סבב 8 — לתעד, לא לשנות)
**באותו פרויקט Supabase (`kxbtskqobynewvnckaaz`) חיים שני מובנים שונים ל-`sl`:**
- **כאן (schar-limud): `sl` = שכר לימוד.** טבלאות `sl_students`, `sl_transactions`
  (כספים!), `sl_settings`, `sl_lists`, `sl_users` — כולן של הפרויקט הזה.
- **ב-hanhala-ruchanit: `sl` = שינה** (sleep). שם אלה **פונקציות JS בלבד** —
  `slSaveData`, `slOpenSession`, `slSupNav`, `slAddTreat` — שנשמרות ב-kv, לא טבלאות.
- **אין לשנות שמות** — מאות אתרי שימוש, סיכון גבוה מדי מול תועלת. ההגנה היא מודעות:
  סשן שעובד מול Supabase חייב לוודא לאיזה פרויקט שייכת ה-`sl_*` שהוא נוגע בה.
  כלל אצבע: `sl_` עם קו תחתון = טבלת שכר לימוד; `sl` כקידומת camelCase = שינה ב-hanhala.

## סבב תיקונים 8 (אוגוסט 2026, `sw.js` v8) — אחידות בין שלושת הפרויקטים
יישור מוסכמות בלבד — בלי שינוי לוגיקה. מה שהשתנה כאן:
- **`sw.js`:** `CACHE` → **`CACHE_NAME`**, `ASSETS` → **`CORE`** (נתיבים יחסיים —
  שקול לנתיב המוחלט, כי הם נפתרים מול מיקום ה-sw.js), סדר מאזינים אחיד
  **install → activate → fetch → message**.
- **רישום ה-SW:** `'/schar-limud/sw.js'` → **`'./sw.js'`** (נפתר לאותו URL תחת Pages).
- **באנר העדכון** עבר לדפוס של hanhala: נבנה ב-JS (`sw-update-banner`), לא HTML סטטי;
  התנאי הוא `state === 'installed'` בלבד (`'activated'` היה מיותר — statechange עובר
  תמיד דרך installed קודם). ה-HTML/CSS הסטטיים של `#update-banner` הוסרו.
- **עוזרי רשת בשמות משותפים:** `slIsNetErr`/`slGuardOnline`/`slErrMsg` →
  **`isNetErr`/`guardOnline`/`errMsg`**, ונוסף **`withTimeout`** (8 שניות; זהה ל-hanhala).
- **קבועי הודעות משותפים:** `MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN` — מילה במילה
  זהים בשלושת הפרויקטים. סמלים: ✅ הצלחה / ⚠️ אזהרה / ❌ שגיאה, תמיד בתחילת ההודעה
  (הוסר דפוס `✓` בסוף).
- **`getDeviceId`** נוסף (מוקשח מול localStorage חסום, מפתח `sl_device_id`) — זהה
  לשלושת הפרויקטים.
- **supabase-js שודרג `2.39.3` → `2.111.0`** (הגרסה ש-yoman כבר מוכיח בשטח). אומת
  בהשוואת בקשות ותוצאות בין שתי הגרסאות על כל שרשראות ה-API שבשימוש — זהה, למעט
  ש-`maybeSingle` כבר לא שולח `Accept: application/json` (אין השפעה) ושתגובת שרת
  לא-JSON חוזרת כ-error עם message גולמי במקום SyntaxError (עדיין error — אותו מסלול).
- **manifest.json:** נוסף `scope: "./"`, `start_url` הפך יחסי (`./index.html`) —
  אותו URL בפועל.
- **CONTEXT.md** נוצר (לא היה — לשני האחרים יש).

## סבב תיקונים 9 (אוגוסט 2026, `sw.js` v9) — מטמון חוצה-פרויקטים
**שלוש האפליקציות חיות על אותו origin (ygtotlrl-lab.github.io) וחולקות CacheStorage אחד.**
ה-activate של כל SW מחק כל מטמון שאינו ה-`CACHE_NAME` שלו — כלומר כל אפליקציה השמידה
את המטמונים של אחיותיה (אומת חי: ביקור ביומן עבודה רוקן את מטמון hanhala מ-11 רשומות
ל-0, כולל סקריפטי ה-CDN). התיקון — בשלושת הפרויקטים באותו סבב:
- **סינון מחיקה לפי קידומת:** ב-activate נמחקים רק מטמונים שמתחילים ב-`'schar-limud-'`
  (וכן `'hanhala-ruchanit-'`/`'yoman-avoda-'` אצל האחיות). **אין להסיר את הסינון** —
  כל SW חדש על ה-origin הזה חייב לסנן לפי קידומת משלו.
- ב-hanhala וב-yoman נוספה גם `ensureCdnCached()` (ריפוי עצמי של סקריפטי CDN חסרים).
  **כאן אין רשימת CDN ב-`sw.js`** — ה-fetch handler שומר כל תגובת GET בזמן-ריצה,
  כולל סקריפטים חוצי-origin — ולכן לא נדרש מנגנון כזה.

## סבב 10 (אוגוסט 2026, `sw.js` v12) — הדשבורד מציג תנועות בפועל
ארבעה תיקונים בלוח הבקרה על בסיס שימוש אמיתי ראשון. **אין שינוי סכימה ואין מיגרציה.**

### 1. כיווני חצים ב-RTL
בממשק RTL **קדימה בזמן = שמאלה**. בורר השנים בדשבורד היה הפוך (הכפתור שמוביל לשנה
הבאה סימן ימינה). הוחלפו הסמלים בלבד — `dashPrevYear` נשארה על הכפתור הימני (`&#9654;`)
ו-`dashNextYear` על השמאלי (`&#9664;`), הפונקציות לא נגעו. תוקן גם רצף החצים בהוראות
מסך ההגדרה (`&#8594;` → `&#8592;`). **אין בורר חודשים נפרד באפליקציה** — הניווט לחודש
הוא לחיצה על עמודה/תא בלוח, וברירת השנה בכרטיס התלמיד היא לשוניות ולא חצים.

### 2. הלוח השנתי הפוך — תלמיד בשורה, חודש בעמודה
היה: תלמידים = עמודות, חודשים = שורות, ולכן השמות נחתכו ב-ellipsis ("בן שמחון ...")
והטבלה הייתה בלתי שמישה במובייל. עכשיו כמו גיליון שכר לימוד קלאסי:
- עמודת השם **דביקה בצד ימין** (`position:sticky;right:0` בתוך `.dash-scroll`),
  החודשים נגללים לרוחב. **הגבול שלה הוא `box-shadow` ולא `border`** — עם
  `border-collapse:collapse` הגבולות שייכים לטבלה ולא לתא ואינם נגללים עם התא הדביק.
- **שמות לא נחתכים באף רזולוציה**: במובייל (`≤560px`) התא עובר ל-`white-space:normal`
  + `overflow-wrap:anywhere` ומתעטף לשתי שורות. אומת בדפדפן אמיתי ב-390px וב-1100px
  (`scrollWidth > clientWidth` על כל תאי השם = 0).
- **`tfoot` עם ארבע שורות סיכום לכל חודש**: «סה"כ נגבה» / «יעד» / «אחוז גבייה» /
  «פעילים». שורת האחוזים צבועה, ולכן נדרשו כללי CSS ספציפיים
  (`.tbl.pivot tfoot tr.sub td.amt-*`) — כלל שורת-הסיכום גובר על `.amt-green` לבדו.
- העמודה האחרונה («סה"כ») נותנת את הסיכום השנתי של כל שורה.

### 3. ⭐ הדשבורד = תנועות בפועל, לא פריסה
**השינוי המהותי.** הדשבורד הציג את **פריסת** התשלומים: תשלום 2000 מ-11 ביולי התפצל
ל-1700 ביולי ו-300 באוגוסט, ולכן החודש שבו נכנס הכסף לא נראה בשום מקום. מעכשיו כל חודש
מציג את **סך התנועות שנרשמו בו לפי תאריך התנועה, בלי שום פריסה** — 2000 מיולי שייכים
כולם ליולי. חל על הטבלה השנתית, על הפירוט החודשי (`showDashMonth`) ועל הגרף.
- **`calcDistribution` (הפריסה) נשארה אך ורק בכרטיס התלמיד האישי** — שם היא נכונה
  ורלוונטית (מול איזה חודש נזקף התשלום ומה החוב בכל חודש). **אין להחזיר אותה לדשבורד.**
- פונקציות חדשות: `actualByMonth(year)` → `{student_id:{monthIdx:סכום}}` לפי תאריך
  התנועה; `studentTuition(s)` (אישי או ברירת מחדל); `monthTarget(year,m)` = סכום שכר
  הלימוד של כל הפעילים בחודש; `pctNum`/`pctText`/`pctClass`/`ofTargetTitle`.
- **אחוז הגבייה מוצג עם שני המספרים שמהם הוא נגזר** — שורות «נגבה» ו«יעד» יושבות
  ישירות מעל שורת האחוז באותה עמודה, יש `title` («נגבה X מתוך יעד Y»), והפירוט החודשי
  אומר זאת במפורש: «נגבה ₪X מתוך יעד ₪Y • Z%».
- **הגרף**: עמודות = הנגבה בפועל, ובנוסף **קו יעד מקווקו** (dataset מסוג `line` בתוך
  גרף `bar` — נתמך ב-chart.js 4), ו-tooltip שמוסיף את אחוז הגבייה. **לא מוערם.**
- **תנועה מחוץ לטווח הפעילות של התלמיד** (תשלום מראש/בדיעבד) מוצגת בכל זאת בתא שלה,
  כדי שסכום עמודת החודש יהיה שווה בדיוק לסך התנועות; `showDashMonth` מציג מאותה סיבה
  גם תלמיד פעיל שמחוץ לטווח אם נרשמה לו תנועה בחודש. תא ריק **ומחוץ לטווח** = `·`.
- תנועה בסעיף `CREDIT_METHOD` **אינה נכנסת ל-`actualByMonth` כלל** — אינה כסף שהתקבל.
  אותו כלל כמו קודם, רק שעכשיו היא פשוט לא נספרת במקום להיות מוצגת ומופחתת בשורה נפרדת.

### 4. יתרת זכות ירדה מהדשבורד
יתרת זכות היא **נתון אישי ולא ניהולי**. הוסרו מהדשבורד שורות «💠 יתרת זכות»,
«↩ מזה זיכוי מיתרה» ו«גבייה בפועל», התגית והסימון ↩ ב-`showDashMonth`, ומחלקת ה-CSS
`.credit-row` (נותרה ללא שימוש). **נשמרו כפי שהיו**: כרטיס התלמיד (`#sc-credit`,
`distCredit`/`distCredApplied`) ותגית «💠 זכות» ברשימת התלמידים בהגדרות — שתיהן
תצוגה ברמת תלמיד יחיד. `studentCredit` ו-`distCredApplied` עדיין בשימוש; אין למחוק.

## הרנסי בדיקה (בסשן, לא בריפו)
`_test_dash.js` (סבב 10 — 49 בדיקות: `actualByMonth` לפי תאריך התנועה, אי-פריסה,
הזיכוי שאינו נספר, `monthTarget` מול שכר לימוד אישי וטווח הצטרפות, האחוזים, `renderDash`
מלא כולל `esc` והיעדר יתרת הזכות, `showDashMonth`, **ואימות שהפריסה בכרטיס התלמיד לא
נגעה**). בנוסף נבנתה תצוגה מבודדת שהורצה ב-Chromium אמיתי עם chart.js@4.4.1 (מ-npm —
ה-CDN חסום ברשת סביבת הענן) ב-390px וב-1100px: אפס שגיאות דף, אפס שמות חתוכים,
העמודה הדביקה נשארת גלויה בגלילה לקצה.

`test_round6_esc.js` (`esc` + `sdRenderList` מול שם עם גרש/מרכאה/תגית),
`test_round6_sync.js` (`syncAll` מול Supabase מדומה: שגיאות, שומר חפיפה, דגל הגיבוי),
`test_credit.js` (24 בדיקות: יתרת זכות, פילוח `cash`/`cred`, **שקילות מלאה של `paid`
לפריסת-הבריכה הישנה**, עמידות הסעיף בלי `sl_lists`) ו-`test_render.js` (עשן על כל
פונקציות הרינדור, עם וללא זיכויים, כולל אימות `esc` על שם עם תגית ומרכאות).
כולם מריצים את הקוד האמיתי המחולץ מ-`index.html`. שווה לשחזר לפני נגיעה באזורים האלה.

---

<!-- SHARED:start id="iron-rules-storage" -->
## ⭐ כללי ברזל של הארגון — עמידות אחסון ואופליין (סבב 11, אוגוסט 2026)
**ארבעת הכללים האלה זהים מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius). הם חלים על כל קוד חדש בארגון.

1. **כל כתיבה ל-localStorage עוברת דרך העוזר המוקשח.** `lsSet(key, value)` או
   `lsSetArray(key, arr, tsOf)` — **ואין קריאה ישירה ל-`localStorage.setItem`**
   בשום מקום מחוץ למודול המשותף. `setItem` זורק באחסון חסום או מלא, וכל נתיב
   לא-מוגן מת באמצע: הזיכרון כבר עודכן, המסך מתרענן, ושום דבר לא נשמר.
   - החריג היחיד המותר הוא **`lsSetRaw`** — העוזר של המודול עצמו, שמחזיר
     `false` בלי להרים באנר. הוא שמור לערכים **חסרי-נזק שנכתבים בכל עליית
     דף** (מזהה מכשיר, יומן הפינוי). **אין להשתמש בו לשום נתון של המשתמש.**
2. **אף כישלון שקט.** `lsSet` מחזירה `true`/`false`, מרימה באנר אדום קבוע
   (`#ls-alert`) שנשאר עד שהמשתמש סוגר אותו, ורושמת ליומן.
   - נקודת האכיפה היא שורה אחת בראש `toast()` של כל אפליקציה:
     `if (typeof lsGuardToast === 'function' && !lsGuardToast(msg)) return;`
     — כל הודעה שמתחילה ב-✅ נבלעת אם כתיבה מקומית נכשלה בשתי וחצי השניות
     האחרונות. **אין להסיר את השורה הזו.** בלעדיה חוזר בדיוק המצב שהסבב הזה
     בא למנוע: כתיבה שנכשלה ומשתמש שרואה "נשמר בהצלחה".
   - במסלול קריטי (רישום נוכחות, שמירת תשלום, הוספת רשומה, רישום תנועה) יש
     בנוסף עצירה מפורשת — `lsFails()` / `lsOkSince(t)`, או ערך מוחזר שנבדק.
3. **עבודה אופליין מלאה היא דרישת בסיס בכל אפליקציה בארגון — מהיום הראשון,
   לא תוספת מאוחרת.** ארבעת הרכיבים, כולם:
   עותק מקומי של **כל** הנתונים · קריאה ממנו בעליית האפליקציה, **לפני** כל
   נגיעה ברשת · סנכרון ברקע · מנוע מיזוג **ברמת רשומה** לפי חותמת עדכון,
   עם tombstones. הכלל היחיד שחשוב במנוע: **היעדר רשומה אצל צד אחד אינו
   מחיקה** — מחיקה היא `deleted=true` + חותמת.
4. **ניטור מכסה ופינוי יזום נבנים יחד עם האופליין, לא אחריו.** עותק מקומי
   בלי ניטור הוא פצצת מכסה מושהית: הוא גדל עד שהכתיבה נכשלת. `lsBoot()`
   נקראת בכל עליית אפליקציה, מודדת את **כל** המפתחות בדומיין (לא רק של
   האפליקציה הנוכחית — המכסה משותפת!), מתריעה מעל 3MB ובאנר מעל 4MB,
   ומפנה מיוזמתה מעל 60%.
5. **כל אפליקציה חדשה בארגון נבנית עם התשתית הזו מלכתחילה** — מעתיקים את
   המודול המשותף כמו שהוא, ממלאים `LS_CFG`, מחברים `lsBoot()` בעלייה,
   את רכיב «מידע טכני ›» (`techInfoMount()`) בהגדרות, ואת שורת השער ב-`toast()`.

### המודול המשותף
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף`. **הוא זהה בית-לבית בארבעתן**
(474 שורות, sha1 `8345d842b294`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `LS_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתאם
`lsToast(msg, dur)` בן שורה.

**ה-API:** `lsSet` · `lsSetArray` · `lsGet` · `lsRemove` · `lsSetRaw` ·
`lsUsage` · `lsBoot` · `lsSweep`/`lsSweepGuarded` · `lsLog`/`lsLogRead` ·
`lsAlert` · `lsGuardToast` · `lsFails`/`lsOkSince` · `lsStatusHTML`/`lsRenderStatus` ·
`lsHorizon`/`lsClearHorizons`.

### סדר הפינוי — קפדני, ואין לשנות אותו
- **א. מטמונים וערכים שניתן לשחזר מהענן בכל רגע** (`LS_CFG.tier1`), הגדול קודם.
- **ב. ארכיון ורשומות ישנות שכבר מסונכרנות** (`LS_CFG.tier2`), **הישן ביותר
  קודם**, ורק מתחת לרצפת גיל פר-מפתח (`minAgeMs`).
- **ג. ⛔ נתון שטרם הסתנכרן לענן לא מפונה בשום מצב, גם אם המקום נגמר לגמרי.**
  במצב כזה עדיף להיכשל ברעש מאשר למחוק. שתי הגנות נפרדות אוכפות את זה:
  `LS_CFG.pending()` — יש משהו בתור? שלב ב מדולג **לגמרי**; ו-`syncedThrough()` —
  רק רשומות שגילן קודם לדחיפה/משיכה המוצלחת האחרונה נחשבות "בענן".
  ספק ⇐ לא מפנים.

**הפינוי שקט כלפי המשתמש אבל לא בלתי נראה** — כל פינוי, וכל **דילוג** על
פינוי, נרשם ביומן (`LS_CFG.logKey`, 12 רשומות אחרונות) ומוצג בהגדרות.

### אופק הפינוי — למה שלב ב לא חסר משמעות
פינוי מקומי בלבד היה חסר תועלת: מנוע המיזוג מחזיר כל רשומה מרוחקת שאין לה
מקבילה מקומית, ולכן הרשומות שפונו היו חוזרות לדיסק תוך שניות. לכן לכל מפתח
שמפונה נשמר **אופק** (`LS_CFG.hzPrefix + key`) — חותמת הרשומה החדשה ביותר
שפונתה — ו-`lsSetArray` מסננת מתחתיו **בכל כתיבה**.
**הזיכרון והענן לא נוגעים; רק העותק שעל הדיסק מצטמצם.** כשיש רשת המשתמש
רואה הכול כרגיל; אופליין הוא רואה את התקופה האחרונה בלבד, וההגדרות
(«מידע טכני ›») אומרות זאת במפורש. המנגנון שמנקה את האופק (`lsRestoreAll`)
קיים במודול אך אינו נגיש מה-UI מאז סבב 15; ניקוי אופק ייעשה בסבב ייעודי
אם יידרש.

### מנגנון חירום
אם למרות הכול כתיבה נכשלת ב-`QuotaExceededError`, `lsSet` מריצה פינוי נוסף
**לפי אותו סדר בדיוק** ומנסה **פעם אחת** נוספת. הצליח — זו הצלחה לכל דבר
(המונה מוחזר וטוסט ההצלחה מותר). נכשל — באנר, טוסט ויומן. אין רקורסיה:
`_lsSweeping` חוסם פינוי בתוך פינוי.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud, `sw.js` v13)
- המודול המשותף נוסף במלואו: כתיבה מוגנת, ניטור, התרעות, שער הטוסט ופאנל
  האבחון (כיום בהגדרות ← «מידע טכני ›» — קופל בסבב 15).
- שתי הכתיבות היחידות שהיו: `sl_device_id` עבר ל-`lsGet` + `lsSetRaw`
  (החריג המתועד), ו-`sl_last_backup` עבר ל-`lsSet`.
- **`lsBoot()`** נקראת ב-`DOMContentLoaded`.

### ~~⚠️ פער מתועד: אין כאן עותק מקומי~~ — **נסגר בסבב 12 שלב 2** (ר' למטה)
schar-limud היא היחידה מארבע האפליקציות ש**קוראת ישירות מ-Supabase בכל
`syncAll`** (כל 3 שניות) ואינה מחזיקה שום עותק מקומי של הנתונים. ב-localStorage
יושבים רק `sl_device_id`, `sl_last_backup` ויומן הפינוי — **פחות מ-1KB**.

לכן `LS_CFG.tier1` ו-`tier2` ריקים: אין כאן מטמון לפנות ואין רשומות ישנות
מקומיות. **זה לא פער במימוש של הסבב הזה — זה תיאור נכון של האפליקציה**, והמודול
נשאר זהה לשלוש האחיות כדי שברגע שייבנה כאן עותק מקומי יהיה רק למלא את הטבלה.

**זה חורג מכלל ברזל 3 של הארגון** (עבודה אופליין מלאה = דרישת בסיס), והוא נשאר
פתוח בכוונה: `sl_transactions` הוא **כספים**, ומנוע מיזוג + תור יוצא על נתוני
כסף הוא החלטת אפיון בפני עצמה — לא משהו שנגרר לתוך סבב תשתית. **הפריט הפתוח
לסבב הבא:** עותק מקומי + מיזוג ברמת רשומה כאן, בדפוס של gius (ר' CLAUDE.md שם —
`MIRROR` / `g_outbox` / `mergeRows`), עם החלטה מפורשת מה קורה לתנועה כספית
שנרשמה אופליין ולא הצליחה לעלות.

עד אז התרומה של המודול כאן היא **הכתיבה המוגנת, הניטור וההתרעות** — ואלה
רלוונטיים במלואם, כי המכסה משותפת עם שלוש האחיות: schar כמעט לא כותבת מקומית,
אבל היא זו שתיפגע ממה שהן מילאו.

---

## שלב א' לאופליין בטוח: `client_id` (אוגוסט 2026, `sw.js` v14, `migrations/006`)

> **זהו שלב א' בלבד — תשתית זהות. שכבת האופליין עצמה לא נבנתה כאן**
> (אין עותק מקומי, אין תור יוצא, אין מנוע מיזוג). היא תיבנה **בסבב נפרד**,
> ותישען על העמודה הזו. הפער מכלל ברזל 3 שמתועד בפרק שמעל **נשאר פתוח**.

### הבעיה שהשלב הזה פותר
המזהה של `sl_transactions` ו-`sl_students` הוא `SERIAL` — `nextval` בצד המסד
(אומת מול המסד). המשמעות: **רשומה שנוצרה במכשיר היא חסרת זהות עד שהיא מגיעה
לשרת.** ולכן:

1. המכשיר שולח את התשלום.
2. המסד מכניס את השורה — **והתשובה אובדת ברשת** (ניתוק, timeout).
3. המכשיר, שלא קיבל תשובה, שולח שוב.
4. המסד מקצה `id` חדש ויוצר **שורה שנייה** — כלומר **תשלום כפול, בכסף**.

אין למסד שום דרך לדעת ששתי השורות הן אותה כוונה. **ביומן-עבודה ובהנהלה הבעיה
הזו לא קיימת** כי שם המזהה הוא uuid שנוצר במכשיר, והמיזוג מזהה כפילות לפי `id`.
בלי לסגור את הפער הזה, כל תור יוצא שייבנה כאן הוא מכונה לייצור תשלומים כפולים.

### מה נעשה
- **`migrations/006_client_id_identity.sql`** (⚠️ **טרם הורצה** — המשתמש יריץ):
  עמודת `client_id TEXT` + אינדקס ייחודי, ב-`sl_transactions` וב-`sl_students`.
  **מותרת ב-NULL בכוונה** — 25 התנועות והתלמידים הקיימים נוצרו לפניה. ב-Postgres
  ערכי NULL נחשבים שונים זה מזה באינדקס ייחודי, ולכן ריבוי NULL-ים אינו מתנגש.
  `000_initial_schema.sql` עודכן גם הוא (כלל 7 — מקור אמת יחיד).
- **`newClientId()`** — uuid v4 בצד הלקוח. `crypto.randomUUID` כשקיים, ואחריו
  **שתי נפילות-חזרה**: `crypto.getRandomValues` ואז `Math.random`. הנפילה-חזרה
  אינה תיאורטית: `randomUUID` קיים רק ב-secure context ובדפדפנים מודרניים,
  וב-WebView ישן הוא פשוט חסר.
- **`upsert` במקום `insert`** ב-`saveTxn` וב-`saveNewStudent`, עם
  `{onConflict:'client_id'}`.
- **המזהה הראשי לא השתנה.** `id` נשאר `SERIAL` והוא עדיין המפתח שאליו מצביע
  `sl_transactions.student_id`. `client_id` הוא מפתח **זהות** מקביל לדה-דופליקציה,
  ולא מפתח קשרים.

### `pendingCid` — למה המזהה קשור לתוכן הטופס
מזהה שנוצר מחדש בכל לחיצה על "שמור" היה מחזיר בדיוק את הכפילות שהעמודה באה
למנוע. לכן `pendingCid(slot, fp)` מחזיק את המזהה של הרשומה **שנרשמת עכשיו**,
מקושר ל-`fp` — טביעת אצבע של תוכן הטופס — ומשוחרר ב-`releaseCid` אחרי שמירה
מוצלחת. שני הכיוונים חשובים, **ושניהם על כסף**:
- **אותו תוכן אחרי כשל = ניסיון חוזר** ⇐ אותו `client_id` ⇐ שורה אחת.
- **תוכן שהשתנה = תשלום אחר** ⇐ מזהה חדש ⇐ שורה חדשה. בלי זה, תשלום שני שנרשם
  אחרי כשל היה **דורס** את הראשון במקום להתווסף אליו — אובדן תשלום, לא כפילות.

בסבב האופליין המלא **התור היוצא יחזיק את המזהה פר-רשומה**, והלוגיקה הזו תהיה
עודפת. עד אז היא מה שהופך את ה-`upsert` למשמעותי כבר היום.

### ⛔ האינדקס חייב להיות מלא ולא חלקי (`migrations/007`)
006 הורצה במסד בגרסה עם אינדקסים **חלקיים** — `... (client_id) WHERE client_id
IS NOT NULL`, בשם `_uidx`. הכוונה סבירה (לא לאנדקס 88 רשומות היסטוריות), אבל
**היא שוברת את ה-UPSERT לגמרי**, ולכן שמירת תשלום ותלמיד חדש מפסיקה לעבוד.

הסיבה: Postgres מסיק את אינדקס-הבורר של `ON CONFLICT` מרשימת העמודות, ועבור
אינדקס **חלקי** ההסקה מצליחה **רק אם המשפט חוזר על התנאי של האינדקס**.
`.upsert(row,{onConflict:'client_id'})` פולט `ON CONFLICT (client_id)` בלבד —
ל-PostgREST אין שום דרך להוסיף predicate. אומת חי מול המסד ב-`EXPLAIN`:

| משפט | תוצאה |
|---|---|
| `ON CONFLICT (client_id) WHERE client_id IS NOT NULL` | ✅ מתכנן, בורר `_uidx` |
| `ON CONFLICT (client_id)` — מה שהאפליקציה שולחת | ❌ `42P10` |

**אינדקס מלא אינו ויתור:** `client_id` מותרת ב-NULL, וב-Postgres ערכי NULL
נחשבים שונים זה מזה באינדקס ייחודי רגיל — כלומר אינדקס מלא מתיר את הרשומות
ההיסטוריות בדיוק כמו החלקי. **מה שנשבר בחלקי הוא ההסקה, לא הייחודיות.**
007 יוצרת את המלא (`_key`) ואז מוחקת את החלקי — בסדר הזה, כדי שלא יהיה רגע בלי
הגנת ייחודיות. `000` ו-`006` עודכנו גם הם (כלל 7). **אין ליצור אינדקס חלקי על
`client_id` בשום מיגרציה עתידית.**

### מה **לא** קיבל `client_id`, ולמה
`sl_lists` בלבד (`addListItem`, `ensureCreditMethod`) — ערכי רשימה, לא כסף,
ואין להם מסלול אופליין מתוכנן. `deleteStudent`, `deleteTxn` ועדכוני הגדרות הם
`update` על `id` קיים ולכן אידמפוטנטיים ממילא.

### בדיקה
9 בדיקות על הקוד האמיתי המחולץ מ-`index.html`: תקינות פורמט uuid v4 בשלושת
המסלולים (`randomUUID`, `getRandomValues`, `Math.random`), 5,000 + 3,000 מזהים
ללא התנגשות, ושלוש התנהגויות `pendingCid` (אותו תוכן ⇐ אותו מזהה, תוכן שהשתנה
⇐ מזהה חדש, שחרור אחרי הצלחה ⇐ מזהה חדש). כל השערים עברו: `node --check` על
שני הקבצים + `sync-setup-sql.py --check`.

---

<!-- SHARED:start id="pending-module" -->
## ⏳ סימון "ממתין לסנכרון" — מודול משותף (סבב 12, שלב 1)
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md.**

### הבעיה
אחרי שמירה מקומית המסך נראה **בדיוק** כמו אחרי שמירה בענן. המשתמש רושם תשלום
ברכבת התחתית, רואה אותו ברשימה, וסוגר את האפליקציה בהנחה שהוא שמור — בזמן שהוא
יושב רק על המכשיר הזה. הסבב הזה הופך את ההבדל לגלוי.

### שלוש רמות תצוגה, כולן ממפה אחת
- **תגית על הרשומה עצמה** — `pendTag(key)`, נכנסת לשורה ברשימה (⏳ ממתין, ובאיחור ⚠️).
- **מונה** — `#pend-bar`, סרגל תחתון קבוע, **מוצג רק כשיש** משהו ממתין.
- **התרעה** — משהו ממתין מעל 24 שעות ⇐ `#pend-alert`, באנר אדום עליון עם כפתור סגירה
  (נסגר ל-6 שעות; **הסימון עצמו לא נמחק בסגירה**).

### ⚠️ ההגדרה של "ממתין" היא ראייתית, לא אופטימית
רשומה מסומנת ברגע שנכתבה במכשיר, ויורדת מהסימון **רק כשיש ראיה שהענן קיבל אותה**.
שלושת מסלולי האישור, לפי אופי האפליקציה:

| מסלול | מי משתמש בו | הראיה |
|---|---|---|
| `pendClear` | schar-limud | תשובת הצלחה מהשרת, או משיכה שמחזירה את ה-`client_id` |
| `pendConfirmPush(prefix, t0)` | hanhala, yoman | דחיפת-מצב מלא שהצליחה |
| `pendSyncOutbox()` | gius | הפעולה ירדה מהתור היוצא |

- **כשל רשת אינו ראיה לכלום ⇒ הסימון נשאר.** תשובה שאבדה ברשת היא בדיוק המקרה שבו
  הרשומה אולי כן נכנסה ואולי לא. ספק ⇐ מסומן.
- **כשל סמכותי כן מוריד את הסימון** (`pendFailed`) — ולידציה/הרשאה/סכימה אומרות
  שהשורה **לא** נכתבה, וסימון "ממתין" עליה מבטיח שליחה חוזרת שלא תקרה לעולם.
- **⚠️ `t0` נלקח לפני שצילום המצב נקרא, לא אחרי שהדחיפה הסתיימה.** רשומה שנכתבה
  באמצע הדחיפה אינה בתוכה, והורדת הסימון ממנה הייתה מאבדת אותה בשקט.
- **חותמת קיימת אינה נדרסת** בסימון חוזר — אחרת עריכה חוזרת של אותה רשומה הייתה
  מאפסת את שעון ה-24 שעות לנצח.

### המודול
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"`. **זהה בית-לבית בארבעתן**
(267 שורות, sha1 `2b3d6b779b1f`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `PEND_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתחתיו בלוק
"חיבור <אפליקציה> למודול" עם הקידומות והעוזרים המקומיים.

**ה-API:** `pendMark`/`pendMarkMany` · `pendClear`/`pendClearMany` · `pendFailed` ·
`pendConfirmPush` · `pendHas`/`pendSince`/`pendCount`/`pendOldest`/`pendLateCount` ·
`pendTag` · `pendRender`/`pendCounterText` · `pendStatusHTML` · `pendBoot`/`pendReload` ·
`pendReset`.

**כתיבה:** דרך `lsSet` בלבד (כלל ברזל 1), למפתח יחיד פר-אפליקציה
(`sl_pending` / `ys_pending` / `tb_pending<מוסד>` / `g_pending`).
**תקרה:** 800 סימונים; בגלישה נגרעים הישנים ביותר **ונרשמים ליומן** — סימון שנעלם
בלי אישור הוא בדיוק מה שהמודול בא למנוע, ולכן הוא לא נעלם בשקט.

### `PEND_CFG`
- **schar-limud:** `sl_pending`.
- **hanhala-ruchanit:** `ys_pending`; `extra()` מוסיף למונה את מספר המפתחות
  ב-`ys_offline_queue` — התור הישן הוא **פר-מפתח kv** ולא פר-רשומה, ובלי זה
  המשתמש היה רואה "0 ממתינות" בזמן שארבעה מפתחות תקועים.
- **yoman-avoda:** המפתח הוא **פונקציה** — `tb_pending` + סיומת המוסד. שני המוסדות
  חולקים localStorage, וסימון של ראשון לציון אינו תקף לרמת אביב. `selectYeshiva`
  קוראת ל-`pendReload()`.
- **gius:** `g_pending`; `extra()` מציג פעולות שנכשלו כשל תוכן חוזר (`dead`).

### מה סומן בכל אפליקציה
| | רשומות מסומנות | קידומות |
|---|---|---|
| schar-limud | תשלום, תלמיד, ומחיקה של כל אחד מהם | `txn:` `student:` `txn-del:` `student-del:` |
| hanhala | סדר נוכחות, סדר שינה, טיפולים, תלמידים | `at-sess:` `sl-sess:` `at-treat:` `sl-treat:` `student:` |
| yoman | רשומת יומן, סנאפשוט ארכיון | `entry:` `arc:` |
| gius | כל שבע הטבלאות, לפי `<table>:<id>` | `g_donors:` `g_txns:` … |

**מסך "⏳ ממתין לסנכרון" נוסף להגדרות בארבעתן** (`pendStatusHTML`) — מה ממתין,
מאיזה גיל, ומצב הרשת.

### ⚠️ מגבלה מתועדת ב-schar-limud (שלב 1)
schar עדיין **אינה עובדת אופליין** (הפער מכלל ברזל 3). לכן הסימון מכסה שם היום
מקרה אחד ומדויק: **תשובה שאבדה ברשת** — הכתיבה נשלחה, השורה אולי נכנסה למסד,
והמכשיר לא יודע. שלב 2 הוא שהופך את זה לכיסוי מלא.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud, `sw.js` v15)
המודול נוסף במלואו; `pendBoot()` ב-`DOMContentLoaded` לצד `lsBoot()`; `saveTxn`,
`saveNewStudent`, `deleteTxn` ו-`deleteStudent` מסמנות לפני השליחה ומורידות לפי
תוצאה; `syncAll` מריצה `pendSyncScan` על המערכים ה**גולמיים** (לפני סינון
ה-`deleted`) — אישור מחיקה הוא ראיית `deleted=true`, והיא נעלמת מיד אחרי הסינון.
תגית ⏳ ביומן התשלומים וברשימת התלמידים שבהגדרות.

**בנוסף — תיקון שקדם לסבב:** ב-`index.html` היה **בייט NUL** בקוד
(`[sid,date,amount,method,note].join('\0')` במקום `' '`). פונקציונלית הוא עבד
כמפריד, אבל git סיווג את `index.html` כקובץ **בינארי** — כלומר `git diff` הציג
"Binary files differ" ולא את השינויים. הוחלף ברווח רגיל; אין שינוי התנהגות
(טביעת האצבע היא בזיכרון בלבד ואינה נשמרת בין רענונים).

---

## ⭐ עבודה אופליין מלאה (סבב 12 שלב 2, `sw.js` v16, `migrations/008`)
**הפער האחרון מכלל ברזל 3 נסגר.** schar-limud קראה עד כה ישירות מ-Supabase
בכל `syncAll` ולא החזיקה עותק מקומי: בלי רשת לא היה מה להציג ולא היה לאן
לכתוב. עכשיו יש כאן את ארבעת הרכיבים, **בדפוס דחיפת-המצב של hanhala-ruchanit
ולא בדפוס התור של gius**.

### `migrations/008` — `updated_at` + טריגר ✅ הורצה ואומתה
בלי חותמת עדכון אין מנוע מיזוג. העמודה נוספה לשתי טבלאות הליבה עם
`DEFAULT NOW()`, ושני טריגרים (`sl_transactions_touch`, `sl_students_touch`)
מעדכנים אותה בכל UPDATE. `000_initial_schema.sql` עודכן גם הוא (כלל 7).
אומת: 25 תנועות / ₪41,952 / 63 תלמידים — זהה למצב שלפני ההרצה.

### מה נבנה
- **`MIRROR`** — עותק מקומי של ארבע הטבלאות ב-`sl_mirror_*`, **כולל
  tombstones**. בלעדיהם מחיקה שנעשתה במכשיר אחר לא הייתה מגיעה לכאן לעולם.
- **`slApplyMirror()`** — נקודת הסינון היחידה של ה-soft-delete (כללים 5–6),
  בדיוק במקום שבו `syncAll` סיננה קודם. כל 11 הקוראים של `STUDENTS` מקבלים
  אותה אוטומטית.
- **קריאה מהעותק בעלייה** — `DOMContentLoaded` טוען את המראה **לפני** כל
  נגיעה ברשת, ו-`enterApp` מרנדר ממנה מיד. **אין יותר `await syncAll()`
  חוסם**: בלי רשת הוא תקע את המסך, ועם רשת איטית השאיר ספינר במקום נתונים
  שכבר על הדיסק.
- **כתיבה מקומית-תחילה** — `saveTxn`/`saveNewStudent`/`deleteTxn`/
  `deleteStudent`/`toggleStudentActive`/`saveStudentSettings` כותבות ל-`MIRROR`
  ומחזירות מיד. **`guardOnline` הוסר מהן** — הוא היה מה שחסם רישום תשלום
  בלי רשת. כשל כתיבה מקומית עוצר ברעש (כלל ברזל 2): בכסף אסור להציג "נשמר"
  על משהו שלא נכתב.
- **דחיפת מצב** — `syncAll` = משיכה → מיזוג → החלה → דחיפת מה שמקומי-וחדש.
  **דוחפים רק לקטגוריה שנמשכה בהצלחה**: בלי לדעת מה בענן, דחיפה עיוורת
  הייתה מחזירה לחיים שורה שנמחקה במכשיר אחר.

### ⚠️ שני מסלולי כתיבה — וזו הגנה על כסף
- **יש `id`** ⇒ `update ... eq(id)`. כולל את 25 התנועות ו-63 התלמידים
  ההיסטוריים, שה-`client_id` שלהם ריק — `upsert` על `client_id` היה יוצר
  להם **שורה כפולה**.
- **אין `id`** (נוצרה במכשיר) ⇒ `upsert` על `client_id`. אידמפוטנטי, ולכן
  ניסיון חוזר אחרי תשובה שאבדה ברשת מעדכן ולא מכפיל תשלום.
- מפתח המיזוג בהתאם: **`c:<client_id>` כשקיים, `i:<id>` אחרת**.

### ⚠️⚠️ הכלל שמונע אובדן עריכה — רשומה מסומנת ⏳ מנצחת במיזוג
**הטריגר בצד השרת דורס `updated_at` ב-`NOW()` בכל UPDATE.** המשמעות: ברגע
שמכשיר א' דוחף את העריכה שלו, השורה בענן מקבלת חותמת **מאוחרת יותר** מעריכה
חדשה יותר שיושבת אצל מכשיר ב' וטרם עלתה. בלי טיפול, מכשיר ב' היה מאמץ את
גרסת א' והעריכה שלו הייתה **נעלמת בשקט**. (הבדיקה חשפה את זה בפועל.)

לכן, בשני מקומות:
- **`slMerge(local, remote, isPending)`** — רשומה מקומית מסומנת ⏳ מנצחת ללא
  תלות בחותמת.
- **`slDirtyRows(..., isPending)`** — רשומה מסומנת נחשבת תמיד "לדחיפה".
  בלי הסעיף הזה היא הייתה נשארת מסומנת לנצח ולעולם לא נדחפת.

**המחיר המודע:** מכשיר שהיה מנותק זמן רב ידרוס בעלייתו עריכה מרוחקת חדשה
יותר. זו העדפה מכוונת — הרשומה מסומנת ⏳ כל אותו זמן ומתריעה אחרי 24 שעות,
בעוד שעריכה שנמחקת בשקט אינה משאירה עקבות. אותה עדיפות בדיוק כמו "היעדר
רשומה אינו מחיקה". **אין להסיר את `isPending` משני המקומות.**

### `updated_at` נשלח מהמכשיר
`slLocalWrite` חותמת `updated_at` ברגע העריכה. ב-**INSERT** הטריגר אינו יורה,
ולכן רשומה שנוצרה אופליין נושאת את חותמת **היצירה** ולא את חותמת ההגעה
לשרת. ב-UPDATE הטריגר כן דורס — וזה מטופל בכלל שמעל.

### כניסה אופליין — `sl_session`
> ⚠️ **הפרק הזה תיאר את המצב עד סבב 24.** «כניסה חדשה דורשת רשת» **כבר לא
> נכון**: מסבב 24 יש אימות מול `pass_fp` שבמראת המשתמשים, והסשן הוא קיצור
> דרך למי שכבר מזוהה ולא התנאי לכניסה. ר' «סבב 24» למטה. שאר הפרק — הסשן
> בלי סיסמה, וההתנהגות של `slRevalidateSession` — תקף כלשונו.

מי שכבר נכנס פעם אחת ממשיך לעבוד אופליין; **כניסה חדשה דורשת רשת**.
⛔ **הסשן אינו מחזיק סיסמה** — שכפול סיסמאות ל-localStorage של כל מכשיר היא
חשיפה מיותרת. אותה החלטה בדיוק כמו `gius.session`. `slRevalidateSession` רץ
ברקע ומנתק **רק על תשובה סמכותית** (המשתמש אינו קיים); כשל רשת אינו ראיה
לכלום, וניתוק עליו היה זורק החוצה בדיוק את מי שעובד אופליין.

### ~~מה **לא** עובד אופליין, ולמה~~ — **נסגר בסבב 13** (ר' למטה)
**`sl_settings` ו-`sl_lists` היו קריאה בלבד.** 008 הוסיפה `updated_at` לשתי
טבלאות הליבה בלבד, ולכן לא הייתה לשתיהן חותמת להשוות ולא היה מנוע מיזוג. הן
היו ממורות לקריאה (הטופס עבד אופליין) אבל **עריכה שלהן דרשה רשת**.
**מ-009 ואילך שתיהן בשכבה במלואה** — ר' «סבב 13». החריג היחיד שנשאר:
`changeAdminPass`.

### `LS_CFG` — מה מותר לפנות
**`tier1` ו-`tier2` ריקים שניהם — ובכוונה.**
⛔ `sl_mirror_transactions` הם **כספים**: רשומה שפונתה אינה נראית אופליין,
וחישוב חוב על נתון חסר גרוע מכל מכסה. `sl_mirror_students` הם אבות המפתח
הזר שלהן ולכן גם הם בחוץ.
⛔ `sl_mirror_settings`/`sl_mirror_lists` **ישבו ב-tier1 עד 009 והוצאו משם**:
ההצדקה הייתה שהן נטענות מהענן בכל סנכרון ולעולם אינן נכתבות מקומית, וזה כבר
לא נכון — מ-009 הן נכתבות מקומית-תחילה. `tier1` נסרק **בלי** מחסום
`pending()`, ולכן פינוי שלהן היה מוחק בשקט הגדרה או פריט רשימה שנרשמו
אופליין. **אין להחזיר אותן.**
`pending()` מחזירה `pendCount() > 0` — יש משהו שטרם עלה ⇒ שלב ב מדולג לגמרי.


---

<!-- SHARED:start id="iron-rule-6-sync" -->
## ⭐ כלל ברזל 6 — שיטת הסנכרון בארגון (סבב 12 שלב 4, אוגוסט 2026)
**הפרק הזה, עד שורת «כאן:» שבסופו, זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את חמשת כללי
הברזל שלמעלה.

> **שיטת הסנכרון בארגון היא דחיפת מצב מלא + מיזוג ברמת רשומה. אין תורים.
> כל רשומה שטרם אושרה בענן מסומנת למשתמש.**

עד הסבב הזה חיו כאן שלוש שיטות שונות: hanhala ו-yoman דחפו מצב מלא, gius
החזיקה תור יוצא פר-פעולה, ו-schar-limud לא עבדה אופליין כלל. מעכשיו — אחת.

### למה דווקא דחיפת מצב
- **אין פריט שיכול "להיתקע" לבדו.** בתור FIFO, פעולה שנכשלה כשל תוכן חוסמת
  את כל מה שאחריה, או מסומנת `dead` ונשארת תלויה לנצח. בדחיפת-מצב כל רשומה
  עומדת בפני עצמה, וכל סנכרון מתקן את מה שחסר — כולל מה שנכשל בפעם הקודמת.
- **אין מצב ביניים.** אין תור שיכול להתפגם, להתמלא או לצאת מסנכרון מול
  הנתונים עצמם. מצב האמת הוא הרשומות, ותו לא.
- **התאוששות אוטומטית.** מכשיר שאיבד את הסימונים עדיין מסתנכרן נכון:
  ההשוואה היא בין העותק המקומי לענן, לא בין יומן פעולות למציאות.

### חמשת הכללים
1. **עותק מקומי מלא של כל הנתונים, כולל tombstones.** רשומה מחוקה **חייבת**
   להישמר עם `deleted=true` + חותמת. היעדר רשומה אצל צד אחד **אינו** מחיקה.
2. **קריאה מהעותק בעליית האפליקציה, לפני כל נגיעה ברשת.**
3. **כתיבה מקומית-תחילה.** הכתיבה נשמרת במכשיר ומוצגת מיד; הרשת היא עניין
   של הרקע. כשל כתיבה מקומית עוצר ברעש ולא מציג "נשמר".
4. **סנכרון = משיכה ← מיזוג ← דחיפת מה שמקומי-וחדש-יותר.** בסדר הזה: אי
   אפשר לדעת מה לדחוף בלי לראות קודם מה בענן, ודחיפה עיוורת מחזירה לחיים
   רשומות שנמחקו במכשיר אחר. **דוחפים רק לקטגוריה שנמשכה בהצלחה.**
5. **מיזוג ברמת רשומה לפי חותמת עדכון**, עם שובר-שוויון דטרמיניסטי (הענן
   מנצח בשוויון), כך ששני מכשירים שממזגים את אותם צדדים מגיעים לאותה תוצאה.

### ⚠️ הכלל שמונע אובדן עריכה — רשומה מסומנת ⏳ מנצחת במיזוג
בשלוש מהאפליקציות החותמת נקבעת **בצד השרת** בטריגר `BEFORE UPDATE`. המשמעות:
ברגע שמכשיר א' דוחף, השורה בענן מקבלת חותמת **מאוחרת יותר** מעריכה חדשה יותר
שיושבת אצל מכשיר ב' וטרם עלתה — ומכשיר ב' היה מאמץ את גרסת א', כלומר **מוחק
את העריכה שלו בשקט**. זה לא תרחיש תיאורטי; הוא נתפס בבדיקה.

לכן, בשני מקומות ולא באחד:
- **במיזוג** — רשומה מקומית מסומנת ⏳ מנצחת ללא תלות בחותמת.
- **בבחירת מה לדחוף** — רשומה מסומנת נחשבת תמיד "לדחיפה". בלי זה היא הייתה
  נשארת מסומנת לנצח ולעולם לא נדחפת.

**המחיר המודע:** מכשיר שהיה מנותק זמן רב ידרוס בעלייתו עריכה מרוחקת חדשה
יותר. זו העדפה מכוונת — הרשומה מסומנת ⏳ כל אותו זמן ומתריעה אחרי 24 שעות,
בעוד שעריכה שנמחקת בשקט אינה משאירה עקבות. אותה עדיפות כמו "היעדר רשומה
אינו מחיקה". **אין להסיר את הבדיקה הזו משני המקומות.**

### ⛔ אין תורים
`g_outbox` הוסר (gius, שלב 3) ולא ייבנה מחדש. `ys_offline_queue` בהנהלה נשאר
בינתיים כמנגנון פר-מפתח kv **בלבד** ואינו תור פעולות; אין להרחיב אותו.
**אין ליצור תור פעולות חדש בשום אפליקציה בארגון.**

### ⛔ מה לא נדחף לעולם
רשומה שהעותק המקומי שלה **חלקי** אינה ניתנת לדחיפת-מצב: הדחיפה הייתה
כותבת את מה שחסר כערך ריק. המקרה השכיח הוא **סיסמה**: היא מוסרת
מהמראה כדי שלא תשב בטקסט גלוי על הדיסק, ולכן אין מה לדחוף.
שני מקרים קיימים היום, ושניהם מתועדים במקומם — `g_users` ב-gius
(`strip: ['password']`) ו-`sl_settings.admin_pass` ב-schar-limud
(`slStripSecrets`). **שינוי סיסמה דורש רשת, בשתי האפליקציות.**

⚠️ **החריגה היא ברמת השורה ולא ברמת הטבלה.** כל שאר השורות של
`sl_settings` נדחפות כרגיל (מ-`migrations/009`). וכשהסוד מוסר מהמראה —
**אסור ששער הכניסה ייפול חזרה לברירת המחדל.** בדיקה כמו
`p !== (SETTINGS['admin_pass'] || 'admin')` הופכת בדיוק ברגע ההסרה
לשער שנפתח לכל מי שמקליד «`admin`». ההשוואה חייבת להיות מול טביעה
נגזרת (PBKDF2) או מול הענן — **ולהיכשל סגור כשאין אף אחת מהן.**

### מזהים
כל רשומה חדשה מקבלת **מזהה שנוצר במכשיר** לפני שראתה שרת — uuid v4.
בטבלאות עם `SERIAL` (schar-limud) זהו `client_id` לצד ה-`id` של המסד, והכתיבה
היא `upsert` עליו. זה מה שהופך ניסיון חוזר אחרי תשובה שאבדה ברשת
לאידמפוטנטי, במקום ליצור שורה שנייה — בכסף, תשלום כפול.
<!-- SHARED:end -->

**כאן:** `MIRROR`/`slMerge`/`slDirtyRows`/`slPushTable`, מפתח מיזוג
`c:<client_id>` או `i:<id>`, ושני מסלולי כתיבה (`update` לפי `id` לרשומות
היסטוריות, `upsert` על `client_id` לרשומות שנוצרו במכשיר).

---

## ⭐ סבב 13 (אוגוסט 2026, `sw.js` v18, `migrations/009`) — ההגדרות והרשימות נכנסו לאופליין
**הפער האחרון בשכבת האופליין נסגר.** מסבב 12 שלב 2 ועד כאן, `sl_settings`
ו-`sl_lists` היו **ממורות לקריאה בלבד**: 008 נתנה `updated_at` לשתי טבלאות
הליבה בלבד, ובלי חותמת אין במה להשוות. הטופס עבד אופליין (אמצעי התשלום
והשיעורות נטענו מהמראה), אבל **עריכה** — שינוי שכר לימוד ברירת מחדל, הוספת
אמצעי תשלום, מחיקת שיעור — נחסמה ב-`guardOnline`.

### `migrations/009` — ✅ הורצה ואומתה
שלושה דברים, ולכל אחד סיבה נפרדת:
1. **`updated_at` + טריגר** (`sl_settings_touch`, `sl_lists_touch`) — חותמת
   המיזוג. אותו דפוס בדיוק כמו 008, כולל הכלל שהטריגר דורס **ב-UPDATE
   בלבד**: ב-INSERT החותמת שהמכשיר קבע ביצירה נשמרת.
2. **`client_id` + אינדקס ייחודי מלא** — `sl_lists.id` הוא SERIAL, ולכן
   פריט שנוצר אופליין חסר זהות עד שהוא מגיע לשרת.
   ⛔ **מלא ולא חלקי** — זו בדיוק התקלה של 006 ש-007 תיקנה. אינדקס חלקי
   שובר את הסקת `ON CONFLICT` ומחזיר `42P10`. **אין ליצור אינדקס חלקי על
   `client_id` בשום מיגרציה עתידית.**
3. **`deleted`/`deleted_at`/`deleted_by` ל-`sl_lists`** — בלי tombstone,
   פריט שנמחק במכשיר אחד חוזר אליו מהענן בסנכרון הבא, שוב ושוב (כלל ברזל 6
   סעיף 1). `sl_settings` **אינה מקבלת אותן בכוונה**: שורותיה הן זוגות
   מפתח/ערך שנכתבים ונקראים ולעולם לא נמחקים, ואין נתיב מחיקה בקוד.

### ⚠️ מפתח המיזוג של `sl_settings` הוא `key`, לא `client_id`
`slKeyOf(m,row)` מחזירה `k:<key>` להגדרות ו-`slKey(row)` לשאר.
זה לא קישוט: המפתח הראשי של `sl_settings` הוא **טקסט שהמכשיר יודע לייצר
לבדו** (`'default_tuition'`), ושני מכשירים מגיעים אליו באופן עצמאי. מיפתוח
לפי `client_id` היה יוצר **שתי שורות לאותה הגדרה** — שני ערכים לאותו מפתח,
ו-`slApplyMirror` בוחרת ביניהם לפי סדר מקרי. מאותה סיבה `slPushRow` שולחת
להגדרות `upsert` על `{onConflict:'key'}` ולא את פיצול שני המסלולים.
העמודה `client_id` נוספה ל-`sl_settings` לסימטריה סכימתית בלבד.

### ⛔⛔ `admin_pass` — הסיסמה אינה יורדת לדיסק בטקסט גלוי
`sl_settings` מחזיקה את סיסמת שער ההגדרות. שמירה נאיבית של הטבלה למראה
הייתה כותבת אותה **בטקסט גלוי** ל-localStorage, באותו origin שבו חיות עוד
שלוש אפליקציות. הפתרון זהה ל-`strip:['password']` על `g_users` בגיוס:
- **`slStripSecrets`** מסירה את השורה **בנקודת הכניסה**, לפני המיזוג. היא
  לא ממוזגת, לא נשמרת, לא נדחפת — ואין מסלול שבו היא מחליקה פנימה.
  `slLocalWrite` גם מסרבת לה במפורש, כהגנה שנייה.
- **`slApplyMirror`** מסננת אותה גם מ-`SETTINGS` שבזיכרון.

**⚠️⚠️ ולכן היה חייב להשתנות גם שער הכניסה, ולא רק האחסון.** הבדיקה הישנה
הייתה `p !== (SETTINGS['admin_pass'] || 'admin')` — וברגע שהשורה נעלמת
מהמראה היא הופכת ל**שער שנפתח לכל מי שמקליד «admin»**. הסרת הסוד בלי
לשנות את הבדיקה הייתה מחליפה חשיפה בפרצה. במקומה:
- `unlockSettings` משווה מול **טביעת PBKDF2-SHA256, 100,000 סיבובים**,
  ששמורה ב-`sl_admin_pass_h` ומתעדכנת בכל משיכת הגדרות מוצלחת.
- אין טביעה (מכשיר שטרם סנכרן, או `crypto.subtle` חסר) ⇒ בדיקה **מול
  הענן**; אין רשת ⇒ **השער לא נפתח** והמשתמש מקבל הסבר.
- הטביעה נשמרת **רק אחרי סיסמה נכונה** — אחרת ניחוש שגוי היה קובע את מה
  שהמכשיר מאמין לו מכאן ואילך.
- **`changeAdminPass` הוא החריג היחיד שנשאר עם `guardOnline`** — לסיסמה
  אין ייצוג מקומי ולכן אין לה מסלול מיזוג.

### מה עוד השתנה בקוד
- `guardOnline` **הוסר** מ-`saveDefaultTuition`, `addListItem`
  ו-`deleteListItem`; שלושתן כותבות ל-`MIRROR` ומחזירות מיד, ומסמנות ⏳.
- `deleteListItem` היא **soft-delete**. הגנת סעיף «זוכה על חשבון יתרת זכות»
  נשמרה כפי שהייתה (חסימה לפני האישור).
- `ensureCreditMethod` עברה מ-`.insert()` ל-`slLocalWrite` + `client_id`
  (כלל 10) — `insert` ישיר היה יוצר שורה שנייה בכל ניסיון חוזר.
- `pendSyncScan` רצה עכשיו **רק כשכל ארבע הקטגוריות נמשכו**. היא מורידה
  סימון מכל מה שאינו ברשימת ה"עדיין ממתין", וקטגוריה שלא נמשכה אינה
  מיוצגת שם — כלומר כשל אחד מארבעה היה הופך את הורדת הסימון להסקה.
- `LS_CFG.tier1` **התרוקן** — ר' הפרק «מה מותר לפנות».

### 🐛 באג שנמצא ותוקן אגב הסבב — כפתורי המחיקה לא עבדו כלל
מאז סבב 12 שלב 2 `deleteTxn`/`deleteStudent` מחפשות לפי **מפתח המיזוג**
(`slKey(x) === String(key)`), אבל שלושת אתרי הקריאה בהמשך העבירו את ה-`id`
הגולמי: `onclick="deleteTxn(5)"` מול `slKey` שמחזיר `'i:5'`. ההשוואה לעולם
לא התאימה, ולכן **כל לחיצה על מחיקת תשלום או תלמיד נפלה בשקט** (או הציגה
"התלמיד לא נמצא"). שלושת האתרים מעבירים עכשיו `esc(slKey(...))` כמחרוזת.
אותה תקלה הייתה נולדת ב-`sl_lists`, ולכן `LISTS` מחזיקה מעכשיו `key`
(מפתח המיזוג) לצד `id`, ו-`findListItem`/`deleteListItem` עובדות לפיו.

### בדיקה
**76 טענות** על הקוד האמיתי המחולץ מ-`index.html` (`_test_009.js`, בסשן):
`slKeyOf` לארבע הטבלאות · `slStripSecrets` (כולל טוהר) · סירוב `slLocalWrite`
לשורת סוד · `slApplyMirror` מסננת סוד ו-tombstones · `slMerge` עם `keyOf`
(מקומי/ענן/שוויון/⏳/שני `client_id` לאותו `key`) · `slDirtyRows` ·
ניתוב `slPushRow` בשלושת המסלולים · `slPayload` · PBKDF2 (דטרמיניזם, פורמט,
**ואימות שהסיסמה אינה מופיעה באף מפתח localStorage**) · `unlockSettings`
בשישה תרחישים כולל «admin» שאינו פותח ואופליין שנכשל סגור · מסלול מלא של
הוספה+מחיקה אופליין · הגנת סעיף הזיכוי · `saveDefaultTuition` · באג מפתחות
המחיקה · `tier1` ריק. כל השערים עברו: `node --check` על שני הקבצים +
`sync-setup-sql.py --check`.


---

<!-- SHARED:start id="round-14-verified-sweep" -->
## ⭐ סבב 14 (אוגוסט 2026) — עדות סנכרון חלופית לפינוי היזום
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

### הבעיה שנמדדה
שלב ב של הפינוי (רשומות ישנות מסונכרנות) מותנה ב**עֵד סנכרון מקומי** —
חותמת שנכתבת רק אחרי דחיפה מוצלחת מהמכשיר הזה (`_tbPushedAt` ביומן,
`_ysPushedAt` בהנהלה). **בדפדפן שקורא ולא כותב** — כרום שבמחשב המנהל, שרק
מסתכלים בו — אין דחיפה, ולכן אין עֵד, ולכן שלב ב מדולג לגמרי: הפינוי מחזיר
0 וההתרעה הצהובה («מעל 3MB») נשארת קבועה, בזמן שכל הנתונים כבר בענן.
**נמדד בפועל: 3.06MB / 61%, ופינוי שהחזיר 0.**

### התיקון — מחליפים את מקור הראיה, לא מרפים מהדרישה
כשאין עֵד מקומי (או שהוא 0), שלב ב עובר ל**אימות ישיר מול הענן**: משיכת
העותק הענני של המפתח המועמד לפינוי, ומחיקה של רשומה **רק** אם היא נמצאת שם
לפי מזהה **וגם** חותמת הענן אינה ישנה מהחותמת המקומית (הענן מחזיק את הגרסה
הזו או חדשה ממנה).

**ה-API החדש במודול המשותף:**
`lsGlobalWitness` · `lsSpecWitness` · `lsVerifySpecs`/`lsHasVerifiers` ·
`lsCloudIndex` · `lsPruneKeyVerified` · `lsSweepNeed` · `lsSweepVerified` ·
`lsScheduleDeferred`/`lsBootDeferred` · `lsBootAlert`.

**שני שדות חדשים ב-spec של `LS_CFG.tier2`:**
- `idOf(rec)` — מזהה הרשומה, אותו מפתח שבו משתמש מנוע המיזוג.
- `verify()` — מחזירה `Promise` של `{ok:true, rows:[...]}` מהענן.
  `spec.ts` הקיימת היא שגוזרת את החותמות משני הצדדים.

### ⛔ ארבעה כללים שאין לשנות
1. **נכשל סגור.** אין רשת, `verify` זרקה, החזירה `ok:false`, או שהמפתח כלל
   אינו קיים בענן ⇒ **לא מפנים דבר**. היעדר ראיה אינו ראיה. (ההגנה משוכפלת
   בשלוש שכבות: בדיקת התשובה, `lsCloudIndex` שמחזירה `null` על קלט שאינו
   מערך, ו-`lsPruneKeyVerified` שיוצאת על `idx` ריק.)
2. **רצפות הגיל (`minAgeMs`) נשארות כפי שהיו** — 60/90 יום ביומן, 180 בהנהלה.
   הן שכבת בטיחות **נפרדת** מהראיה ולא תחליף לה: רשומה מאומתת בענן שעדיין
   בתוך הרצפה אינה מפונה.
3. **`LS_CFG.pending()` נבדק גם כאן**, בדיוק כמו בשלב ב הסינכרוני: יש משהו
   שטרם עלה ⇒ לא נוגעים ברשת ולא מפנים דבר.
4. **⛔⛔ פינוי רצף בלבד — עוצרים ברשומה הראשונה שלא אומתה, לא מדלגים עליה.**
   זו הנקודה המסוכנת בסבב, והיא **לא** אופטימיזציה: אחרי הפינוי נשמר
   `hz` = חותמת הרשומה החדשה ביותר שנמחקה, ו-`lsSetArray` מסננת **בכל
   כתיבה** כל רשומה שחותמתה ≤ `hz`. דילוג על רשומה לא-מאומתת והמשך מעליה
   היה מציב אופק **מעליה**, וכתיבת המערך הבאה הייתה מוחקת אותה בשקט —
   דווקא את הרשומה היחידה שאין לה עותק בענן. (מאומת: מוטציה שהחליפה
   `break` ב-`continue` הפילה שלוש טענות, כולל האימות ש-`lsSetArray`
   מוחקת בפועל את היתומה.)

### ⚠️ אסינכרוני — ולכן גם ההתרעה נדחית
`lsBoot` סינכרונית ורצה **לפני** שהמסך עולה, ואילו האימות דורש רשת. לכן:
- `lsBoot` מבצעת כרגיל את שלב א ואת שלב ב הסינכרוני, ואם עדיין מעל הסף
  **ויש מפתחות שניתן לאמת** — היא מתזמנת `lsBootDeferred()` (3 שניות)
  **ודוחה איתו את ההתרעה**. הצגתה בעלייה הייתה אזהרה צהובה על מצב שנפתר
  שתי שניות אחריה.
- `lsBootDeferred` מריצה את האימות, מודדת מחדש, ורק אז קוראת ל-`lsBootAlert`.
  אין מה לפנות או אין רשת ⇒ ההתרעה כן מוצגת, וזה הנכון.
- `lsBoot` מחזירה עכשיו `{usage, swept, deferred}`, ומקבלת `opts`
  אופציונלי (`noDefer`, `deferMs`) — לבדיקות ולנתיבים שאסור לדחות בהם.
- `lsSweepNow()` (הכפתור בהגדרות) מחזירה **Promise**: היא מריצה את שני
  המסלולים ומדווחת אחרי שניהם.
- מסך «מידע טכני ›» (הגדרות) מציג אילו מפתחות תלויים באימות מול הענן.

### תיאום גרסאות
`sw.js`: hanhala **v29** · yoman **v18** (`app-version` **12-2026-08-10-1**) ·
schar **v19** · gius **gius-v6**.

### המודול המשותף — גודל חדש
הבלוק שבין `/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף` הוא כעת **691 שורות**, sha1
`22c1fb96b0ea` (היה 474). **זהה בית-לבית בארבע האפליקציות** — כל שינוי בו
חייב להיעשות בארבעתן באותו סבב.
<!-- SHARED:end -->

### בדיקה
⚠️ **פרק פרטי — מתאר את מה שבאמת רץ באפליקציה הזו** (כלל ברזל 8, סעיף 5).
עד סבב 18 ישב כאן נוסח אחד משוכפל לארבעת הריפו; הוא תיאר תצורה שקיימת
רק בהנהלה וביומן, ולכן היה שקרי בשתי האחרות. **אין ליישר אותו מריפו אחר.**

**כאן לא הוגדרו `idOf`/`verify` ו-`tier1`/`tier2` נשארו ריקים** (ר' «מה
נעשה כאן» למטה) — ולכן אין באפליקציה הזו פינוי מאומת, אין מבחן קבלה של
פינוי, ואין מה להריץ מולו. מה שכן נבדק כאן:
- **זהות בית-לבית של המודול המשותף** מול שלוש האחיות — הבלוק שבין
  `/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
  `/* ═══════════════ סוף המודול המשותף`, 691 שורות, sha1 `22c1fb96b0ea`.
- **שערי הדחיפה:** `node --check` על ה-JS המחולץ מ-`index.html` ועל `sw.js`,
  ו-`python3 tools/sync-setup-sql.py --check`.

חבילת 84 הטענות (`test_verify.js` + `harness.js`) רצה מול התצורות של
**hanhala-ruchanit ושל yoman-avoda** — שתי האפליקציות היחידות שיש להן
מפתחות לאימות מול הענן. ⛔ אין להעתיק את תיאורה לכאן.

### מה נעשה כאן (schar-limud) — המודול בלבד, המדיניות נשארת ריקה
המודול המשותף עודכן במלואו (כלל «זהה בית-לבית»), אבל **`tier1` ו-`tier2`
נשארים ריקים ואין כאן אף `verify`** — וזו החלטה, לא השמטה:
- ⛔ `sl_mirror_transactions` הם **כספים**. אימות מול הענן מוכיח שהרשומה
  שם, אבל **אינו הופך רשומה כספית לניתנת לפינוי**: פינוי שלה מסתיר תנועה
  מהמסך אופליין, וחישוב חוב על נתון חסר גרוע מכל מכסה.
  `sl_mirror_students` הם אבות המפתח הזר שלהם ולכן גם הם בחוץ.
- ⛔ `sl_mirror_settings`/`sl_mirror_lists` נכתבות מקומית-תחילה מאז 009
  (ר' הפרק «מה מותר לפנות» בסבב 12 שלב 2). **אין להחזיר אותן ל-`tier1`.**
- **המשמעות המעשית, ובמפורש:** schar אינה מפנה מקום. אם הדומיין מלא, היא
  זו שתתריע בלי לפתור — הפתרון הוא yoman ו-hanhala, שכן מפנות, ולא צמצום
  העותק המקומי של הכסף. `lsBoot` כאן מחזירה `deferred:false` ומתריעה מיד,
  כפי שהיה.


---

## 🪟 מדיניות «מראה-חלון» לטבלאות התנועות — עיגון להמשך (תיעוד בלבד)
**הפרק הזה אינו מתאר קוד שנכתב.** הוא מעגן החלטה, כדי שסשן עתידי שיתבקש
«לחסוך מקום» יידע בדיוק מה מותר לו לעשות — ובשכר לימוד התשובה היא **כלום**.

### המצב היום — ואין לשנות אותו נקודתית
`sl_mirror_students` ו-`sl_mirror_transactions` **אינן מפונות לעולם**
במתכונת הנוכחית. הנימוקים כתובים בקוד עצמו, ליד `LS_CFG`, ולא הומצאו כאן:
- **תנועות הן כספים.** רשומה שפונתה מהדיסק אינה נראית אופליין, וחישוב חוב
  על נתון חסר גרוע מכל מכסה.
- **תלמידים הם אבות המפתח הזר של התנועות.** פינוי שלהם משאיר תנועות בלי
  שם מוצג.
- אימות מול הענן (סבב 14) **אינו משנה את זה**: הוא מוכיח שהרשומה בענן,
  ולא שמותר להסתיר אותה מהמסך.

### 🚫 הכלל המבצעי לסשן עתידי
**אף סשן אינו מוסיף פינוי לשתי הטבלאות האלה — גם לא פינוי «בטוח לכאורה».**
זה כולל: `tier1`/`tier2` חדשים, `idOf`/`verify` עליהן, אופק, «רק תנועות
של לפני שנתיים», «רק תלמידים לא-פעילים» וכל וריאציה אחרת. הבקשה «חסוך
מקום כאן» היא **בקשה לפתוח את סבב מראה-החלון** שלמטה, לא לאלתר טלאי.
פינוי חלקי מסוכן יותר מאי-פינוי: הוא מסתיר כסף בלי שאף שכבה יודעת על כך.

### התוכנית העתידית — דפוס «מראה-חלון» פר-שנת-לימודים
כשטבלאות התנועות יגדלו, הכיוון **אינו** פינוי לפי גיל אלא **חלון מוגדר**:
- **במכשיר:** כל הישויות החיות (תלמידים, הגדרות, רשימות) + **תנועות שנת
  הלימודים הנוכחית בלבד**.
- **בענן:** שנים סגורות במלואן. הן נשלפות לפי דרישה כשמסתכלים עליהן,
  ולא יושבות על הדיסק.
- **טקס סגירת-שנה:** מעבר מפורש ומתועד שמעביר שנה מ«נוכחית» ל«סגורה» —
  לא גלישה שקטה לפי תאריך.

### ⛔ זה סבב תכנון שלם — אסור לממש חלקית
החלון אינו שינוי ב-`LS_CFG` בלבד. **הוא חייב לכלול באותו סבב גם את
התאמת הדחיפה והמשיכה לגבולות החלון**, אחרת נוצר בדיוק המצב שכל שכבת
הסנכרון בנויה למנוע:
- **דחיפה** שאינה מודעת לחלון תקרא «אין לי את התנועה הזו» כ«היא נמחקה»
  ותמחק בענן שנה שלמה של כסף.
- **משיכה** שאינה מודעת לחלון תחזיר את השנים הסגורות לדיסק בסנכרון הבא,
  והחלון יהיה חסר משמעות — בדיוק הלקח של «אופק הפינוי» מסבב 11.
ולכן: או שהסבב מבוצע במלואו — מדיניות + דחיפה + משיכה + טקס הסגירה +
בדיקות — או שלא נוגעים.

### מתי לפתוח אותו
**הטריגר הוא המוקדם מבין השניים:**
1. **סוף שנת הלימודים הראשונה** שנצברה כאן במלואה, או
2. **חציית כמה מאות KB** בטבלאות התנועות המקומיות (נמדד בהגדרות ←
   «מידע טכני», פילוח לפי מפתח).

עד שאחד מהם מתקיים — הנתונים נשארים במלואם על המכשיר, וזו ההתנהגות הנכונה.

---

## סבב תאומים (אוגוסט 2026, `sw.js` v20) — ערכת אייקונים ירוקה
נבנה בענף `claude/twin-round-hanhala-schar-green-4lujzd`, בשני הריפו יחד
(בהנהלה: מעטפת android/ + מפתח קבוע + workflow; כאן: הצביעה הירוקה).

- **המקור הגרפי היחיד של הארגון הוא קובץ המאסטר שבריפו hanhala-ruchanit** —
  `טונקל בלוי מיט ווייסן הינטערגרונט.png` (1024×1024, דיו `#18335c` על לבן).
  המאסטר נצבע כאן לירוק **`#307535`** באלפא-דיו: `alpha=(255-L)/(255-L_ink)`
  קטום ל-0..1 (L = בהירות הפיקסל, L_ink = בהירות הדיו), והירוק הורכב על לבן.
- **`icons/master-green-1024.png` = התוצר, והוא המקור לגזירות עתידיות כאן** —
  אין צורך לחזור לריפו הנהלה בשביל אייקון ירוק חדש.
- ממנו נגזר ב-LANCZOS אותו סט בדיוק כמו בהנהלה: `icon-512` / `icon-192` /
  `apple-touch-icon` (180) / `favicon-32` / `favicon-16` — חמשת הקבצים
  ב-`icons/` הוחלפו — וכן **`icons/icon-maskable-512.png`** (הלוגו ב-72%
  במרכז קנבס לבן). **אומתה התאמה מבנית ≥99.9% בין הסט הירוק לסט הנייבי
  בכל גודל** (אלפא מנורמל פר-דיו, סף 0.5) — אותה גיאומטריה, רק צבע.
- **manifest.json:** האייקונים המלאים פוצלו ל-`purpose:"any"` בלבד וה-maskable
  נוסף כ-`purpose:"maskable"` נפרד («any maskable» על אייקון מלא = קרניים
  נחתכות בעיגול — אין לחזור לזה). `theme_color` עודכן מהזהב הישן `#c9a84c`
  ל-**`#307535`**, וכך גם `<meta name="theme-color">` ב-`index.html`.
- **mipmap במעטפת `android/` הוחלף לירוק** (launcher מלא על לבן + foreground
  דיו שטוח על שקוף, לוגו ב-66% — אותה תבנית כמו קודם), כך שה-APK הבא
  מה-workflow ייצא ירוק. (כפתור «נסה שוב» בדף השגיאה של המעטפת נשאר בזהב —
  שינוי מעטפת נפרד; לא נגענו בקוד Java בסבב הזה.)
- `sw.js` קודם v19→v20 (אין כאן מנגנון `app-version` — הוא של yoman; הרענון
  הוא דרך `CACHE_NAME`).

---

<!-- SHARED:start id="round-15-tech-info" -->
## ⭐ סבב 15 (אוגוסט 2026) — קיפול מסכי האחסון ל«מידע טכני»
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

מסך «💾 אחסון מקומי» בהגדרות קופל מלוח-מחוונים למידע טכני צנוע. העיקרון:
המנהל צריך לדעת שהכל מתנהל מושלם מאחורי הקלעים, בלי להתבלבל ממספרים,
גרפים וכפתורים. **שינוי תצוגה בלבד — המודול המשותף של סבב 14 לא נגוע**
(אומת: הבלוק שבין סמני המודול זהה בית-לבית ל-HEAD בארבעתן, sha1 `18c08fcc4b67`).

- **אין יותר כפתורי פעולה באזור האחסון.** «🧹 פנה מקום עכשיו», «↻ רענן»
  ו-«☁️ טען הכל מחדש מהענן» אינם מוצגים — מוסתרים ב-CSS
  (`.tech-info .ls-status button{display:none}`); ה-HTML שלהם עדיין נוצר
  ע"י `lsStatusHTML` שבמודול הקפוא, ולכן המודול לא נערך. **המנגנון
  האוטומטי הוא הדרך היחידה לפינוי.** `lsSweepNow`/`lsRestoreAll` קיימות
  אך אינן נגישות מה-UI.
- **האבחון נעשה דרך רכיב «מידע טכני ›»** — שורה מוקטנת בתחתית מסך
  ההגדרות, סגורה כברירת מחדל (`<details class="tech-info">`). בפתיחה
  `techInfoToggle` קורא ל-`lsRenderStatus`, והתוכן — ניצולת, פילוחים,
  אזהרות האופק/האימות ויומן הפינוי — מרונדר לתוך `#ls-status-box` שחי
  עכשיו בתוך הרכיב. **התוכן לא השתנה, רק המיקום.**
- **שקט הוא ברירת המחדל** — במקום המסך הישן לא מוצג דבר.
- **חריג יחיד ומכוון:** מעל `LS_CRIT_BYTES` (4MB) — מצב שהמנגנון האוטומטי
  לא הצליח למנוע — `techInfoHTML` מציג שורת מצב עדינה (`.ls-crit-line`)
  גם מחוץ ל«מידע טכני». זו רשת הביטחון היחידה שמוצגת ביוזמת המערכת.
  (באנרי המודול עצמו — `#ls-alert`, התרעות lsBootAlert — לא נגעו.)
- **אחידות:** בלוק ה-JS (`techInfoHTML`/`techInfoToggle`/`techInfoMount`,
  יושב מיד אחרי «סוף המודול המשותף») ובלוק ה-CSS זהים בית-לבית בארבעתן
  (sha1 `72fc844f885e` / `f74f1f7510e7`). כל שינוי בהם — בארבע באותו סבב.
- מסך «⏳ ממתין לסנכרון» **לא נגוע** — הוא אינו ממסכי האחסון.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud, `sw.js` v21)
כרטיס «💾 אחסון מקומי» הוסר מ-`settings-main`; `<div id="tech-info-box">`
נוסף בתחתית המסך, אחרי כרטיס הסעיפים; `renderSettingsLists()` קורא
`techInfoMount()` במקום `lsRenderStatus()`. (המסך עדיין מאחורי שער סיסמת
המנהל — כמו כל ההגדרות כאן.)

---

<!-- SHARED:start id="round-16-pend-delay" -->
## ⭐ סבב 16 (אוגוסט 2026) — יישור ניסוחים + השהיית ציור ה-⏳
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

### חלק א — יישור אזכורים תפעוליים אחרי קיפול סבב 15 (תיעוד בלבד)
אפס שינוי התנהגות. כלל ההבחנה שהופעל:
- שורות הכפתורים בתוך `lsStatusHTML` שבמודול האחסון הקפוא — **לא נגעו**
  (חלק מהמודול, מוסתרות ב-CSS, מתועד בסבב 15).
- תיאורים היסטוריים בפרקי הסבבים — **לא נגעו**. היסטוריה נשארת היסטוריה.
- אזכורים **תפעוליים בהווה** של מסך «💾 אחסון מקומי» הישן יושרו
  ל-«הגדרות ← מידע טכני ›»: הערת מיגרציית השרידים ב-`index.html` של יומן,
  ובארבעת ה-CLAUDE.md — פרק אופק הפינוי (כפתור «☁️ טען הכל מחדש מהענן»
  אינו נגיש מה-UI מאז סבב 15 — נוסח מחדש: המנגנון `lsRestoreAll` קיים
  במודול, וניקוי אופק ייעשה בסבב ייעודי אם יידרש), כלל ברזל 5 (חיווט
  אפליקציה חדשה: `techInfoMount()` במקום `lsRenderStatus()`), שורת «אילו
  מפתחות תלויים באימות» בפרק סבב 14, ותיאורי המיקום בסעיפי «מה נעשה כאן».

### חלק ב — השהיית ציור ⏳ (שינוי UI בלבד, בארבעתן)
עד היום כתיבה מקומית ציירה ⏳ מיידית, והדחיפה המוצלחת מחקה אותו — באונליין
תקין זה היה הבהוב של שבריר שנייה על כל שמירה. מעכשיו הציור מאחר
**`PEND_DRAW_DELAY_MS` = 2000 מ״ש**: הגיע אישור הענן לפני — ה-⏳ לא מופיע
כלל; לא הגיע (אופליין, רשת איטית/חולה) — הוא מופיע וממשיך להתנהג בדיוק
כמו היום.

⚠️ **זו השהיית *ציור* בלבד — ארבעה גבולות קשיחים:**
1. **הסימון הלוגי נכתב ל-localStorage מיידית וללא שינוי** (`pendMark`
   ומקביליו) — הוא חלק ממנגנון הבטיחות: הפינוי, בחירת מה לדחוף והמיגרציות
   נשענים עליו. ההחזקה (`_pendDrawHold`) חיה **בזיכרון בלבד** ואינה נשמרת.
2. **מכשיר שנסגר בתוך שתי השניות** — בפתיחה הבאה ה-⏳ מוצג מיד: ההשהיה
   חלה רק על סימון שנוצר הרגע בכתיבה חדשה, לא על סימון שנטען מהדיסק.
3. **מסך «⏳ ממתין לסנכרון» בהגדרות והמונים הלוגיים** (`pendCount`/`pendHas`/
   `pendSince`/`pendStatusHTML`) מציגים תמיד את האמת הלוגית המלאה, בלי
   השהיה. גם סרגל `#pend-bar`, כשהוא מוצג, מציג את המונה הלוגי המלא —
   ההשהיה קובעת רק **אם** מציירים, לא **מה** המספר.
4. **מימוש אחיד בארבעתן** — אותן פונקציות עזר במודול המשותף
   (`pendHoldDraw` · `pendDrawSince` · `pendDrawCount` · `pendDrawFlush` ·
   `pendScheduleFlush`), אותו קבוע. ההתאמה הפר-אפליקציתית היא שדה
   **`PEND_CFG.redraw`** בלבד: בתום ההחזקה, אם הסימון עדיין קיים,
   `pendDrawFlush` מרנדר מחדש את הרשימות שמציירות `pendTag` — כדי שהתגית
   תיכנס לשורות שכבר רונדרו בלעדיה. אושר בינתיים ⇒ אין redraw ואין רינדור
   מיותר.

### המודול המשותף — גודל חדש
הבלוק שבין `/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"` הוא כעת **328 שורות**, sha1
`a6fbdd2c73ff` (היה 267 / `2b3d6b779b1f`). **זהה בית-לבית בארבע
האפליקציות** — כל שינוי בו חייב להיעשות בארבעתן באותו סבב. מודול האחסון
הקפוא של סבב 14 **לא נגוע** (אומת: הבלוק זהה בית-לבית ל-HEAD בארבעתן).
<!-- SHARED:end -->

### בדיקה
**28 טענות** על הקוד האמיתי המחולץ מ-`index.html` (`test_pend_delay.js`,
בסשן, עם שעון ו-`setTimeout` מדומים): כתיבה+אישור מהיר ⇒ הסימון הלוגי
מיידי ב-localStorage, אפס תגית, אפס סרגל, אפס redraw · כתיבה בלי אישור ⇒
תגית וסרגל אחרי 2ש׳ בדיוק (ב-1.9ש׳ — עדיין לא) עם redraw יחיד · סימון
שנטען מהדיסק ⇒ ציור מיידי · עירוב ותיק+טרי ⇒ הסרגל מציג את המונה הלוגי
המלא · `pendMarkMany`/`pendClearMany` מנקים החזקות · `pendReload` מאפס
החזקות (החלפת מוסד). וכן `node --check` על ה-JS המחולץ ועל `sw.js`
בארבעתן, `sync-setup-sql.py --check` ב-schar ו-`scripts/check-js.mjs`
ב-gius.

### מה נעשה כאן (schar-limud, `sw.js` v22)
`PEND_CFG.redraw` מרנדר מחדש את שלושת מציירי התגיות: `renderTxnLog` (יומן
התשלומים), **`renderScAnnual`** (רשימת התנועות שבכרטיס התלמיד — בכוונה לא
`renderStudentCard`, שמאפס גם את שדות העריכה של הכרטיס ולכן היה מוחק
הקלדה באמצע), ו-`renderSettingsLists` (תלמידים ורשימות בהגדרות).

---

<!-- SHARED:start id="iron-rule-7-status-area" -->
## ⭐ כלל ברזל 7 — רכיב משותף = קוד זהה + מיקום זהה + חיווט חי (סבב 17, אוגוסט 2026)
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את ששת כללי
הברזל שלמעלה.

> **רכיב שמוכרז «משותף» חייב שלושה דברים, לא אחד: אותו קוד, באותו מקום
> במסך, ומחובר לקוד חי. תיעוד לבדו אינו אוכף אף אחד מהם — ולכן יש בדיקה
> אוטומטית שרצה עם שערי התחביר לפני כל דחיפה.**

### הבעיה שנמדדה
סבב 15 קיפל את מסכי האחסון לרכיב «מידע טכני ›» בארבע האפליקציות, והתיעוד
הכריז שהבלוק זהה בית-לבית. הבלוק אכן היה זהה — **וכל השאר לא**:

| | מסך «⏳ ממתין לסנכרון» | «מידע טכני ›» |
|---|---|---|
| schar-limud | **ראש** מסך ההגדרות | סוף המסך ✅ |
| yoman-avoda | **אמצע** מסך ההגדרות | סוף המסך ✅ |
| hanhala | **אמצע** מודול המערכת | **אמצע**, לפני ההרשאות |
| gius | כרטיס «☁️ מצב סנכרון» נפרד | נבנה בכל render() — **`techInfoMount` הייתה קוד מת** |

כלומר: קוד זהה, שלוש חוויות שונות, ובאחת מהן הרכיב לא היה מחובר בכלל.
בגיוס הייתה לזה גם תוצאה גלויה — מסך ההגדרות נבנה מחדש בכל `render()`,
העוגן חזר ריק, ו«מידע טכני» **נסגר בכל שמירה**.

### ההגדרה המחייבת — «אזור מצב»
בתחתית מסך (או מודול) ההגדרות של כל אפליקציה בארגון יושבים **תמיד, ובסדר
הזה, שני האלמנטים האחרונים** — חשופים, לא עטופים בכרטיס של האפליקציה:

1. **`<div id="sync-status-box">` — בלוק «☁️ סנכרון».** מציג את מסך ה-⏳
   הקיים (`pendStatusHTML`, **בלי שינוי בתוכנו**) ומתחתיו שורת מצב תמציתית:
   מצב חיבור, מועד הסנכרון המוצלח האחרון, וכפתור «🔄 סנכרן עכשיו».
2. **`<div id="tech-info-box">` — רכיב «מידע טכני ›».** האלמנט האחרון במסך.

שניהם מורכבים בקריאה אחת — **`statusAreaMount()`** — וזו נקודת החיווט
היחידה. מסך הגדרות שאינו קורא לה הוא מסך בלי אזור מצב, והבדיקה נכשלת.

### שלושת הבלוקים המשותפים
זהים בית-לבית בארבע האפליקציות, וכל שינוי בהם נעשה בארבעתן באותו סבב:

| בלוק | סמנים | חתימה (sha256, 16 תווים) |
|---|---|---|
| CSS אזור המצב | `/* ═══ אזור מצב — CSS משותף (סבב 15/17)` … `/* ═══ סוף CSS אזור המצב` | `aa10b52e9ce1157e` |
| «מידע טכני» | `/* ═══ "מידע טכני" — קיפול מסכי האחסון` … `/* ═══ סוף רכיב "מידע טכני"` | `83096d1117e813f0` |
| «☁️ סנכרון» | `/* ═══ אזור מצב — בלוק "☁️ סנכרון"` … `/* ═══ סוף בלוק "☁️ סנכרון"` | `65ff4f75bc4f6756` |

**ה-API:** `syncFmtTime` · `syncLineHTML` · `syncStatusHTML` ·
`syncStatusMount` · `syncNowFromStatus` · `statusAreaMount` ·
`techInfoHTML`/`techInfoToggle`/`techInfoMount`.

מעל הבלוק יושבת **`SYNC_CFG`** — הדבר היחיד שנבדל בין האפליקציות — עם שני
שדות בלבד: `lastSync()` (חותמת הסנכרון המוצלח האחרון) ו-`sync()` (מה
שהכפתור מפעיל, מחזיר Promise).

### ⚠️ שלוש הבהרות שאין לפספס
1. **הכפתור «סנכרן עכשיו» אינו מנגנון סנכרון.** הסנכרון האוטומטי ממשיך
   לרוץ ברקע בדיוק כמו קודם; זו רק דרך לבקש אותו עכשיו ולראות תשובה. לכן
   גם **אין בבלוק טוסט כשל משלו** — מסלול הסנכרון של כל אפליקציה כבר מדווח
   על כשל פעם אחת, וכפילות הייתה רעש.
2. **`lastSync()` אינו עֵד דחיפה ואינו משמש לפינוי.** הוא חותמת תצוגה,
   שנכתבת על כל שיחה מוצלחת עם הענן — כולל בדפדפן שרק קורא. לפינוי יש
   `_ysPushedAt`/`_tbPushedAt`/`syncedThrough` (סבבים 11 ו-14), **ואין
   לערבב ביניהם.** ⛔ אין להזין `LS_CFG.syncedThrough` מ-`lastSync()`.
3. **הבלוק אינו נשען על מחלקות הכרטיס של אף אפליקציה** — הן נבדלות ביניהן
   — אלא מרנדר חלונית משלו (`.sync-box`), וה-CSS יורש צבע וגופן. זה מה
   שמאפשר לו להישאר זהה בית-לבית.

### מצב הפתיחה של «מידע טכני» חי בזיכרון
`_techInfoOpen` (משתנה מודול, **לא** localStorage) שומר אם הרכיב פתוח.
בלעדיו, אפליקציה שבונה את מסך ההגדרות מחדש בכל רינדור מקבלת עוגן ריק,
ה-`details` הקודם כבר לא קיים, והרכיב נסגר בכל שמירה ובכל ציור מושהה של
⏳ (סבב 16). המצב בזיכרון בכוונה: זו העדפת תצוגה של הרגע, ולא נתון שראוי
לצרוך ממנו מכסת אחסון משותפת.

### הודעת חזרת הסנכרון — נוסח אחד
**`MSG_SYNC_BACK = '☁️ הסנכרון חזר לפעול'`** בארבע האפליקציות, לצד
`MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN`. הוא החליף את «☁️ החיבור לנתונים
חזר» ב-schar-limud וב-gius, ונוסף ל-yoman שלא הייתה לה הודעת התאוששות
בכלל. ⛔ **אין להשתמש במחרוזת קשיחה** — הבדיקה נכשלת עליה.

### הבדיקה האוכפת
`tools/check-status-area.mjs` (ב-gius: `scripts/`) — **זהה בית-לבית
בארבעת הריפו פרט לבלוק `APP` שבראשו**, ורצה עם שערי התחביר לפני כל דחיפה.
היא נכשלת על שלושת סוגי הסטייה:
- **א. מיקום** — `sync-status-box` ו-`tech-info-box` אינם שני ה**ילדים
  הישירים** האחרונים של מיכל ההגדרות, בסדר הזה. ההשוואה היא על ילדים
  ישירים דווקא: עוגן שיושב בתוך כרטיס היה עובר בדיקה רופפת («שני המזהים
  מופיעים בסוף») גם כשמשהו אחר מוצג אחריו. ב-gius, שבונה את המסך כמחרוזת,
  הבדיקה היא על זנב `viewSettings()`.
- **ב. קוד** — אחד משלושת הבלוקים אינו תואם לחתימה שבטבלה למעלה.
- **ג. חיווט** — `statusAreaMount()` אינה נקראת בשום מקום מחוץ לבלוק
  המשותף, כלומר הרכיב קיים אך אינו מחובר. זו בדיוק התקלה של gius.
  בנוסף נכשלת על שריד `id="pend-status-box"` מחוץ לאזור המצב.

**שינוי מכוון בבלוק משותף** = עדכון הבלוק בארבע האפליקציות **ובארבעת
עותקי קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב.

### תיאום גרסאות
`sw.js`: hanhala **v33** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v23** · gius **gius-v9**.
<!-- SHARED:end -->

### בדיקה
**60 טענות** על הקוד האמיתי המחולץ מ-`index.html` של ארבע האפליקציות
(`test_status_area.mjs`, בסשן — שני הבלוקים המשותפים רצים ב-`vm` מעל DOM
מזויף שמדמה את החוזה שהקוד נשען עליו, כולל `ontoggle` שנורה גם על פתיחה
תכנותית): שורת המצב בשלושת מצביה (טרם הסתנכרן / מועד אמיתי / מנותק) ·
`pendStatusHTML` **לפני** שורת המצב · שני העוגנים החסרים כ-no-op ·
«סנכרן עכשיו» שנועל בזמן ריצה ולא מפעיל סנכרון כפול · `statusAreaMount`
שמרכיב סנכרון→טכני בסדר הזה · ⭐ **מצב פתוח ששורד רינדור מלא של העוגן**
(התרחיש של gius) וסגירה שנשמרת גם היא · `MSG_SYNC_BACK` · וזהות בית-לבית
של שלושת הבלוקים ושל קובץ הבדיקה.

אומת גם ב-**מוטציות**: היפוך סדר העוגנים, אלמנט שנוסף אחריהם, עוגן שנעטף
בכרטיס, שינוי בבלוק המשותף והסרת החיווט החי — כל אחת מפילה את
`check-status-area.mjs`; והחזרת מצב הפתיחה ל-DOM בלבד מפילה את טענת
ה-⭐. `node --check` על ה-JS המחולץ ועל `sw.js` עבר בארבעתן, וכן
`sync-setup-sql.py --check` ב-schar ו-`scripts/check-js.mjs` ב-gius.
המודולים הקפואים לא נגעו — בלוק האחסון (סבב 14) ובלוק ה-pending (סבב 16)
זהים בית-לבית ל-HEAD בארבע האפליקציות.

### מה נעשה כאן (schar-limud, `sw.js` v23)
- מסך «⏳ ממתין לסנכרון» ישב ב**ראש** מסך ההגדרות והועבר לאזור המצב;
  `<div id="sync-status-box"></div>` נוסף לפני `tech-info-box`, ושניהם
  ילדים ישירים של `settings-main`. `renderSettingsLists()` קורא
  `statusAreaMount()` במקום `pendRenderStatus()` + `techInfoMount()`.
- **`pendRenderStatus` הוסרה** (וכך גם העוגן `pend-status-box`) — תוכן מסך
  ה-⏳ עצמו לא נגע, הוא מוגש מתוך `#sync-status-box`.
- `SYNC_CFG`: `lastSync` = `_slLastPullOk` (נכתב רק כשכל ארבע הקטגוריות
  נמשכו בהצלחה), `sync` = `syncAll()`.
- הטוסט «☁️ החיבור לנתונים חזר» הוחלף ב-`MSG_SYNC_BACK`.
- הבדיקה: `node tools/check-status-area.mjs` — **שער חובה נוסף לפני push**,
  לצד `node --check` ו-`sync-setup-sql.py --check`.

---

<!-- SHARED:start id="iron-rule-8-docs" -->
## ⭐ כלל ברזל 8 — פרוטוקול תיעוד קבוע (סבב 18, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שבעת כללי הברזל שלמעלה.

> **התיעוד עצמו הוא רכיב משותף, ולכן חלים עליו אותם תנאים שכלל ברזל 7
> קבע לקוד משותף: אותו נוסח, במקום קבוע, ומסומן במפורש. פרק שאינו מסומן
> «משותף» הוא פרטי לאפליקציה — ואין לגעת בו על סמך נוסח מריפו אחר.**

### הבעיה שנמדדה
ביקורת חיצונית סרקה את ארבעת קבצי ה-CLAUDE.md ומצאה עשרות פרקים שאמורים
להיות זהים. רובם אכן היו — אבל שום מנגנון לא אכף את זה, ולכן ארבעה נסחפו
בלי שאיש שם לב. באחד מהם הסחיפה הייתה **הפוכה**: פרק «בדיקה» של סבב 14
הועתק כלשונו לארבעת הריפו, כולל לשתי אפליקציות שבהן הבדיקות שהוא מתאר
מעולם לא רצו ולא יכלו לרוץ (אין בהן `verify` ואין `tier2`). תיעוד
שמועתק «כי כך כתוב אצל האחות» גרוע מתיעוד חסר: הוא נקרא כעדות.

### ההגדרה המחייבת — סימון משותף/פרטי
כל פרק שאמור להיות זהה בארבעת הריפו עטוף בסימוני HTML-comment:

```html
<!-- SHARED:start id="שם-מזהה-קצר" -->
…
<!-- SHARED:end -->
```

**הסימון הוא מקור האמת:**
- מה שבתוך הסימון — **חייב** להיות זהה בית-לבית בארבעת הריפו.
- מה שמחוצה לו — **פרטי לאפליקציה**, ואיש אינו מיישר אותו.

הסימון עוטף **רצף** של פרקים, לא בהכרח פרק בודד: פרק סבב שכולו משותף
פרט לזנב פר-אפליקציה (למשל שורת «**כאן:**» שבסוף «מזהים», או «מה נעשה
כאן») נחתך בדיוק שם — הרצף המשותף בפנים, הזנב בחוץ.

### שבעת הכללים
1. **כל סבב מוסיף פרק סבב במקום קבוע ובמבנה קבוע** — בסוף הקובץ, אחרי
   פרק הסבב הקודם, עם שלושת התת-פרקים ובסדר הזה: **מה נעשה** (משותף) ·
   **מה נעשה כאן** (פר-אפליקציה) · **אימותים** (פר-אפליקציה).
2. **כל סבב מקדם את שורת «עודכן לאחרונה»** שבראש הקובץ, בפורמט הקבוע
   `עודכן לאחרונה: סבב N · YYYY-MM-DD`. גם סבב שנגע רק בקובץ אחד מקדם
   אותה בקובץ שבו נגע.
3. **סבב שנוגע בקוד משותף חייב לעדכן את הפרק המשותף בארבעת הריפו באותו
   סבב** — לעולם לא באחד בלבד. פרק מסומן SHARED שמשתנה, משתנה בארבעתם,
   באותו שינוי בדיוק, ובאותו קומיט-סבב.
4. **⛔ אסור לסשן לשנות תוכן של פרק שאינו מסומן SHARED על סמך נוסח מריפו
   אחר.** זה בדיוק מה שייצר את באג «84 הטענות»: טקסט נכון באפליקציה אחת
   שהועתק לשלוש שבהן הוא שקרי. אם נראה שפרק פרטי «חסר» משהו שיש לאחות —
   זו אינדיקציה לבדוק את **המציאות באפליקציה הזו**, לא להעתיק.
5. **«בדיקה» ו«חתימת APK» הם פרקים פרטיים בהגדרה.** «בדיקה» מתאר מה
   באמת רץ באפליקציה הזו ולכן הוא נבדל בהכרח; «חתימת APK» מחזיק
   keystore, alias וטביעת SHA ייחודיים — **איחוד שלו יהיה שגוי ומסוכן**,
   ולכן הבדיקה נכשלת אם הוא סומן SHARED.
6. **סבב שכותב בדיקות שומר אותן כקובץ בריפו.** רתמה שרצה בסשן בלבד
   נעלמת ברגע שהסשן נסגר — הסשן הבא אינו יכול להריץ אותה, לא לפני שינוי
   ולא אחריו, ולכן היא אינה מגינה על דבר.
   **⛔ ובפרט: אין לתעד בדיקות כאלה כאילו הן קיימות.** «N טענות עברו»
   בפרק סבב, בלי קובץ שאפשר להריץ, נקרא כעדות שאינווריאנטה שמורה — בזמן
   שאין שום דבר ששומר עליה. זה בדיוק הכשל של סעיף 4, בכיוון אחר: תיעוד
   שמתאר מציאות שאיננה.
   ⚠️ הכלל חל על **בדיקות** — טענות שניתן להריץ מחדש. סריקה חד-פעמית או
   מדידה שנעשתה כדי להכריע החלטה אינן בדיקות; הן נרשמות כמדידה, ואין
   להציג אותן כשער.
7. **⛔ אין לזרוע פרטי כניסה — שם משתמש, סיסמה, מפתח, טוקן — בשום קובץ
   שנדחף לגיט.** לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ
   בדיקה. ארבעת הריפו **ציבוריים**, ומפתחות ה-anon של Supabase יושבים
   ב-`index.html` — כלומר סיסמה שנדחפה היא סיסמה שכל אחד יכול להשתמש בה
   מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותה.** הערך נשאר קריא בהיסטוריית ה-git
   לנצח, ומי שכבר מצא אותו מחזיק בו. הטיפול היחיד בסוד שנדחף הוא
   **להחליף אותו במסד**, ורק אחר כך לנקות את הקובץ.
   המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר איך — עם מצייני מקום
   ולא עם ערכים אמיתיים.

### הבדיקה האוכפת
`tools/check-docs.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא לה) —
**זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם שערי
התחביר לפני כל דחיפה. היא נכשלת על חמישה סוגי סטייה:
- **א. סימון** — `SHARED:start`/`SHARED:end` אינם מאוזנים, מקוננים, בלי
  `id`, עם `id` כפול, או שרשימת המזהים בקובץ אינה תואמת לרשימה הקנונית.
- **ב. תוכן** — תוכנו של בלוק SHARED אינו תואם לחתימת ה-sha256 הקנונית
  הרשומה בקובץ הבדיקה.
- **ג. שורת העדכון** — «עודכן לאחרונה» חסרה, אינה בראש הקובץ, או בפורמט
  שגוי.
- **ד. פרק פרטי שסומן** — «חתימת APK» או «בדיקה» נמצאים בתוך בלוק
  SHARED, בניגוד לסעיף 5.
- **ה. שורת עדכון שלא קודמה** (סבב 20) — `CLAUDE.md` שונה מול
  `origin/main` אבל שורת «עודכן לאחרונה» זהה לזו שב-`origin/main`.
  סעיפים א–ד בודקים את **צורת** השורה; זה בודק שהיא באמת **קודמה**.
  ⚠️ **הסעיף הזה מדלג ואינו מפיל** כשאין git, כשאין `origin/main`, או
  כש-`CLAUDE.md` אינו קיים שם — ר' פרק סבב 20.

סימונים שבתוך גדר קוד (```) אינם נספרים — אחרת הדוגמה שבפרק הזה עצמו
הייתה נקראת כבלוק אמיתי.

**שינוי מכוון בפרק משותף** = עדכון הפרק בארבעת הריפו **ובארבעת עותקי
קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב. אותו נוהל בדיוק שכלל
ברזל 7 קבע לבלוקי הקוד המשותפים.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-18-what" -->
## ⭐ סבב 18 (אוגוסט 2026) — תיעוד אחיד ואכיפה

### מה נעשה
**סבב תיעוד וכלי בדיקה בלבד.** `index.html`, `sw.js` וגרסאות המטמון
**לא נגעו באף אחת מארבע האפליקציות**; התוספת היחידה מחוץ ל-CLAUDE.md היא
קובץ הבדיקה החדש והקריאה אליו בשער הדחיפה.

1. **יישור הפרקים שנסחפו.** מדידה של ארבעת הקבצים מצאה **46 כותרות
   משותפות לארבעתם, מהן 40 זהות בית-לבית ו-6 שנסחפו** (שלוש מהן הן
   שלושת מופעי «בדיקה»). הטיפול:
   - **«בדיקה» (סבב 14)** — 🔴 החמור מכולם. הנוסח «84 טענות» היה זהה
     בארבעת הריפו, אבל התצורה שהוא מתאר — `tier2` + `idOf` + `verify` +
     מבחן הקבלה — קיימת רק ב-hanhala וב-yoman. ב-schar-limud וב-gius
     סבב 14 היה **עדכון המודול בלבד**, ו-`tier1`/`tier2` נשארו ריקים
     במפורש ומנומק. הפרק נכתב מחדש בכל אפליקציה כך שיתאר את מה שבאמת
     רץ בה, **והוכרז פרטי** (כלל ברזל 8, סעיף 5).
   - **«מזהים»** — הכלל עצמו כבר היה זהה בית-לבית; מה שנבדל הוא שורת
     «**כאן:**» שבסופו, שהיא פר-אפליקציה מעצם טבעה. הכלל סומן SHARED
     והשורה נשארה מחוצה לו, ומשפט הפתיח של כלל ברזל 6 עודכן בהתאם.
   - **«כללי ענפים»** — ב-gius היה נוסח מקוצר (שתי נקודות במקום ארבע),
     כי הוא נכתב מחדש כשנוסף מנקה הענפים ואיבד את שתי הנקודות על אישור
     המיזוג ועל מחיקת ענפים מרוחקים. אוחד לנוסח הקנוני בן ארבע הנקודות.
   - **«חתימת APK — מפתח קבוע»** — ⛔ **לא אוחד ולא ייאוחד.** לכל
     אפליקציה keystore, alias וטביעת SHA משלה, ואיחוד כאן היה שגוי
     ומסוכן. הפרק פרטי בהגדרה, והבדיקה נכשלת אם יסומן SHARED.
2. **סימון משותף/פרטי** — עשרה בלוקי `SHARED` בכל קובץ, שעוטפים יחד
   43 פרקים: `branch-rules` · `iron-rules-storage` · `pending-module` ·
   `iron-rule-6-sync` · `round-14-verified-sweep` · `round-15-tech-info` ·
   `round-16-pend-delay` · `iron-rule-7-status-area` · `iron-rule-8-docs` ·
   `round-18-what`.
3. **שורת «עודכן לאחרונה»** נוספה בראש כל אחד מארבעת הקבצים.
4. **כלל ברזל 8** — פרוטוקול התיעוד הקבוע (הפרק שמעל).
5. **`check-docs.mjs`** — הבדיקה האוכפת, בדפוס `check-status-area.mjs`
   של סבב 17, ונוספה לשער הדחיפה בארבעת הריפו.

**משפטי הפתיח של פרקי הסבבים תוקנו לפי הגבול האמיתי** של כל בלוק: סבבים
14/16/17 אומרים עכשיו «עד הסעיף «בדיקה»» (ולא «עד הסעיף «מה נעשה כאן»»,
שהיה כולל בתוכו את «בדיקה» ולכן היה מקור הבאג); פרק «⏳ סימון ממתין
לסנכרון» אומר «עד הסעיף «מה נעשה כאן»»; וכלל ברזל 6 אומר «עד שורת
«כאן:» שבסופו».
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud)
- «כללי ענפים» כבר היה בנוסח הקנוני — לא נגע, רק סומן.
- **«בדיקה» של סבב 14 נכתב מחדש.** הנוסח הישן תיאר פינוי מאומת ומבחן קבלה
  שמעולם לא רצו כאן: `tier1` ו-`tier2` ריקים בשכר לימוד **בכוונה** —
  `sl_mirror_transactions` הם כספים ו-`sl_mirror_students` הם אבות המפתח
  הזר שלהם — ולכן אין `idOf`, אין `verify`, ואין מה לאמת. הפרק אומר את זה
  במפורש ומפנה את קורא-העתיד לתצורות של hanhala ושל yoman.
- עשרה בלוקי SHARED סומנו. **«חתימת APK — מפתח קבוע»** (`signing/schar.keystore`,
  alias `schar`, SHA256 `29:32:D9:B5:…`) נשאר **מחוץ** לסימון, כמתחייב.
- `tools/check-docs.mjs` נוסף, וקריאה אליו נוספה לפרק «לפני כל push» —
  שער חובה רביעי לצד `node --check`, `sync-setup-sql.py --check`
  ו-`node tools/check-status-area.mjs`.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v23** (סבב 17),
  ואין מיגרציה חדשה — 005 עדיין היחידה שלא הורצה, ובכוונה.

### אימותים
- **`check-docs.mjs` עובר בארבעת הריפו:** שורת העדכון, עשרה בלוקי SHARED
  מאוזנים, כל עשר החתימות תואמות, ופרקי «חתימת APK» ו«בדיקה» מחוץ לבלוקים.
- **הזהות נמדדה בפועל, לא הוכרזה** — עשרת הבלוקים חושבו בארבעת הקבצים
  והושוו זה לזה: זהים. וקובץ הבדיקה עצמו זהה בארבעת הריפו פרט לשורת
  ה-`app` שבבלוק `APP` (אומת ב-`diff`).
- **הבודק אינו חותמת גומי — תשע מוטציות, כל אחת מפילה אותו:**
  1. שינוי מילה בתוך בלוק SHARED («ענף ייעודי» ← «ענף נפרד») ⇒ חתימה שונה.
  2. הסרת `SHARED:end` אחד ⇒ קינון + בלוק חסר + חתימה שגויה.
  3. הסרת שורת «עודכן לאחרונה» ⇒ חסרה.
  4. סימון SHARED על «חתימת APK» ⇒ מזהה לא-קנוני **וגם** פרק פרטי בתוך בלוק.
  5. `id` כפול ⇒ כפילות + בלוק חסר.
  6. `SHARED:start` בלי `id` ⇒ סימון פסול + `end` בלי `start`.
  7. פורמט שגוי בשורת העדכון (`12.8.2026`) ⇒ פורמט.
  8. שורת העדכון שהוזזה לאמצע הקובץ ⇒ אינה בראש הקובץ.
  9. סימון SHARED על «בדיקה» — **הבאג שהסבב הזה בא למנוע** ⇒ נכשל.
  ובקרה חיובית: הדוגמה שבתוך גדר הקוד בפרק כלל ברזל 8 **אינה** נספרת
  כבלוק, והבדיקה עוברת על המסמך כמות שהוא.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק בארבעת הריפו.**
- שאר השערים הורצו ועברו ללא שינוי: `node --check` על ה-JS המחולץ ועל `sw.js`,
  `python3 tools/sync-setup-sql.py --check`, ו-`node tools/check-status-area.mjs`.

---

## ⭐ סבב 19 (אוגוסט 2026, `sw.js` v24) — סיסמה בת שש ספרות

⚠️ **פרק פרטי — אינו מסומן `SHARED`.** הסבב נגע ב-schar-limud וב-gius בלבד
(«אל תיגע בהנהלה וביומן»), הוא אינו נוגע באף בלוק קוד משותף, ולכן אין לו
פרק «מה נעשה» משותף. **⛔ אין ליישר אותו מריפו אחר** — נוסח gius מתאר
מסלולים אחרים (`saveUser`/`pass-save`) ומצב אבטחה אחר לגמרי.

> **הערת רקע:** הסעיף הזה נכלל כבר בפרומפט של סבב 18 ולא בוצע, מפני שסבב 18
> נתפס כסבב תיעוד בלבד («`index.html` ו-`sw.js` לא נגעו»). כאן הנגיעה
> ב-`index.html` היא הכוונה המפורשת.

### מה נעשה כאן
- **ארבעת שדות הסיסמה קיבלו `inputmode="numeric"` ו-`maxlength="6"`** —
  `au-pass` (כניסה), `set-pass-input` (שער ההגדרות), `set-new-pass`
  ו-`set-new-pass2` (שינוי סיסמת מנהל). `type="password"` **לא שונה** באף
  אחד מהם: המיסוך הוא ההגנה מפני מבט מהצד, ו-`inputmode` מביא את המקלדת
  המספרית בלי לוותר עליו.
- **`PASS_SIX_RE = /^[0-9]{6}$/` ו-`MSG_PASS_SIX = 'הסיסמה חייבת להיות שש
  ספרות'`** נוספו ליד `SL_SECRET_SETTINGS`. ⛔ **הם לא נוספו לבלוק
  ההודעות המשותף** (`MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN`/`MSG_SYNC_BACK`,
  סבב 8) — הבלוק ההוא מוכרז «מילה במילה זהה» בארבעת הפרויקטים, והוספת קבוע
  אליו בשניים מהם בלבד הייתה יוצרת בדיוק את הסחיפה שכלל ברזל 8 בא למנוע.
- **הוולידציה נוספה ב-`changeAdminPass` בלבד** — מסלול היצירה/שינוי היחיד
  שיש כאן. (אין בשכר לימוד מסך ניהול משתמשים; `sl_users` נזרעת ב-SQL.)
  הסדר: ריק ⇐ «נא להזין סיסמה» · לא שש ספרות ⇐ `MSG_PASS_SIX` · אי-התאמה
  ⇐ «הסיסמאות אינן תואמות».

### ⛔⛔ הוולידציה אינה במסלולי הכניסה — וזה לא פספוס
**`doLogin` ו-`unlockSettings` בודקים התאמה בלבד, ולא פורמט.** אכיפת פורמט
שם הייתה **נועלת בחוץ סיסמה קיימת ותקפה** שנקבעה לפני הסבב הזה — משתמש
שסיסמתו `admin` היה מקבל «הסיסמה חייבת להיות שש ספרות» ולא היה יכול להיכנס
כלל, בלי שום מסלול שחזור.

ובשער ההגדרות יש גם סיבה שנייה, חזקה יותר: מאז סבב 13 ההשוואה שם היא מול
**טביעת PBKDF2 חד-כיוונית** (`sl_admin_pass_h`). אי אפשר לגזור מטביעה את
הפורמט של הסיסמה שממנה היא נוצרה — כלומר אין שום דרך לדעת מראש אם הסיסמה
השמורה היא בת שש ספרות. **אין להוסיף שם בדיקת פורמט בשום סבב עתידי.**

`maxlength="6"` על שדות הכניסה הוא **רמז קלט ולא אכיפה** — הוא מגביל הקלדה
חדשה, ומי שסיסמתו הקיימת ארוכה יותר עדיין יכול להדביק אותה. זו המגבלה
הנכונה: מכוונת את המשתמש לפורמט החדש בלי לחסום את הישן.

### מה **לא** נגע
⛔ `type="password"` · ⛔ מנגנון ה-PBKDF2 (`slHashPass`/`SL_PASS_SALT`/
`SL_PASS_ITER`/`slStripSecrets`) · ⛔ `guardOnline` שב-`changeAdminPass` ·
⛔ שלושת הבלוקים המשותפים ושני המודולים הקפואים · ⛔ הסכימה — **אין
מיגרציה חדשה**, 005 עדיין היחידה שלא הורצה, ובכוונה.

### אימותים
- **`node --check`** על ה-JS המחולץ מ-`index.html` ועל `sw.js` — עבר.
- **`python3 tools/sync-setup-sql.py --check`** · **`node tools/check-status-area.mjs`**
  (שלושת הבלוקים המשותפים + מיקום + חיווט חי) · **`node tools/check-docs.mjs`**
  — שלושתם עברו ללא שינוי.
- **בדיקת התנהגות על ה-`PASS_SIX_RE` האמיתי המחולץ מהקובץ** — 13 מקרים:
  `123456`/`000000` מתקבלים; `12345`, `1234567`, ריק, `12345a`, `abcdef`,
  `12 456`, ` 123456`, `+12345`, `12.456` נדחים; **`123456\n` נדחה**
  (ב-JS, בניגוד לפייתון, `$` בלי `m` אינו מתיר שורה חדשה נגררת);
  **`۱۲۳۴۵۶` (ספרות מזרח-ערביות) נדחה** — `[0-9]` ולא `\d` עם `u`.
- **אומת שמסלולי הכניסה נקיים:** `PASS_SIX_RE` **אינו מופיע** בגוף
  `doLogin` ואינו מופיע בגוף `unlockSettings`.
- אומת ששלושת עותקי `inputmode="numeric"`+`maxlength="6"` יושבים על ארבעת
  ה-`id` הנכונים.

---

<!-- SHARED:start id="round-20-what" -->
## ⭐ סבב 20 (אוגוסט 2026) — «עודכן לאחרונה» שקודמה באמת

### מה נעשה
**סבב כלי בדיקה ותיעוד בלבד.** `index.html`, `sw.js` וגרסאות המטמון
**לא נגעו באף אחת מארבע האפליקציות**; השינוי היחיד מחוץ ל-CLAUDE.md הוא
סעיף אחד שנוסף ל-`check-docs.mjs`.

סבב 18 דיווח בכנות על פער בבודק שהוא עצמו הוסיף, ואומת חיצונית שהפער
אכן קיים: הבודק ידע לוודא ש**קיימת** שורת «עודכן לאחרונה», שהיא בראש
הקובץ ושהפורמט תקין — כלומר את **צורת** השורה בלבד. סשן שערך פרק פרטי
והשאיר «סבב 18» עבר את השער בלי הערה, וזו בדיוק ההפרה של סעיף 2 בחמשת
הכללים שלמעלה.

**התיקון — סעיף ה בבודק:** אם `CLAUDE.md` שונה מול `origin/main` אבל
שורת «עודכן לאחרונה» זהה לזו שב-`origin/main` — הבדיקה נכשלת בהודעה
**«CLAUDE.md שונה ושורת העדכון לא קודמה»**. ההשוואה היא בין **הקובץ
שעל הדיסק** לבין `git show origin/main:./CLAUDE.md`, ולכן היא תופסת גם
שינוי שכבר קומט בענף וגם שינוי שטרם קומט — ואינה תלויה ב-`git status`.

שני מקרים שאינם כישלון, במכוון: **קובץ זהה ל-`origin/main`** (אין
שינוי, אין מה לקדם) ו**שורת עדכון שלא קיימת ב-`origin/main`** (היא
נוספת בשינוי הזה, כלומר קודמה מאין).

### ⚠️ מדלג ולא מפיל — היעדר בסיס להשוואה אינו סטייה
לסעיף ה, ולו בלבד, יש מסלול דילוג: **אין `git` בסביבה** · **אין
`origin/main`** · **`CLAUDE.md` אינו קיים ב-`origin/main`**. בכל אחד
מהם מודפסת אזהרה מפורשת (⚠️) עם הסיבה, **והשער אינו נופל** — clone
רדוד, ריפו בלי remote או ארכיון שחולץ מ-tarball הם מצבים לגיטימיים
בסביבת בנייה, ושער שנשבר בהם היה גורם לעקיפה של הבדיקה כולה. ארבעת
הסעיפים האחרים אינם מדלגים לעולם.

⛔ **אין להפוך את הדילוג לכישלון ואין להרחיב אותו** לשום מקרה אחר:
הראשון שובר סביבות בנייה, והשני מחזיר בדיוק את הפער שהסבב הזה סגר.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud)
- `tools/check-docs.mjs` — נוסף סעיף ה, ולוגיקת גדרות-הקוד חולצה
  ל-`fenceMask()`/`stampOf()` כדי ששני צדי ההשוואה (הדיסק ו-`origin/main`)
  יזהו את שורת העדכון באותה דרך בדיוק. שאר הסעיפים לא נגעו.
- שני בלוקי SHARED עודכנו: `iron-rule-8-docs` (חמישה סוגי סטייה במקום
  ארבעה) ו-`round-20-what` שנוסף — ושתי החתימות עודכנו בארבעת עותקי
  קובץ הבדיקה, באותו סבב.
- שורת «עודכן לאחרונה» קודמה מ-«סבב 19» ל-**סבב 20 · 2026-08-12**.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v24** (סבב 19),
  ואין מיגרציה חדשה — 005 עדיין היחידה שלא הורצה, ובכוונה.

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `python3 tools/sync-setup-sql.py --check`,
  `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **⭐ מוטציית הפער — הבדיקה שהסבב הזה נועד לה:** שינוי תוכן ב-`CLAUDE.md`
  בלי לקדם את שורת העדכון ⇒ השער **נפל** בהודעה «CLAUDE.md שונה ושורת
  העדכון לא קודמה». לפני הסבב אותה מוטציה בדיוק **עברה**.
- **מסלולי הדילוג נבדקו ואינם מפילים:** ריפו בלי `origin/main` ⇒ אזהרה
  ויציאה 0; `git` שאינו זמין ב-`PATH` ⇒ אזהרה ויציאה 0.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**


---

<!-- SHARED:start id="iron-rule-9-security-spread" -->
## ⭐ כלל ברזל 9 — שדרוג אבטחתי מתפשט (סבב 21, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שמונת כללי הברזל שלמעלה.

> **כשסבב מחליף מנגנון אבטחה באפליקציה אחת — טיפול בסודות, אימות, הצפנה,
> הרשאות — הוא חייב לרשום במפורש, בפרק הסבב, האם המנגנון החדש רלוונטי גם
> לשאר האפליקציות. רלוונטי ולא יושם ⇐ נרשם פער מתועד עם טריגר.
> שקט אינו תשובה.**

### הבעיה שנמדדה
סבב 13 העביר את schar-limud מהשוואת סיסמה גלויה ל**טביעת PBKDF2-SHA256**.
ההחלטה שם נכונה ומנומקת: מיגרציה 009 הורידה את `sl_settings` למראה המקומי,
ו-`admin_pass` בטקסט גלוי **על הדיסק** לא היה מתקבל על הדעת.

מה שלא נעשה: **לא נרשם באותו סבב אם המנגנון רלוונטי גם לשלוש האחיות.**
לא «כן והוחל», לא «לא, וזו הסיבה» — פשוט כלום. התוצאה היא ש-hanhala
ו-gius המשיכו שמונה סבבים עם מודל סיסמאות אחר לגמרי, **בלי שאיש החליט
על כך** — ההבדל התגלה רק בביקורת של סבב 21. תיעוד שותק אינו מתעד החלטה;
הוא מסתיר את היעדרה.

### שלוש התשובות המותרות — ורק שלוש
סבב שנגע במנגנון אבטחה חייב לרשום **אחת** מהן, במפורש:
1. **הוחל בכולן** — עם ציון מה השתנה בכל אחת.
2. **אינו רלוונטי לאחרות** — עם **הנימוק**, לא כהצהרה. «אין שם סוד על
   הדיסק» הוא נימוק; «לא נדרש» אינו.
3. **רלוונטי, ולא יושם** — ואז **פער מתועד עם טריגר**: מה חסר, מה הסיכון,
   ו**מה האירוע שמחייב לפתוח את הסבב** (ר' הדוגמה החיה: «הוספת משתמשים
   ל-gius» בפרק הפער שם).

⛔ **מה שאסור הוא הרביעית — לא לכתוב כלום.**

### ⚠️ הכלל דורש **רישום**, לא יישור — ואינו גובר על כלל ברזל 8 סעיף 4
הרישום נעשה **בפרק הפרטי של כל אפליקציה, לפי המציאות שלה**. כלל ברזל 9
**אינו** רישיון להעתיק נוסח מריפו אחר, ו**אינו** מחייב שהמנגנונים יהיו
זהים: אפליקציות שונות חיות במודלי איום שונים, ואחידות מנגנון אינה מטרה
בפני עצמה. שני הכללים אומרים יחד דבר אחד: **מה שנבדל — נבדל בכוונה
ובכתב.**

### ⛔ הלקח נרשם בארבעתן — גם כשהמנגנון רלוונטי רק לאחת (סבב 26)
**לקח אבטחתי שנלמד באפליקציה אחת נרשם כהערת ⛔ בארבע האפליקציות**, ליד
המקום בקוד שבו הוא עלול להישבר — ולא רק באפליקציה שבה הוא נלמד.

⚠️ **וזו אינה כפילות של שלוש התשובות שלמעלה.** הרישום שהן מחייבות הוא
**החלטה**: «הוחל» / «אינו רלוונטי, וזה הנימוק» / «פער עם טריגר», והוא
יושב בפרק הפרטי של כל אפליקציה. ההערה בקוד עונה על שאלה אחרת לגמרי —
**מה אסור לעשות כאן בעתיד.** אפליקציה שהמנגנון «אינו רלוונטי» לה עדיין
חשופה ל**כשל** שהוליד אותו: סשן עתידי יוסיף בתום לב בדיוק את הדבר
שהוסר במקום אחר, מפני שאצלו הוא מעולם לא היה — ולכן גם מעולם לא נכתב
אצלו שאסור.

> **הערה שיושבת במקום אחד אינה מגינה על הארגון.** היא מגינה על הקובץ
> שהיא יושבת בו, ועל שום קובץ אחר.

**הדוגמה החיה** היא הסבב שהוליד את הסעיף: שער הסיסמה הנפרד של מסך
ההגדרות הוסר מ-schar-limud (סבב 26), ובשלוש האחיות שער כזה לא היה
מעולם — כלומר לפי שלוש התשובות, «אינו רלוונטי». ובכל זאת ההערה נוספה
**בארבעתן**, בראש אזור ההגדרות: לכל אחת מהן יש מסך הגדרות, וכל אחת מהן
היא מקום שבו אפשר להמציא את השער הזה מחדש.

### ⛔ החלטת מודל אבטחה היא של המנהל, לא של הסשן
מנגנון שנבחר במפורש ע"י המנהל — כולל בחירה **לא** להקשיח — אינו «חוב
טכני» שסשן עתידי מוסמך לסגור מיוזמתו. סבב שמוצא פער מתועד **מדווח עליו
ומציע**; ההגירה עצמה מחייבת החלטה מפורשת. ר' הדוגמה החיה בפרקי הסיסמאות
של hanhala ושל gius: שם ההחלטה על טקסט גלוי היא **בחירה תפעולית מודעת**,
והמרתה ל-PBKDF2 בלי אישור הייתה **שוברת יכולת ניהול שהמנהל בחר בה בכוונה**.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-21-what" -->
## ⭐ סבב 21 (אוגוסט 2026) — לקחים תפעוליים, כלל ברזל 9, ושני תיקוני אבטחה

### מה נעשה
סבב מעורב: **תיעוד בארבעת הריפו, וקוד ב-hanhala-ruchanit בלבד.**
`index.html` ו-`sw.js` של yoman-avoda, schar-limud ו-gius **לא נגעו**,
ולכן גם `CACHE_NAME` שלהן לא קודם — קידום בלי שינוי קוד מייצר באנר
«גרסה חדשה זמינה» לשום שינוי.

1. **שני כללי תפעול נוספו ל«כללי ענפים»** (בלוק `branch-rules`): רענון
   `main` ב-`--ff-only` לפני כל מיזוג, ו-`git -C <path>` בכל פקודה בעבודה
   רב-ריפו. שניהם נלמדו מכשל שנמדד בסבב 20, ושניהם מתוארים שם במלואם.
2. **כלל ברזל 9 — שדרוג אבטחתי מתפשט** (הפרק שמעל).
3. **תיעוד ההחלטה על מודל הסיסמאות ב-hanhala וב-gius** — פרק **פרטי**
   בכל אחת מהן, שמעגן את הטקסט הגלוי בשש ספרות כ**בחירה תפעולית מודעת**
   לפי מודל האיום, ולא כפער שממתין לסגירה. זהו היישום הראשון של כלל
   ברזל 9: schar-limud היא החריגה, והנימוק שלה כתוב — סבב 13 הוריד שם
   את `sl_settings` למכשיר.
4. **hanhala — הסרת מסלול האתחול `admin`/`123456`** (פרטי, ר' שם).
5. **hanhala — צמצום `ys_users_cache` למשתמש המחובר בלבד** (פרטי, ר' שם).

### תיאום גרסאות
`sw.js`: hanhala **v34** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v24** · gius **gius-v10**. **רק hanhala קודמה** — היא היחידה שקודה
השתנה בסבב הזה.
<!-- SHARED:end -->
### מה נעשה כאן (schar-limud)
- **תיעוד בלבד.** בלוק `branch-rules` עודכן, ושני בלוקים חדשים נוספו:
  `iron-rule-9-security-spread` ו-`round-21-what`. שלוש החתימות עודכנו
  בארבעת עותקי `check-docs.mjs`, באותו סבב.
- שורת «עודכן לאחרונה» קודמה מ-«סבב 20» ל-**סבב 21 · 2026-08-12**.
- **כלל ברזל 9 — כאן נולד המנגנון, וכאן נרשם למה הוא נשאר יחיד.**
  ה-PBKDF2 של סבב 13 **לא הוחל על hanhala ועל gius, וזה נכון**: הוא נדרש
  כאן מסיבה שקיימת רק כאן — מיגרציה 009 הורידה את `sl_settings` **למראה
  המקומי**, ולכן `admin_pass` היה יורד לדיסק בטקסט גלוי, באותו origin שבו
  חיות עוד שלוש אפליקציות. באחיות הסוד יושב **בענן בלבד**, וזו החלטה
  תפעולית מודעת של המנהל (ר' הפרקים הפרטיים שם). ⛔ **אין להסיק מכאן
  שיש להגר אותן** — סעיף «החלטת מודל אבטחה היא של המנהל» בכלל ברזל 9.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v24** (סבב 19),
  ואין מיגרציה חדשה — 005 עדיין היחידה שלא הורצה, ובכוונה.

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `python3 tools/sync-setup-sql.py --check`,
  `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **שלושת הבלוקים החדשים/המעודכנים נמדדו כזהים בית-לבית בארבעת הקבצים** —
  חושבו בארבעתם והושוו זה לזה, לא הוכרזו.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**

---

## ⭐ סבב 24 (אוגוסט 2026, `sw.js` v25, `migrations/010`) — כניסה אופליין, הקשחת סינון, וניקוי ברירת מחדל

⚠️ **פרק פרטי — אינו מסומן `SHARED`.** הסבב נגע ב-schar-limud בלבד, ואינו
נוגע באף בלוק קוד משותף. ⛔ **אין ליישר אותו מריפו אחר** — הפרקים
המקבילים ב-hanhala (סבב 22) וב-gius (סבב 23) מתארים טבלאות אחרות, מסלולי
כתיבה אחרים ומודל הרשאות אחר.

### הרקע
בדיקה חיצונית של יכולות ארבע האפליקציות מצאה ש-schar-limud היא **היחידה
שנשארה בלי כניסה אופליין**: `doLogin` שלח שאילתה לענן
(`sl_users.select().eq('username').eq('password')`), ולכן בלי רשת אי אפשר
היה להיכנס כלל. זה היה חור בולט דווקא כאן — **כל שאר האפליקציה עובדת
אופליין במלואה מסבב 12 שלב 2**, כולל רישום תשלומים. מי שכבר נכנס פעם אחת
המשיך דרך `sl_session`, אבל מכשיר שמישהו אחר נכנס בו אחרון נעל בחוץ את כל
השאר. hanhala פתרה זאת בסבב 22 ו-gius בסבב 23, באותו מנגנון — והוא אומץ
כאן בהתאמה למבנה של schar.

### 1. `migrations/010` — ⏳ ממתינה להרצה
`pass_salt` + `pass_fp` (שתיהן `TEXT`) ל-`sl_users`. **אדיטיבית
ואידמפוטנטית**, ואינה נוגעת בנתונים קיימים. `000_initial_schema.sql`
עודכן גם הוא (כלל 7 — מקור אמת יחיד).
⛔ **`password` לא נגע** — נשאר טקסט גלוי בענן, במכוון: אין כאן מסך ניהול
משתמשים, והמנהל מנהל סיסמאות מלוח הבקרה של Supabase (כלל ברזל 9). הטביעה
**נוספת לצידו** ואינה מחליפה אותו. ⚠️ אין להסיק שהמסד עבר להצפנה.
**עד שהמיגרציה תורץ** האפליקציה עובדת בדיוק כמו קודם: כל שאילתה על
העמודות החדשות נופלת-חזרה ל-`id,username` (`slIsMissingFpCol`), והכניסה
המקוונת אינה נפגעת — מה שלא יעבוד הוא הכניסה האופליין, והיא אומרת זאת
במפורש («טרם הוכן לכניסה ללא רשת»).

### 2. הטביעה — PBKDF2-SHA256, 100k, מלח אקראי פר-משתמש
`slPassFp` / `slMakePassFp` / `slPassFields`, בדפוס `G_PASS_CTX` של gius.
**קידומת ההקשר `SL_PASS_CTX = 'schar-limud/sl_users/v1/'` נכנסת למלח** —
ה-origin `ygtotlrl-lab.github.io` משותף לארבע האפליקציות, ובלי הפרדת הקשר
טבלה מחושבת מראש עבור אחת מהן הייתה תקפה גם כאן. זו לא הערה תיאורטית.
הטביעה יורדת למכשיר; **הסיסמה לעולם לא**. ההשהיה של 100k היא מה שהופך
ניחוש-בכוח על העותק שבדיסק ליקר גם מול סיסמה בת שש ספרות (סבב 19).

**מסלול ההשלמה — `slEnsurePassFp`, רץ בכניסה מקוונת מוצלחת בלבד**, הרגע
היחיד שבו האפליקציה מחזיקה סיסמה נכונה של משתמש. ⚠️ **בכוונה אינו סורק
את שאר המשתמשים** (בשונה מ-gius, שם ההשלמה היא פעולת בעלים על כל הטבלה):
סריקה כזו הייתה מחייבת למשוך את הסיסמאות **הגלויות של כולם** לזיכרון
המכשיר, ואין כאן תפקיד «בעלים» שמצדיק זאת. כל משתמש משלים את הטביעה שלו
בכניסה המקוונת הראשונה שלו, ומאותו רגע נכנס באותו מכשיר בלי רשת.

⚠️ **שלושה מצבים, וההבדל ביניהם הוא ההבדל בין הגנה לפרצה:**
- הטביעה קיימת ותואמת ⇒ אין מה לעשות, אין קריאת רשת.
- אין טביעה ⇒ גוזרים וכותבים. כשל גזירה = **דילוג שקט** (אין טביעה ישנה,
  אין מה לאפס).
- **הטביעה קיימת ואינה תואמת** ⇒ הסיסמה שונתה מחוץ לאפליקציה (לוח הבקרה).
  הטביעה השמורה **תקפה לסיסמה הישנה**, כלומר פותחת כניסה אופליין בסיסמה
  שכבר בוטלה. כאן, ורק כאן, **כשל גזירה מאפס את שני השדות** דרך
  `slPassFields` — עדיף בלי כניסה אופליין מאשר כניסה אופליין בסיסמה
  מבוטלת. ⛔ אין להחליף את האיפוס בדילוג.
⚠️ אי-התאמה נקבעת **רק** כשהצלחנו לגזור טביעה. `slPassFp` שמחזירה `null`
(אין `crypto.subtle`) אינה ראיה לכלום — אז מדלגים ולא נוגעים בכלום.

### 3. מראת המשתמשים — `sl_mirror_users`
⛔ **`password` לעולם לא יורד למכשיר, וההגנה היא במבנה ולא בזהירות:**
- **רשימת היתר** (`SL_USER_COLS = ['id','username','pass_salt','pass_fp']`)
  ולא הסרה של עמודה אחת. רשימת-היתר נשארת נכונה גם כשתיווסף לטבלה עמודה
  רגישה חדשה; רשימת-איסור לא.
- **ה-`select` עצמו מבקש רק את ארבע העמודות** — הסיסמאות של המשתמשים
  האחרים אינן מגיעות לזיכרון הדפדפן מלכתחילה, ולא רק שאינן נשמרות.
- אותה רשימה נאכפת בשלוש הנקודות: משיכה (`slPullUsers`), טעינה
  (`slUsersLoad`) ושער הדיסק (`slUsersSave`).

⛔ **המראה הזו אינה חלק מ-`MIRROR` ואינה נדחפת לעולם.** היא מחזיקה
טביעות ולא סיסמאות, ודחיפת-מצב שלה הייתה כותבת `password` ריק לכל
המשתמשים. המשיכה מווסתת ל-60 שניות (`SL_USERS_PULL_MS`) — הסנכרון רץ כל
3 שניות ורשימת המשתמשים כמעט אינה משתנה.

### 4. `doLogin` — עם רשת בדיוק כמו קודם
המסלול המקוון לא השתנה: השאילתה לענן היא מקור האמת, וההשוואה מול
`password` שבמסד. מה שנוסף: `select` של עמודות מפורשות במקום `*`, השלמת
טביעה ורענון המראה **ברקע ואחרי** שהמסך עלה, ומסלול אופליין.
בלי רשת — `doLoginOffline` מול `slVerifyOffline`.

⚠️ **ארבעה מצבים, ארבע הודעות שונות** (`MSG_OFF_UNKNOWN` · `MSG_OFF_NO_FP`
· `MSG_OFF_NO_CRYPTO` · `MSG_BAD_LOGIN`). «המשתמש אינו בעותק המקומי»
ו«אין לו טביעה» **אינם** אומרים שהסיסמה שגויה — הם אומרים שאין במה לאמת
אותה. `MSG_BAD_LOGIN` על מצב כזה הוא כשל שקט: הוא מוביל את המשתמש להקליד
שוב ושוב סיסמה נכונה לחלוטין, במקום לומר לו את הדבר היחיד שיפתור — חיבור
אחד. ⛔ **אין לאחד את ההודעות.** שלוש החדשות נמצאות **מחוץ** לבלוק
ההודעות המשותף (סבב 8) — הוספת קבוע לבלוק ההוא באפליקציה אחת בלבד הייתה
יוצרת בדיוק את הסחיפה שכלל ברזל 8 בא למנוע; אותו שיקול כמו `MSG_PASS_SIX`.

**נוסף גם מסלול לכשל רשת באמצע כניסה מקוונת** (`isNetErr` ⇒ נפילה-חזרה
לאימות אופליין): `navigator.onLine` משקר לא פעם ב-WebView, ואין סיבה
להציג «שגיאה» למי שיש לו טביעה תקפה על המכשיר.

⛔ **אין אכיפת פורמט שש ספרות באף מסלול כניסה** — לא במקוון, לא באופליין
ולא בשער ההגדרות. ההנחיה מסבב 19 נשמרה כלשונה: אכיפה שם הייתה נועלת בחוץ
סיסמה קיימת ותקפה שנקבעה לפני הסבב ההוא, בלי שום מסלול שחזור.
`changeAdminPass` נשאר המקום היחיד שאוכף.

### 5. הקשחת סינון הסודות — משתי נקודות לשלוש
`slStripSecrets` יושם עד היום **רק בנקודת המשיכה** מהענן. זה היה בטוח
בפועל — אבל בטוח **במקרה** ולא **במבנה**: מה שהגן היה היעדר מסלול שכותב
סוד למראה, ולא מחסום. נתיב חדש אחד — כתיבה מקומית, מיגרציית שרידים,
ייבוא — היה כותב את `admin_pass` בטקסט גלוי ל-localStorage, באותו origin
שבו חיות עוד שלוש אפליקציות. מעכשיו, בדפוס שלוש הנקודות של gius (סבב 23):
1. **משיכה** — `slStripSecrets` ב-`syncAll`, כפי שהיה.
2. **כתיבה מקומית** — `slLocalWrite` מסרבת במפורש לשורת סוד, כפי שהיה.
3. ⭐ **שער הדיסק** — `slMirrorSave` עובר דרך `slSanitizeRows` החדשה, ולכן
   גם מראה שהורעלה בעקיפת שני הקודמים אינה מגיעה לדיסק.

### 6. ⛔ הסרת משתמש ברירת המחדל `admin`/`admin`
הסכימה זרעה `INSERT INTO public.sl_users … VALUES ('admin','admin')` —
כלומר **כל התקנה טרייה נולדה עם חשבון שסיסמתו ידועה לכל העולם**, על מסד
שה-RLS שלו `USING (true)` ושכתובתו מופיעה בקוד המקור. במסד הייצור הנוכחי
הוא אינו קיים (אומת: משתמש אחד בלבד, סיסמה בת שש ספרות), אבל לכל התקנה
**עתידית** זו פצצה רדומה — בדיוק זו שהוסרה מ-hanhala בסבב 21.
השורה הוסרה מ-`000_initial_schema.sql` (וממילא מהעותק המוטבע ב-`index.html`),
ובמקומה הנחיה ליצירת המשתמש הראשון ידנית. **האפליקציה מזהה טבלה ריקה
ומציגה את ההנחיה מעצמה** (`MSG_NO_USERS`) במקום «שם משתמש או סיסמה
שגויים», ומסך ההגדרה הראשונית מציג את פקודת ה-INSERT כשלב ④.
⚠️ **`sl_settings.admin_pass` הוא דבר אחר** — סיסמת שער מסך ההגדרות ולא
חשבון כניסה — והוא **כן** נשאר עם ערך פתיחה, כדי שמסך ההגדרות יהיה נגיש
בהתקנה טרייה. יש להחליפו מתוך «הגדרות ← שנה סיסמה».

### כלל ברזל 9 — רישום ההתפשטות
הסבב נגע בשלושה מנגנוני אבטחה, ולכל אחד נרשמת כאן תשובה מפורשת:
1. **טביעת PBKDF2 למשתמשים** — **הוחל בכולן, וזה הסבב שסוגר את המעגל.**
   hanhala בסבב 22, gius בסבב 23, ו-schar-limud כאן. **yoman-avoda —
   אינו רלוונטי**: אין בה חשבונות משתמש כלל (אין `doLogin`, אין טבלת
   משתמשים; `MSG_BAD_LOGIN` קיים שם רק כחלק מבלוק ההודעות המשותף), ולכן
   אין מה לאמת ואין מה להצפין.
2. **הקשחת הסינון לשלוש נקודות** — **אומץ מ-gius (סבב 23)**, ורלוונטי לכל
   אפליקציה שהמראה שלה יכולה להחזיק סוד. כאן המקרה היחיד הוא
   `sl_settings.admin_pass`; בגיוס — `g_users.password`, וטופל שם.
   ב-hanhala המקרה המקביל (`ys_users_cache`) טופל בסבב 21, וב-yoman אין
   סוד במראה.
3. **הסרת משתמש ברירת המחדל** — **רלוונטי, ולא יושם מחוץ לכאן** (הסבב
   מוגבל ל-schar-limud). מה שנמדד בפועל: hanhala כבר טופלה בסבב 21;
   yoman אינה רלוונטית (אין משתמשים). **⚠️ פער מתועד — gius:**
   `migrations/0001_init.sql` שם זורעת משתמש בעלים עם **סיסמה אמיתית
   כתובה בקובץ**, בריפו **ציבורי**. זה אינו אותו כשל בדיוק (זו אינה
   סיסמת ברירת-מחדל גנרית שכל אחד מנחש), אבל היא חשופה לקריאה פומבית.
   **הטריגר:** הסבב הבא שנוגע ב-gius, או כל שינוי ב-`0001_init.sql`.
   ⛔ ההחלטה מה לעשות שם היא של המנהל, לא של הסשן — כלל ברזל 9, סעיף
   «החלטת מודל אבטחה».

### מה **לא** נגע
⛔ `password` בענן · ⛔ מנגנון ה-PBKDF2 של **שער ההגדרות**
(`slHashPass`/`SL_PASS_SALT`/`sl_admin_pass_h`) — הוא נפרד ולא נגע, ושתי
מערכות הטביעה חיות זו לצד זו · ⛔ `changeAdminPass` ו-`guardOnline` שבו ·
⛔ `unlockSettings` · ⛔ שלושת הבלוקים המשותפים ושני המודולים הקפואים ·
⛔ מנוע המיזוג, `MIRROR` ו-`SL_COLS` — מראת המשתמשים חיה לצידם ולא בתוכם.

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `python3 tools/sync-setup-sql.py --check`,
  `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **109 טענות** על הקוד האמיתי המחולץ מ-`index.html` (`test_offline_login.mjs`
  + `harness.mjs`, בסשן — הקוד רץ ב-`vm` מעל DOM, `localStorage`, `crypto`
  ולקוח Supabase מזויפים; **הבדיקות מריצות את הפונקציות האמיתיות ולא העתק**):
  > ⚠️ **עדכון בהשלמת סבב 24:** הרתמה הזו רצה **בסשן ולא נשמרה**, וזה מה
  > שהוליד את סעיף 6 בכלל ברזל 8. היא נכתבה מחדש ונשמרה כ-**`tools/test_round24.mjs`**
  > (140 טענות — נמדד מחדש, לא הועתק). הרשימה שלמטה מתארת את הכיסוי; הקובץ
  > שבריפו הוא מקור האמת, והוא זה שיש להריץ.
  - **גזירה** — דטרמיניזם, רגישות למלח ולסיסמה, פורמט hex בן 64, **התאמה
    מדויקת ל-PBKDF2-SHA256 100k שחושב בנפרד ב-`node:crypto`**, ⭐ אימות
    שקידומת ההקשר באמת נכנסת למלח, מלח אקראי 16 בתים ושונה בכל קריאה,
    ושלושת מסלולי הכישלון מחזירים `null`.
  - **מראת המשתמשים** — רשימת ההיתר מפילה `password` ועמודה זרה; **השרת
    מחזיר בכוונה גם `password` והמראה נשארת נקייה**; ה-`select` מבקש
    עמודות מפורשות; round-trip מהדיסק; מראה פגומה נטענת ריקה;
    נפילה-חזרה כשהמיגרציה טרם הורצה; הוויסות, ה-`force` והדילוג באופליין.
  - ⭐ **כניסה אופליין למשתמש שאינו האחרון שנכנס** — נכנס, בלי שגיאה,
    **באפס קריאות רשת** · סיסמה שגויה נדחית · משתמש בלי טביעה מקבל
    `MSG_OFF_NO_FP` **ולא** `MSG_BAD_LOGIN` · משתמש לא מוכר, ובלי crypto —
    כל אחד עם הודעתו · ארבע ההודעות נבדלות זו מזו · הסשן מחזיק
    `id`+`username` בלבד · **סיסמה ישנה שאינה שש ספרות נכנסת בכל זאת**.
  - ⭐ **אחרי מסלול מלא (משיכה → כניסה → סשן): ערך הסיסמה והמחרוזת
    `password` אינם מופיעים באף מפתח `localStorage`.**
  - **כניסה מקוונת** — מצליחה, ההשוואה עדיין מול `password` שבמסד,
    `CUR_USER` בלי סיסמה · סיסמה שגויה ⇒ `MSG_BAD_LOGIN` · ⭐ טבלה ריקה ⇒
    `MSG_NO_USERS` · עובדת גם לפני הרצת 010 · ⭐ כשל רשת באמצע ⇒
    נפילה-חזרה מוצלחת לאימות אופליין.
  - **`slEnsurePassFp`** — משלימה כשאין טביעה (והטביעה שנכתבה מאומתת
    מול חישוב עצמאי) · שותקת כשהיא תואמת · מחליפה טביעה בת-תוקף לסיסמה
    ישנה · ⭐⭐ **טביעה לא-תואמת + כשל גזירה ⇒ שני השדות מאופסים** ·
    אין-טביעה + כשל גזירה ⇒ דילוג שקט · בלי crypto לא נוגעת בטביעה
    קיימת · אופליין לא נוגעת ברשת.
  - **הסינון** — שלוש נקודות האכיפה, כולל ⭐ **שער הדיסק שמסנן את הסוד
    גם כשהמראה הורעלה ישירות**, ואימות ש-`SETTINGS` שבזיכרון נקי.
  - **הסכימה** — אין `INSERT` ל-`sl_users` ב-000, בעותק המוטבע וב-
    `supabase-setup.sql`; אין את הצמד `admin`/`admin` באף אחד מהם; 010
    אדיטיבית (בדיוק שני `ADD COLUMN IF NOT EXISTS`) ואינה נוגעת ב-`password`.
  - **מקורות** — `PASS_SIX_RE` **אינו** מופיע בגוף `doLogin`,
    `doLoginOffline` ו-`slVerifyOffline`, **וכן** מופיע ב-`changeAdminPass`;
    אין `select('*')` על `sl_users`; אין `password` ברשימת ההיתר.
- **⭐ עשר מוטציות, כל אחת מפילה את החבילה** (הבודק אינו חותמת גומי):
  שער הדיסק בלי סינון · «אין טביעה» שמקבל את הודעת הסיסמה השגויה ·
  כשל גזירה שמדלג במקום לאפס · `password` שחוזר לרשימת ההיתר · הסרת
  קידומת הפרדת-ההקשר · החזרת `admin`/`admin` לסכימה · ניטרול המסלול
  האופליין · היעדר crypto שנחשב אי-התאמה · הסרת בדיקת הטבלה הריקה ·
  התרת כתיבה מקומית של שורת סוד.
- **`CACHE_NAME` קודם v24 ← v25.**

---

<!-- SHARED:start id="round-24b-what" -->
## ⭐ סבב 24 — השלמה (אוגוסט 2026): מקור אמת אחד, בלי סודות זרועים

### מה נעשה
השלמה לסבב 24, לפני המיזוג. **התיעוד המשותף השתנה בארבעת הריפו; קוד
נגע ב-schar-limud וב-gius בלבד.**

1. **כלל ברזל 8 גדל לשבעה כללים** (הפרק שמעל) — שני החדשים נלמדו ממה
   שנמדד בסבב 24 עצמו:
   - **סעיף 6 — בדיקות נשמרות כקובץ.** סבב 24 כתב 109 טענות, הריץ אותן,
     ותיעד אותן — ולא שמר אותן. הפרק «אימותים» הכריז על אינווריאנטה
     («`password` אינו נוגע בדיסק») שאין שום שער שמגן עליה, בדיוק הדפוס
     שסעיף 4 בא למנוע.
   - **סעיף 7 — ⛔ אין לזרוע פרטי כניסה.** שתי מיגרציות התקנה זרעו
     חשבונות: `admin`/`admin` ב-schar (סיסמת ברירת מחדל גנרית, הוסרה
     בסבב 24) ו**סיסמה אמיתית של המנהל** ב-gius — בריפו ציבורי, מול
     מסד שה-RLS שלו `using (true)`.
2. **schar-limud — העותק המוטבע של הסכימה הוסר** (פרטי, ר' שם).
3. **gius — זריעת המשתמש הוסרה מ-`0001_init.sql`** (פרטי, ר' שם).
4. **schar-limud — 109 הטענות של סבב 24 נשמרו כקובץ בריפו** (פרטי, ר' שם).

### תיאום גרסאות
`sw.js`: hanhala **v35** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v26** · gius **gius-v11**. **רק schar קודמה** בהשלמה הזו — היא
היחידה שקוד האפליקציה שלה השתנה. ב-gius השינוי הוא בקובץ מיגרציה,
בפיקסטורת בדיקה ובתיעוד; `index.html` ו-`sw.js` שם לא נגעו.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud, `sw.js` v26)

**א. `SETUP_SQL_FALLBACK` הוסר — ואיתו `tools/sync-setup-sql.py`.**
עד כאן ישבו בפרויקט **שני** עותקים של הסכימה: `migrations/000_initial_schema.sql`
ועותק מיוצר שלו בתוך `index.html`, וכלי פייתון ייעודי (`sync-setup-sql.py`)
שרץ בשער הדחיפה כדי לוודא שהם לא נפרדו. schar-limud הייתה **היחידה מארבע
האפליקציות** עם `CREATE TABLE` בתוך `index.html`.
- **הנימוק להסרה:** ה-`fetch` שמושך את קובץ המיגרציה נעשה **מאותו origin
  שממנו נטענה האפליקציה עצמה**. אם הוא נכשל, האפליקציה כבר לא הייתה עולה —
  כלומר הגיבוי הגן מפני תרחיש שאינו קיים, בעוד שהמחיר (מקור אמת שני שמתיישן
  בשקט) הוא אמיתי **ונמדד**: זו בדיוק התקלה של סבב 6, שבה התקנה טרייה לפי
  מסך האפליקציה לא יכלה לשמור או למחוק תשלום.
- **כשל משיכה מציג עכשיו הודעה ולא סכימה.** `loadSetupSql` מחזירה
  `{ok, text}`; ב-`ok:false` התיבה וכפתור ההעתקה **מוסתרים**, ובמקומם
  `#setup-sql-err` עם קישור ישיר ל-`SETUP_SQL_GITHUB`. ⛔ סכימה שאיננו
  יודעים אם היא עדכנית, בתיבה שהמשתמש מעתיק ומריץ על המסד, גרועה מהודעה.
- `copySql` קיבלה שומר (`if(!ta || !ta.value) return;`) — בלעדיו לחיצה על
  כפתור שנשאר גלוי הייתה זורקת.
- **שער הדחיפה ירד מארבעה לשלושה** — `sync-setup-sql.py --check` הוסר מפרק
  «לפני כל push», מכלל קריטי 7 ומ-`CONTEXT.md`. ⛔ **אין להחזיר עותק
  מוטבע**: הוא הופך את כלל קריטי 7 לאכיפה ידנית בכלי חיצוני.
- ⛔ **`migrations/000_initial_schema.sql` עצמו לא נגע.** מסך ההגדרה
  הראשונית ממשיך למשוך אותו ולהציג אותו כרגיל.

**ב. 140 טענות סבב 24 נשמרו כקובץ — `tools/test_round24.mjs`.**
פרק «אימותים» של סבב 24 דיווח על 109 טענות שרצו **בסשן בלבד**
(`test_offline_login.mjs` + `harness.mjs`), ולכן נעלמו איתו. זה בדיוק הכשל
שסעיף 6 החדש בכלל ברזל 8 בא למנוע: הפרק הכריז על אינווריאנטה
(«`password` אינו נוגע בדיסק») שאין שום שער ששומר עליה.
- הקובץ נכתב מחדש בדפוס `tools/test_round22.mjs` של hanhala ו-
  `scripts/test_round23.mjs` של gius — הפונקציות **נחתכות מ-`index.html`
  לפי שם** ורצות ב-`vm`, כך שמוטציה בקוד האמיתי מפילה טענה.
- ⚠️ **המספר הוא 140 ולא 109** — הוא נמדד עכשיו ולא הועתק מהדיווח הקודם.
  נוספו לו גם הטענות על ההסרה שבסעיף א.
- ⚠️ **פרטי ל-schar-limud** — בניגוד ל-`check-status-area.mjs` ול-
  `check-docs.mjs`, אינו זהה לאף ריפו אחר ואין ליישר אותו.

**ג. `CACHE_NAME` → v26** — `index.html` השתנה, ולכן הקידום חובה.

**ד. ⚠️ פער שנמדד ולא נסגר — `sl_settings.admin_pass`.**
כלל ברזל 8 סעיף 7 החדש אוסר לזרוע פרטי כניסה בקובץ שנדחף לגיט. הסכימה כאן
עדיין זורעת `INSERT INTO public.sl_settings (key, value) VALUES ('admin_pass', 'admin')`
— **סיסמת שער ההגדרות**, לא חשבון כניסה. היא לא הוסרה כאן, משתי סיבות:
1. **הסרה לבדה אינה משנה דבר** — `slPassOf` נופלת ל-`'admin'` כשהשורה חסרה,
   ולכן שער ההגדרות ייפתח ב-«admin» בדיוק כמו היום. הערך חי גם בקוד.
2. **שינוי אמיתי הוא החלטת מודל אבטחה של המנהל** (כלל ברזל 9): הוא מחייב
   להחליט מה קורה בהתקנה טרייה — שער נעול עד שנקבעת סיסמה, או ברירת מחדל
   מוצהרת — ולתקן את `slPassOf` בהתאם.
⛔ סשן שמזהה את זה **מדווח ומציע**, ואינו מיישם מיוזמתו. הסיסמה בייצור
הוחלפה מזמן מ«admin» דרך «הגדרות ← שנה סיסמה», ולכן אין כאן סוד חי שנדחף.

### אימותים
- **שלושת השערים עברו** (אחרי שהרביעי הוסר): `node --check` על ה-JS המחולץ
  מ-`index.html` ועל `sw.js`, `node tools/check-status-area.mjs`,
  ו-`node tools/check-docs.mjs`.
- **`node tools/test_round24.mjs` — 140 עברו, 0 נכשלו.**
- **⚠️ באג שנמצא בבדיקה עצמה, באימות חיצוני, ותוקן — הבדיקה הייתה
  תלוית-שעון.** האימות החיצוני קיבל «138 עברו, 2 נכשלו», ושתי הנופלות היו
  בדיוק «ההשלמה נכתבה» ו«ההשלמה אינה נוגעת ב-password».
  **הקוד היה תקין; הבדיקה הייתה שגויה.** `doLogin` מפעילה את
  `slEnsurePassFp` כ**שרשרת רקע** אחרי שהמסך כבר עלה (במכוון — ר' סעיף 4
  בפרק סבב 24), וההשלמה כוללת גזירת PBKDF2 של 100,000 סיבובים. הבדיקה
  המתינה לה ב-`setTimeout` קבוע של **30 מ״ש**: על המכונה כאן הגזירה לוקחת
  **~20 מ״ש ולכן עברה**, ועל מכונת האימות — יותר מ-30, ולכן הטענות נבדקו
  **לפני** שההשלמה הספיקה לכתוב. זהו **מרוץ**, לא כשל אמיתי.
  **התיקון — ב*בדיקה*, לא בקוד:** נוספה `waitFor(pred, label, 5000)`
  שממתינה ל**אירוע** (הופעת ה-`update` ברשימת השאילתות) ולא לשעון, עם
  תקרה שנכשלת ברעש אם ההשלמה לא רצה כלל.
  ⛔ **אין להחזיר `setTimeout` קבוע** — זה בדיוק מה שנשבר.
  **שוחזר ואומת:** קיצור ההמתנה ל-5 מ״ש משחזר במדויק «138 עברו, 2 נכשלו»
  עם אותן שתי טענות; אחרי התיקון — 8 ריצות רצופות נקיות, ועוד שתיים
  **תחת עומס מלאכותי של ארבעה מעבדים תפוסים**, כולן 140/0.
- **⭐ שבע מוטציות, כל אחת מפילה את החבילה** (הבודק אינו חותמת גומי; נמדד):
  1. `password` מוחזר לרשימת ההיתר ⇒ **12** טענות נופלות.
  2. «אין טביעה» מקבל את הודעת הסיסמה השגויה ⇒ 4.
  3. `slPassFields` מדלג במקום לאפס טביעה ישנה ⇒ 3.
  4. הסרת קידומת הפרדת-ההקשר מהמלח ⇒ 4.
  5. החזרת `INSERT` של `admin`/`admin` ל-`000` ⇒ 2.
  6. שער הדיסק בלי `slSanitizeRows` ⇒ 1.
  7. ניטרול המסלול האופליין ב-`doLogin` ⇒ 2 (והריצה נעצרת).
  8. ⭐ **ההשלמה אינה כותבת כלל** (`update` שהוחלף בהצלחה מזויפת) — הבאג
     שהאימות החיצוני חשד בו ⇒ **3** טענות נופלות, ובראשן «ההשלמה ברקע —
     לא קרה בתוך 5000ms». כלומר התיקון **לא החליש את הטענות**: הן עדיין
     תופסות את הכשל האמיתי, ועכשיו גם מנקבות בשמו.
- **שלושת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.

---

## ⭐ סבב 26 (אוגוסט 2026, `sw.js` v27, `migrations/011`) — הרשאה לפי תפקיד, במקום סיסמה שנייה

⚠️ **פרק פרטי — אינו מסומן `SHARED`.** הסבב נגע ב-schar-limud וב-gius בלבד,
ואינו נוגע באף בלוק קוד משותף. סימון `SHARED` היה מצהיר על זהות שאינה
קיימת ב-hanhala וב-yoman — בדיוק הכשל שכלל ברזל 8 סעיף 4 בא למנוע. ⛔ **אין
ליישר אותו מריפו אחר**: פרק סבב 26 של gius מתאר מנגנון אחר לגמרי (נתיב
עדכון חלקי למראת המשתמשים), ואין ביניהם דבר משותף מלבד המספר.

> **ואין כאן פרק «סבב 25» — במכוון.** לא היה סבב 25 בריפו הזה, והמספור
> ממשיך מ-24 ל-26. זו אינה השמטה.

### הבעיה שנמדדה
schar-limud הייתה **היחידה מארבע האפליקציות עם שכבת הרשאה שנייה**: סיסמת
`admin_pass` שיושבת ב-`sl_settings` ושומרת על מסך ההגדרות, **מעל** הכניסה
הרגילה. בהנהלה, בגיוס וביומן ההרשאה נגזרת מהמשתמש שנכנס, ותו לא.

ומעבר לאי-האחידות, למנגנון היו **שלושה מחירים ממשיים**:
1. ⛔ **פרצת התקנה טרייה.** `slPassOf` נפלה למחרוזת `'admin'` כשהשורה
   חסרה, ולכן מסד חדש שטרם נזרע בו `admin_pass` נפתח לכל מי שהקליד
   «admin». הזריעה בסכימה הייתה הדבר היחיד שעמד בין ההתקנה לשער הפתוח —
   כלומר הגנה שתלויה בכך שמישהו הריץ קובץ.
2. **הוא לא עבד אופליין.** ההשוואה הייתה מול טביעת PBKDF2 מקומית, ובהיעדרה
   מול הענן — ולכן **פתיחה ראשונה במכשיר דרשה רשת**, בזמן שכל שאר
   האפליקציה עובדת אופליין במלואה מסבב 12 שלב 2.
3. **סיסמה שנייה לאותו אדם אינה שכבת אבטחה נוספת** — היא מנגנון נפרד עם
   מסך משלו, מסלול שינוי משלו, טביעה משלו ותחזוקה משלה.

### 1. `migrations/011` — ✅ הורצה ואומתה
`role` ל-`sl_users`. אינה נוגעת בסיסמאות, בטביעות או בכל נתון קיים פרט
לשורות שקדמו לעמודה. `000_initial_schema.sql` עודכן גם הוא (כלל 7 —
מקור אמת יחיד).
> ⚠️ **הפרק הזה מתאר את הניסוח הראשון של הסבב.** בהשלמה שלפני המיזוג
> ה-`DEFAULT 'admin'` **הוסר** והעמודה היא `NOT NULL` בלי DEFAULT כלל —
> ר' «סבב 26 — השלמה» בסוף הקובץ, ששם הנימוק המלא. הסעיף האחרון כאן
> נשמר כתיעוד ההחלטה שהוחלפה, ומסומן ככזה.
- **`'admin'`** = גישה מלאה, כולל מסך ההגדרות. **כל ערך אחר** = משתמש רגיל.
- ⛔ **אין `CHECK` על הערכים, במכוון.** המנהל יוצר משתמשים מלוח הבקרה של
  Supabase, ו-CHECK היה הופך שגיאת הקלדה ל-INSERT שנכשל. במקום זה **הקוד
  נכשל סגור**: תפקיד שהוקלד בטעות **שולל** הרשאה ולא מעניק אותה.
- ~~⚠️ **`DEFAULT 'admin'` — והמשמעות לעתיד.** במסד יש היום משתמש אחד,
  והוא המנהל; ברירת המחדל נותנת לו את התפקיד הנכון בלי `UPDATE` על
  נתונים. אבל משתמש חדש שנוצר בלי לציין `role` יקבל `'admin'`.~~ —
  **בוטל בהשלמת סבב 26.** ההנמקה הזו העדיפה נוחות על פני הכלל שהסבב
  עצמו קבע: ברירת מחדל שמעניקה הרשאה היא בדיוק מה שנסגר כאן. **אין
  `DEFAULT` כלל**, ויצירת משתמש מחייבת `role` מפורש — INSERT בלעדיו
  נכשל במסד. ה-`UPDATE` שהוחלף בו מסויג ב-`WHERE role IS NULL`, ולכן
  נוגע רק בשורות שקדמו לעמודה. ר' «סבב 26 — השלמה».

### 2. שער הסיסמה הוסר — ההרשאה נגזרת מהמשתמש
`unlockSettings`, מסך הסיסמה, כרטיס «שינוי סיסמת מנהל» ו-`_settingsUnlocked`
**הוסרו כליל**. במקומם `slSettingsAccess(u)` שמחזירה שלושה מצבים:
- **`'ok'`** — `role === 'admin'`. `renderSettingsPanel` מציג את המסך.
- **`'denied'`** — יש תפקיד והוא אינו admin, **או שאין משתמש מחובר כלל**.
- **`'no-role'`** — אין עמודת `role` (011 טרם הורצה).

⚠️ **ההפרדה בין שני האחרונים היא ההבדל בין הודעה לכשל שקט**, ואותו שיקול
בדיוק כמו ארבע הודעות הכניסה האופליין (סבב 24): «אין לך הרשאה» למנהל
שפשוט טרם הריץ מיגרציה שולח אותו לחפש באג בהרשאות, במקום לומר לו את הדבר
היחיד שיפתור. `MSG_SET_NO_ROLE` מפנה ל-`migrations/011_users_role.sql`
בשמו. ⛔ **אין לאחד את שתי ההודעות.**

**⭐ ומעכשיו ההרשאה עובדת אופליין** — `role` יורד עם מראת המשתמשים, ולכן
`renderSettingsPanel` אינה קוראת לרשת כלל. השער הישן דרש חיבור אחד לפני
שנפתח אי-פעם במכשיר.

### 3. ⚠️ `role` נשמר גם בסשן — וזה הכרחי, לא נוחות
`slSaveSession` שומרת `{id, username, role}`. בלי `role`, מכשיר שנפתח מחדש
**בלי רשת** היה מעלה `CUR_USER` בלי תפקיד ⇒ `'no-role'` ⇒ מסך ההגדרות נעול
בפני המנהל עד החיבור הבא — בדיוק הפגם שהמנגנון הישן סבל ממנו, בכיסוי אחר.
⛔ הסשן **אינו** מקור האמת: `slResolveUser` מעדיפה תמיד את **מראת
המשתמשים** כשהיא מכירה את המשתמש (היא נמשכת כל 60 שניות ותופסת שינוי
תפקיד שנעשה במכשיר אחר), ו-`slPullUsers` מרעננת את `CUR_USER` ואת הסשן
אחריה. הסשן הוא הערך האחרון הידוע, לנתיב שבו אין מראה ואין רשת.
⚠️ הרענון ב-`slPullUsers` נעשה **אחרי** שהמראה נשמרה — הפוך מזה, הוא היה
קורא ערך שעוד לא נכתב.

### 4. ניקוי המנגנון הישן — בקוד, בסכימה, ובמכשיר
- **בקוד:** `slPassOf` · `slHashPass` · `slRememberPassHash` ·
  `SL_PASS_HASH_KEY` · `SL_PASS_SALT` · `SL_PASS_ITER` · `changeAdminPass` ·
  `unlockSettings` · `_settingsUnlocked` · העוגנים `settings-lock`,
  `set-pass-input`, `set-new-pass`, `set-new-pass2` — **כולם הוסרו.**
- **בסכימה:** שורת `INSERT INTO public.sl_settings … ('admin_pass','admin')`
  הוסרה מ-`000_initial_schema.sql`.
- ⭐ **במכשיר:** `slDropLegacyPassHash()` — מיגרציה מקומית חד-פעמית שמוחקת
  את `sl_admin_pass_h` בעלייה הראשונה אחרי העדכון, **ורושמת ליומן הפינוי**.
  localStorage שורד עדכוני קוד; ערך שאין לו קורא הוא בדיוק סוג השריד שסשן
  עתידי מוצא ומנחש למה שימש. ⛔ ומחיקה **שקטה** הייתה סותרת את כלל ברזל 2
  — לכן היא נרשמת.
- **`PASS_SIX_RE`/`MSG_PASS_SIX` ירדו גם הם**, כי `changeAdminPass` היה
  **אתר האכיפה היחיד שלהם** באפליקציה הזו (סבב 19). ⚠️ זו אינה נסיגה מכלל
  שש הספרות: הכלל נאכף היום במקום שבו סיסמאות באמת נקבעות — ה-INSERT הידני
  שקובץ ההתקנה מדגים — ו-`au-pass` שומר `inputmode="numeric"` ו-`maxlength="6"`
  כרמז קלט. ⛔ **אין להחזיר אכיפת פורמט לאף מסלול כניסה**, מאותו נימוק
  שנכתב בסבב 19: היא נועלת בחוץ סיסמה קיימת ותקפה, בלי מסלול שחזור.

### 5. ⚠️⚠️ פער שנמדד ולא נסגר — שורת `admin_pass` שנשארה במסד
הבקשה לסבב הזה כללה «הסר את `admin_pass` מ-`SL_SECRET_SETTINGS`». **הוא
לא הוסר משם, וזו החלטה מנומקת ולא השמטה.**

הרשימה הזו קובעת אילו שורות של `sl_settings` **לעולם אינן יורדות לדיסק**.
נמדד מול מסד הייצור: השורה `admin_pass` **עדיין קיימת ומחזיקה ערך אמיתי בן
שש ספרות** (לא `'admin'` — היא הוחלפה בזמנו דרך «שנה סיסמה»). הוצאתה
מהרשימה הייתה מורידה את הערך הזה **בטקסט גלוי** ל-localStorage של כל מכשיר,
באותו origin שבו חיות עוד שלוש אפליקציות — כלומר **מחליפה מנגנון שהוסר
בדליפה חדשה**, וסביר שגם דולפת סיסמה שהמנהל משתמש בה במקום נוסף.

מה שכן נעשה: הקבוע **שונה שם ל-`SL_NEVER_MIRROR_SETTINGS`** ותועד מחדש —
הוא כבר לא «רשימת סודות של מנגנון חי» אלא «שורות שאסור להן להגיע לדיסק»,
ו-`admin_pass` יושב בה כ**שריד**. שלוש נקודות האכיפה של סבב 24 נשארו
במלואן.

**🔑 מה נדרש כדי לסגור את הפער — פעולה של המנהל:**
```sql
DELETE FROM public.sl_settings WHERE key = 'admin_pass';
```
⛔ הפעולה **לא בוצעה** — מחיקת נתונים ב-`sl_*` מחייבת אישור מפורש (ר' «גישת
Supabase» בראש הקובץ). אחרי שהיא תרוץ, סבב עתידי יוכל לרוקן את
`SL_NEVER_MIRROR_SETTINGS` — **ולא לפני כן.** ⚠️ ובכל מקרה: אם הסיסמה
ההיא משמשת את המנהל במקום אחר, כדאי להחליף אותה שם.

### כלל ברזל 9 — רישום ההתפשטות
הסבב החליף **מנגנון הרשאות**, ולכן נדרש רישום מפורש:
**⛔ אינו רלוונטי לשלוש האחיות — עם הנימוק, לא כהצהרה.** מה שהוסר כאן הוא
**שכבת ההרשאה השנייה**, וכזו לא הייתה קיימת בהן מעולם: ב-hanhala, ב-gius
וב-yoman ההרשאה נגזרת מהמשתמש שנכנס (ב-gius: `g_users.role`, `owner` מול
`manager`), ואין בהן שום סיסמת שער נוספת מעל הכניסה. הסבב הזה **מיישר את
schar אליהן** ואינו מייצא אליהן דבר. ⛔ אין פער פתוח ואין טריגר.
⚠️ ובפרט: `g_users.role` של gius **אינו** מקבל את הערכים של כאן. שם
`owner`/`manager`, כאן `admin` — שתי טבלאות נפרדות בשני פרויקטים נפרדים,
ואין ליישר ביניהן.

### מה **לא** נגע
⛔ מנגנון הכניסה עצמו — `doLogin`, `doLoginOffline`, `slVerifyOffline`,
`slEnsurePassFp`, `slPassFp`/`slMakePassFp`/`slPassFields`, `SL_PASS_CTX`
ו-`migrations/010` · ⛔ `password` בענן · ⛔ שלוש נקודות סינון הסודות
(סבב 24) · ⛔ שלושת הבלוקים המשותפים ושני המודולים הקפואים · ⛔ מנוע
המיזוג, `MIRROR` ו-`SL_COLS`.

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **`node tools/test_round26.mjs` — 93 עברו, 0 נכשלו** (קובץ חדש בריפו,
  כלל ברזל 8 סעיף 6). שמונה פרקים: שלושת מצבי `slSettingsAccess` כולל
  נכשל-סגור על תפקיד לא מוכר · ⭐ admin נכנס / לא-admin נחסם / «אין עמודה»
  — כל אחד עם ההודעה שלו, **ובאפס קריאות רשת** · `refreshUI` שאינו מרנדר
  הגדרות למי שאינו רואה אותן · ניקוי `sl_admin_pass_h` כולל אי-נגיעה
  במפתחות אחרים ורישום ליומן · ⭐ התפקיד ששורד עלייה בלי רשת, והמראה
  שגוברת על הסשן · אימות שכל המנגנון הישן הוסר ושאין בקוד נפילה-חזרה
  ל-`'admin'` · הסכימה · ו-⛔ שמסלולי הכניסה לא נגעו.
- **`node tools/test_round24.mjs` — 140 עברו, 0 נכשלו.** ⚠️ שש טענות בו
  **עודכנו במכוון**, ולא כדי «להשתיק»: חמש מהן קיבעו את רשימת ההיתר על
  ארבע עמודות (`role` נוסף לה), ואחת בדקה ש-`PASS_SIX_RE` מופיע ב-
  `changeAdminPass` — פונקציה שהוסרה. **הכוונה של הטענה נשמרה במלואה**:
  במקומה נבדק ש-`PASS_SIX_RE` ירד מהקובץ **ושמסלולי הכניסה נקיים ממנו**.
  בנוסף שתי טענות «CACHE_NAME קודם ל-vNN» (כאן ובגיוס) הומרו לבדיקת
  **תבנית** — טענה שמקבעת מספר נכשלת על כל קידום עתידי, כלומר חוסמת בדיוק
  את מה שכלל קריטי 2 מחייב.
- **⭐ תשע מוטציות, כל אחת מפילה טענות** (הבודק אינו חותמת גומי; נמדד):
  1. כל תפקיד מקבל גישה ⇒ **10** טענות נופלות.
  2. תפקיד חסר נחשב admin (נכשל **פתוח** במקום סגור) ⇒ 5.
  3. שתי הודעות החסימה מאוחדות ⇒ 1.
  4. הניקוי המקומי אינו מוחק את `sl_admin_pass_h` ⇒ 3.
  5. `role` יורד מרשימת ההיתר ⇒ 8.
  6. הסשן אינו שומר `role` (נעילת המנהל אופליין) ⇒ 3.
  7. זריעת `admin_pass` חוזרת ל-`000` ⇒ 2.
  8. נפילה-חזרה ל-`'admin'` חוזרת לקוד ⇒ 1.
  9. מסך ההגדרות מוצג ללא תלות בהרשאה ⇒ 2.
- **⚠️ ההמתנה בבדיקה היא לאירוע ולא לשעון** — `waitFor(pred, label, 5000)`,
  בדיוק כמו ב-`test_round24.mjs` אחרי הבאג תלוי-השעון שנמצא שם באימות
  חיצוני. ⛔ אין להחזיר `setTimeout` קבוע.
- **מצב המיגרציות אומת מול המסד החי, לא הוצהר** — ר' הטבלה למעלה.
- **שלושת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.

---

<!-- SHARED:start id="iron-rule-10-users" -->
## ⭐ כלל ברזל 10 — מודל המשתמשים וההרשאות בארגון (סבב 26, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את תשעת כללי הברזל שלמעלה.

> **אפליקציה בארגון שיש בה משתמשים נבנית לפי שמונת הסעיפים שלמטה, מהיום
> הראשון.** הדפוס הזה לא תוכנן מראש — הוא התגבש בסבבים 13–26, סעיף אחרי
> סעיף, כל אחד מהם אחרי כשל שנמדד בפועל. הניסוח כאן נועד לדבר אחד:
> שהאפליקציה **הבאה** לא תעבור את אותה סדרה מחדש.

### ⚠️ למה זה כלל ולא פרק תיעוד
מי שבונה כאן אפליקציה חדשה **מעתיק ריפו קיים** — הוא לוקח את המודול
המשותף, את `LS_CFG`, את אזור המצב ואת מבנה `index.html`, ומתחיל לעבוד.
הוא **אינו** קורא שלושים פרקי סבבים היסטוריים כדי לגלות למה `role` הוא
בלי `DEFAULT` או למה הסיסמה אינה יורדת לדיסק. **דפוס שחי רק בפרקי
היסטוריה אינו עובר בהעתקה** — הוא נשאר מאחור, והאפליקציה החדשה נולדת עם
בדיוק הכשלים שכבר שילמנו עליהם. כלל ברזל כן עובר, מפני שהוא נקרא לפני
שכותבים ולא אחרי שנשבר.

### שמונת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם, ואין צורך לחפש.

1. **טבלת משתמשים: `role TEXT NOT NULL`, ⛔ בלי `DEFAULT`.**
   *(נלמד בסבב 26 והושלם בהשלמה שלו.)* יצירת משתמש בלי תפקיד **נכשלת
   במסד**, וזו ההתנהגות הרצויה: **תפקיד הוא החלטה, ולא ערך שנופל
   מאליו.** ברירת מחדל ש**מעניקה** הרשאה היא הכשל החמור מבין השניים,
   אבל גם ברירת מחדל שנכשלת סגור סוטה מהדפוס.
   ⚠️ **הערכים עצמם אינם משותפים ואין ליישר אותם** — כל אפליקציה ומודל
   ההרשאות שלה, בטבלה נפרדת ולעיתים בפרויקט Supabase נפרד.

2. **⛔ אין שער סיסמה נפרד מעל מסך ההגדרות.** *(נלמד בסבב 26.)* ההרשאה
   נגזרת **מהמשתמש המחובר בלבד**. סיסמה שנייה לאותו אדם אינה שכבת
   אבטחה נוספת — היא מנגנון שני עם מסך משלו, מסלול שינוי משלו ותחזוקה
   משלו, ובהתקנה טרייה היא נוטה ליפול חזרה לערך ברירת מחדל ולהיפתח לכל
   מי שמנחש אותו. ⚠️ הכלל חל גם על אפליקציה שאין בה משתמשים כלל: שם שער
   כזה אינו «יישור לאחיות» אלא **שכבת הרשאה שלמה יש מאין**.

3. **⛔ אין לזרוע משתמש, סיסמה או תפקיד בשום קובץ שנדחף לגיט.**
   *(נלמד בסבב 21 בהנהלה, ובסבב 24 ובהשלמתו ב-schar-limud וב-gius.)*
   לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ בדיקה. הריפו
   **ציבורי**, ומפתחות ה-anon יושבים ב-`index.html` — כלומר סוד שנדחף
   הוא סוד שכל אחד יכול להשתמש בו מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותו:** הערך נשאר קריא בהיסטוריית ה-git
   לנצח. הטיפול היחיד בסוד שנדחף הוא **להחליף אותו במסד**, ורק אחר כך
   לנקות את הקובץ. המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר
   איך — עם מצייני מקום ולא עם ערכים.

4. **הסיסמה גלויה בענן, ⛔ אך לעולם אינה יורדת למכשיר.**
   *(נלמד בסבב 13 ב-schar-limud, והורחב בסבבים 22–24.)* הטקסט הגלוי
   בענן הוא **החלטה תפעולית מתועדת** — המנהל צריך לראות ולנהל סיסמאות —
   ולא פער שממתין לסגירה; ⛔ שינוי שלה הוא החלטת המנהל, לא של הסשן
   (כלל ברזל 9). מה שיורד למכשיר הוא **`pass_salt` + `pass_fp` בלבד**:
   PBKDF2-SHA256, **100,000 סיבובים**, **מלח אקראי פר-משתמש**, ועם
   **קידומת הקשר פר-אפליקציה** בתוך המלח — ה-origin משותף לארבע
   האפליקציות, ובלי הפרדת הקשר טבלה מחושבת מראש עבור אחת מהן תקפה
   לכולן.
   ⛔ **כישלון גזירה מאפס את שני השדות ולא מדלג על הכתיבה** — טביעה
   ישנה ששורדת שינוי סיסמה ממשיכה לפתוח את הכניסה האופליין עם הסיסמה
   **הקודמת**, לנצח.

5. **כל נתיבי הכתיבה למראת המשתמשים עוברים דרך פונקציית סינון אחת.**
   *(נלמד בסבב 23 ב-gius, וחזר בסבב 26 שם.)* **רשימת היתר** ולא רשימת
   איסור — רשימת היתר נשארת נכונה גם כשתיווסף לטבלה עמודה רגישה חדשה.
   ⛔ **ואין נתיב שני שעוקף אותה.** זו הנקודה שנשברה בפועל יותר מפעם
   אחת: מסנן שיושב במשיכה בלבד, וכתיבה מקומית שכותבת את הסוד לדיסק
   מסביבו. כל נתיב חדש — משיכה, כתיבה מקומית, עדכון חלקי, מיגרציה,
   ייבוא — חייב לעבור דרך אותה פונקציה, ורצוי שגם שער הדיסק יסנן שוב
   כשכבה שנייה.

6. **כניסה אופליין מרובת-משתמשים היא דרישה, לא תוספת.**
   *(נלמד בסבב 22 בהנהלה, בסבב 23 ב-gius ובסבב 24 ב-schar-limud.)*
   **כל משתמש נכנס בכל מכשיר, גם בלי רשת** — מול הטביעה שבסעיף 4, ולא
   מול הענן. מטמון שמחזיק רק את המשתמש האחרון שנכנס **שובר את הדרישה**:
   מי שקיבל מכשיר שמישהו אחר עבד בו נעול בחוץ.
   ⚠️ **וההודעות נבדלות זו מזו.** «אין לך במכשיר במה לאמת» אינו «סיסמה
   שגויה»: ⛔ הודעה מאוחדת היא כשל שקט שמוביל את המשתמש להקליד שוב ושוב
   סיסמה נכונה לחלוטין, במקום לומר לו את הדבר היחיד שיפתור — חיבור אחד.

7. **קובץ התקנה שמוכרז «אידמפוטנטי» חייב שורת התכנסות לכל שינוי מבני —
   ולא רק `create table if not exists`.** *(נלמד בסבב 27.)*
   `create table if not exists` **מדלג על טבלה קיימת, ועל כל מה
   שבתוכה**: עמודה שנוספה אחרי ההתקנה הראשונה, אינדקס, טריגר, פוליסה,
   אילוץ, או `drop default`. כלומר התקנה קיימת שמריצה את הקובץ מחדש
   מקבלת «רץ בהצלחה» — **ונשארת בלי השינוי**, בשקט.
   לכן לכל שינוי מבני שנעשה מאז ההתקנה הראשונה יושבת בקובץ **גם** שורת
   התכנסות: `add column if not exists` · `create index if not exists` ·
   `create or replace` לטריגר · `drop policy if exists` + `create policy` ·
   `alter column … drop default` · `revoke`+`grant` מפורשים.
   ⛔ **ואין לגעת בנתונים — מבנה בלבד.** ה-`UPDATE` היחיד שמותר הוא
   מילוי עמודה שזה עתה נוספה, ורק ב-`WHERE <col> IS NULL`.
   ⚠️ **הסעיף יושב דווקא כאן** מפני שקובץ ההתקנה הוא המקום שבו נולדים
   טבלת המשתמשים, ה-`role` והטביעה — כלומר בדיוק שלושת הסעיפים
   הראשונים שלמעלה. התקנה שהריצה את הקובץ מחדש ולא קיבלה את
   `pass_salt`/`pass_fp` או את `role` היא התקנה **בלי כניסה אופליין
   ובלי הרשאות**, בלי שאיש יידע.

8. **⛔ האיסור על זריעת פרטי כניסה חל על כל סוגי הקבצים — לא רק על
   מיגרציות.** *(נלמד בסבב 27.)* SQL · HTML · Markdown · README ·
   קובץ בדיקה · פיקסטורה · כל קובץ שנדחף לגיט. ⛔ **וגם ערך «לדוגמה»
   אסור** — מציין מקום בלבד, לעולם לא סיסמה אמיתית ולא סיסמה גנרית
   שמישהו עלול להקליד.
   ⚠️ **הרקע, וזה מה שהופך את הסעיף לנחוץ:** סעיף 3 כבר אסר את זה
   מסבב 21 — ובכל זאת סוד שרד בהנהלה עד סבב 27, ב-`setup-db.html`,
   מפני שהחיפוש שאישר את «אין בריפו פרטי כניסה» נעשה **בקבצי SQL
   ובקוד בלבד**. כלי עזר ב-HTML לא נסרק, והצהרת סבב 24 נכתבה על סמך
   סריקה חלקית. ⛔ **סריקה שמוגבלת לסוג קובץ אחד אינה ראיה** —
   היא ראיה לסוג הקובץ שנסרק, ותו לא.

9. **`anon` ו-`authenticated` מקבלים `SELECT`, `INSERT`, `UPDATE` — ותו
   לא.** *(נלמד בסבב 29.)* ⛔ **אין להעניק להם `DELETE` או `TRUNCATE`.**
   מחיקה בארגון היא **תמיד סימון** — `deleted=true` + חותמת, או tombstone
   בתוך הערך — ו**לעולם לא הסרת שורה** (כלל ברזל 6 סעיף 1). כלומר ההרשאה
   מיותרת בהגדרה, ובתמורה היא פותחת למי שמחזיק את המפתח הגלוי
   שב-`index.html` דרך למחוק שורות ואף לרוקן טבלה שלמה.
   ⚠️ **והכלל חל על שני התפקידים גם כשאין משתמשי Auth:** `authenticated`
   קיים בכל פרויקט Supabase ומחזיק את אותן הרשאות בירושה, ולכן פתיחת
   signup או Auth בעתיד הייתה פוערת את ההגנה **בשקט**. ⛔ `service_role`
   אינו בכלל הזה — הוא תפקיד השרת ואינו נגיש מהדפדפן.
   ⚠️ **וזה אינו מתקיים מאליו:** פרויקט Supabase סטנדרטי מגיע עם
   `alter default privileges … grant all on tables to anon, authenticated`,
   ולכן **כל טבלה נולדת עם `DELETE` ו-`TRUNCATE`**. ⛔ **`GRANT` הוא
   אדיטיבי ואינו מסיר דבר** — נדרש `REVOKE` מפורש, וכל מיגרציה שמוסיפה
   טבלה חייבת אחד משלה.
   ⛔ **וטבלאות יומן הן החריג, בכיוון ההפוך:** `sync_log` ו-`kv_backup`
   מקבלות `INSERT` ו-`SELECT` בלבד — **בלי `UPDATE` ובלי `DELETE`, לשני
   התפקידים כאחד** — כדי שלא ניתן יהיה לזייף רישום קיים ביומן ראיות ולא
   להעלים אותו. ⚠️ אין «ליישר» אותן לשאר הטבלאות בשם האחידות: הסט הצר
   שם **הוא** ההגנה, ו-`UPDATE` נחוץ בשאר הטבלאות רק מפני שהכתיבה שם
   היא upsert.
   ⛔ **ובדיקת הרשאות סורקת את שני התפקידים, תמיד** *(נלמד בהשלמת סבב
   29)* — **הרשאה שהוחלה על תפקיד אחד בלבד היא הגנה חלקית.** זה נמדד
   כאן בפועל: שתי טבלאות היומן החזיקו `INSERT, SELECT` ל-`anon` ואת
   הסט **המלא** ל-`authenticated`, ולכן ההגנה שתועדה כקיימת התקיימה מול
   מפתח אחד בלבד. ⚠️ סריקה שכיסתה תפקיד אחד היא ראיה לתפקיד שנסרק,
   ותו לא — בדיוק כמו סעיף 8 לגבי סוגי קבצים.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-26b-what" -->
## ⭐ סבב 26 — השלמה (אוגוסט 2026): ברירת מחדל שאינה מעניקה הרשאה, והערה שנרשמת בארבעתן

### מה נעשה
השלמה לסבב 26, לפני המיזוג. **התיעוד המשותף וההערה שבקוד השתנו בארבעת
הריפו; מעבר לכך נגע קוד ב-schar-limud וב-gius בלבד** — קבצי הסכימה
שלהן וקובץ הבדיקה של schar.

1. **`role` ב-`sl_users` — `NOT NULL` בלי `DEFAULT` כלל** (פרטי
   ל-schar-limud, ר' שם). הניסוח הראשון של סבב 26 היה `DEFAULT 'admin'`,
   כלומר ברירת מחדל ש**מעניקה** הרשאה — וזו הייתה החריגה היחידה בארגון:
   ב-`ys_users` אין `DEFAULT` כלל, וב-`g_users` יש `DEFAULT` אבל של
   התפקיד ה**נמוך** (`'manager'`), כלומר גם הוא נכשל סגור. גם
   `DEFAULT 'user'` היה עדיין סוטה מהדפוס: **תפקיד הוא החלטה, ולא ערך
   שנופל מאליו.**

   ⚠️ **המשמעות המעשית לשלוש האפליקציות שיש בהן משתמשים: יצירת משתמש
   מחייבת ציון `role` מפורש.** INSERT בלי העמודה הזו נכשל במסד, וזו
   ההתנהגות הרצויה. הערכים אינם משותפים:
   `'admin'` / `'user'` ב-schar-limud · `'admin'` / `'senior'` /
   `'junior'` ב-hanhala-ruchanit · `'owner'` / `'manager'` ב-gius.
   ⛔ **ואין ליישר ביניהם** — שלוש טבלאות נפרדות בשני פרויקטי Supabase
   נפרדים, כל אחת עם מודל ההרשאות שלה. ל-yoman-avoda אין משתמשים כלל.

2. **הערת ההגנה של אזור ההגדרות — נוספה בארבע האפליקציות.** הלקח של
   סבב 26 (שער סיסמה נפרד מעל מסך ההגדרות = שכבת הרשאה כפולה, ובהתקנה
   טרייה גם שער שנפתח למי שהקליד «admin») נרשם עד כה בריפו אחד בלבד —
   זה שבו הכשל נמדד. מעכשיו ההערה יושבת בראש אזור ההגדרות של כל אחת
   מארבע האפליקציות, **זהה בית-לבית** פרט לשורת «כאן:» שבסופה.
   היא קובעת שלושה דברים: ⛔ אין להוסיף שער סיסמה נפרד מעל מסך ההגדרות;
   ההרשאה נגזרת מהמשתמש המחובר בלבד; ⛔ ואין לזרוע ערך ברירת מחדל
   לסיסמה או לתפקיד בשום מקום.

3. **כלל ברזל 9 — סעיף חדש** (בלוק `iron-rule-9-security-spread`):
   לקח אבטחתי שנלמד באפליקציה אחת נרשם כהערת ⛔ בארבעתן, גם כשהמנגנון
   עצמו רלוונטי רק לאחת. הסעיף מבחין במפורש בין ה**החלטה** ששלוש
   התשובות מחייבות («הוחל» / «אינו רלוונטי, וזה הנימוק» / «פער עם
   טריגר») לבין ה**איסור** שיושב בקוד: הראשון עונה «האם ליישם כאן»,
   השני עונה «מה אסור לעשות כאן בעתיד».

4. **⭐ `g_users.role` — ה-DEFAULT הוסר, ושלוש הטבלאות מיושרות.** בהחלטת
   המנהל בוצע `ALTER TABLE public.g_users ALTER COLUMN role DROP DEFAULT`,
   אחרי שאומת שאין שורה עם `role` ריק ושהבעלים הקיים נשאר `owner`.
   מעכשיו `g_users` · `ys_users` · `sl_users` — שלושתן `TEXT NOT NULL`
   **בלי `DEFAULT`**, ובשלושתן **יצירת משתמש בלי `role` נכשלת במסד**.
   ⚠️ ⛔ הערכים עצמם עדיין אינם משותפים ואין ליישר אותם.
   ⚠️ ב-gius ה-DEFAULT הקודם (`'manager'`) **נכשל סגור** — הוא היה
   התפקיד הנמוך — ולכן זהו תיקון אחידות ולא סגירת פרצה.

5. **כלל ברזל 10 — מודל המשתמשים וההרשאות בארגון** (בלוק
   `iron-rule-10-users`): ששת הסעיפים שהתגבשו בסבבים 13–26, מנוסחים
   כדפוס מחייב לאפליקציה הבאה במקום כלקח שפזור בפרקי היסטוריה. הנימוק
   כתוב שם: מי שבונה אפליקציה חדשה **מעתיק ריפו קיים** ואינו קורא
   שלושים פרקי סבבים, ולכן דפוס שחי רק בהיסטוריה אינו עובר בהעתקה.

### ⚠️ `CACHE_NAME` לא קודם באף אחת מהארבע — וזו החלטה, לא השמטה
מה שנכנס ל-`index.html` כאן הוא **הערה בלבד**: אפס שינוי התנהגות, אפס
שינוי בקוד שרץ. קידום `CACHE_NAME` היה מציג לכל המשתמשים באנר «גרסה
חדשה זמינה» ומכריח טעינה מחדש — בשביל טקסט שאיש מהם לא יראה.
⚠️ **המחיר המודע:** עד הקידום הבא, המכשירים שכבר ביקרו ימשיכו להגיש
מהמטמון `index.html` בלי ההערה. זה מקובל בדיוק מפני שההערה מכוונת
לסשן הבא שקורא את הריפו, ולא למשתמש שמריץ את האפליקציה.
⛔ **הכלל עצמו לא נחלש:** כל שינוי שנוגע בקוד שרץ — ולו שורה אחת —
מחייב קידום, ר' «עדכוני Service Worker» וכללי הברזל למעלה.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud)

**א. ⭐ `role` — `NOT NULL` בלי `DEFAULT`, בשני קבצי הסכימה.**
`migrations/011_users_role.sql` ו-`000_initial_schema.sql` הגדירו
`role TEXT NOT NULL DEFAULT 'admin'`. מעכשיו `role TEXT NOT NULL`, בלי
`DEFAULT` כלל — הנימוק המלא בפרק המשותף שלמעלה ובראש קובץ המיגרציה.
- **⚠️ המיגרציה כבר הורצה במסד החי, כולל הסרת ה-DEFAULT** — העדכון כאן
  הוא **יישור הקובץ למצב בפועל**, לא שינוי שממתין להרצה. אומת מול המסד:
  `role` הוא `text`, `is_nullable = 'NO'`, **`column_default = NULL`**,
  והמשתמש היחיד מחזיק `'admin'`.
- **⚠️ סדר שלושת השלבים אינו ניתן לקיצור, ולכן הקובץ נכתב מחדש ולא
  נערך במילה אחת:** `ADD COLUMN … NOT NULL` **בלי** DEFAULT **נכשל** על
  טבלה שיש בה שורות — Postgres אינו יכול למלא את העמודה. לכן:
  `ADD COLUMN role TEXT` (nullable) → `UPDATE … WHERE role IS NULL` →
  `ALTER COLUMN role SET NOT NULL` → `ALTER COLUMN role DROP DEFAULT`.
  אותו רצף בדיוק נכנס לבלוק השדרוג שב-`000`.
- **⚠️ ה-UPDATE הוא הנגיעה היחידה בנתונים בכל המיגרציה**, והוא מסויג
  ב-`WHERE role IS NULL` — כלומר נוגע **רק** בשורות שקדמו לעמודה. שורה
  שכבר יש לה תפקיד אינה נגעת. ⛔ הסייג הזה אינו קישוט: בלעדיו ה-UPDATE
  היה **דורס תפקידים קיימים** ומעניק `'admin'` לכולם.
- **יצירת משתמש מחייבת `role` מפורש** — `'admin'` (גישה מלאה, כולל
  הגדרות) או `'user'`. INSERT בלי העמודה נכשל במסד, וזו ההתנהגות הרצויה.
  שתי הדוגמאות כתובות בקובץ המיגרציה, וה-INSERT שבמסך ההגדרה הראשונית
  (`index.html`) כבר כלל `role` מסבב 26.
- ⛔ **`index.html` לא נגע** מלבד הערת ההגנה שבסעיף ב — לוגיקת
  `slSettingsAccess` כבר נכשלת סגור על תפקיד חסר או לא-מוכר, ולכן היעדר
  ה-DEFAULT אינו משנה בה דבר.

**ב. הערת ההגנה של אזור ההגדרות** נוספה ב-`index.html`, מיד מעל הערת
סבב 26 הקיימת שמעל `showPanel`/`renderSettingsPanel`. הליבה זהה
בית-לבית לשלוש האחיות; שורת «כאן:» היא היחידה שנבדלת. ⚠️ **זו האפליקציה
שבה הלקח נמדד** — ההערה כאן היא תיעוד של מה שקרה, ובשלוש האחרות היא
מניעה של מה שעלול לקרות.

**ג. `tools/test_round26.mjs` — 93 ← 97 טענות.** ארבע חדשות, ושלוש
קיימות **חודדו ולא הוחלשו**:
- הטענה «000 מגדיר `role`» עברה מ-`DEFAULT 'admin'` ל-`NOT NULL,`,
  ולצידה נוספה טענה הפוכה ומפורשת: ⛔ **אין ל-`role` שום DEFAULT** בשום
  קובץ סכימה (שורת `DROP DEFAULT` מנוטרלת מהבדיקה — היא ההפך ממה שנאסר).
- הטענה «011 אינה מוחקת ואינה מעדכנת נתונים» **לא נמחקה** — היא פוצלה
  לשלוש טענות צרות יותר, כי 011 כן מריצה עכשיו `UPDATE` אחד ו-`DROP`
  אחד: ה-`UPDATE` **היחיד** חייב לשאת `WHERE role IS NULL` ו-`SET role
  = 'admin'`; ה-`DROP` **היחיד** חייב להיות `DROP DEFAULT`; ואין
  `DELETE` ואין `DROP TABLE`/`DROP COLUMN`.
- הטענה «ברירת המחדל היא admin» **הוחלפה בהיפוכה**: 011 אינה משאירה
  `DEFAULT` על `role`, ונועלת `SET NOT NULL`.
- נוספה טענה ש-`000` כולל את שני שלבי הנעילה בבלוק השדרוג.

**ד. כלל ברזל 10 — הסעיף שבו schar היא המקור לשלושה מששת הסעיפים.**
הבלוק `iron-rule-10-users` הוא משותף, ולכן זהה בארבעתם — אבל שלושה
מסעיפיו נלמדו כאן: **סעיף 1** (`role` בלי DEFAULT, סבב 26 והשלמתו),
**סעיף 2** (⛔ אין שער סיסמה נפרד — זו האפליקציה **היחידה** שהיה בה
שער כזה, והוא הוסר), ו**סעיף 4** (הטביעה במקום הסוד — נולד כאן בסבב 13
על `admin_pass`, והורחב למשתמשים בסבב 24). ⚠️ ובכל זאת הבלוק מנוסח
**בלשון הארגון ולא בלשון schar** — הוא כלל לאפליקציה הבאה, לא סיכום של
ההיסטוריה כאן.

**ה. שלוש חתימות SHARED עודכנו** בארבעת עותקי `tools/check-docs.mjs`:
`iron-rule-9-security-spread` · `iron-rule-10-users` (בלוק חדש) ·
`round-26b-what` (הורחב בהשלמה).

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **`node tools/test_round26.mjs` — 97 עברו, 0 נכשלו.**
- **`node tools/test_round24.mjs` — 140 עברו, 0 נכשלו** (לא נגע).
- **⭐ שלוש מוטציות על הטענות החדשות, כל אחת מפילה** (הבודק אינו חותמת
  גומי; נמדד):
  1. `DEFAULT 'admin'` חוזר ל-`000` ⇒ **2** טענות נופלות.
  2. ה-`UPDATE` ב-011 מאבד את `WHERE role IS NULL` — כלומר דורס תפקידים
     קיימים ⇒ 1 נופלת.
  3. `ALTER COLUMN role DROP DEFAULT` נמחק מ-011 ⇒ 1 נופלת.
- **מצב העמודה אומת מול מסד הייצור, לא הוצהר** — ר' סעיף א.
- **ליבת הערת ההגנה נמדדה כזהה בית-לבית בארבע האפליקציות** — חושבה
  בארבעתן והושוותה זו לזו: sha256[:16] = `0c399aa3a60d0cb2` (הקטע
  שמתחילת הבלוק ועד שורת «כאן:»).
- **שלושת בלוקי ה-SHARED נמדדו כזהים בית-לבית בארבעת הקבצים** — חושבו
  בארבעתם והושוו זה לזה, לא הוכרזו: `8ab6dcc38c449f51` (כלל ברזל 9) ·
  `91911ec339c9e6fb` (כלל ברזל 10) · `46da920b39793d46` (round-26b).
- ⛔ **`git diff` על `sw.js` — ריק.** ב-`index.html` הדיפרנס הוא **הערה
  בלבד** — אומת שאין בו שום שורת קוד.

---

<!-- SHARED:start id="capability-matrix" -->
## ⭐ מטריצת היכולות של ארבע האפליקציות (סבב 27, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> **מה שנבדל בין האפליקציות — נבדל בכתב, בטבלה אחת.** סבבים 17–26 חזרו
> ומצאו שוב ושוב הבדלי יכולת שאיש לא החליט עליהם ואיש לא רשם אותם: אחת
> עובדת אופליין והשנייה לא, לאחת כניסה מרובת-משתמשים ולשנייה מטמון של
> משתמש אחד, לאחת קובץ התקנה ולשנייה שדרוגים בלי מקור אמת. בכל אחד
> מהמקרים ה**גילוי** עלה סבב שלם. התשובה לשאלה «מה ההבדל בין
> האפליקציות» צריכה להיות **כתובה**, לא משהו שמחפשים בקוד.

⚠️ **הטבלה נמדדה מהקוד ולא הוצהרה** — כל תא אומת מול `index.html`,
`sw.js`, `migrations/`, `signing/` ו-`android/` של האפליקציה שלו.
⛔ **ומאותה סיבה אין לעדכן אותה «לפי ההיגיון»:** סבב שמשנה יכולת מודד
מחדש את התא שהוא שינה ומעדכן אותו **בארבעת הקבצים באותו סבב** (כלל
ברזל 8 סעיף 3).

### שלושת הערכים — ומה ההבדל ביניהם
- **✅** — היכולת קיימת ופעילה.
- **❌** — היכולת **חסרה** באפליקציה שהיא כן רלוונטית לה. כל ❌ נושא
  **נימוק**, ואם הוא פער — גם **טריגר**: האירוע שמחייב לפתוח סבב.
- **לא רלוונטי** — אין לאפליקציה הזו את המושג שהיכולת מדברת עליו, ולכן
  אין מה לממש. ⛔ זה **אינו** «❌ מנומס»: ❌ אומר «חסר»; «לא רלוונטי»
  אומר «אין כאן שאלה כזו».

| # | יכולת | yoman-avoda | hanhala-ruchanit | schar-limud | gius |
|---|---|---|---|---|---|
| 1 | כניסה אופליין | לא רלוונטי | ✅ | ✅ | ✅ |
| 2 | עריכת נתונים אופליין | ✅ | ✅ | ✅ | ✅ |
| 3 | מודל משתמשים | אין | רב-משתמשים | רב-משתמשים | רב-משתמשים |
| 4 | נתיב עדכון חלקי למראת המשתמשים | לא רלוונטי | ✅ | ❌ | ✅ |
| 5 | סוד במכשיר | אין | טביעה | טביעה | טביעה |
| 6 | פינוי אוטומטי | ✅ | ✅ | ❌ | ✅ |
| 7 | אימות פינוי מול הענן | ✅ | ✅ | לא רלוונטי | ❌ |
| 8 | שיתוף קבצים | ✅ | ❌ | ❌ | ❌ |
| 9 | מעטפת APK (WebView) | ✅ | ✅ | ✅ | ✅ |
| 10 | מפתח חתימה קבוע בריפו | ✅ | ✅ | ✅ | ✅ |
| 11 | מקור אמת יחיד לסכימה | ✅ | ❌ | ✅ | ✅ |
| 12 | קובץ התקנה מלא | ✅ | ✅ | ✅ | ✅ |
| 13 | גיבוי יומי אוטומטי | ✅ | ✅ | ✅ | ✅ |
| 14 | יומן פעולות | ✅ | ✅ | ✅ | ✅ |
| 15 | נתונים בטבלאות מובנות | ❌ | ❌ | ✅ | ✅ |

### הנימוקים — שורה אחר שורה
**1 · כניסה אופליין.** ל-yoman **אין טבלת משתמשים, אין מסך כניסה ואין
תפקידים** — הכניסה היא בחירת מוסד, ולכן אין מה לאמת. בשלוש האחרות
האימות הוא מול `pass_salt`+`pass_fp` שבמכשיר (סעיף 4 של כלל ברזל 10):
hanhala בסבב 22, gius בסבב 23, schar-limud בסבב 24.

**2 · עריכת נתונים אופליין.** בארבעתן, לפי כלל ברזל 6: כתיבה
מקומית-תחילה, מיזוג ברמת רשומה, tombstones, וסימון ⏳ עד שיש ראיה
מהענן. schar-limud הייתה האחרונה שנסגרה (סבב 12 שלב 2), ו-gius לפניה
(סבב 11).

**3 · מודל משתמשים.** ל-yoman אין. בשלוש האחרות `role TEXT NOT NULL`
בלי `DEFAULT`, ו⛔ **הערכים אינם משותפים ואין ליישר אותם**:
`'admin'`/`'senior'`/`'junior'` בהנהלה · `'admin'`/`'user'` ב-schar ·
`'owner'`/`'manager'` ב-gius.

**4 · נתיב עדכון חלקי למראת המשתמשים.** כתיבת שורת משתמש **בודדת**
למראה, בלי משיכה מלאה — כך שהחלפת משתמש במכשיר אינה תלויה בסנכרון הבא.
hanhala (`ysUsersCacheSave`) ו-gius (`usersCacheSave`, סבב 26) — ✅.
❌ **ב-schar-limud, וזה אינו פער:** שם `doLogin` מריצה **משיכה מלאה
כפויה** (`slPullUsers(true)`) מיד אחרי כניסה מוצלחת, וזה ממלא בדיוק את
אותו תפקיד. ⛔ ואם ייבנה שם נתיב חלקי בעתיד — הוא חייב לעבור דרך
`slUserPub`, כמו שלוש נקודות האכיפה הקיימות (כלל ברזל 10 סעיף 5).
ל-yoman אין מראת משתמשים כלל.

**5 · סוד במכשיר.** בשלוש שיש בהן משתמשים יורדת **טביעה בלבד** —
PBKDF2-SHA256, 100,000 סיבובים, מלח אקראי פר-משתמש עם קידומת הקשר.
⛔ **הסיסמה עצמה אינה יורדת לדיסק באף אחת מהן.** ב-yoman אין סוד
כלשהו במכשיר, מפני שאין משתמשים.

**6 · פינוי אוטומטי.** yoman (`tb_archive`/`tb_entries`, רצפות 60/90
יום) · hanhala (ארבעת מפתחות ה-`kv`, 180 יום) · gius (`g_mirror_txns`/
`g_mirror_pledges` 400 יום, `g_mirror_tasks` 180).
❌ **ב-schar-limud, וזו החלטה מתועדת ולא פער:** `tier1` ו-`tier2` ריקים
בכוונה — `sl_mirror_transactions` הם **כספים**, ו-`sl_mirror_students`
הם אבות המפתח הזר שלהם. ⛔ אין להוסיף שם פינוי; בקשה «לחסוך מקום»
היא בקשה לפתוח את סבב «מראה-חלון» המתועד שם, לא לאלתר טלאי.

**7 · אימות פינוי מול הענן** (סבב 14 — `idOf`+`verify` על `tier2`).
yoman ו-hanhala — ✅. ל-schar אין מה לאמת, מפני שאין שם פינוי כלל
(שורה 6) — לכן «לא רלוונטי» ולא ❌.
❌ **ב-gius, וזה אינו פער:** העֵד שלה `g_synced_at` נכתב אחרי **משיכה
מלאה מוצלחת של כל שבע הטבלאות**, כלומר הוא כבר עדות מהענן ונכתב גם
בדפדפן שרק קורא — ולכן הבאג שסבב 14 בא לפתור מעולם לא היה קיים שם.

**8 · שיתוף קבצים.** ל-yoman יש גשר מקורי לשיתוף דוח כתמונה, **מוגבל
ל-origin שלנו** ב-`addWebMessageListener` עם נפילה-חזרה נעולה-כפליים
(סבב 13). בשלוש האחרות **אין `navigator.share` בקוד כלל** (אומת
ב-grep), ולכן אין גשר — וזו ההחלטה הנכונה: ⛔ לא נותנים גשר מקורי לדף
שנטען מהרשת בלי צורך. אם יידרש שם שיתוף — **מעתיקים את הדפוס של
yoman**, לעולם לא `addJavascriptInterface` חשוף.

**9 · מעטפת APK.** בארבעתן WebView מקורי שטוען מהרשת. ⛔ **לעולם לא
TWA ולא PWABuilder** — TWA מריץ את האתר בתוך כרום, וסינון התוכן
במכשירי המשתמשים חוסם את כרום. זה נמדד בפועל: ה-TWA של gius פשוט לא
נפתח.

**10 · מפתח חתימה קבוע בריפו.** לכל אחת keystore משלה, וטביעת ה-SHA
שלה כתובה בפרק «חתימת APK» ה**פרטי** שלה. ⛔ הפרק הזה פרטי בהגדרה
(כלל ברזל 8 סעיף 5) — איחוד שלו יהיה שגוי ומסוכן.

**11 · מקור אמת יחיד לסכימה** — קובץ אחד בריפו שהוא ההגדרה המלאה של
המסד. schar-limud: `migrations/000_initial_schema.sql`. gius:
`migrations/0001_init.sql` (+0002/0003 קדימה). yoman-avoda:
`migrations/000_initial_schema.sql`, שנוצר בסבב 28.
❌ **ב-hanhala בלבד — פער מתועד שנותר פתוח.** קובץ ההתקנה נוצר גם שם
בסבב 28 (שורה 12), אבל `index.html` **מחזיק עדיין עותק חלקי** של
הסכימה: `showSetupScreen` ו-`setupWithServiceKey` בונים `create table`
ל-`ys_users` בלבד. ⛔ שני מקורות לאותה טבלה הם בדיוק הדפוס שהתיישן
בשקט ב-schar (סבב 6) והוסר שם בהשלמת סבב 24.
**⚠️ הטריגר:** הסבב הבא שנוגע במסך ההגדרה הראשונית שב-`index.html` —
הוא מחליף את ה-SQL המוטבע בהפניה לקובץ, בדיוק כפי ש-`setup-db.html`
כבר עושה. ⛔ סבב 28 לא נגע בו, מפני שהוא סבב **הערות בלבד** בקוד הרץ.

**12 · קובץ התקנה מלא** — קובץ שהרצתו על מסד ריק נותנת התקנה עובדת.
✅ **בארבעתן, מסבב 28.** שתי האפליקציות שחסרו קיבלו שם קובץ שמופה
**מהמסד החי** ולא מקריאת הקוד:
- **yoman-avoda:** `migrations/000_initial_schema.sql` — `kv_rishon`
  ו-`kv_ramataviv` עם RLS, פוליסות והרשאות, ופרק שמסביר שההפרדה בין
  המוסדות היא **טבלה בענן וסיומת מפתח במכשיר**.
  ⚠️ `supabase-multitenant.sql` **אינו** קובץ התקנה ומעולם לא היה כזה:
  הוא מסתיים ב-`INSERT … SELECT … FROM public.kv`, כלומר הגירה חד-פעמית
  מטבלה שכבר אינה קיימת. הוא סומן בכותרתו כהיסטורי. ⛔ אין להריץ אותו.
- **hanhala-ruchanit:** `migrations/000_initial_schema.sql` — `kv`,
  `ys_users` (כולל `pass_salt`/`pass_fp` מסבב 22 וה-`CHECK` על `role`),
  `sync_log` ו-`kv_backup`. `setup-db.html` **מושך אותו** ואינו מחזיק
  עותק.
- **⚠️ ומה שעדיין לא נעשה, במפורש:** שני הקבצים **לא הורצו מול המסד**
  ולא נגעו בנתונים. הם נמסרים לאימות חיצוני **טבלה-טבלה** לפני המיזוג,
  וכל מקום שבו לא הושגה ודאות רשום בגוף הקובץ. ⛔ «נכתב» אינו «אומת».

**13 · גיבוי יומי אוטומטי** (סבב 30, שלב א) — ✅ **בארבעתן**, מאותו מודול
משותף. עד הסבב הזה גיבו רק hanhala ו-schar, כל אחת במימוש משלה; הליבה
חולצה, והכיסוי הורחב. ⚠️ **וזה נמדד ולא הונח:** הגיבוי של hanhala
**הפסיק לרוץ ב-2026-07-20** ואיש לא ידע — בדיוק הסיבה ש«גובה לאחרונה»
נוסף ל«מידע טכני» בארבעתן. ⛔ אין להשוות «יש גיבוי» ל«הגיבוי רץ».

**14 · יומן פעולות** (סבב 30, שלב א) — ✅ **בארבעתן**, ל-`sync_log`.
עד הסבב הזה היה רק בהנהלה. ⚠️ **מה שנרשם נבדל לפי האפליקציה** ואינו
מיושר: הנהלה רושמת כניסות וסנכרון, gius כניסות וגיבוי, schar ו-yoman
גיבוי בלבד. ⛔ **המשותף הוא המנגנון — הטבלה, התור האופליין והתקרה —
ולא רשימת האירועים**, שהיא פר-אפליקציה מעצם טבעה.

**15 · נתונים בטבלאות מובנות** — שורה לכל רשומה, ולא ערך יחיד.
schar-limud (חמש טבלאות `sl_`) ו-gius (שבע טבלאות `g_`) — ✅ מאז ההקמה.
❌ **ב-yoman-avoda, וזה פער שנפתח בסבב 30 ועדיין לא נסגר:** שכבת השורות
נכתבה (`migrations/002`+`003`, `tbRowsGet`/`tbRowsPush`), אבל **המיגרציה
טרם הורצה** ולכן הנתונים עדיין יושבים כערך יחיד ב-`kv_rishon`/
`kv_ramataviv`. ⛔ «נכתב» אינו «אומת», והתא יתהפך רק אחרי שהמיגרציה תרוץ
ובדיקת השקילות תחזור נקייה. **⚠️ הטריגר:** הרצת שלב א ואימות שהגיבוי
כתב בפועל, ואז 002+003.
❌ **ב-hanhala-ruchanit, וזה פער מתועד ולא החלטה:** **כל** נתוני
האפליקציה יושבים כ-15 מפתחות ב-`kv`, כשהגדול בהם (`ys_attend_sessions`)
הוא ~286KB — כלומר כל סימון נוכחות דורס את מלוא הערך.
**⚠️ הטריגר:** אחרי ש-yoman תרוץ על הטבלאות שבועיים ותוכיח את הדפוס,
הוא מועתק לכאן בסבב ייעודי. ⛔ ולא לפני — מעבר שני לפני שהראשון אומת
מכפיל סיכון במקום ללמוד ממנו.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-27-what" -->
## ⭐ סבב 27 (אוגוסט 2026) — תיעוד ואיכות

### מה נעשה
**סבב תיעוד ואיכות. ⛔ אפס שינוי התנהגות בארבע האפליקציות.** מה שנכנס
ל-`index.html` הוא **הערות בלבד**, ומה שנכנס לקבצי הסכימה הוא **שורות
התכנסות מבניות** שאינן נוגעות בנתונים.

1. **תיעוד פנימי ב-gius** (פרטי, ר' שם) — כותרות בלוק לכל אזור לוגי,
   נימוקים להחלטות שנראות שרירותיות, והערות ⛔ במקומות שבהם «תיקון
   טבעי» היה מחזיר באג שכבר תוקן.
2. **מטריצת היכולות** — הפרק שמעל, בלוק `capability-matrix`. שתים-עשרה
   יכולות × ארבע אפליקציות, כל תא ✅ / ❌ / «לא רלוונטי», וכל ❌ עם
   נימוק — ואם הוא פער, גם עם טריגר.
3. **אידמפוטנטיות אמיתית של קבצי ההתקנה** — ר' סעיף 4, ובפרקים הפרטיים
   של schar-limud ושל gius.
4. **כלל ברזל 10 גדל לשמונה סעיפים** — **סעיף 7** קובע שקובץ התקנה
   שמוכרז אידמפוטנטי חייב **שורת התכנסות לכל שינוי מבני**, ולא רק
   `create table if not exists`; **סעיף 8** מרחיב את איסור זריעת פרטי
   הכניסה **לכל סוגי הקבצים** — SQL, HTML, Markdown וקבצי בדיקה — אחרי
   שסוד שרד בהנהלה שישה סבבים מפני שהסריקה כיסתה קבצי SQL בלבד.

### ⚠️ שלוש הנחות שנמדדו — ושתיים מהן לא אושרו
הבקשה לסבב נשענה על מדידה חיצונית. ⛔ **המדידה הוחזרה כאן מהקוד ולא
הועתקה**, ולכן נרשם גם מה שלא התאים — כלל ברזל 8 סעיף 6 בכיוון שלו:
מספר שמדווח בלי שנמדד מחדש נקרא כעדות.

1. **«יחס ההערות ב-gius ~5% מול 11–13% באחיות».** ⛔ **לא שוחזר.**
   נמדד כאן על **האזורים הפרטיים בלבד** (חמשת הבלוקים המשותפים
   והמודולים הקפואים הוחרגו — הם זהים בארבעתן ולכן מטים כל השוואה),
   על ה-JS המוטבע: **gius 11.6% · hanhala 11.0% · yoman 14.2% ·
   schar 20.9%**. כלומר gius אכן בקצה הנמוך — אבל **hanhala נמוכה
   ממנה**, והפער אינו פי-שניים ואינו פי-שלושה. ⚠️ **העבודה נעשתה
   במלואה בכל זאת**, מפני שהיא נכונה לגופה ולא בגלל המספר: התוספת היא
   נימוקים והערות ⛔ במקומות שנמדדו כמסוכנים, לא מילוי מכסה.
2. **«ב-schar 5 טבלאות מול 2 שורות התכנסות».** ⛔ **לא שוחזר.** נסרקו
   כל העמודות שנוספו ב-001–011 מול שורות ההתכנסות שב-`000_initial_schema.sql`:
   **לכל אחת מהן כבר יש `add column if not exists`**, וכך גם לכל אינדקס,
   טריגר, פוליסה ואילוץ. מה שנעשה שם הוא **עיגון הכלל בכותרת הקובץ**
   ותיקון תיאור מיושן שנשאר בו, ולא הוספת שורות חסרות.
3. **«ב-gius 7 טבלאות מול שורת התכנסות אחת».** ✅ **שוחזר, ותוקן.**
   שלושה פערים אמיתיים נמצאו ונסגרו שם — ר' הפרק הפרטי של gius.

### תיאום גרסאות
`sw.js`: hanhala **v35** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v26** · gius **gius-v13**. **רק gius קודמה** — היא היחידה
ש-`index.html` שלה השתנה בסבב הזה.
⛔ **ובשלוש האחרות הקידום היה שגוי:** קידום בלי שינוי קוד שולח לכל
המשתמשים באנר «גרסה חדשה זמינה» לשום שינוי.
<!-- SHARED:end -->
### מה נעשה כאן (schar-limud)

**א. ⚠️ הכלל עוגן בכותרת `migrations/000_initial_schema.sql` — ולא נוספו
שורות התכנסות, מפני שלא היו חסרות.**
ההנחה שבבקשה («5 טבלאות מול 2 שורות התכנסות») **נבדקה ולא שוחזרה**:
נסרקו כל העמודות שנוספו במיגרציות 001–011 והושוו לשורות שב-`000`, ולכל
אחת מהן כבר יש `ADD COLUMN IF NOT EXISTS` — וכך גם לכל אינדקס, טריגר,
פוליסה ואילוץ (האילוץ ב-`DO` block עם בדיקת `pg_constraint`, כי אין
`ADD CONSTRAINT IF NOT EXISTS` ב-Postgres). ⛔ **ולכן לא נוספה כאן אף
שורת SQL** — הוספת שורות «ליתר ביטחון» לקובץ שרץ על מסד ייצור היא בדיוק
מה שאסור.
מה שכן נעשה בכותרת:
- **הכלל נכתב במפורש** — למה `CREATE TABLE IF NOT EXISTS` לבדו הוא
  אידמפוטנטיות חסרת ערך, אילו צורות התכנסות נדרשות, ו⛔ שאין לגעת
  בנתונים (ה-`UPDATE` היחיד בקובץ הוא זה שממלא `role`, מסויג
  ב-`WHERE role IS NULL`).
- **✅ הרישום שהמצב נמדד** — ולא הוצהר — כדי שהסבב הבא לא יסרוק שוב.
- ⚠️ **ותוקן תיאור מיושן שנשאר בכותרת:** היא עדיין אמרה ש-`index.html`
  מחזיק «עותק-גיבוי מוטבע» ושיש `tools/check-sql-sync.py` שנכשל אם הם
  נפרדו. **שניהם הוסרו בהשלמת סבב 24**, וכתוב שם למה. תיאור שער שאינו
  קיים הוא בדיוק הכשל של כלל ברזל 8 סעיף 6.
- **⚠️ תוספת לכלל:** מיגרציה חדשה מחייבת **גם** שורה כאן. קובץ שגדל רק
  ב-`00N_` ולא ב-`000` שובר את ההבטחה שבראש הקובץ עצמו.

**ב. ⛔ `index.html` ו-`sw.js` לא נגעו.** `CACHE_NAME` נשאר **v27**
(סבב 26), ואין מיגרציה חדשה — 005 עדיין היחידה שלא תורץ, ובכוונה.

**ג. שורות 6–7 ו-11–12 במטריצה — מה שנרשם על האפליקציה הזו.**
✅ **קובץ התקנה מלא ומקור אמת יחיד לסכימה** — זו אחת משתי האפליקציות
שיש לה. ❌ **פינוי אוטומטי** — `tier1`/`tier2` ריקים בכוונה
(`sl_mirror_transactions` הם כספים), ולכן «אימות פינוי מול הענן» מסומן
**«לא רלוונטי» ולא ❌**: אין כאן פינוי, ולכן אין מה לאמת. ⛔ שני התאים
האלה אינם פער וההבחנה ביניהם אינה קוסמטית.

**ד. שלוש חתימות SHARED עודכנו** בארבעת עותקי `tools/check-docs.mjs`:
`iron-rule-10-users` (סעיף 7 החדש) · `capability-matrix` (בלוק חדש) ·
`round-27-what` (בלוק חדש).

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **`node tools/test_round24.mjs` — 140 עברו, 0 נכשלו** (לא נגע).
- **`node tools/test_round26.mjs` — 97 עברו, 0 נכשלו** (לא נגע).
- **⚠️ ביקורת ההתכנסות רצה כמדידה ולא כשער** — היא סריקה חד-פעמית שנעשתה
  כדי להכריע החלטה, ולכן ⛔ **אינה מוצגת כבדיקה ולא נשמרה כקובץ**
  (כלל ברזל 8 סעיף 6, הסייג שבסופו).
- **שלושת בלוקי ה-SHARED נמדדו כזהים בית-לבית בארבעת הקבצים** — חושבו
  בארבעתם והושוו זה לזה, לא הוכרזו.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**

---

<!-- SHARED:start id="iron-rule-11-comments" -->
## ⭐ כלל ברזל 11 — תקן ההערות בקוד (סבב 28, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את עשרת כללי הברזל שלמעלה.

> **הערה על אותו סוג דבר נראית אותו דבר בארבע האפליקציות.** כלל ברזל 7
> דרש עד היום זהות **תוכן** — הבלוקים המשותפים זהים בית-לבית. מעכשיו
> נדרשת גם זהות **צורה** באזורים הפרטיים: כותרת בלוק אחת, שלושה סימנים,
> תבנית איסור אחת ומילון מונחים אחד — כדי ששתי אפליקציות זו לצד זו
> ייקראו כשני עותקים של אותו ספר, ולא כשני פרויקטים של שני אנשים.

### הבעיה שנמדדה
סבב 27 הוסיף תיעוד פנימי ל-gius, ומדד אגב כך שהאחיות נבדלות זו מזו
**לא באחוזים אלא בכיסוי ובצורה**: אזורים לוגיים שלמים בלי שום כותרת,
הערות ⛔ שאומרות «אל תעשה» בלי לומר **מתי זה נלמד ולמה**, סימנים
שהומצאו לצורך שורה אחת, ואותו מושג בשלושה כתיבים.
⛔ **והנזק אינו אסתטי: הערת איסור בלי סבב ובלי סיבה היא הזמנה להסיר
אותה.** הסשן הבא קורא «אל תעשה X», אינו מוצא הצדקה, ומסיק שזו קפריזה —
ומחזיר בדיוק את הבאג שהאיסור בא למנוע.

### ששת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם.

1. **כותרת בלוק לכל אזור לוגי, בפורמט אחד ויחיד.**
   *(נלמד בסבב 27 ב-gius, והורחב לארבעתן בסבב 28.)* המסגרת היא **בדיוק
   74 תווי `═`**, שורת השם היא שלושה רווחים ואחריהם שם האזור (עד 78
   תווים), ואחריה מסגרת נוספת — גוף אופציונלי — ומסגרת סוגרת:

   ```
   /* ══…(74)══
      <שם האזור>
      ══…(74)══
      [גוף אופציונלי — נימוקים, ⛔, ⚠️]
      ══…(74)══ */
   ```

   ⚠️ **הרוחב הקבוע אינו קישוט:** הוא מה שהופך «אותו דבר» למדיד. מסגרת
   ברוחב אחר נראית לעין כמעט זהה, ולכן היא נסחפת בשקט — בדיוק הסחיפה
   שכלל ברזל 7 נבנה נגדה.

2. **שלושה סימנים בלבד — ⛔ איסור · ⚠️ אזהרה · ⭐ החלטה מרכזית.**
   *(נלמד בסבב 28.)* ⛔ **ואין להמציא סימנים נוספים.** סימן רביעי אינו
   מוסיף מידע — הוא מוריד את המשקל של השלושה: קורא שרואה חמישה סמלים
   שונים מפסיק להתייחס לאף אחד מהם כאל עצירה.
   ⚠️ הכלל חל על **פתיחת שורת הערה**. ✅/❌/⏳ בתוך משפט, או בתוך מחרוזת
   שמוצגת למשתמש, אינם סימון — הם טקסט.

3. **כל ⛔ נושא את הסבב שבו נלמד ואת הסיבה** — בתבנית
   **«⛔ אל תעשה X (סבב N) — הסיבה»**. *(נלמד בסבב 28.)* שני החלקים
   נדרשים **יחד**: הסבב הוא הדרך למצוא את הרקע המלא בלי לחפש, והסיבה
   היא מה שמונע מהסשן הבא לבטל את האיסור בתום לב.
   ⚠️ איסור שקדם למספור הסבבים נרשם כ«(נרשם בסבב N)» — האמת היא שהוא
   **תועד** אז, לא שנולד אז, ואין לכתוב מספר סבב שלא היה.

4. **טרמינולוגיה אחידה — אותו מונח עברי לאותו מושג בארבעתן.**
   *(נלמד בסבב 28.)* **מראה** (לא «מירור») · **טביעה** (לא «טביעת
   אצבע») · **אופק הפינוי** · **שער** · **פינוי** · **דחיפת-מצב**
   (עם מקף) · **מטמון** (לא «קאש») · **אופליין** · **tombstone**
   באנגלית. ⚠️ שני כתיבים לאותו מושג הופכים כל חיפוש טקסטואלי בין
   הריפו ל**חלקי** — וחיפוש חלקי הוא בדיוק איך שנשמט נתיב כתיבה שני
   (כלל ברזל 10 סעיף 5).

5. **כל החלטה ארכיטקטונית שנראית שרירותית מנומקת במקומה.**
   *(נלמד בסבב 27 ב-gius.)* קבוע שנראה שרירותי (רצפת גיל, תקרה,
   השהיה), טבלה שמוחרגת מרשימה, סדר שתי שורות שאסור להפוך — הנימוק
   יושב **ליד הקוד**, ולא רק ב-CLAUDE.md. ⚠️ מי שעורך את הקוד אינו
   קורא שלושים פרקי סבבים לפני שהוא נוגע בשורה.

6. **⛔ הבלוקים המשותפים והמודולים הקפואים אינם באזור התחולה.**
   *(נלמד בסבב 28.)* חמשת הבלוקים של סבבים 11–17 זהים בית-לבית
   ונשמרים ע"י `check-status-area.mjs`; עריכת הערה בתוכם מחייבת שינוי
   בארבעת הריפו **ובחתימות**. כלומר תקן שהיה חל עליהם היה **מייצר
   בעצמו** את הסחיפה שהוא בא למנוע. התקן חל על **הקוד הפרטי בלבד**.

### הבדיקה האוכפת
`tools/check-comments.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא
לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם
שערי התחביר לפני כל דחיפה. היא נכשלת על ארבעה סוגי סטייה:
- **א. כותרת בלוק** — כותרת שצורתה אינה תקנית (רוחב המסגרת, שורת השם),
  או **רצף של יותר מ-400 שורות קוד פרטי לא-ריקות בלי כותרת**.
  ⚠️ הכיסוי נמדד כמרווח בין כותרות ולא כ«כותרת לכל פונקציה»: אזור לוגי
  הוא רעיון, לא הצהרה. והמרווח נספר ב**שורות קוד פרטי לא-ריקות** ולא
  במספרי שורות — בין שני אזורים יושבים HTML ו-CSS, וספירה גולמית הייתה
  מדווחת «1700 שורות בלי כותרת» על אזור שאין בו קוד כלל.
- **ב. ⛔ בלי סבב או בלי סיבה** — בלוק הערה שיש בו ⛔ חייב לשאת
  `סבב <N>` **וגם** מפריד « — » שאחריו הנימוק.
- **ג. סימן שאינו מהרשימה** — שורת הערה שנפתחת בסמל שאינו ⛔/⚠️/⭐.
- **ד. טרמינולוגיה** — מונח מהרשימה בכתיב שאינו התקני.

⚠️ **הבדיקה קוראת הערות בלבד, דרך טוקניזציה של JS** (מחרוזות, תבניות
ו-regex). ⛔ **אין להחליף אותה בביטוי רגולרי על `//` ועל פתיחת בלוק:**
כל URL מכיל `//`, ולכן מחרוזת הייתה נקראת כהערה — `'✅ נשמר'` היה נספר
כסימן אסור, וביטוי רגולרי שמכיל כוכבית היה פותח בלוק שלא נסגר.

### ⛔ מה התקן אינו
**אינו יעד אחוזים.** ⛔ אין להשוות יחסי הערות בין האפליקציות, ואין
להמציא הערות כדי להגיע למספר. הדרישה היא **כיסוי**: כל אזור לוגי, כל
החלטה ארכיטקטונית וכל באג שתוקן — מכוסים, באותה צורה, בארבעתן.
⚠️ ואחוז גבוה אינו ראיה לכיסוי: קובץ יכול להיות מלא בהערות שחוזרות על
שם הפונקציה, ולא לענות על אף אחת מארבע השאלות שהתקן שואל.
<!-- SHARED:end -->
---

<!-- SHARED:start id="round-28-what" -->
## ⭐ סבב 28 (אוגוסט 2026) — תקן ההערות, וקבצי התקנה לשתיים שחסרו

### מה נעשה
**שני דברים, ושניהם נמדדו ולא הוצהרו:** תקן ההערות (כלל ברזל 11) הוחל
על ארבע האפליקציות עם בדיקה שאוכפת אותו, ושתי האפליקציות שהיו בלי קובץ
התקנה קיבלו אחד — **ממיפוי הסכימה החיה, ולא מקריאת הקוד**.

1. **כלל ברזל 11 — תקן ההערות בקוד** (הפרק שמעל).
2. **סריקת כיסוי ויישור לתקן בארבעת קבצי ה-`index.html`** — כותרת בלוק
   לכל אזור לוגי, ⛔ שנושא סבב וסיבה, שלושה סימנים בלבד וטרמינולוגיה
   אחידה. ⛔ **הערות בלבד, וזה נמדד ולא הוצהר:** הסרת כל שורות ההערה
   משתי הגרסאות — `HEAD` והעץ הנוכחי — נותנת קוד רץ **זהה בית-לבית**
   בכל אחת מהארבע, פרט לשורת **מזהה הגרסה** במקום שבו היא יושבת
   ב-`index.html`. ⚠️ היא נרשמת במפורש בפרק הפרטי של אותה אפליקציה:
   קידומה הוא חובה (ר' «עדכוני Service Worker»), והשמטתה הייתה משאירה
   מזהה שמדווח מספר שאינו הגרסה שרצה — הבאג שתוקן בסבב 21.
3. **`tools/check-comments.mjs`** — הבדיקה האוכפת, בדפוס
   `check-status-area.mjs` של סבב 17 ו-`check-docs.mjs` של סבב 18,
   ונוספה לשער הדחיפה בארבעת הריפו.
4. **קובץ התקנה מלא ל-yoman-avoda ול-hanhala-ruchanit** — הפער ששורה 12
   במטריצת היכולות תיארה, והטריגר שכתוב בה («סבב ייעודי: מיפוי סכימה
   מלא מול המסד החי»). ר' הסעיף הבא.

### ⚠️ קבצי ההתקנה — מה נעשה, ומה **לא** נעשה
- **מופו מהמסד החי** — טבלאות, עמודות וטיפוסים, `identity`, אינדקסים,
  אילוצים, טריגרים, פוליסות RLS והרשאות `anon`/`authenticated`/
  `service_role` — ב-`SELECT` בלבד.
- **אידמפוטנטיים לפי כלל ברזל 10 סעיף 7:** `create table if not exists`
  לבדו אינו מספיק, ולכן לכל שינוי מבני שנעשה מאז ההתקנה הראשונה יש
  **גם** שורת התכנסות (`add column if not exists` · `drop policy if
  exists` + `create policy` · `alter column … drop default` ·
  `revoke`+`grant` מפורשים).
- ⛔ **אפס פרטי כניסה, גם לא «לדוגמה»** — מצייני מקום בלבד (כלל ברזל 10
  סעיף 8). המשתמש הראשון נוצר ידנית, והקובץ מסביר איך.
- ⛔ **לא הורצה שום פקודה מול המסד ולא נגעו נתונים.** הקבצים נכתבו
  לקריאה ול**אימות חיצוני לפני המיזוג**, טבלה-טבלה מול הסכימה החיה.
- ⚠️ **וכל מקום שבו לא הושגה ודאות נרשם בקובץ עצמו במפורש**, ולא הושלם
  בניחוש. ⛔ קובץ התקנה שמשלים פערים מהיגיון הוא בדיוק סוג התיעוד שכלל
  ברזל 8 אוסר: הוא **נקרא כעדות** בזמן שאיש לא אימת אותו.

### ⚠️ מה שהתקן לא כיסה, במכוון
תקן ההערות **אינו חל על חמשת הבלוקים המשותפים ועל שני המודולים
הקפואים** (סעיף 6 של כלל ברזל 11), ולכן `git diff` עליהם ריק בארבעתן —
בדיוק כפי שנדרש מהם מאז סבב 14.

### תיאום גרסאות
`sw.js`: hanhala **v36** · yoman **v22** (`app-version` **16-2026-08-13-1**) ·
schar **v28** · gius **gius-v14**. **ארבעתן קודמו** — `index.html` של כל
אחת מהן השתנה.
⚠️ **ותוקן כאן מספר שנסחף:** שורת התיאום של סבב 27 אמרה «schar **v26**»
בזמן שהקובץ כבר היה **v27** מסבב 26. המספר נמדד עכשיו מ-`sw.js` ולא
הועתק מהשורה הקודמת — ⛔ מספר גרסה שמועתק ולא נמדד הוא בדיוק סוג
העדות שכלל ברזל 8 סעיף 6 אוסר.
⚠️ **וזו הבחנה מכוונת מול השלמת סבב 26**, שבה `CACHE_NAME` **לא** קודם
על הערה: שם הייתה **שורה אחת** בכל ריפו, וכאן אלה עשרות אזורים בכל
קובץ — הפרש שגורם ל-`index.html` שבמטמון להיות שונה מהותית ממה שבריפו,
וזה בדיוק מה שמקשה על אבחון בהמשך.
<!-- SHARED:end -->
### מה נעשה כאן (schar-limud, `sw.js` v28)
**סבב הערות בלבד כאן — אין מיגרציה חדשה, ואין שינוי בקוד הרץ.**
005 עדיין היחידה שלא תורץ, ובכוונה.

**א. תקן ההערות — 10 אזורים לוגיים, 30 הערות ⛔.**
⚠️ **וזה ההרכב ההפוך מ-yoman**, וזה מה שנמדד: כאן היו הכי הרבה הערות
איסור מכל הארבע והכי מעט כותרות בלוק — כלומר **הידע היה כתוב אבל לא
היה ממופה**. הקורא ידע «אל תעשה X» בעשרות מקומות, ולא ידע לאיזה אזור
לוגי הוא נכנס. הכותרות הן מה שהשתנה כאן בעיקר; ה-⛔ הקיימים קיבלו את
מספר הסבב ואת הנימוק במקומות שבהם חסרו.

**ב. הטרמינולוגיה — כאן היא נמדדה כמסוכנת במיוחד.** ⛔ בפרויקט הזה
`sl` הוא **שכר לימוד** ובהנהלה `sl` הוא **שינה** (ר' «התנגשות שמות»,
סבב 8), ולכן חיפוש טקסטואלי בין הריפו הוא כלי האבחון העיקרי — ושני
כתיבים לאותו מושג הופכים אותו לחלקי. «מראה», «טביעה», «אופק הפינוי»
ו«דחיפת-מצב» כתובים עכשיו בכתיב אחד.

**ג. `tools/check-comments.mjs`** נוסף, וקריאה אליו נוספה לפרק «לפני כל
push» — לצד `node --check`, `check-status-area.mjs`, `check-docs.mjs`
ושתי בדיקות הסבבים (`test_round24.mjs`, `test_round26.mjs`).

**ד. ⚠️ `CACHE_NAME` → v28, ותוקן מספר שנסחף בתיעוד.** שורת התיאום של
סבב 27 אמרה «schar **v26**», בזמן ש-`sw.js` היה כבר **v27** מסבב 26.
המספר כאן נמדד מהקובץ ולא הועתק מהשורה הקודמת. ⛔ מספר גרסה שמועתק
במקום להימדד הוא בדיוק סוג ההצהרה שכלל ברזל 8 סעיף 6 אוסר.

**ה. ⛔ שני קבצי ההתקנה החדשים אינם נוגעים כאן.** מקור האמת לסכימה
כאן הוא `migrations/000_initial_schema.sql` **מסבב 6**, והוא לא נגע —
לא בכותרתו, לא בשורות ההתכנסות ולא בהרשאות.

### אימותים
- **שלושת השערים הקבועים עברו:** `node --check` על ה-JS המחולץ
  מ-`index.html` ועל `sw.js`, `node tools/check-status-area.mjs`,
  ו-`node tools/check-docs.mjs`. **והחדש:**
  `node tools/check-comments.mjs`.
- **`node tools/test_round24.mjs` — 140 עברו, 0 נכשלו** (לא נגע).
- **`node tools/test_round26.mjs` — 97 עברו, 0 נכשלו** (לא נגע).
- **⭐ «הערות בלבד» נמדד ולא הוצהר:** הסרת כל ההערות (כולל נגררות,
  בטוקניזציה מלאה של JS) מ-`HEAD:index.html` ומהעץ הנוכחי נותנת
  **1,942 שורות קוד רץ, זהות בית-לבית**.
- **הבודק אינו חותמת גומי** — חמש המוטציות מתוארות בפרק הפרטי של
  hanhala-ruchanit, שם הן הורצו; כאן הורצה בקרה חיובית בלבד (0 כשלים).
- **חמשת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.
- ⛔ **המסד לא נגע**, ואין מיגרציה חדשה.

---

<!-- SHARED:start id="round-29-what" -->
## ⭐ סבב 29 (אוגוסט 2026) — יישור הרשאות המפתח הציבורי

### מה נעשה
**סבב הרשאות מסד. ⛔ אפס שינוי בקוד הרץ בארבע האפליקציות** — לא
ב-`index.html`, לא ב-`sw.js`, ולכן **אף `CACHE_NAME` לא קודם**. מה שנוסף
הוא ארבע מיגרציות (schar-limud · hanhala-ruchanit ×2 · yoman-avoda),
שורות התכנסות בקבצי ההתקנה, וסעיף 9 בכלל ברזל 10.

### הבעיה שנמדדה
מיפוי `information_schema.role_table_grants` מול **שני** פרויקטי
ה-Supabase מצא ש-**gius לבדה מוגנת**: שבע טבלאות ה-`g_` שלה מעניקות
ל-`anon` ול-`authenticated` `SELECT, INSERT, UPDATE` בלבד (מיגרציה
`0002_revoke_delete`, סבב 11). **תשע טבלאות בפרויקט המשותף** —
`sl_lists` · `sl_settings` · `sl_students` · `sl_transactions` ·
`sl_users` · `ys_users` · `kv` · `kv_rishon` · `kv_ramataviv` — מעניקות
לשני התפקידים גם `DELETE`, `TRUNCATE`, `REFERENCES` ו-`TRIGGER`.
מפתח ה-anon יושב **גלוי** ב-`index.html` בארבעת הריפו הציבוריים, וה-RLS
הוא `using (true)` — כלומר מי שמחזיק בו יכול תיאורטית למחוק שורות ואף
לרוקן טבלה שלמה.

⚠️ **מאיפה זה הגיע — לא מקבצי הסכימה שלנו.** פרויקט Supabase סטנדרטי
מגיע עם `alter default privileges in schema public grant all on tables to
anon, authenticated, service_role`, ולכן **כל טבלה נולדה עם ההרשאות
האלה**, וה-`grant` שבקובץ ההתקנה רק תיאר את המצב שכבר היה.
⛔ **`GRANT` הוא אדיטיבי בלבד ואינו מסיר דבר** — רק `REVOKE` מסיר.

### ⛔ למה ההסרה בטוחה — נמדד, לא הונח
**אין באף אחת מארבע האפליקציות מסלול מחיקה מול המסד:** סריקה של ארבעת
הריפו מצאה **אפס** קריאות `.delete()` ל-PostgREST (ההתאמות היחידות הן
`caches.delete()` של ה-service worker ו-`Set.delete` בשתי רתמות בדיקה).
מחיקה בארגון היא **תמיד סימון** — `deleted=true` + חותמת ב-`sl_*`,
tombstone בתוך הערך ב-`kv`/`kv_rishon`/`kv_ramataviv` — ותמיד מתבצעת
ב-`UPDATE` (כלל ברזל 6 סעיף 1). ההרשאה מיותרת לחלוטין, וההסרה אינה
שוברת דבר.

### ⛔ שתי טבלאות היומן — החריג, והוא בכיוון ההפוך
`sync_log` ו-`kv_backup` מקבלות **`INSERT` ו-`SELECT` בלבד — בלי
`UPDATE` ובלי `DELETE`, לשני התפקידים**. הן יומני ראיות: מי שאינו יכול
לעדכן אינו יכול לזייף רישום קיים, ומי שאינו יכול למחוק אינו יכול
להעלים אותו. ⛔ **אין «ליישר» אותן לשאר הטבלאות בשם האחידות** — הסט
הצר שם **הוא** ההגנה, ואין להוסיף להן הרשאות.
⚠️ הצד של `anon` היה כבר נכון; הצד של `authenticated` **לא** — ר'
הסעיף הבא.

### ⭐ `authenticated` צומצם גם הוא — בהחלטת המנהל
הבקשה המקורית הייתה על המפתח הציבורי בלבד. ההרחבה נעשתה **בהחלטה
מפורשת**: לתפקיד יש **היום** בדיוק אותן הרשאות מלאות, מאותה ירושה, ולכן
פתיחת signup או Supabase Auth בעתיד הייתה פוערת את ההגנה **בשקט**. זהו
גם יישור למצב קיים ולא החלטה חדשה — `0002` של gius כבר צמצמה את שני
התפקידים.

⚠️ **והצמצום נעשה כשאין משתמשי Auth כלל** — `select count(*) from
auth.users` מחזיר **0 בשני הפרויקטים** — ולכן הוא **אינו נבדק מול מסלול
חי**. אם ייפתח Auth בעתיד, יש לוודא ש-`SELECT, INSERT, UPDATE` מספיקים
למסלול שייבנה. ⛔ `service_role` לא נגע: הוא תפקיד השרת ואינו נגיש
מהדפדפן.

### ⭐ טבלאות היומן — הפער נמדד, ונסגר בהשלמה
המדידה חשפה דבר שלא היה ידוע לפניה: `sync_log` ו-`kv_backup` מעניקות
ל-`anon` `INSERT, SELECT` בלבד — אבל ל-**`authenticated`** את **הסט
המלא**, כולל `UPDATE`, `DELETE` ו-`TRUNCATE`. כלומר ההגנה מפני זיוף
רישום ביומן ראיות התקיימה **מול תפקיד אחד בלבד**.

הניסוח הראשון של הסבב רשם את זה כפער עם טריגר («כשייפתח Auth»), מפני
שההנחיה «אל תיגע ב-`sync_log` וב-`kv_backup`» נקראה כחלה על שתיהן
במלואן. **ההנחיה חלה על `anon` ועל האיסור להוסיף להן `UPDATE` «בשם
האחידות» — לא על השארת `authenticated` פתוח.** ⛔ **הפער נסגר בהשלמה
ואין לו טריגר:** `hanhala/migrations/002_revoke_log_tables.sql` מצמצמת
את שתיהן ל-`INSERT` + `SELECT` בלבד, **לשני התפקידים**, וקובץ ההתקנה
עודכן כך שהתקנה טרייה נולדת נכונה.

⛔ **הלקח, והוא מה שנשאר מהפער הזה** — **הרשאה שהוחלה על תפקיד אחד
בלבד היא הגנה חלקית.** בדיקת הרשאות סורקת את **כל** התפקידים הנגישים
(`anon` **וגם** `authenticated`), ולא מסתפקת בזה שהתפקיד הבולט נראה
תקין. ⚠️ זה אותו כשל בדיוק שסעיף 8 בכלל ברזל 10 מתאר על **סוגי
קבצים** — סריקה חלקית שנקראת כראיה מלאה — והפעם בציר התפקידים.
הוא נכנס לסעיף 9.

### מה נכתב
1. **ארבע מיגרציות**, בדפוס `gius/migrations/0002_revoke_delete.sql`:
   לולאה על שמות הטבלאות עם `revoke` ואז `grant` מפורש, **בסדר הזה**.
   ⛔ אידמפוטנטיות ו**אינן נוגעות בנתונים** — הרשאות בלבד, בלי `insert`,
   `update` או `delete` על אף שורה.
   ⚠️ שלוש מהן על טבלאות הנתונים (`SELECT, INSERT, UPDATE`), והרביעית —
   `hanhala/002_revoke_log_tables.sql` — על **טבלאות היומן**, עם סט צר
   יותר: `INSERT` + `SELECT` בלבד. ⛔ אין לאחד ביניהן.
2. **`alter default privileges in schema public revoke delete, truncate on
   tables from anon, authenticated`** בכל אחת מהן, כדי שטבלה עתידית לא
   תיוולד שוב עם ההרשאות האלה. ⚠️ **אינו תחליף ל-`revoke` המפורש** — הוא
   משפיע רק על ברירות מחדל שבבעלות התפקיד שמריץ אותו, ורק על טבלאות
   שייווצרו **מכאן והלאה**. ⛔ ולכן הכלל נשאר: **כל מיגרציה שמוסיפה טבלה
   חייבת `revoke` משלה.**
3. **קבצי ההתקנה עודכנו** — כדי שהתקנה טרייה תיוולד נכונה, **וגם**
   כשורת התכנסות להתקנה קיימת (כלל ברזל 10 סעיף 7).
4. **כלל ברזל 10 גדל לתשעה סעיפים** — סעיף 9, הפרק שמעל.

### ✅ המיגרציות הורצו ואומתו — חיצונית
הסשן עצמו **לא נגע במסד**: כל המיפוי נעשה ב-`SELECT` בלבד, וארבע
המיגרציות נמסרו לאישור והרצה חיצונית. ⭐ **הן אושרו, הורצו ואומתו**,
בשני צדי ההרצה:
- **לפני:** אומת שאף אחת מהן אינה נוגעת בנתונים — `REVOKE`/`GRANT`
  בלבד, בלי `insert`/`update`/`delete` על אף שורה.
- **אחרי:** **תשע טבלאות הנתונים** מחזיקות `INSERT, SELECT, UPDATE`,
  ו**שתי טבלאות היומן** `INSERT, SELECT` — ⛔ **אפס `DELETE` ואפס
  `TRUNCATE`**. gius אומתה כמוגנת מראש: שבע טבלאות, אותו סט.

⚠️ **והאימות נעשה על שני התפקידים, לא על `anon` לבדו** — זה בדיוק מה
שסעיף 9 מחייב, ובדיוק מה שהיעדרו הסתיר את הפער בטבלאות היומן עד
ההשלמה. ⛔ `service_role` לא נגע.
⚠️ **מה שעדיין לא נבדק:** `auth.users` ריקה בשני הפרויקטים, ולכן
הצמצום של `authenticated` **אינו נבדק מול מסלול חי**. אם ייפתח Auth —
יש לוודא שהסט שנשאר מספיק למסלול שייבנה.

### תיאום גרסאות
`sw.js`: hanhala **v36** · yoman **v22** (`app-version` **16-2026-08-13-1**) ·
schar **v28** · gius **gius-v14**. **אף אחת לא קודמה** — אין שינוי בקוד
הרץ, וקידום היה שולח לכל המשתמשים באנר «גרסה חדשה זמינה» לשום שינוי.
⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו ולא הועתקו משורת התיאום של סבב 28.
<!-- SHARED:end -->
### מה נעשה כאן (schar-limud)

**א. ⭐ `migrations/012_revoke_delete_anon.sql` — חדש.** לולאה על חמש
טבלאות ה-`sl_`: `sl_lists` · `sl_settings` · `sl_students` ·
`sl_transactions` · `sl_users`.
- ⚠️ **`sl_transactions` הם כספים**, ו-`sl_students` הם אבות המפתח הזר
  שלהם — כאן `truncate` בידי מי שמחזיק את המפתח הגלוי הוא אובדן
  היסטוריית גבייה שלמה. ⛔ ומחיקה כאן היא soft-delete בלבד (כללים
  קריטיים 5 ו-6), ולכן ההרשאה מיותרת בהגדרה.
- ⚠️ **הרשאות ה-SEQUENCE נשארות ואין לצמצם אותן** — `INSERT` על טבלת
  `SERIAL` דורש `USAGE` על הרצף, וצמצום שלהן היה שובר שמירת תשלום
  ותלמיד חדש.
- ⛔ `sync_log` ו-`kv_backup` (של האחיות, באותו פרויקט) **אינן כאן**. הן
  שייכות להנהלה רוחנית, ומטופלות שם ב-`002_revoke_log_tables.sql`
  (השלמת סבב 29) — `INSERT`+`SELECT` בלבד, לשני התפקידים.

**ב. `000_initial_schema.sql` — חמשת בלוקי ההרשאות** נכתבו מחדש בדפוס
`REVOKE` → `GRANT` לשני התפקידים, `service_role` שומר את הסט המלא,
ושורת ההיסטוריה שבכותרת עודכנה ב-012. ⛔ שאר הקובץ לא נגע.

**ג. שורת 012 נוספה לטבלת «מצב המיגרציות במסד הייצור»** — ✅ **הורצה
ואומתה** (עודכן אחרי ההרצה החיצונית; ⛔ הניסוח הראשון היה «⏳ ממתינה
להרצה»). ⚠️ **005 חוזרת להיות היחידה שאינה במצב «הורצה»**, והיא
**לא תורץ בכוונה** — האפליקציה זורעת את הסעיף בעצמה. ⛔ אין לבלבל
בין «לא תורץ בכוונה» לבין «ממתינה».

**ד. ⛔ `index.html` ו-`sw.js` לא נגעו.** `CACHE_NAME` נשאר **v28**
(סבב 28).

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `node tools/check-status-area.mjs`,
  `node tools/check-docs.mjs`, ו-`node tools/check-comments.mjs`.
- **`node tools/test_round24.mjs` — 140 עברו, 0 נכשלו** (לא נגע).
- **`node tools/test_round26.mjs` — 97 עברו, 0 נכשלו** (לא נגע).
- **⭐ ההרשאות נמדדו מול המסד החי ולא הוצהרו** — כל חמש טבלאות ה-`sl_`
  מחזיקות היום `DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE,
  UPDATE` ל-`anon` ול-`authenticated`.
- **⭐ «אין מסלול מחיקה» נמדד ולא הונח** — סריקת `.delete(` בכל הריפו:
  ההתאמה היחידה היא `caches.delete()` שב-`sw.js`. אין אף `.delete()`
  מול PostgREST, ובפרט לא על `sl_transactions`.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**
- ⛔ **הסשן לא נגע במסד** — המיפוי ב-`SELECT` בלבד.
- ⭐ **012 הורצה ואומתה חיצונית.** אחרי ההרצה כל חמש טבלאות ה-`sl_`
  מחזיקות `INSERT, SELECT, UPDATE` בשני התפקידים — ⛔ אפס `DELETE`
  ואפס `TRUNCATE`. ⚠️ הרשאות ה-SEQUENCE לא נגעו, ולכן שמירת תשלום
  ותלמיד חדש תקינה.

---

<!-- SHARED:start id="round-30-what" -->
## ⭐ סבב 30 (אוגוסט 2026) — גיבוי ויומן בארבעתן, ויומן-עבודה לטבלאות

### ⛔⛔ סדר מחייב — שני שלבים, ואין לערבב ביניהם
**שלב א נכנס לתוקף ומוכח בפועל לפני ששלב ב רץ.** שלב ב מזיז 1.2MB של
נתוני יומן לטבלאות, ובלי גיבוי פעיל **שאומת** אין נקודת חזרה.
⛔ **הסשן שכתב את שני השלבים לא הריץ אף מיגרציה ולא נגע במסד** — כל
המדידות נעשו ב-`SELECT` בלבד. ההרצה חיצונית, **בשני גלים**:
1. שלב א ← אימות ש-`kv_backup` קיבלה שורה טרייה ← ורק אז
2. שלב ב (`002` ואז `003`), עם בדיקת השקילות שבגוף `003`.

### שלב א — גיבוי יומי ויומן פעולות בארבע האפליקציות

**הבעיה שנמדדה.** גיבו רק hanhala-ruchanit ו-schar-limud, כל אחת
במימוש משלה, ויומן פעולות היה רק בהנהלה. ⚠️ **ומעבר לכיסוי החלקי —
הגיבוי של הנהלה הפסיק לרוץ ב-2026-07-20** (החותמת האחרונה של כל ארבעת
המפתחות שלה) בעוד ש-schar גיבתה באותו יום עצמו. שלושה שבועות בלי גיבוי,
בלי שאיש ידע: **מנגנון שקט שנעצר נראה בדיוק כמו מנגנון שקט שעובד.**

**מה נעשה.**
1. **⛔ הליבה חולצה ולא הומצאה מחדש.** שני המימושים הקיימים נקראו, זוהתה
   הליבה המשותפת — נעילת ריצה, דגל-יומי שנכתב **אחרי** ההצלחה, בדיקת
   `error` מפורשת — והיא הפכה ל**בלוק משותף זהה בית-לבית בארבעתן**,
   בין `/* ═══ גיבוי יומי ויומן פעולות` לבין `סוף מודול הגיבוי היומי`.
   מעליו יושבת `BK_CFG`, שהיא הדבר היחיד שנבדל.
   ה-API: `bkBoot` · `bkMaybeDaily` · `bkLastAt`/`bkStale` ·
   `bkStatusHTML`/`bkStatusMount` · `logAction`/`logFlush` · `bkToday`/`bkSig`.
2. **⭐ גיבוי דיפרנציאלי** — ערך שלא השתנה מאז הגיבוי האחרון אינו נכתב
   שוב, לפי חתימה מקומית (אורך + FNV-1a) שאינה נוגעת ברשת. ⚠️ זה אינו
   קיצור דרך אלא **תנאי לכך שהגיבוי יהיה בר-קיימא**: הערך הגדול בארגון
   הוא 1.2MB, וכתיבה יומית עיוורת שלו לבדו הייתה מוסיפה ~36MB בחודש.
   ⛔ ובספק — כותבים: חתימה חסרה או מכשיר חדש מביאים לכתיבה מלאה.
3. **הכיסוי הורחב בזכות זה:** הנהלה עברה מארבעה מפתחות ל-14, ו-yoman
   ו-gius הצטרפו. ⛔ **וטבלאות שיש בהן סוד מוחרגות או נשלפות בעמודות
   מפורשות** — `kv_backup` קריאה ל-`anon`, וגיבוי גורף של טבלת משתמשים
   היה מעתיק סיסמאות למקום שני.
4. **`kv_backup` ו-`sync_log` נוצרו בפרויקט של gius**
   (`migrations/0004_backup_log.sql`) — ⚠️ היא חיה בפרויקט **נפרד**
   ולכן אין לה גישה לאלה שבפרויקט המשותף. הסכימה זהה בית-לבית, וזה מה
   שמאפשר למודול להיות זהה. ⛔ `INSERT`+`SELECT` לשני התפקידים
   (כלל ברזל 10 סעיף 9).
5. **«גובה לאחרונה» ב«מידע טכני», בארבעתן.** ⛔ הרכיב **נתלה אחרי**
   הבלוק הקפוא ואינו עורך אותו — «מידע טכני» חתום, ועריכתו הייתה
   מחייבת שינוי בארבעת הריפו ובחתימות עבור שורת תצוגה אחת.
6. **שימור `kv_backup` — נמדד, והוצע ולא בוצע.** 136 שורות · 5,940 kB,
   מהן ~2.6MB **גיבויים ידניים חד-פעמיים** (`PRE_SYNC_UNIFY_`,
   `PRE_ROUND3B_`, `ORPHAN_`) ש⛔ **אין למחוק לעולם** — הם אינם מצטברים
   ואין להם עותק אחר. ההצעה (שמירת 30 אחרונים לכל מפתח **אוטומטי**)
   יושבת מוערת ב-`hanhala/migrations/003_backup_retention.sql`.
   ⛔ **לא נמחקה אף שורה**, ומחיקת נתוני גיבוי היא החלטת המנהל.

### שלב ב — yoman-avoda עוברת לטבלאות מובנות

**הבעיה שנמדדה.** yoman היא היחידה בארגון שנתוניה אינם בטבלאות אלא
כ-13 מפתחות ב-`kv_rishon`/`kv_ramataviv`, כשכל מפתח הוא **ערך יחיד**:
`tb_archive` של ראשון לציון לבדו **1,205,501 בתים · 164 סנאפשוטים**.
כלומר עריכת רשומה בודדת דורסת את מלוא הערך, והמיזוג בענן הוא ברמת קובץ.

**⛔ הדפוס לא תוכנן מאפס** — schar-limud עברה בדיוק את המעבר הזה
בסבבים 12–13, והמימוש שם נקרא והועתק: זהות שנוצרת במכשיר, חותמת עדכון,
tombstones, מיזוג ברמת רשומה, ומודול ה-⏳ הקיים.

**מה נכתב (⛔ ולא הורץ).**
- **`migrations/002`** — `tb_entries` ו-`tb_archive`, שורה לכל רשומה,
  עם עמודת `yeshiva`, `client_id`, `updated_at`, tombstones ו**אינדקסים
  מלאים** (⛔ לא חלקיים — הלקח מ-`007` של schar).
- **`migrations/003`** — פירוק הערכים לשורות, **אדיטיבי בלבד**:
  ⛔ המפתחות הישנים ב-`kv` נשארים במקומם ואינם נמחקים. בגוף הקובץ
  **בדיקת שקילות דו-כיוונית** שהיא תנאי למעבר, ופרק **נתיב חזרה**.
- **בקוד:** שכבת שורות עם נפילה-חזרה לבלוק, ⭐ כך ש**הקוד בטוח לפריסה
  לפני שהמיגרציה רצה** — וזה מה שמאפשר את סדר שני הגלים.
  ⭐ **כתיבה כפולה** — לטבלאות וגם לבלוק הישן, כדי שנתיב החזרה יהיה
  החלפת דגל אחד ולא שחזור נתונים.
  ⚠️ **המחיר המודע:** כתיבת ה-1.2MB לא נעלמה בסבב הזה; היא נעלמת בסבב
  שיפסיק את הכתיבה הכפולה. מה שכן הושג כבר: משיכה ומיזוג פר-רשומה.
- ⛔ **מחיקת המפתחות הישנים אינה בסבב הזה.** **הטריגר:** שבועיים של
  עבודה תקינה על הטבלאות. ⚠️ וגם אז — קודם מפסיקים את הכתיבה הכפולה,
  ורק בסבב שאחריו מוחקים; ⛔ לא שניהם יחד, אחרת אין חלון לגלות טעות.

### ⛔ אחידות — נבדקה בכל סעיף, ולא הונחה
כל רכיב שנוסף כאן נבדק מול שלוש האחרות **לפני** שהסעיף נסגר, ורכיב
שאינו נכנס לאפליקציה מסוימת קיבל נימוק כתוב במטריצה — ואם הוא פער, גם
טריגר. שלוש שורות נוספו למטריצה: **גיבוי יומי אוטומטי · יומן פעולות ·
נתונים בטבלאות מובנות**.
⚠️ הרקע: לאורך סבבים 17–29 התגלו שוב ושוב רכיבים שהוחלו על אפליקציה
אחת בלבד — פינוי, כניסה אופליין, הרשאות, גיבוי, יומן — **כל אחד התגלה
במקרה ולא בבדיקה**.

### תיאום גרסאות
`sw.js`: hanhala **v37** · yoman **v23** (`app-version` **17-2026-08-14-1**) ·
schar **v29** · gius **gius-v15**. **ארבעתן קודמו** — `index.html` של כל
אחת מהן השתנה (מודול הגיבוי נכנס לכולן).
⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו ולא הועתקו משורת התיאום של סבב 29.
<!-- SHARED:end -->

### מה נעשה כאן (schar-limud, `sw.js` v29)

**שלב א בלבד — ⛔ שלב ב אינו נוגע כאן.** schar כבר יושבת על טבלאות
מובנות מאז ההקמה (חמש טבלאות `sl_`), ולכן שורה 15 במטריצה היא ✅ והמעבר
של yoman אינו משנה כאן דבר.

- **`slMaybeDailyBackup` היא מעטפת דקה** מעל `bkMaybeDaily`; רשימת
  הטבלאות עברה ל-`BK_CFG`. ⛔ אין להחזיר לכאן מימוש עצמאי.
- ⛔ **`sl_users` אינה ברשימת הגיבוי** — היא מחזיקה `password` בטקסט
  גלוי ואת `pass_salt`/`pass_fp`, ו-`kv_backup` קריאה ל-`anon`; גיבוי
  שלה היה מעתיק את הסוד למקום שני ומבטל את מה שסבב 24 בנה. שחזור
  משתמשים נעשה ידנית ע"י המנהל, כמתואר בקובץ ההתקנה.
- **יומן פעולות נוסף כאן לראשונה** — עד היום schar לא כתבה ל-`sync_log`
  כלל, למרות שהטבלה יושבת באותו פרויקט. כרגע היא רושמת את הגיבוי;
  ⚠️ **רשימת האירועים היא פר-אפליקציה ואינה מיושרת** — המשותף הוא
  המנגנון.
- **«גובה לאחרונה»** נתלה ב«מידע טכני» אחרי `statusAreaMount()`.
- ⚠️ **הגיבוי כאן הוא היחיד שרץ ברציפות** — החותמת האחרונה ב-`kv_backup`
  היא היום, בעוד שההנהלה נעצרה ב-2026-07-20. זו הייתה הבקרה שאיפשרה
  לזהות שהמנגנון עצמו תקין ושהבעיה היא בהפעלה שלו.
- ⛔ **אין מיגרציה חדשה.** 005 עדיין היחידה שלא תורץ, ובכוונה.

### אימותים
- **ארבעת השערים עברו** + **`test_round24.mjs` — 140 עברו** ו-
  **`test_round26.mjs` — 97 עברו** (שניהם לא נגעו), ו-
  **`node tools/test_round30_stage_a.mjs` — 59 עברו, 0 נכשלו.**
- **הבודק אינו חותמת גומי** — שש המוטציות של שלב א מתוארות בפרק הפרטי
  של hanhala-ruchanit, שם הן הורצו; כאן הורצה בקרה חיובית בלבד.
- **⭐ קובץ הבדיקה נמדד כזהה בית-לבית בארבעת הריפו** פרט לשורת ה-`app`.
- ⛔ **הסשן לא נגע במסד**, ואין מיגרציה חדשה.

---

<!-- SHARED:start id="iron-rule-12-capabilities" -->
## ⭐ כלל ברזל 12 — יכולת משותפת נמדדת בשלושה ממדים (סבב 30 — השלמה, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את אחד-עשר כללי הברזל שלמעלה.

> **יכולת שמוכרזת «משותפת» מוגדרת בשלושה ממדים, לא באחד: ליבה זהה
> בית-לבית · **נקודת הפעלה זהה** · רישום תואם במטריצת היכולות.
> ⛔ שני מימושים שונים לאותה יכולת הם פער — גם כששניהם עובדים היום.
> הפער מתגלה רק כשאחד מהם נשבר, וזה בדיוק מאוחר מדי.**

### הבעיה שנמדדה
סבב 30 חילץ את הגיבוי היומי לבלוק משותף אחד, זהה בית-לבית בארבעתן —
כלומר קיים את כלל ברזל 7 במלואו. ובכל זאת התברר שהגיבוי של
hanhala-ruchanit **הפסיק לרוץ ב-2026-07-20** ואיש לא ידע:

| | היכן הופעל הגיבוי | התוצאה |
|---|---|---|
| schar-limud | בכניסה למערכת (`enterApp`) | ✅ רץ בכל יום שבו מישהו נכנס |
| hanhala-ruchanit | **מתוך מסלול הדחיפה** (`ysPushToCloud`) | 🔴 25 יום בלי גיבוי |

ההבדל אינו באג בקוד — **הקוד עצמו היה תקין בשתיהן.** ההבדל הוא היכן
הוא מופעל: בהנהלה הגיבוי רץ רק כשמישהו דחף אחד מארבעת המפתחות שהוא
גיבה. בתקופה סטטית — חופשה, שבוע בלי עריכות — אין דחיפה, אין גיבוי,
ואין שום סימן. ⛔ **מנגנון בטיחות שתלוי בפעילות רגילה אינו מנגנון
בטיחות:** הוא נעצר בדיוק בתקופה שבה תקלה עוברת בלי שאיש שם לב.

⚠️ **וזה נמדד ולא הונח:** `sync_log` מראה **444 פעולות ב-30 יום ו-47
בשבוע** באותה תקופה — האפליקציה הייתה בשימוש מלא. תשע מפתחות נוספים לא
גובו **38 יום**. כלומר לא «אף אחד לא נכנס», אלא «אף אחד לא דחף את
המפתחות הנכונים».

### שלושת הממדים
1. **ליבה זהה בית-לבית** — מה שכלל ברזל 7 כבר דורש, ומה
   ש-`check-status-area.mjs` כבר אוכף.
2. **⭐ נקודת הפעלה זהה.** לכל יכולת משותפת יש **תפקיד** קבוע שממנו היא
   מופעלת, זהה בארבעתן, גם כששם הפונקציה נבדל:
   - **יכולות עלייה** (`lsBoot` · `pendBoot` · `bkBoot`) — **פונקציית
     העלייה של האפליקציה**, זו שכבר קוראת לשתי האחרות.
   - **יכולות מסך** (`statusAreaMount` · `bkStatusMount`) — פונקציית
     מסך ההגדרות.
   ⛔ **ואין נקודת הפעלה שנייה.** קריאה נוספת לאותה יכולת ממקום אחר היא
   בדיוק הסטייה הזו בהתהוותה, גם אם היא «רק ליתר ביטחון».
3. **רישום תואם במטריצה** — יכולת שמסומנת ✅ קיימת בקוד, ומה שקיים בקוד
   מסומן ✅. ⛔ מטריצה שאינה תואמת לקוד גרועה מהיעדר מטריצה: היא נקראת
   כעדות.

### ⛔ מעטפת דקה אינה «נוחות» — היא נקודת הפעלה שנייה
כשיכולת נקראת דרך פונקציה מקומית שרק עוטפת אותה (`ysMaybeDailyBackup`,
`slMaybeDailyBackup`), **כל מקום בקוד יכול לקרוא לה** — והכלל שקובע
נקודת הפעלה אחת הופך להמלצה. לכן המעטפות הוסרו, והמודול חושף **קריאה
אחת** שקוד האפליקציה רשאי לעשות. הפונקציה הפנימית נשארת חשופה לבדיקות
ולמודול עצמו, ו-`check-capabilities.mjs` נכשל על כל קריאה אליה מקוד
האפליקציה.

### ⚠️ התרעה על מנגנון שקט שנעצר — `BK_STALE_DAYS`
המדד השני שהיה חסר: **גם אחרי שהגיבוי מופעל נכון, אין דרך לדעת שהוא
באמת רץ.** לכן «מידע טכני ›» מציג «גובה לאחרונה», ומעל
**`BK_STALE_DAYS` = 3 ימים** נוספת שורת התרעה.
⚠️ **הסף הוא שלושה ימים ולא יום אחד** — הדגל היומי אומר שכשל בודד הוא
רעש לגיטימי (מכשיר אופליין, סוף שבוע), ואילו שלושה ימים הם **דפוס**.
עם הסף הזה, ההשבתה בת 25 הימים הייתה מתגלה ביום הרביעי.
⛔ **ולא התראה קופצת** — סבב 15 קבע ששקט הוא ברירת המחדל באזור המצב,
וההתרעה יושבת במקום שבו מסתכלים כשבודקים.

### הבדיקה האוכפת
`tools/check-capabilities.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs`
קורא לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה
עם שערי התחביר לפני כל דחיפה. היא נכשלת על שלושה סוגי סטייה:
- **א. ליבה** — הבלוק המשותף של היכולת אינו תואם לחתימה הקנונית.
- **ב. נקודת הפעלה** — פונקציית החיווט אינה נקראת מתוך הפונקציה
  הקנונית, נקראת **גם** ממקום אחר, או שפונקציה אסורה (`bkMaybeDaily`)
  נקראת מקוד האפליקציה.
- **ג. מטריצה** — יכולת שמסומנת ✅ אינה קיימת בקוד, או להפך.

⚠️ **הסריקה נעשית על קוד בלבד**, אחרי טוקניזציה שמנקה הערות ומחרוזות.
⛔ אין להחליף אותה בחיפוש טקסטואלי: שם הפונקציה מופיע גם ברשימת ה-API
שבהערת המודול, וכל אזכור בתיעוד היה נספר כקריאה.

**שינוי מכוון ביכולת משותפת** = עדכון בארבע האפליקציות **ובארבעת עותקי
קובץ הבדיקה** (החתימות ונקודות ההפעלה כתובות בתוכו), באותו סבב.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-30b-what" -->
## ⭐ סבב 30 — השלמה (אוגוסט 2026): נקודת הפעלה אחת, והתרעה על גיבוי שנעצר

### מה נעשה
השלמה לסבב 30, לפני המיזוג. **הבלוק המשותף של הגיבוי, שער הדחיפה
והתיעוד השתנו בארבעת הריפו.** ⛔ אף מיגרציה לא הורצה, והמסד לא נגע.

### ⚠️ האירוע, כפי שנמדד — והוא הסיבה לכל השאר
סבב 30 דיווח ש«הגיבוי של הנהלה הפסיק לרוץ ב-2026-07-20», ותלה את זה
בכיסוי החלקי. **בדיקה חיצונית מצאה את הסיבה האמיתית, והיא אחרת:**
- **ארבעת המפתחות המגובים** (`ys_students` · `ys_attend` ·
  `ys_approvals` · `ys_attend_sessions`) — חותמת אחרונה **2026-07-20**,
  כלומר **25 יום בלי גיבוי**.
- **תשעה מפתחות נוספים** לא גובו **38 יום**.
- ובאותה תקופה בדיוק `sync_log` מראה **444 פעולות ב-30 יום** ו-**47
  בשבוע** — האפליקציה הייתה בשימוש מלא.

⛔ **כלומר זו לא הייתה תקלה ולא היעדר שימוש, אלא הבדל מימוש:**
`slMaybeDailyBackup` של schar נקראה **בכניסה למערכת**, ואילו
`ysMaybeDailyBackup` של הנהלה נקראה **מתוך מסלול הדחיפה** — רק כשמישהו
דחף אחד מארבעת המפתחות שהיא גיבתה. שתי האפליקציות «עבדו»; הפער נחשף רק
כשאחת מהן נעצרה. **מנגנון בטיחות שתלוי בפעילות רגילה אינו מנגנון
בטיחות.**

### מה תוקן
1. **⛔ נקודת הפעלה אחת בארבעתן — `bkBoot()`.** המודול המשותף חושף
   מעכשיו **קריאה אחת** שקוד האפליקציה עושה, והיא נקראת מ**פונקציית
   העלייה** של כל אפליקציה — זו שכבר קוראת ל-`lsBoot()` ול-`pendBoot()`.
   ⛔ **המעטפות המקומיות הוסרו** (`ysMaybeDailyBackup` בהנהלה,
   `slMaybeDailyBackup` ב-schar): מעטפת דקה היא נקודת הפעלה שנייה בפועל,
   ולכן היא מה שאיפשר את הסטייה מלכתחילה.
   ⚠️ **`logFlush` הוא החריג המכוון** — הוא ריקון תור חוזר, ולכן מותר
   לקרוא לו שוב על חזרת רשת או על כניסה מוצלחת. נקודת ה**עלייה** שלו
   נשארת `bkBoot` בלבד.
2. **⚠️ התרעת גיבוי מיושן — `BK_STALE_DAYS` = 3.** «מידע טכני ›» מציג
   «גובה לאחרונה», ומעל שלושה ימים נוספת שורת התרעה. ⛔ **ולא התראה
   קופצת** (החלטת השקט של סבב 15). זה מה שהיה הופך את 25 הימים לגלויים
   ביום הרביעי.
3. **כלל ברזל 12** — הפרק שמעל.
4. **`check-capabilities.mjs`** — הבדיקה האוכפת, בדפוס
   `check-status-area.mjs` של סבב 17, ונוספה לשער הדחיפה בארבעת הריפו.
   ⚠️ היא סורקת גם את חמש היכולות המשותפות האחרות (אחסון · ⏳ · אזור
   המצב · יומן פעולות · כניסה אופליין), ולא את הגיבוי בלבד — הבדיקה
   שסבב 30 היה צריך כדי לתפוס את הפער **לפני** שהוא נמדד מבחוץ.

### ⛔ אותה בדיקה הורצה על כל יכולת שהסבב נגע בה — ולא רק על הגיבוי
כלל ברזל 12 נולד מפער אחד, אבל הוחל על כולן באותה נשימה:
**אחסון** (`lsBoot`) · **⏳** (`pendBoot`) · **אזור המצב**
(`statusAreaMount`) · **גיבוי** (`bkBoot` + `bkStatusMount`) —
כולן נמדדו, וכולן יושבות היום בפונקציה הקנונית **בארבע האפליקציות**.
**יומן הפעולות** ו**הכניסה האופליין** נבדקים מול המטריצה בלבד: רשימת
האירועים והמסלול נבדלים פר-אפליקציה מעצם טבעם (ר' שורות 14 ו-1
במטריצה), ולכן אין להם נקודת הפעלה משותפת לאכוף.

### תיאום גרסאות
`sw.js`: hanhala **v38** · yoman **v24** (`app-version` **18-2026-08-14-2**) ·
schar **v30** · gius **gius-v16**. **ארבעתן קודמו** — `index.html` של כל
אחת מהן השתנה (נקודת ההפעלה והתרעת הגיבוי המיושן).
⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו ולא הועתקו משורת התיאום של סבב 30.
<!-- SHARED:end -->
### מה נעשה כאן (schar-limud, `sw.js` v30)
**⭐ זו האפליקציה שהדפוס הנכון נלקח ממנה** — הגיבוי כאן רץ ברציפות מפני
שהוא הופעל **בכניסה למערכת** ולא ממסלול דחיפה. מה שהשתנה הוא שהדפוס
הפך למפורש ואחיד:
- ⛔ **`slMaybeDailyBackup` הוסרה** — היא הייתה מעטפת דקה מעל
  `bkMaybeDaily`, וקיומה איפשר לקרוא למודול מכל מקום. ⚠️ **גם מעטפת
  שנקראת מהמקום הנכון היא נקודת הפעלה שנייה בפוטנציה**, וזה בדיוק מה
  שכלל ברזל 12 סוגר.
- ⭐ **מטפל ה-`DOMContentLoaded` האנונימי קיבל שם — `slBoot()`.** בלי שם
  אין «פונקציית עלייה» שאפשר להצביע עליה, והבודק אינו יכול לאכוף נקודת
  הפעלה. `lsBoot()` · `pendBoot()` · `bkBoot()` יושבות בה, בסדר הזה.
- `bkStatusMount()` נשארת ב-`renderSettingsLists()`, אחרי `statusAreaMount()`.
- `tools/check-capabilities.mjs` נוסף, וקריאה אליו נוספה לפרק «לפני כל
  push» — **שער חובה חמישי**.
- ⚠️ **`tools/test_round26.mjs` — 97 ← 98 טענות, וטענה אחת חודדה ולא
  הוחלשה.** הטענה «`slDropLegacyPassHash` מחווטת בעלייה» חיפשה את הקריאה
  **אחרי** המחרוזת `DOMContentLoaded` בקוד המקור, ולכן **נפלה** ברגע
  שהמטפל האנונימי קיבל שם והוגדר לפני שורת הרישום. ⛔ הכוונה נשמרה
  במלואה: במקומה נבדק שהקריאה יושבת **בתוך `slBoot`** — נקודת ההפעלה
  הקנונית של כלל ברזל 12 — ונוספה טענה שנייה ש-`slBoot` היא זו שנרשמת
  ל-`DOMContentLoaded`. ⚠️ כלומר הבדיקה נעשתה **צרה יותר**, לא רופפת.
- ⛔ **אין מיגרציה חדשה.** 005 עדיין היחידה שלא תורץ, ובכוונה.

### אימותים
- **חמשת השערים עברו** + **`test_round24.mjs` — 140 עברו**,
  **`test_round26.mjs` — 98 עברו** ו-**`test_round30_stage_a.mjs` — 59 עברו**.
- **⭐ שתי הטענות המחודדות אינן חותמת גומי — נמדד:** הסרת
  `slDropLegacyPassHash()` מ-`slBoot` ⇒ טענה נופלת; עטיפת `slBoot`
  בפונקציה אנונימית בשורת הרישום ⇒ טענה נופלת; שחזור ⇒ 98/0.
- **⭐ חמש המוטציות על הבודק החדש** מתוארות בפרק הפרטי של yoman-avoda,
  שם הן הורצו; כאן הורצה בקרה חיובית בלבד (0 כשלים).
- ⛔ **הסשן לא נגע במסד**, ואין מיגרציה חדשה.
