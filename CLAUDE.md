# שכר לימוד — CLAUDE.md

עודכן לאחרונה: סבב 53 · 2026-08-26

## סביבת עבודה
- **ריפו:** `ygtotlrl-lab/schar-limud`
- **Pages:** `https://ygtotlrl-lab.github.io/schar-limud/`
- **קובץ ראשי:** `index.html`
- **Supabase:** `kxbtskqobynewvnckaaz`
- **אופן העבודה (נכון ליולי 2026):** העבודה מתנהלת בסשני ענן (Claude Code cloud) — הריפו משוכפל טרי בתחילת כל סשן, העבודה נעשית על ענף ייעודי, והדחיפה בסוף הסשן. **אין עותקים מקומיים קבועים.**
- **עבודה מקומית במחשב (אם בכל זאת):** שכפול **אך ורק** לתיקייה יציבה כמו `C:\Users\F\Documents\repos\` — **לעולם לא ל-Temp** (מנקה Windows / Storage Sense מוחק משם קבצים באמצע עבודה; זה גרם ל"מחיקות רפאים" חוזרות). טוקן: מאוחסן ב-Windows Credential Manager (host `github.com`) — לעולם לא בקובץ; `git push`/`clone` מושכים אותו אוטומטית דרך GCM.

## גישת Supabase
כשזמין ה-Supabase MCP, נהג לפי הכללים הבאים — ללא יוצאים מן הכלל:
- **שינויי סכימה** (יצירה/שינוי/מחיקת טבלאות, עמודות, פוליסות, הרשאות) — **אך ורק דרך `apply_migration`** עם שם ברור ותיאורי (למשל `add_sl_transactions_soft_delete`). לא דרך `execute_sql`.
- **שאילתות אבחון וקריאה** (SELECT, בדיקת מבנה, ספירות, `list_tables` וכו') — **חופשיות**, ללא אישור.
- **עדכון או מחיקת נתונים בטבלאות `sl_*`** (`sl_students`, `sl_transactions`, `sl_settings`, `sl_lists`, `sl_users`) — **מחייבים אישור מפורש מהמשתמש לפני הרצה**. אין להריץ `UPDATE`/`DELETE`/`upsert` על נתונים בלי אישור. (בפרט: `sl_transactions` הוא כספים — מחיקה היא soft-delete בלבד, `deleted=true`.)


<!-- SHARED:start id="branch-rules" -->
## כללי ענפים
- כל עבודה נעשית על **ענף ייעודי** — לא ישירות על `main`.
- **מיזוג ל-`main` רק לאחר אישור מפורש מהמשתמש**, אחרי שבדק את השינוי בבדיקה חיצונית.
- **מחיקת ענפים מרוחקים חסומה בסביבת הענן** — אין לנסות למחוק; לדלג ולדווח למשתמש שהענף נותר.
- **ענפים ממוזגים נמחקים אוטומטית אחרי מיזוג** (`.github/workflows/cleanup-merged-branches.yml`, רץ ב-push ל-`main`); ההגנה — prefix `claude/` + בדיקת `merge-base --is-ancestor`.

### ⚠️ שני כללי תפעול (סבב 21) — שניהם נלמדו מכשל אמיתי בסבב 20

**1. לפני כל מיזוג — מרעננים את `main` המקומי, ורק אז ממזגים.**
```bash
git -C <path> fetch origin
git -C <path> checkout main
git -C <path> merge --ff-only origin/main   # חייב להצליח
git -C <path> merge <ענף-העבודה>
```
⛔ **אין למזג מעל `main` שאינו זהה ל-`origin/main`.** סשן ענן מחזיק עותק
שנוצר ברגע פתיחתו והוא **קופא שם**: כל מה שנדחף ל-`origin/main` מאז — מסבב
מקביל, מסשן אחר, או מהמשתמש — פשוט אינו קיים בעותק הזה. בסבב 20 נמצא
`main` מקומי שפיגר **בשני סבבים שלמים**. `--ff-only` הוא מה שהופך את זה
לגלוי: הוא **נכשל** במקום ליצור קומיט מיזוג שקט שמחזיר את הקוד אחורה.

**2. בעבודה רב-ריפו — כל פקודת git מציינת את הריפו במפורש: `git -C <path> …`.**
בסבב 20 פקודת מיזוג רצה בתיקייה של ריפו **אחר**, בגלל משתנה לולאה שדלף
מבלוק קודם. שם היא הייתה no-op **במקרה** — הענף לא היה קיים באותו ריפו.
⚠️ **וזה בדיוק מה שלא ניתן לסמוך עליו כאן:** שם ענף העבודה **זהה בארבעת
הריפו** (אותה תבנית `claude/<נושא>-<סיומת>`), ולכן פקודה שרצה בתיקייה
הלא-נכונה תמצא שם ענף תקף ותמזג אותו — **מיזוג שקט בריפו הלא נכון**, בלי
שגיאה ובלי סימן. ⛔ **אין להסתמך על `cd` ואין להסתמך על תיקיית העבודה
הנוכחית** — הנתיב נכתב בפקודה עצמה.
<!-- SHARED:end -->

## לפני כל push — חובה: שער אחד, `node tools/check-js.mjs`
**כל שינוי בקוד (`index.html` או `sw.js`) חייב לעבור את השער הזה לפני דחיפה.
דחיפה ללא הבדיקה — אסורה.**

```bash
node tools/check-js.mjs
```

⭐ **מסבב 33 זו פקודה אחת ולא רשימה.** השער מחלץ את כל ה-JS המוטבע
מ-`index.html`, מריץ עליו `node --check` ועל `sw.js`, מאמת את כללי-האמת
שפרסר אינו תופס (תבנית `CACHE_NAME`, דילוג על supabase, נעילת גרסת ה-CDN),
ואז מריץ **את כל שערי האחידות ואת כל חבילות בדיקות הסבבים** — כך שאין דרך
לדחוף בלי שהם רצו.
⚠️ בדיקת איזון-סוגריים בלבד אינה מספיקה (עיוורת לשגיאות בתוך מחרוזות);
`node --check` = parse מלא אמיתי ב-V8. node זמין בסביבת הענן.

**מה שהשער מריץ, בסדר הזה:**
- **`check-structure.mjs`** — המבנה הקנוני (סבב 33, כלל ברזל 14).
- **`check-status-area.mjs`** — אחידות «אזור מצב» (סבב 17, כלל ברזל 7).
- **`check-docs.mjs`** — אחידות התיעוד (סבב 18, כלל ברזל 8).
- **`check-comments.mjs`** — תקן ההערות בקוד (סבב 28, כלל ברזל 11).
- **`check-capabilities.mjs`** — אחידות היכולות המשותפות (סבב 30 השלמה, כלל ברזל 12).
- **`check-gaps.mjs`** — אכיפת פרק «פערים פתוחים» (סבב 39, כלל ברזל 15).
- **`test_round50_icons.mjs`** — שכבת האייקונים: `icons/` בדיוק ששת
  הנכסים הקנוניים, `design/` קובץ מאסטר אחד, ⛔ ואף קובץ מוגש אינו מפנה
  אליה — עם **חמש מוטציות** ומוטציית-נגד אחת (שינוי תוכן אינו מפיל).
- **`test_round24_offline_login.mjs`** — כניסה אופליין + `password` שאינו נוגע בדיסק.
- **`test_round26_roles.mjs`** — הרשאות לפי תפקיד + ניקוי שער הסיסמה.
- **`test_round30_stage_a.mjs`** — גיבוי יומי ויומן פעולות (מודול משותף).
- **`test_round33_structure.mjs`** — הנעילה המבנית ושער ה-JS, עם מוטציות.
- **`test_round35_hotwin.mjs`** — מודול החלון החם ויכולות סבב 35, עם מוטציות.
- **`test_round35c_cron.mjs`** — פינוי הגיבויים במסד (pg_cron), עם מוטציות.
- **`test_round37_merge_pending.mjs`** — הגנת ה-⏳ במנוע המיזוג וחסימת
  משתמש מושבת בכניסה אופליין, עם מוטציות.
- **`test_round37_matrix.mjs`** — אכיפת מטריצת היכולות: היפוך כל תא מכוסה
  חייב להפיל את `check-capabilities`.
- **`test_round38_merge_core.mjs`** — ליבת המיזוג המשותפת: כלל ההכרעה
  מופיע פעם אחת, שלושת כללי ברזל 6, וידיות המדיניות — עם חמש מוטציות.
- **`test_round39_gaps.mjs`** — אכיפת פרק «פערים פתוחים»: פער שנסגר מפיל
  את השער, ושורה `לא-מדיד` עוברת — עם שמונה מוטציות.
- **`test_round39_md.mjs`** — שלושת קובצי ה-md הנלווים (`README.md` ·
  `CONTEXT.md` · `android/README.md`): השלד (סבב 39) **ותוכן שש הפסקאות
  המשותפות** שנחתמות ב-sha256 (סבב 41א), עם שש מוטציות.
- **`test_round42_sw.mjs`** — קו הבסיס ההתנהגותי של ה-service worker: מה
  חוזר ל-`respondWith` בכל תרחיש (ניווט, תת-משאב, 404, supabase, סריקת
  המטמון) ומה נכנס למטמון. ⚠️ **נמדד לפני האיחוד** ומשמש מאז כשער: ⛔ כל
  תא שלא סומן `defect` חייב להישאר זהה בדיוק, וחמשת תאי הליקוי הופכו
  בסבב 42ג. מסבב 42ג `defectCount` הוא **0**.
- **`test_round42c_swcore.mjs`** — ליבת ה-service worker: החתימה, מספר
  השורות, `SW_CFG` מעל הליבה, ועשר ידיות המדיניות שנמדדו בסבב 40 —
  עם **ארבע מוטציות**, שלוש מהן מריצות את רתמת קו-הבסיס על עותק מוטב
  בתיקייה זמנית ודורשות שהיא **תיפול**.
- **`test_round46b_workflows.mjs`** — היפוך ברירת המחדל בשני ה-workflows:
  חתימה אחת ל-`build-apk.yml` ואחת ל-`cleanup-merged-branches.yml` בארבעתן,
  ⛔ ושם הריפו הוא הדבר היחיד המוכרז פרטי — עם שש מוטציות.
- **`test_round45_android.mjs`** — שער שכבת האנדרואיד: חוזה המניפסט (חמש
  שורות), `configChanges` כסופרסט הקנוני, שלושת רכיבי גשר השיתוף כקבוצה
  אחת, ו**חתימה על החלק המשותף** של `AndroidManifest.xml` ושל
  `android/app/build.gradle` — עם **שמונה מוטציות**, שלוש שחייבות להזיז
  את החתימה וחמש שאסור להן.
- **`test_round45_lists.mjs`** — שקילות שתי רשימות המודולים המשותפים:
  `CAPS` שב-`check-capabilities.mjs` מול `SHARED_BLOCKS` שב-`check-comments.mjs`.
  ⛔ מודול שקיים באחת ואינו בשנייה מפיל את הדחיפה — עם **שלוש מוטציות**,
  וטענה שרשימה ריקה היא כישלון ולא «אין הפרש».
- **`test_round44_retry.mjs`** — מודול הניסיון החוזר בסנכרון: הדריכה
  המותנית, שלושת התנאים, הנסיגה האקספוננציאלית והגנת הריצה הכפולה —
  ברתמת `vm` עם שעון מזויף, ועם **ארבע מוטציות**.
- **`test_round41_build.mjs`** — שער הבנייה והחתימה: שם ה-workflow, שם
  ה-artifact, שם קובץ הפלט, ⛔ היעדר `apksigner` ב-YAML, ו-`EXPECTED_SHA256`
  שאינו ריק ואינו מציין-מקום — עם שמונה מוטציות.

⚠️ **אין יותר `sync-setup-sql.py`** — הוא הוסר בהשלמת סבב 24 יחד עם העותק
המוטבע של הסכימה. אין מה לסנכרן: הסכימה יושבת ב-`migrations/000_initial_schema.sql`
בלבד, ומסך ההגדרה מושך אותה ב-`fetch`. ⛔ אין להחזיר עותק מוטבע.

אם `node --check` מדווח שגיאה — אסור לדחוף עד שהיא מתוקנת.
אם `check-structure.mjs` נכשל — ר' הפרק «כלל ברזל 14» למטה. ⛔ קובץ שורש
חדש או תיקייה חדשה מחייבים שורה **מנומקת** ברשימת-ההיתר, בארבעת עותקי
הבדיקה — «קיים רק כאן, בשקט» אינו מצב חוקי.
אם `check-status-area.mjs` נכשל — ר' הפרק «כלל ברזל 7» למטה. שינוי בבלוק
משותף מחייב עדכון בארבע האפליקציות ובארבעת עותקי קובץ הבדיקה, באותו סבב.
אם `check-docs.mjs` נכשל — ר' הפרק «כלל ברזל 8» למטה. אותו כלל חל על פרק
תיעוד מסומן `SHARED`: משנים בארבעתם ובארבעת עותקי הבדיקה, באותו סבב.
אם `check-comments.mjs` נכשל — ר' הפרק «כלל ברזל 11» למטה. ⛔ התקן חל על
הקוד הפרטי בלבד; הבלוקים המשותפים והמודולים הקפואים מוחרגים ממנו במפורש.
אם `check-capabilities.mjs` נכשל — ר' הפרק «כלל ברזל 12» למטה. ⛔ נקודת
ההפעלה של יכולות העלייה היא **פונקציית העלייה של האפליקציה** — זו שקוראת
גם ל-`lsBoot()` וגם ל-`pendBoot()` — ואין להזיז אותה למסלול אחר.

## עדכוני Service Worker
- **כל שינוי בקוד מחייב קידום `CACHE_NAME` ב-`sw.js` לגרסה הבאה** — בלי זה המשתמשים לא יקבלו את העדכון.
- מנגנון באנר "גרסה חדשה זמינה" קיים באפליקציה — המשתמשים מקבלים את העדכון בלחיצה על הבאנר.
- ⛔ **אין לקדם גרסה בלי שינוי בקוד הרץ (סבב 26 השלמה) — קידום סרק שולח
  באנר «גרסה חדשה זמינה» לכל המשתמשים לשום שינוי.** ⚠️ שינוי הערות נרחב
  ב-`index.html` הוא כן שינוי שמקודם (תקדים סבב 28); הערה בודדת — לא.

## Push
```bash
git add . && git commit -m "תיאור השינוי"
git push -u origin <שם-הענף>   # דחיפה לענף העבודה — לא ל-main
```

## סיום משימה
בסיום כל משימה משמעותית — **עדכן קובץ זה בתמצית** לפני סיום הסשן: מה שונה, מה הוחלט. כך הסשן הבא מתחיל עם תמונת מצב עדכנית.

## כללים קריטיים
1. **בדיקת תחביר עם `node --check` לפני כל push** (הסעיף למעלה) — חובה מוחלטת. כל שינוי ב-`index.html` חייב לעבור חילוץ-JS + `node --check` (וגם `sw.js`). דחיפה בלי זה — אסורה.
2. **קידום `CACHE_NAME` ב-`sw.js` בכל שינוי קוד** (הסעיף למעלה) — בלי זה העדכון לא מגיע למשתמשים.
3. Supabase: כל טבלה חדשה — GRANT מפורש ל-anon, authenticated, service_role + RLS
4. **מקור אמת יחיד = `index.html`** — זה הקובץ ש-Pages מגיש ושאליו מצביע `start_url` במניפסט. כל עדכון קוד נכנס לכאן בלבד. אסור ליצור קבצי HTML כפולים של האפליקציה. (אין כאן מנגנון אוטו-אפדייט פנימי — בניגוד ל-yoman-avoda. אין יותר sl_p1/p2/p3.txt + gen.py — index.html נערך ישירות. הרענון הוא דרך ה-Service Worker.)
5. **`sl_transactions` = כספים** — מחיקה היא soft-delete (`deleted=true`+`deleted_at`+`deleted_by`), לעולם לא מחיקה פיזית. כל שליפה/סיכום מסננת `deleted=false`. הוספה ממלאת `created_by`.
6. **`sl_students` = גם soft-delete** (יולי 2026, `migrations/002`) — `deleteStudent` הריץ `DELETE` פיזי, ועל `sl_transactions` מוגדר `ON DELETE CASCADE`: מחיקת תלמיד השמידה פיזית את **כל היסטוריית הכספים שלו**, בעקיפה של כלל 5. מעכשיו התלמיד מסומן `deleted=true` והתנועות נשארות. הסינון נעשה בנקודה אחת — ב-`syncAll`, בדיוק כמו `TRANSACTIONS` — ולכן כל 11 הקוראים של `STUDENTS` מקבלים אותו אוטומטית. **אין להחזיר `.delete()` על אף אחת מהטבלאות האלה.**
7. **מקור אמת יחיד לסכימה = `migrations/000_initial_schema.sql`** (יולי 2026, סבב 6; העותק המוטבע הוסר בהשלמת סבב 24). מסך ההגדרה ב-`index.html` **מושך** את קובץ המיגרציה ב-`fetch` ומציג אותו. **⛔ אין סכימה מוטבעת ב-`index.html`, ואין להחזיר אותה** — כשל משיכה מציג הודעה עם קישור ישיר לקובץ ב-GitHub. **אין להחזיר סכימה כתובה-ביד לאף אחד מהשניים.**
8. **בריחת HTML** — כל ערך שמקורו בנתוני משתמש ונכנס ל-`innerHTML` חייב לעבור `esc()` (סבב 6).
9. **גרסאות CDN נעוצות במדויק** — אין `@2`/`@4` צפים (סבב 6).
10. **`client_id` — מפתח זהות שנוצר במכשיר** (אוגוסט 2026, `migrations/006`).
    `sl_transactions.id` ו-`sl_students.id` הם SERIAL, כלומר המסד מקצה אותם.
    **כל רשומה חדשה של תנועה או תלמיד חייבת לקבל `client_id`** מ-`newClientId()`,
    והכתיבה לענן היא **`upsert` עם `{onConflict:'client_id'}` — לא `insert`**.
    זה התנאי לעבודה אופליין בטוחה. **אין להחזיר `.insert()` על שתי הטבלאות האלה.**

## הגדרת Supabase — פעם ראשונה בלבד
הרץ **`migrations/000_initial_schema.sql`** ב-Supabase SQL Editor:
https://supabase.com/dashboard/project/kxbtskqobynewvnckaaz/sql
(אידמפוטנטי, כולל כבר את 001–009.)

טבלאות: sl_users, sl_students, sl_transactions, sl_settings, sl_lists

⛔ **אין משתמש ברירת מחדל, ואין ליצור אחד** (סבב 24). עד אז זרעה הסכימה
`admin`/`admin`, כלומר כל התקנה טרייה נולדה עם חשבון שסיסמתו ידועה לכל
העולם, על מסד שה-RLS שלו `USING (true)` וכתובתו בקוד המקור. המשתמש הראשון
נוצר ידנית ב-SQL Editor — **מסבב 26 עם `role` במפורש**:
`INSERT INTO public.sl_users (username, password, role) VALUES ('<שם>', '<שש ספרות>', 'admin');`
והאפליקציה מציגה את ההנחיה מעצמה כשהטבלה ריקה.
⚠️ **`sl_settings.admin_pass` — בוטלה בסבב 26.** היא הייתה סיסמת **שער
מסך ההגדרות**, שכבת הרשאה שנייה שהתקיימה כאן ולא בשלוש האחיות. ההרשאה
נגזרת עכשיו מ-`sl_users.role`, הזריעה הוסרה מהסכימה, ו⛔ **אין להחזיר
אותה** — ר' הפרק «הרשאות — `role`, במקום שער סיסמה» למטה.

## חתימת APK — מפתח קבוע (לעולם לא משתנה!)

| | |
|---|---|
| **קובץ** | `signing/schar.keystore` (PKCS12, RSA 2048) |
| **alias** | `schar` |
| **storepass** | `schar123` |
| **keypass** | `schar123` (זהה ל-storepass) |
| **תוקף** | 10,000 יום — 07.08.2026 עד 23.12.2053 |
| **SHA256** | `29:32:D9:B5:94:69:D4:E4:53:EF:C7:EE:3B:10:55:C9:CE:4B:EE:D6:9B:BB:78:EC:EE:18:BD:C6:BE:2D:0F:87` |
| **SHA1** | `F5:BD:6A:6E:BE:EF:B5:85:78:9F:70:B1:19:60:8F:1B:DE:90:1B:D4` |
| **DN** | `CN=schar, OU=Yeshiva, O=Yeshiva, L=Rishon LeZion, ST=Israel, C=IL` |

### ⛔ אזהרה — אין להחליף את המפתח לעולם

אנדרואיד מזהה אפליקציה מותקנת לפי **חתימת המפתח**, לא לפי שם הקובץ או מספר הגרסה.
APK שנחתם במפתח אחר נחשב אפליקציה **זרה**, וההתקנה מעל הקיימת נכשלת בשגיאת
`INSTALL_FAILED_UPDATE_INCOMPATIBLE`.

**המשמעות המעשית של החלפת מפתח:** כל משתמש שכבר התקין יצטרך **להסיר את האפליקציה
ולהתקין מחדש** — ואין דרך לעקוף את זה. אין שחזור, אין מיגרציה, אין "חתימה מחדש
במפתח הישן" אחרי שהוא אבד.

לכן:
1. **לעולם לא להריץ `keytool -genkeypair` שוב** עבור הפרויקט הזה. הריצה הראשונה
   כבר בוצעה, והקובץ בריפו הוא התוצאה שלה.
2. **לעולם לא למחוק, לדרוס או "לרענן" את `signing/schar.keystore`.** הוא חלק מהריפו
   בדיוק כדי שלא יאבד — כמו שקרה ב-yoman-avoda כשהמפתח ישב ב-`/tmp`.
3. **כל APK חדש נחתם אך ורק במפתח הזה**, גם אם נבנה בכלי אחר (PWABuilder, Bubblewrap,
   Android Studio).
4. אחרי חתימה — לאמת שה-SHA256 תואם לטבלה למעלה.

### חתימה
⭐ **מסלול אחד ויחיד** (סבב 53, גזום לצפיפות של הנהלה):
`./signing/sign-apk.sh app-unsigned.apk schar-limud.apk` — zipalign,
apksigner, ובסוף `apksigner verify --print-certs`; דורש Android
build-tools ב-PATH. ⛔ החלופות הידניות (apksigner/jarsigner) הוסרו
מכאן — הן יושבות בסקריפט עצמו, ומסלול חתימה שני בתיעוד הוא בדיוק הדרך
שבה APK נחתם במפתח הלא-נכון. אימות: `keytool -list -v -keystore
signing/schar.keystore -storepass schar123`.

## APK — מעטפת WebView מקורית שטוענת מהרשת (⛔ לא TWA) — אוגוסט 2026
שכר לימוד עברה לדפוס המעטפת-מהרשת של yoman-avoda (סבב 13 שם): תיקיית
**`android/`** מכילה מעטפת WebView מקורית שטוענת את
`https://ygtotlrl-lab.github.io/schar-limud/` **מהרשת** — לא מנכסים מוטבעים.
פרטים מלאים ב-`android/README.md`. עיקרי הדברים:

- **לעולם לא TWA ולא PWABuilder** — TWA מריץ את האתר בתוך כרום, וסינון התוכן
  במכשירי המשתמשים חוסם את כרום (נמדד בפועל ב-gius). WebView מרנדר בתוך
  התהליך ולא נחסם.
- **package `com.schar.limud`, versionCode 3** — המעטפת הראשונה כאן, טוענת
  מהרשת מהיום הראשון (בניגוד ליומן, לא היה כאן שלב `file://`).
  `usesCleartextTraffic=false`, minSdk 21 / targetSdk 34.
- ⛔ **אין נכסים מוטבעים, ואין להוסיף** — עותק `file://` הוא origin אחסון
  נפרד: תשלום שנרשם אליו אופליין לא היה נראה לאפליקציה האמיתית לעולם, וזה
  כסף. בנוסף הוא מקור אמת שני (כלל קריטי 4). ה-service worker + שכבת
  ה-MIRROR נותנים אופליין אחרי עלייה מוצלחת אחת.
- ⛔ **אין גשר שיתוף — וזה ההבדל היחיד מהתבנית של יומן.** בקוד של שכר לימוד
  אין `navigator.share`, ולכן `AndroidShareBridge` הושמט כליל — Java, manifest
  (אין FileProvider/queries) ותלויות (אין androidx). אם אי-פעם יידרש גשר —
  מעתיקים את הדפוס הכפול-נעילה של יומן; לעולם לא `addJavascriptInterface` חשוף
  על דף שנטען מהרשת.
- **`http`/`https` נשארים בתוך המעטפת** (מסירה לדפדפן = כרום = חסום);
  `tel:`/`mailto:`/`whatsapp:` וכו' נמסרים למערכת.
- **עדכוני קוד web לא מצריכים APK חדש** — מגיעים דרך ה-service worker ובאנר
  העדכון, כמו בדפדפן. APK חדש נדרש רק לשינוי במעטפת עצמה.
- **צנרת בנייה:** `.github/workflows/build-apk.yml` — Actions → **Build Signed
  APK** → Run workflow (temurin 17, בנייה, חתימה עם `signing/schar.keystore`
  הקבוע alias `schar`, אימות `apksigner verify`). ה-artifact:
  **`schar-limud-apk`**.

### ⚠️ אזהרת מעבר-origin חד-פעמי (אותו עיקרון כמו ביומן)
ה-WebView של ה-APK מחזיק **מחיצת אחסון נפרדת** מזו של הדפדפן באותו מכשיר.
מי שעבד בדפדפן ועובר ל-APK מתחיל עם localStorage ריק — כניסה מחדש, וה-MIRROR
נטען מהענן (מקור האמת). ⛔ **מה שכן יכול ללכת לאיבוד: רשומה שסומנה ⏳ בדפדפן
וטרם עלתה לענן.** לכן לפני מעבר מכשיר ל-APK — לוודא בדפדפן שההגדרות ←
«⏳ ממתין לסנכרון» מציג **0**.

## לקחים חיים בקוד — מזוקק מסבבי התיקונים 6–10 (יולי–אוגוסט 2026)
כל שורה כאן היא כלל עומד; הרקע המלא בהיסטוריית git ובהערות שבקוד עצמו.
- **`syncAll` בודק `rs[i].error` פר-קטגוריה** — supabase-js לא זורק, ולכן
  `catch` לבדו הוא קוד מת; רק תוצאה תקינה נכתבת (עדיף ישן מרוקן), והמשתמש
  מקבל טוסט פעם אחת (`_syncWarned`) + טוסט התאוששות.
- **`_syncBusy` משוחרר ב-`finally`** (סבב 6) — דגל שנתקע דלוק מקפיא את
  הסנכרון לתמיד.
- **`isSupabaseRequest()` ב-`sw.js`** (סבב 7) — ⛔ אין להחזיר תגובות API
  למטמון: נתוני כספים ישנים שמוגשים כטריים גרועים מכישלון גלוי.
- **`ON DELETE RESTRICT` על `sl_transactions`** (`migrations/003`, הורצה
  ואומתה) — גם `DELETE` ידני מלוח הבקרה לא ישמיד היסטוריית כספים דרך
  CASCADE.
- **בריחת HTML דרך `esc()`** לכל ערך משתמש שנכנס ל-`innerHTML`; ⛔ אין
  קוד JS מורכב במאפייני HTML (סבב 6) — `data-*` + פונקציה במקום.
- **`sw.js`:** מחיקת מטמונים לפי קידומת `'schar-limud-'` בלבד (סבב 9) —
  ה-origin משותף לארבע האפליקציות. מסבב 35 יש כאן גם רשימת CDN נעוצה
  שנטענת מראש ב-`install` עם `mode:'cors'`, ו-`ensureCdnCached` לריפוי
  עצמי — הדפוס של hanhala (סבב 9 שם); ה-fetch handler ממשיך לשמור
  תגובות GET בזמן-ריצה כשכבה שנייה.
- **גרסאות CDN נעוצות** — supabase-js `2.111.0`, `chart.js@4.4.1`.

## חודש הצטרפות / עזיבה לתלמיד (`migrations/004`)
עד כה כל תלמיד פעיל חויב אוטומטית ב-12 חודשי השנה, ולכן מי שהצטרף באמצע השנה הוצג
כחייב גם על חודשים שלא היה בהם. נוספו ל-`sl_students` שתי עמודות טקסט בפורמט `YYYY-MM`:
- **`start_month`** — החודש הראשון שבו התלמיד פעיל. **ריק = כל השנה** (התנהגות שלפני 004),
  כדי לא לשבור נתונים קיימים.
- **`end_month`** — החודש האחרון שבו היה פעיל. ריק = עדיין פעיל. (מי שהוגדר לו רק
  `end_month` עדיין נחסם אחרי העזיבה — היעדר `start_month` מסיר את הגבול התחתון בלבד.)

**גבולות שנת הלימודים לא נגעו:** `YEAR_MONTHS` ו-`acadYearOf` נשארו ספטמבר–אוגוסט.
הטווח כאן הוא חודשי לוח גרגוריאני, וההשוואה **לקסיקוגרפית** — `YYYY-MM` ממוין
כרונולוגית כמחרוזת, ולכן אין המרה לתאריך. הגשר בין השניים הוא `monthKey(y,m)`
(היפוך `acadYearOf`: אינדקסים 8–11 שייכים ל-`y`, השאר ל-`y+1`).

- **`calcDistribution`** מסמן חודש מחוץ לטווח כ-`{req:0,paid:0,off:true}` — לא מחויב,
  לא נספר כחוב, והתשלומים **מדלגים עליו** ונפרסים על חודשי הטווח בלבד. הדגל `off`
  מבדיל בתצוגה בין "לא היה בישיבה" לבין "חודש מחויב שלא שולם".
- **דשבורד:** נוספה עמודת **"פעילים"** — מצבת התלמידים בכל חודש (`countInMonth`);
  בשורת הסיכום — כמה היו פעילים אי-פעם באותה שנה. `showDashMonth` מציג רק תלמידים
  שהיו בטווח באותו חודש.
- **UI:** בוררי `input[type=month]` בכרטיס התלמיד ובטופס ההוספה (ברירת מחדל בהוספה =
  החודש הנוכחי). האימות ב-`readMonthRange` **חובה** — פיירפוקס לא תומך ב-type=month
  ונופל ל-input טקסט חופשי; המסד מגבה בשני CHECK (פורמט + `end >= start`).
- **`handled_months` הוסרה** (מ-000 ומ-004): לא נקראה ולא נכתבה באף מקום, וכל הרשומות
  החזיקו בה ערך ריק. **אין להחזיר אותה.**
- ⚠️ **מסלול «`start_month` ריק = כל השנה» הוא תאימות-לאחור בלבד** ולא המצב
  הרווח בנתונים; ⛔ אין להסיר אותו (הוא ההתנהגות הנכונה לרשומה חסרה), אבל
  בדיקה ידנית של הטווח נעשית מול תלמיד שהטווח שלו מוגדר.

## יתרת זכות + סעיף «זוכה על חשבון יתרת זכות» (`migrations/005` — לא תורץ בכוונה)
עודף תשלום נשמט עד כה בשקט: `calcDistribution` פרס את התשלומים עד גובה החיוב והשאר נעלם
מהתצוגה. עם טווח ההצטרפות/עזיבה (004) יש פחות חודשים לפרוס עליהם, והמצב נפוץ יותר.
**שני מושגים נפרדים — אין לערבב:**
- **יתרת זכות** = שולם בשנת לימודים מעבר לסך החיוב בטווח החודשים של התלמיד.
  מוחזרת מ-`calcDistribution` תחת המפתח **`credit`** (שאר המפתחות הם אינדקסי חודש 0–11,
  אין התנגשות), ונקראת דרך `distCredit(dist)` / `studentCredit(sid,year)`.
  **אינה חוב שלילי:** לא מקוזזת מסך החוב, לא נכנסת ל"סה"כ שולם" ולא לאף סיכום חוב —
  שדה נפרד, בצבע נפרד (כחול `--credit`, נבדל מירוק=שולם ומאדום=חוב).
  מוצגת ב-3 מקומות: כרטיס התלמיד (שורת `#sc-credit` מתחת לטבלה השנתית), רשימת
  התלמידים בהגדרות (תגית «💠 זכות ₪X» לשנה הנוכחית) והדשבורד (שורת «💠 יתרת זכות»).
- **זיכוי מיתרה** = תנועה שנרשמה עם `payment_method === CREDIT_METHOD`
  (`'זוכה על חשבון יתרת זכות'`). מקטינה את חוב התלמיד בדיוק כמו תשלום, אבל **אינה כסף
  שהתקבל** ולכן אינה נספרת בשום סיכום גבייה/הכנסה.

**למה אמצעי תשלום ולא רשימת ה-sections:** `payment_method` הוא השדה היחיד שנשמר **על
התנועה**. `sl_lists.sections` משמשת לשיוך תלמיד (`card_settings.section`) ואין לה ייצוג
ברמת התנועה — שימוש בה היה מחייב עמודה חדשה ב-`sl_transactions`, כלומר שינוי סכימה שאינו
נדרש — ובפרט מיגרציה נוספת שהפיצ'ר היה תלוי בהרצתה הידנית כדי לעבוד בכלל.

**`calcDistribution` עבר מ"בריכה" ל"תור תשלומים לפי תאריך"** — כל שקל שנפרס על חודש יודע
מאיזו תנועה בא, ולכן `result[m]` מחזיק גם `cash` (תקבול) ו-`cred` (זיכוי), כאשר
`paid === cash + cred`. **הסכומים זהים בדיוק לפריסה הישנה** (מאומת בבדיקה) — הפיצול הוא
סיווג בלבד. בלעדיו אי אפשר להוציא זיכויים מסיכומי הגבייה: בבריכה הם מתמזגים עם הכסף האמיתי.

**הבחירה בדשבורד — שורות נפרדות, לא הפחתה שקטה:** שורת «סה"כ» נשארת כיסוי החיוב כפי שהיה
(אין רגרסיה במספר המוכר), ומתחתיה נוספו **«↩ מזה זיכוי מיתרה»** ו**«גבייה בפועל»** —
מוצגות רק כשיש זיכויים בשנה. הגרף מציג גבייה בפועל, והזיכויים נערמים מעליו כ-dataset שני
(עם legend) רק כשקיימים. גם `showDashMonth` מפצל כך, ותאי חודש עם זיכוי נצבעים כחול ומסומנים ↩.

**עמידות הסעיף (חובה לשמר):**
1. **הסיווג לפי המחרוזת ששמורה על התנועה** (`isCreditTxn`), לא לפי קיום הפריט ב-`sl_lists` —
   מחיקת הפריט אינה משנה שום סיווג היסטורי בשקט.
2. `updateDropdowns` **מזריק את האפשרות לטופס תמיד** כשהיא חסרה מהרשימה.
3. `deleteListItem` **מסרב** למחוק אותה (טוסט מסביר), והיא מוצגת בהגדרות עם «🔒 סעיף מערכת»
   בלי כפתור מחיקה — הבדיקה לפי ערך, ולכן חלה על שתי הקטגוריות.
4. `ensureCreditMethod()` זורע את הפריט אחרי סנכרון תקין (`_lastSyncOk`, פעם אחת בסשן),
   כדי שיופיע בלי הרצת SQL ידנית. **`migrations/005` היא אופציונלית** (זריעת נתון בלבד,
   `WHERE NOT EXISTS` — ל-`sl_lists` אין אילוץ ייחודיות ולכן `ON CONFLICT DO NOTHING`
   אינו מונע כפילות שם). **הוחלט שלא תורץ** (אוגוסט 2026) — האפליקציה זורעת בעצמה,
   ולכן היא נשארת כתיעוד ולשימוש ידני בלבד. 003 ו-004, לעומתה, כן הורצו ואומתו.

## מוסכמת הודעות קומיט (סבב 8 — משותפת לשלושת הפרויקטים)
**עברית חופשית**, לא conventional-commits. זה הדפוס שהתגבש בפועל בשלושת הריפו,
ומעכשיו הוא ההחלטה המוצהרת:
- שורה ראשונה: תיאור תמציתי בעברית של מה השתנה (עד ~72 תווים).
- עבודה מרוכזת מקבלת קידומת `סבב N:` (למשל `סבב 8: אחידות בין שלושת הפרויקטים`).
- בלי קידומות `feat:`/`fix:` באנגלית.

## ⚠️ התנגשות שמות: הקידומת `sl` (סבב 8 — לתעד, לא לשנות)
**באותו פרויקט Supabase (`kxbtskqobynewvnckaaz`) חיים שני מובנים שונים ל-`sl`:**
- **כאן (schar-limud): `sl` = שכר לימוד.** טבלאות `sl_students`, `sl_transactions`
  (כספים!), `sl_settings`, `sl_lists`, `sl_users` — כולן של הפרויקט הזה.
- **ב-hanhala-ruchanit: `sl` = שינה** (sleep). שם אלה **פונקציות JS בלבד** —
  `slSaveData`, `slOpenSession`, `slSupNav`, `slAddTreat` — שנשמרות ב-kv, לא טבלאות.
- **אין לשנות שמות** — מאות אתרי שימוש, סיכון גבוה מדי מול תועלת. ההגנה היא מודעות:
  סשן שעובד מול Supabase חייב לוודא לאיזה פרויקט שייכת ה-`sl_*` שהוא נוגע בה.
  כלל אצבע: `sl_` עם קו תחתון = טבלת שכר לימוד; `sl` כקידומת camelCase = שינה ב-hanhala.

## הדשבורד — תנועות בפועל, לא פריסה (סבב 10)
**השינוי המהותי.** הדשבורד הציג את **פריסת** התשלומים: תשלום 2000 מ-11 ביולי התפצל
ל-1700 ביולי ו-300 באוגוסט, ולכן החודש שבו נכנס הכסף לא נראה בשום מקום. מעכשיו כל חודש
מציג את **סך התנועות שנרשמו בו לפי תאריך התנועה, בלי שום פריסה** — 2000 מיולי שייכים
כולם ליולי. חל על הטבלה השנתית, על הפירוט החודשי (`showDashMonth`) ועל הגרף.
- **`calcDistribution` (הפריסה) נשארה אך ורק בכרטיס התלמיד האישי** — שם היא נכונה
  ורלוונטית (מול איזה חודש נזקף התשלום ומה החוב בכל חודש). **אין להחזיר אותה לדשבורד.**
- פונקציות חדשות: `actualByMonth(year)` → `{student_id:{monthIdx:סכום}}` לפי תאריך
  התנועה; `studentTuition(s)` (אישי או ברירת מחדל); `monthTarget(year,m)` = סכום שכר
  הלימוד של כל הפעילים בחודש; `pctNum`/`pctText`/`pctClass`/`ofTargetTitle`.
- **אחוז הגבייה מוצג עם שני המספרים שמהם הוא נגזר** — שורות «נגבה» ו«יעד» יושבות
  ישירות מעל שורת האחוז באותה עמודה, יש `title` («נגבה X מתוך יעד Y»), והפירוט החודשי
  אומר זאת במפורש: «נגבה ₪X מתוך יעד ₪Y • Z%».
- **הגרף**: עמודות = הנגבה בפועל, ובנוסף **קו יעד מקווקו** (dataset מסוג `line` בתוך
  גרף `bar` — נתמך ב-chart.js 4), ו-tooltip שמוסיף את אחוז הגבייה. **לא מוערם.**
- **תנועה מחוץ לטווח הפעילות של התלמיד** (תשלום מראש/בדיעבד) מוצגת בכל זאת בתא שלה,
  כדי שסכום עמודת החודש יהיה שווה בדיוק לסך התנועות; `showDashMonth` מציג מאותה סיבה
  גם תלמיד פעיל שמחוץ לטווח אם נרשמה לו תנועה בחודש. תא ריק **ומחוץ לטווח** = `·`.
- תנועה בסעיף `CREDIT_METHOD` **אינה נכנסת ל-`actualByMonth` כלל** — אינה כסף שהתקבל.
  אותו כלל כמו קודם, רק שעכשיו היא פשוט לא נספרת במקום להיות מוצגת ומופחתת בשורה נפרדת.

### 4. יתרת זכות ירדה מהדשבורד
יתרת זכות היא **נתון אישי ולא ניהולי**. הוסרו מהדשבורד שורות «💠 יתרת זכות»,
«↩ מזה זיכוי מיתרה» ו«גבייה בפועל», התגית והסימון ↩ ב-`showDashMonth`, ומחלקת ה-CSS
`.credit-row` (נותרה ללא שימוש). **נשמרו כפי שהיו**: כרטיס התלמיד (`#sc-credit`,
`distCredit`/`distCredApplied`) ותגית «💠 זכות» ברשימת התלמידים בהגדרות — שתיהן
תצוגה ברמת תלמיד יחיד. `studentCredit` ו-`distCredApplied` עדיין בשימוש; אין למחוק.

---

<!-- SHARED:start id="iron-rules-storage" -->
## ⭐ כללי ברזל של הארגון — עמידות אחסון ואופליין (סבב 11, אוגוסט 2026)
**ארבעת הכללים האלה זהים מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius). הם חלים על כל קוד חדש בארגון.

1. **כל כתיבה ל-localStorage עוברת דרך העוזר המוקשח.** `lsSet(key, value)` או
   `lsSetArray(key, arr, tsOf)` — **ואין קריאה ישירה ל-`localStorage.setItem`**
   בשום מקום מחוץ למודול המשותף. `setItem` זורק באחסון חסום או מלא, וכל נתיב
   לא-מוגן מת באמצע: הזיכרון כבר עודכן, המסך מתרענן, ושום דבר לא נשמר.
   - החריג היחיד המותר הוא **`lsSetRaw`** — העוזר של המודול עצמו, שמחזיר
     `false` בלי להרים באנר. הוא שמור לערכים **חסרי-נזק שנכתבים בכל עליית
     דף** (מזהה מכשיר, יומן הפינוי). **אין להשתמש בו לשום נתון של המשתמש.**
2. **אף כישלון שקט.** `lsSet` מחזירה `true`/`false`, מרימה באנר אדום קבוע
   (`#ls-alert`) שנשאר עד שהמשתמש סוגר אותו, ורושמת ליומן.
   - נקודת האכיפה היא שורה אחת בראש `toast()` של כל אפליקציה:
     `if (typeof lsGuardToast === 'function' && !lsGuardToast(msg)) return;`
     — כל הודעה שמתחילה ב-✅ נבלעת אם כתיבה מקומית נכשלה בשתי וחצי השניות
     האחרונות. **אין להסיר את השורה הזו.** בלעדיה חוזר בדיוק המצב שהסבב הזה
     בא למנוע: כתיבה שנכשלה ומשתמש שרואה "נשמר בהצלחה".
   - במסלול קריטי (רישום נוכחות, שמירת תשלום, הוספת רשומה, רישום תנועה) יש
     בנוסף עצירה מפורשת — `lsFails()` / `lsOkSince(t)`, או ערך מוחזר שנבדק.
3. **עבודה אופליין מלאה היא דרישת בסיס בכל אפליקציה בארגון — מהיום הראשון,
   לא תוספת מאוחרת.** ארבעת הרכיבים, כולם:
   עותק מקומי של **כל** הנתונים · קריאה ממנו בעליית האפליקציה, **לפני** כל
   נגיעה ברשת · סנכרון ברקע · מנוע מיזוג **ברמת רשומה** לפי חותמת עדכון,
   עם tombstones. הכלל היחיד שחשוב במנוע: **היעדר רשומה אצל צד אחד אינו
   מחיקה** — מחיקה היא `deleted=true` + חותמת.
4. **ניטור מכסה ופינוי יזום נבנים יחד עם האופליין, לא אחריו.** עותק מקומי
   בלי ניטור הוא פצצת מכסה מושהית: הוא גדל עד שהכתיבה נכשלת. `lsBoot()`
   נקראת בכל עליית אפליקציה, מודדת את **כל** המפתחות בדומיין (לא רק של
   האפליקציה הנוכחית — המכסה משותפת!), מתריעה מעל 3MB ובאנר מעל 4MB,
   ומפנה מיוזמתה מעל 60%.
5. **כל אפליקציה חדשה בארגון נבנית עם התשתית הזו מלכתחילה** — מעתיקים את
   המודול המשותף כמו שהוא, ממלאים `LS_CFG`, מחברים `lsBoot()` בעלייה,
   את רכיב «מידע טכני ›» (`techInfoMount()`) בהגדרות, ואת שורת השער ב-`toast()`.

### מפתח ניווט — כללי הברזל 6–19
⚠️ **מפה, לא תוכן.** חמשת הכללים שבפרק הזה (1–5) הם עמידות אחסון ואופליין;
ארבעה-עשר הכללים שאחריהם מפוזרים לאורך הקובץ, וכל אחד מהם יושב בפרק משלו.
המפתח כאן קיים כדי שסשן שנכנס לקובץ הזה בפעם הראשונה יידע **מה קיים**
ולאן ללכת, בלי לגלול שלושים פרקי סבבים.
⛔ **אין למספר מחדש** — המספרים הם **כתובות**, והפניות בקוד, בהערות ובקבצי
הבדיקה מצביעות עליהן. כלל חדש מקבל תמיד את המספר הבא בתור.

**מדרגת החומרה:** ⛔ = מוחלט, אין שיקול דעת · ⚠️ = מחייב, והיישום נתון
לשיקול דעת מנומק.

| נושא | כלל | חומרה |
|---|---|---|
| **בטיחות-מסד ונתונים** | **6** — שיטת הסנכרון בארגון: דחיפת-מצב + מיזוג ברמת רשומה, בלי תורים | ⛔ |
| | **9** — שדרוג אבטחתי מתפשט: נרשם בכל ארבעתן, ושקט אינו תשובה | ⛔ |
| | **10** — מודל המשתמשים וההרשאות (תשעה סעיפים, כולל ההרשאות במסד) | ⛔ |
| **אחידות בין הארבע** | **7** — רכיב משותף = קוד זהה + מיקום זהה + חיווט חי | ⛔ |
| | **12** — יכולת משותפת נמדדת בשלושה ממדים (ליבה · נקודת הפעלה · מטריצה) | ⛔ |
| | **14** — קיים באחת = קיים בארבעתן, או חריגה מנומקת במקום שהשער קורא | ⛔ |
| | **15** — פרק «פערים פתוחים» נאכף בשער: כל שורה נושאת תג מדיד/לא-מדיד | ⛔ |
| **תהליך העבודה** | **16** — הנחת השריד: אין מוחקים «שריד» לפני מדידה שאין לו קורא | ⛔ |
| | **17** — סריקת האזור שנגעו בו: באג שקט · מה היה אמור להיות משותף · תיעוד | ⛔ |
| **תהליך העבודה** | **13** — סבב שנוגע ב-SHARED נפתח מלכתחילה עם ארבעת הריפו | ⛔ |
| | **19** — משמעת קריאה: חיפוש ממוקד, קומיט אחרי כל סעיף, ⛔ בלי לקרוא קובץ שלם | ⛔ |
| **תיעוד** | **8** — פרוטוקול התיעוד: סימון SHARED, מבנה פרק הסבב, שורת «עודכן לאחרונה» | ⛔ |
| | **18** — תקציב תיעוד: חלון של שישה סבבים ותקרה של 3,000 שורות | ⛔ |
| | **11** — תקן ההערות בקוד: כותרת בלוק, שלושה סימנים, טרמינולוגיה | ⚠️ |

⚠️ **11 הוא היחיד ב-⚠️, ובכוונה:** הצורה נאכפת (`check-comments.mjs`),
אבל **הכיסוי** — מה נחשב «אזור לוגי» ומה ראוי להערה — הוא שיקול דעת.
⛔ הוא אינו יעד אחוזים, ואין להמציא הערות כדי להגיע למספר.

### המודול המשותף
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף`. **הוא זהה בית-לבית בארבעתן**
(474 שורות, sha1 `8345d842b294`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `LS_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתאם
`lsToast(msg, dur)` בן שורה.

**ה-API:** `lsSet` · `lsSetArray` · `lsGet` · `lsRemove` · `lsSetRaw` ·
`lsUsage` · `lsBoot` · `lsSweep`/`lsSweepGuarded` · `lsLog`/`lsLogRead` ·
`lsAlert` · `lsGuardToast` · `lsFails`/`lsOkSince` · `lsStatusHTML`/`lsRenderStatus` ·
`lsHorizon`/`lsClearHorizons`.

### סדר הפינוי — קפדני, ואין לשנות אותו
- **א. מטמונים וערכים שניתן לשחזר מהענן בכל רגע** (`LS_CFG.tier1`), הגדול קודם.
- **ב. ארכיון ורשומות ישנות שכבר מסונכרנות** (`LS_CFG.tier2`), **הישן ביותר
  קודם**, ורק מתחת לרצפת גיל פר-מפתח (`minAgeMs`).
- **ג. ⛔ נתון שטרם הסתנכרן לענן לא מפונה בשום מצב, גם אם המקום נגמר לגמרי.**
  במצב כזה עדיף להיכשל ברעש מאשר למחוק. שתי הגנות נפרדות אוכפות את זה:
  `LS_CFG.pending()` — יש משהו בתור? שלב ב מדולג **לגמרי**; ו-`syncedThrough()` —
  רק רשומות שגילן קודם לדחיפה/משיכה המוצלחת האחרונה נחשבות "בענן".
  ספק ⇐ לא מפנים.

**הפינוי שקט כלפי המשתמש אבל לא בלתי נראה** — כל פינוי, וכל **דילוג** על
פינוי, נרשם ביומן (`LS_CFG.logKey`, 12 רשומות אחרונות) ומוצג בהגדרות.

### אופק הפינוי — למה שלב ב לא חסר משמעות
פינוי מקומי בלבד היה חסר תועלת: מנוע המיזוג מחזיר כל רשומה מרוחקת שאין לה
מקבילה מקומית, ולכן הרשומות שפונו היו חוזרות לדיסק תוך שניות. לכן לכל מפתח
שמפונה נשמר **אופק** (`LS_CFG.hzPrefix + key`) — חותמת הרשומה החדשה ביותר
שפונתה — ו-`lsSetArray` מסננת מתחתיו **בכל כתיבה**.
**הזיכרון והענן לא נוגעים; רק העותק שעל הדיסק מצטמצם.** כשיש רשת המשתמש
רואה הכול כרגיל; אופליין הוא רואה את התקופה האחרונה בלבד, וההגדרות
(«מידע טכני ›») אומרות זאת במפורש. המנגנון שמנקה את האופק (`lsRestoreAll`)
קיים במודול אך אינו נגיש מה-UI מאז סבב 15; ניקוי אופק ייעשה בסבב ייעודי
אם יידרש.

### אימות פינוי מול הענן — ⛔ ארבעה כללים שאין לשנות (סבב 14)
עֵד הסנכרון המקומי נכתב **רק** אחרי דחיפה מוצלחת מהמכשיר, ולכן ב**דפדפן
שקורא ולא כותב** שלב ב מדולג לגמרי: הפינוי מחזיר 0 וההתרעה נשארת קבועה
בזמן שכל הנתונים כבר בענן (נמדד בפועל: 3.06MB / 61%). ⭐ לכן כשאין עֵד
מקומי — או שהוא 0 — שלב ב עובר ל**אימות ישיר מול הענן**: משיכת העותק
הענני של המפתח המועמד לפינוי, ומחיקת רשומה **רק** אם היא נמצאת שם לפי
מזהה **וגם** חותמת הענן אינה ישנה מהמקומית. ⛔ מחליפים את מקור הראיה,
ולא מרפים מהדרישה.
ה-spec של `LS_CFG.tier2` נושא לשם כך שני שדות — `idOf(rec)` (מזהה
הרשומה, אותו מפתח שבו משתמש מנוע המיזוג) ו-`verify()` (מחזירה `Promise`
של `{ok:true, rows:[...]}`), ו-`spec.ts` הקיימת היא שגוזרת את החותמות
משני הצדדים. **ה-API:** `lsGlobalWitness` · `lsSpecWitness` ·
`lsVerifySpecs`/`lsHasVerifiers` · `lsCloudIndex` · `lsPruneKeyVerified` ·
`lsSweepNeed` · `lsSweepVerified` · `lsScheduleDeferred`/`lsBootDeferred` ·
`lsBootAlert`.

1. **נכשל סגור.** אין רשת, `verify` זרקה, החזירה `ok:false`, או שהמפתח
   כלל אינו קיים בענן ⇒ **לא מפנים דבר**. היעדר ראיה אינו ראיה. ⛔ וההגנה
   משוכפלת בשלוש שכבות (סבב 14) — בדיקת התשובה, `lsCloudIndex` שמחזירה
   `null` על קלט שאינו מערך, ו-`lsPruneKeyVerified` שיוצאת על `idx` ריק.
2. **רצפות הגיל (`minAgeMs`) נשארות כפי שהיו** — הן שכבת בטיחות **נפרדת**
   מהראיה ולא תחליף לה: רשומה מאומתת בענן שעדיין בתוך הרצפה אינה מפונה.
3. **`LS_CFG.pending()` נבדק גם כאן**, בדיוק כמו בשלב ב הסינכרוני: יש
   משהו שטרם עלה ⇒ לא נוגעים ברשת ולא מפנים דבר.
4. ⛔⛔ **פינוי רצף בלבד — עוצרים ברשומה הראשונה שלא אומתה ואין מדלגים
   עליה (סבב 14) — דילוג היה מציב אופק מעליה**, וכתיבת המערך הבאה הייתה
   מוחקת אותה בשקט: דווקא את הרשומה היחידה שאין לה עותק בענן. (מאומת:
   מוטציה שהחליפה `break` ב-`continue` הפילה שלוש טענות, כולל האימות
   ש-`lsSetArray` מוחקת בפועל את היתומה.)

⚠️ **והאימות אסינכרוני — ולכן גם ההתרעה נדחית.** `lsBoot` סינכרונית ורצה
**לפני** שהמסך עולה: היא מבצעת את שלב א ואת שלב ב הסינכרוני, ואם עדיין
מעל הסף **ויש מפתחות שניתן לאמת** — היא מתזמנת `lsBootDeferred()` (3
שניות) **ודוחה איתו את ההתרעה**; הצגתה בעלייה הייתה אזהרה צהובה על מצב
שנפתר שתי שניות אחריה. `lsBootDeferred` מאמתת, מודדת מחדש, ורק אז קוראת
ל-`lsBootAlert` — ואם אין מה לפנות או אין רשת, ההתרעה כן מוצגת וזה הנכון.
`lsBoot` מחזירה `{usage, swept, deferred}` ומקבלת `opts` אופציונלי
(`noDefer`, `deferMs`) לבדיקות ולנתיבים שאסור לדחות בהם, ו-`lsSweepNow()`
מחזירה **Promise** שמריצה את שני המסלולים ומדווחת אחרי שניהם.

### מנגנון חירום
אם למרות הכול כתיבה נכשלת ב-`QuotaExceededError`, `lsSet` מריצה פינוי נוסף
**לפי אותו סדר בדיוק** ומנסה **פעם אחת** נוספת. הצליח — זו הצלחה לכל דבר
(המונה מוחזר וטוסט ההצלחה מותר). נכשל — באנר, טוסט ויומן. אין רקורסיה:
`_lsSweeping` חוסם פינוי בתוך פינוי.
<!-- SHARED:end -->

### היישום כאן (schar-limud)
- המודול המשותף במלואו; `sl_device_id` דרך `lsGet`+`lsSetRaw` (החריג
  המתועד). `lsBoot()` נקראת מתוך `slBoot()` — פונקציית העלייה (כלל
  ברזל 12).
- **`tier1` ו-`tier2` ריקים — ובכוונה**; ר' «מה מותר לפנות» ו«מראה-חלון»
  למטה. התרומה של המודול כאן היא הכתיבה המוגנת, הניטור וההתרעות — המכסה
  משותפת עם שלוש האחיות, ו-schar היא שתיפגע ממה שהן מילאו.

### היישום כאן — אימות הפינוי מול הענן (מסבב 14)
המודול המשותף עודכן במלואו (כלל «זהה בית-לבית»), אבל **`tier1` ו-`tier2`
נשארים ריקים ואין כאן אף `verify`** — וזו החלטה, לא השמטה:
- ⛔ `sl_mirror_transactions` הם **כספים**. אימות מול הענן מוכיח שהרשומה
  שם, אבל **אינו הופך רשומה כספית לניתנת לפינוי**: פינוי שלה מסתיר תנועה
  מהמסך אופליין, וחישוב חוב על נתון חסר גרוע מכל מכסה.
  `sl_mirror_students` הם אבות המפתח הזר שלהם ולכן גם הם בחוץ.
- ⛔ `sl_mirror_settings`/`sl_mirror_lists` נכתבות מקומית-תחילה מאז 009
  (ר' הפרק «מה מותר לפנות» בסבב 12 שלב 2). **אין להחזיר אותן ל-`tier1`.**
- **המשמעות המעשית, ובמפורש:** schar אינה מפנה מקום. אם הדומיין מלא, היא
  זו שתתריע בלי לפתור — הפתרון הוא yoman ו-hanhala, שכן מפנות, ולא צמצום
  העותק המקומי של הכסף. `lsBoot` כאן מחזירה `deferred:false` ומתריעה מיד,
  כפי שהיה.

---

## שלב א' לאופליין בטוח: `client_id` (`migrations/006`–`007`)
### הבעיה שהשלב הזה פותר
`sl_transactions.id` ו-`sl_students.id` הם `SERIAL`, כלומר **רשומה שנוצרה
במכשיר חסרת זהות עד שהיא מגיעה לשרת**. ⛔ תשובה שאבדה ברשת אחרי כתיבה
מוצלחת גררה ניסיון חוזר, והמסד הקצה `id` שני — **תשלום כפול, בכסף**.
`client_id` שנוצר במכשיר הוא מה שהופך את הניסיון החוזר לאידמפוטנטי.

### מה נעשה
- **`migrations/006_client_id_identity.sql`** (✅ הורצה; האינדקסים הוחלפו למלאים ב-007):
  עמודת `client_id TEXT` + אינדקס ייחודי, ב-`sl_transactions` וב-`sl_students`.
  **מותרת ב-NULL בכוונה** — 25 התנועות והתלמידים הקיימים נוצרו לפניה. ב-Postgres
  ערכי NULL נחשבים שונים זה מזה באינדקס ייחודי, ולכן ריבוי NULL-ים אינו מתנגש.
  `000_initial_schema.sql` עודכן גם הוא (כלל 7 — מקור אמת יחיד).
- **`newClientId()`** — uuid v4 בצד הלקוח. `crypto.randomUUID` כשקיים, ואחריו
  **שתי נפילות-חזרה**: `crypto.getRandomValues` ואז `Math.random`. הנפילה-חזרה
  אינה תיאורטית: `randomUUID` קיים רק ב-secure context ובדפדפנים מודרניים,
  וב-WebView ישן הוא פשוט חסר.
- **`upsert` במקום `insert`** ב-`saveTxn` וב-`saveNewStudent`, עם
  `{onConflict:'client_id'}`.
- **המזהה הראשי לא השתנה.** `id` נשאר `SERIAL` והוא עדיין המפתח שאליו מצביע
  `sl_transactions.student_id`. `client_id` הוא מפתח **זהות** מקביל לדה-דופליקציה,
  ולא מפתח קשרים.

### `pendingCid` — למה המזהה קשור לתוכן הטופס
מזהה שנוצר מחדש בכל לחיצה על "שמור" היה מחזיר בדיוק את הכפילות שהעמודה באה
למנוע. לכן `pendingCid(slot, fp)` מחזיק את המזהה של הרשומה **שנרשמת עכשיו**,
מקושר ל-`fp` — טביעת אצבע של תוכן הטופס — ומשוחרר ב-`releaseCid` אחרי שמירה
מוצלחת. שני הכיוונים חשובים, **ושניהם על כסף**:
- **אותו תוכן אחרי כשל = ניסיון חוזר** ⇐ אותו `client_id` ⇐ שורה אחת.
- **תוכן שהשתנה = תשלום אחר** ⇐ מזהה חדש ⇐ שורה חדשה. בלי זה, תשלום שני שנרשם
  אחרי כשל היה **דורס** את הראשון במקום להתווסף אליו — אובדן תשלום, לא כפילות.

בסבב האופליין המלא **התור היוצא יחזיק את המזהה פר-רשומה**, והלוגיקה הזו תהיה
עודפת. עד אז היא מה שהופך את ה-`upsert` למשמעותי כבר היום.

### ⛔ האינדקס חייב להיות מלא ולא חלקי (`migrations/007`)
006 הורצה במסד בגרסה עם אינדקסים **חלקיים** — `... (client_id) WHERE client_id
IS NOT NULL`, בשם `_uidx`. הכוונה סבירה (לא לאנדקס 88 רשומות היסטוריות), אבל
**היא שוברת את ה-UPSERT לגמרי**, ולכן שמירת תשלום ותלמיד חדש מפסיקה לעבוד.

הסיבה: Postgres מסיק את אינדקס-הבורר של `ON CONFLICT` מרשימת העמודות, ועבור
אינדקס **חלקי** ההסקה מצליחה **רק אם המשפט חוזר על התנאי של האינדקס**.
`.upsert(row,{onConflict:'client_id'})` פולט `ON CONFLICT (client_id)` בלבד —
ל-PostgREST אין שום דרך להוסיף predicate. אומת חי מול המסד ב-`EXPLAIN`:

| משפט | תוצאה |
|---|---|
| `ON CONFLICT (client_id) WHERE client_id IS NOT NULL` | ✅ מתכנן, בורר `_uidx` |
| `ON CONFLICT (client_id)` — מה שהאפליקציה שולחת | ❌ `42P10` |

**אינדקס מלא אינו ויתור:** `client_id` מותרת ב-NULL, וב-Postgres ערכי NULL
נחשבים שונים זה מזה באינדקס ייחודי רגיל — כלומר אינדקס מלא מתיר את הרשומות
ההיסטוריות בדיוק כמו החלקי. **מה שנשבר בחלקי הוא ההסקה, לא הייחודיות.**
007 יוצרת את המלא (`_key`) ואז מוחקת את החלקי — בסדר הזה, כדי שלא יהיה רגע בלי
הגנת ייחודיות. `000` ו-`006` עודכנו גם הם (כלל 7). **אין ליצור אינדקס חלקי על
`client_id` בשום מיגרציה עתידית.**

### מה **לא** קיבל `client_id`, ולמה
`sl_lists` בלבד (`addListItem`, `ensureCreditMethod`) — ערכי רשימה, לא כסף,
ואין להם מסלול אופליין מתוכנן. `deleteStudent`, `deleteTxn` ועדכוני הגדרות הם
`update` על `id` קיים ולכן אידמפוטנטיים ממילא.

---

<!-- SHARED:start id="pending-module" -->
## ⏳ סימון "ממתין לסנכרון" — מודול משותף (סבב 12, שלב 1)
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md.**

### הבעיה
אחרי שמירה מקומית המסך נראה **בדיוק** כמו אחרי שמירה בענן. המשתמש רושם תשלום
ברכבת התחתית, רואה אותו ברשימה, וסוגר את האפליקציה בהנחה שהוא שמור — בזמן שהוא
יושב רק על המכשיר הזה. הסבב הזה הופך את ההבדל לגלוי.

### שלוש רמות תצוגה, כולן ממפה אחת
- **תגית על הרשומה עצמה** — `pendTag(key)`, נכנסת לשורה ברשימה (⏳ ממתין, ובאיחור ⚠️).
- **מונה** — `#pend-bar`, סרגל תחתון קבוע, **מוצג רק כשיש** משהו ממתין.
- **התרעה** — משהו ממתין מעל 24 שעות ⇐ `#pend-alert`, באנר אדום עליון עם כפתור סגירה
  (נסגר ל-6 שעות; **הסימון עצמו לא נמחק בסגירה**).

### ⚠️ ההגדרה של "ממתין" היא ראייתית, לא אופטימית
רשומה מסומנת ברגע שנכתבה במכשיר, ויורדת מהסימון **רק כשיש ראיה שהענן קיבל אותה**.
שלושת מסלולי האישור, לפי אופי האפליקציה:

| מסלול | מי משתמש בו | הראיה |
|---|---|---|
| `pendClear` | schar-limud | תשובת הצלחה מהשרת, או משיכה שמחזירה את ה-`client_id` |
| `pendConfirmPush(prefix, t0)` | hanhala, yoman | דחיפת-מצב מלא שהצליחה |
| `pendSyncOutbox()` | gius | הפעולה ירדה מהתור היוצא |

- **כשל רשת אינו ראיה לכלום ⇒ הסימון נשאר.** תשובה שאבדה ברשת היא בדיוק המקרה שבו
  הרשומה אולי כן נכנסה ואולי לא. ספק ⇐ מסומן.
- **כשל סמכותי כן מוריד את הסימון** (`pendFailed`) — ולידציה/הרשאה/סכימה אומרות
  שהשורה **לא** נכתבה, וסימון "ממתין" עליה מבטיח שליחה חוזרת שלא תקרה לעולם.
- **⚠️ `t0` נלקח לפני שצילום המצב נקרא, לא אחרי שהדחיפה הסתיימה.** רשומה שנכתבה
  באמצע הדחיפה אינה בתוכה, והורדת הסימון ממנה הייתה מאבדת אותה בשקט.
- **חותמת קיימת אינה נדרסת** בסימון חוזר — אחרת עריכה חוזרת של אותה רשומה הייתה
  מאפסת את שעון ה-24 שעות לנצח.

### המודול
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"`. **זהה בית-לבית בארבעתן**
(267 שורות, sha1 `2b3d6b779b1f`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `PEND_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתחתיו בלוק
"חיבור <אפליקציה> למודול" עם הקידומות והעוזרים המקומיים.

**ה-API:** `pendMark`/`pendMarkMany` · `pendClear`/`pendClearMany` · `pendFailed` ·
`pendConfirmPush` · `pendHas`/`pendSince`/`pendCount`/`pendOldest`/`pendLateCount` ·
`pendTag` · `pendRender`/`pendCounterText` · `pendStatusHTML` · `pendBoot`/`pendReload` ·
`pendReset`.

**כתיבה:** דרך `lsSet` בלבד (כלל ברזל 1), למפתח יחיד פר-אפליקציה
(`sl_pending` / `ys_pending` / `tb_pending<מוסד>` / `g_pending`).
**תקרה:** 800 סימונים; בגלישה נגרעים הישנים ביותר **ונרשמים ליומן** — סימון שנעלם
בלי אישור הוא בדיוק מה שהמודול בא למנוע, ולכן הוא לא נעלם בשקט.

### ⏳ השהיית הציור — `PEND_DRAW_DELAY_MS` = 2000 מ״ש (סבב 16)
עד סבב 16 כתיבה מקומית ציירה ⏳ מיידית והדחיפה המוצלחת מחקה אותו —
באונליין תקין זה היה הבהוב של שבריר שנייה על כל שמירה. מעכשיו הציור
מאחר: הגיע אישור הענן לפני — ה-⏳ **לא מופיע כלל**; לא הגיע (אופליין,
רשת איטית או חולה) — הוא מופיע וממשיך להתנהג בדיוק כמו קודם.
⚠️ **זו השהיית *ציור* בלבד — ארבעה גבולות קשיחים:**
1. **הסימון הלוגי נכתב ל-localStorage מיידית וללא שינוי** (`pendMark`
   ומקביליו) — הוא חלק ממנגנון הבטיחות: הפינוי, בחירת מה לדחוף
   והמיגרציות נשענים עליו. ההחזקה (`_pendDrawHold`) חיה **בזיכרון בלבד**
   ואינה נשמרת.
2. **מכשיר שנסגר בתוך שתי השניות** — בפתיחה הבאה ה-⏳ מוצג מיד: ההשהיה
   חלה רק על סימון שנוצר הרגע בכתיבה חדשה, לא על סימון שנטען מהדיסק.
3. **מסך «⏳ ממתין לסנכרון» והמונים הלוגיים** (`pendCount`/`pendHas`/
   `pendSince`/`pendStatusHTML`) מציגים תמיד את האמת הלוגית המלאה, בלי
   השהיה. גם סרגל `#pend-bar`, כשהוא מוצג, מציג את המונה הלוגי המלא —
   ⛔ ההשהיה קובעת רק **אם** מציירים, לא **מה** המספר (סבב 16).
4. **מימוש אחיד בארבעתן** — `pendHoldDraw` · `pendDrawSince` ·
   `pendDrawCount` · `pendDrawFlush` · `pendScheduleFlush`, ואותו קבוע.
   ההתאמה הפר-אפליקציתית היא שדה **`PEND_CFG.redraw`** בלבד: בתום
   ההחזקה, אם הסימון עדיין קיים, `pendDrawFlush` מרנדר מחדש את הרשימות
   שמציירות `pendTag`, כדי שהתגית תיכנס לשורות שכבר רונדרו בלעדיה.
   אושר בינתיים ⇒ אין redraw ואין רינדור מיותר.

### `PEND_CFG`
- **schar-limud:** `sl_pending`.
- **hanhala-ruchanit:** `ys_pending`; `extra()` מוסיף למונה את מספר המפתחות
  ב-`ys_offline_queue` — התור הישן הוא **פר-מפתח kv** ולא פר-רשומה, ובלי זה
  המשתמש היה רואה "0 ממתינות" בזמן שארבעה מפתחות תקועים.
- **yoman-avoda:** המפתח הוא **פונקציה** — `tb_pending` + סיומת המוסד. שני המוסדות
  חולקים localStorage, וסימון של ראשון לציון אינו תקף לרמת אביב. `selectYeshiva`
  קוראת ל-`pendReload()`.
- **gius:** `g_pending`; `extra()` מציג פעולות שנכשלו כשל תוכן חוזר (`dead`).

### מה סומן בכל אפליקציה
| | רשומות מסומנות | קידומות |
|---|---|---|
| schar-limud | תשלום, תלמיד, ומחיקה של כל אחד מהם | `txn:` `student:` `txn-del:` `student-del:` |
| hanhala | סדר נוכחות, סדר שינה, טיפולים, תלמידים | `at-sess:` `sl-sess:` `at-treat:` `sl-treat:` `student:` |
| yoman | רשומת יומן, סנאפשוט ארכיון | `entry:` `arc:` |
| gius | כל שבע הטבלאות, לפי `<table>:<id>` | `g_donors:` `g_txns:` … |

**מסך "⏳ ממתין לסנכרון" נוסף להגדרות בארבעתן** (`pendStatusHTML`) — מה ממתין,
מאיזה גיל, ומצב הרשת.

<!-- SHARED:end -->

### היישום כאן (schar-limud)
המודול נוסף במלואו; `pendBoot()` ב-`DOMContentLoaded` לצד `lsBoot()`; `saveTxn`,
`saveNewStudent`, `deleteTxn` ו-`deleteStudent` מסמנות לפני השליחה ומורידות לפי
תוצאה; `syncAll` מריצה `pendSyncScan` על המערכים ה**גולמיים** (לפני סינון
ה-`deleted`) — אישור מחיקה הוא ראיית `deleted=true`, והיא נעלמת מיד אחרי הסינון.
תגית ⏳ ביומן התשלומים וברשימת התלמידים שבהגדרות.

**בנוסף — תיקון שקדם לסבב:** ב-`index.html` היה **בייט NUL** בקוד
(`[sid,date,amount,method,note].join('\0')` במקום `' '`). פונקציונלית הוא עבד
כמפריד, אבל git סיווג את `index.html` כקובץ **בינארי** — כלומר `git diff` הציג
"Binary files differ" ולא את השינויים. הוחלף ברווח רגיל; אין שינוי התנהגות
(טביעת האצבע היא בזיכרון בלבד ואינה נשמרת בין רענונים).

### היישום כאן — השהיית ציור ה-⏳ (מסבב 16)
`PEND_CFG.redraw` מרנדר מחדש את שלושת מציירי התגיות: `renderTxnLog` (יומן
התשלומים), **`renderScAnnual`** (רשימת התנועות שבכרטיס התלמיד — בכוונה לא
`renderStudentCard`, שמאפס גם את שדות העריכה של הכרטיס ולכן היה מוחק
הקלדה באמצע), ו-`renderSettingsLists` (תלמידים ורשימות בהגדרות).

---

## ⭐ עבודה אופליין מלאה (סבב 12 שלב 2, `migrations/008`)
**הפער האחרון מכלל ברזל 3 נסגר.** schar-limud קראה עד כה ישירות מ-Supabase
בכל `syncAll` ולא החזיקה עותק מקומי: בלי רשת לא היה מה להציג ולא היה לאן
לכתוב. עכשיו יש כאן את ארבעת הרכיבים, **בדפוס דחיפת-המצב של hanhala-ruchanit
ולא בדפוס התור של gius**.

### `migrations/008` — `updated_at` + טריגר
בלי חותמת עדכון אין מנוע מיזוג. העמודה קיימת בשתי טבלאות הליבה עם
`DEFAULT NOW()`, ושני טריגרים (`sl_transactions_touch`, `sl_students_touch`)
מעדכנים אותה בכל UPDATE. `000_initial_schema.sql` מחזיק אותה גם הוא (כלל 7).

### מה נבנה
- **`MIRROR`** — עותק מקומי של ארבע הטבלאות ב-`sl_mirror_*`, **כולל
  tombstones**. בלעדיהם מחיקה שנעשתה במכשיר אחר לא הייתה מגיעה לכאן לעולם.
- **`slApplyMirror()`** — נקודת הסינון היחידה של ה-soft-delete (כללים 5–6),
  בדיוק במקום שבו `syncAll` סיננה קודם. כל 11 הקוראים של `STUDENTS` מקבלים
  אותה אוטומטית.
- **קריאה מהעותק בעלייה** — `DOMContentLoaded` טוען את המראה **לפני** כל
  נגיעה ברשת, ו-`enterApp` מרנדר ממנה מיד. **אין יותר `await syncAll()`
  חוסם**: בלי רשת הוא תקע את המסך, ועם רשת איטית השאיר ספינר במקום נתונים
  שכבר על הדיסק.
- **כתיבה מקומית-תחילה** — `saveTxn`/`saveNewStudent`/`deleteTxn`/
  `deleteStudent`/`toggleStudentActive`/`saveStudentSettings` כותבות ל-`MIRROR`
  ומחזירות מיד. **`guardOnline` הוסר מהן** — הוא היה מה שחסם רישום תשלום
  בלי רשת. כשל כתיבה מקומית עוצר ברעש (כלל ברזל 2): בכסף אסור להציג "נשמר"
  על משהו שלא נכתב.
- **דחיפת מצב** — `syncAll` = משיכה → מיזוג → החלה → דחיפת מה שמקומי-וחדש.
  **דוחפים רק לקטגוריה שנמשכה בהצלחה**: בלי לדעת מה בענן, דחיפה עיוורת
  הייתה מחזירה לחיים שורה שנמחקה במכשיר אחר.

### ⚠️ שני מסלולי כתיבה — וזו הגנה על כסף
- **יש `id`** ⇒ `update ... eq(id)`. כולל את 25 התנועות ו-63 התלמידים
  ההיסטוריים, שה-`client_id` שלהם ריק — `upsert` על `client_id` היה יוצר
  להם **שורה כפולה**.
- **אין `id`** (נוצרה במכשיר) ⇒ `upsert` על `client_id`. אידמפוטנטי, ולכן
  ניסיון חוזר אחרי תשובה שאבדה ברשת מעדכן ולא מכפיל תשלום.
- מפתח המיזוג בהתאם: **`c:<client_id>` כשקיים, `i:<id>` אחרת**.

### ⚠️⚠️ הכלל שמונע אובדן עריכה — רשומה מסומנת ⏳ מנצחת במיזוג
**הטריגר בצד השרת דורס `updated_at` ב-`NOW()` בכל UPDATE.** המשמעות: ברגע
שמכשיר א' דוחף את העריכה שלו, השורה בענן מקבלת חותמת **מאוחרת יותר** מעריכה
חדשה יותר שיושבת אצל מכשיר ב' וטרם עלתה. בלי טיפול, מכשיר ב' היה מאמץ את
גרסת א' והעריכה שלו הייתה **נעלמת בשקט**. (הבדיקה חשפה את זה בפועל.)

לכן, בשני מקומות:
- **`slMerge(local, remote, isPending)`** — רשומה מקומית מסומנת ⏳ מנצחת ללא
  תלות בחותמת.
- **`slDirtyRows(..., isPending)`** — רשומה מסומנת נחשבת תמיד "לדחיפה".
  בלי הסעיף הזה היא הייתה נשארת מסומנת לנצח ולעולם לא נדחפת.

**המחיר המודע:** מכשיר שהיה מנותק זמן רב ידרוס בעלייתו עריכה מרוחקת חדשה
יותר. זו העדפה מכוונת — הרשומה מסומנת ⏳ כל אותו זמן ומתריעה אחרי 24 שעות,
בעוד שעריכה שנמחקת בשקט אינה משאירה עקבות. אותה עדיפות בדיוק כמו "היעדר
רשומה אינו מחיקה". **אין להסיר את `isPending` משני המקומות.**

### `updated_at` נשלח מהמכשיר
`slLocalWrite` חותמת `updated_at` ברגע העריכה. ב-**INSERT** הטריגר אינו יורה,
ולכן רשומה שנוצרה אופליין נושאת את חותמת **היצירה** ולא את חותמת ההגעה
לשרת. ב-UPDATE הטריגר כן דורס — וזה מטופל בכלל שמעל.

### כניסה אופליין — מול הטביעה, ⛔ ולא מול סשן שמור
⛔ **`sl_session` הוסר בסבב 53** — המשתמש המחובר חי בזיכרון בלבד (ר' הפרק
«מודל הסשן»), ⛔ ואין להחזיר שמירה כזו. הכניסה מוכרעת מול `pass_fp` שבמראת
המשתמשים (ר' «כניסה ומשתמשים»), ולכן היא עובדת בלי רשת בדיוק כמו קודם —
ההבדל היחיד הוא שגם רענון דף מחזיר למסך הכניסה.
⛔ **`slRevalidateSession` ירדה איתו (סבב 53)** — היא אימתה סשן משוחזר
מול הענן, ואין עוד סשן משוחזר לאמת.


### `LS_CFG` — מה מותר לפנות
**`tier1` ו-`tier2` ריקים שניהם — ובכוונה.**
⛔ `sl_mirror_transactions` הם **כספים**: רשומה שפונתה אינה נראית אופליין,
וחישוב חוב על נתון חסר גרוע מכל מכסה. `sl_mirror_students` הם אבות המפתח
הזר שלהן ולכן גם הם בחוץ.
⛔ `sl_mirror_settings`/`sl_mirror_lists` **ישבו ב-tier1 עד 009 והוצאו משם**:
ההצדקה הייתה שהן נטענות מהענן בכל סנכרון ולעולם אינן נכתבות מקומית, וזה כבר
לא נכון — מ-009 הן נכתבות מקומית-תחילה. `tier1` נסרק **בלי** מחסום
`pending()`, ולכן פינוי שלהן היה מוחק בשקט הגדרה או פריט רשימה שנרשמו
אופליין. **אין להחזיר אותן.**
`pending()` מחזירה `pendCount() > 0` — יש משהו שטרם עלה ⇒ שלב ב מדולג לגמרי.



---

<!-- SHARED:start id="iron-rule-6-sync" -->
## ⭐ כלל ברזל 6 — שיטת הסנכרון בארגון (סבב 12 שלב 4, אוגוסט 2026)
**הפרק הזה, עד שורת «כאן:» שבסופו, זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את חמשת כללי
הברזל שלמעלה.

> **שיטת הסנכרון בארגון היא דחיפת מצב מלא + מיזוג ברמת רשומה. אין תורים.
> כל רשומה שטרם אושרה בענן מסומנת למשתמש.**

עד הסבב הזה חיו כאן שלוש שיטות שונות: hanhala ו-yoman דחפו מצב מלא, gius
החזיקה תור יוצא פר-פעולה, ו-schar-limud לא עבדה אופליין כלל. מעכשיו — אחת.

### למה דווקא דחיפת מצב
- **אין פריט שיכול "להיתקע" לבדו.** בתור FIFO, פעולה שנכשלה כשל תוכן חוסמת
  את כל מה שאחריה, או מסומנת `dead` ונשארת תלויה לנצח. בדחיפת-מצב כל רשומה
  עומדת בפני עצמה, וכל סנכרון מתקן את מה שחסר — כולל מה שנכשל בפעם הקודמת.
- **אין מצב ביניים.** אין תור שיכול להתפגם, להתמלא או לצאת מסנכרון מול
  הנתונים עצמם. מצב האמת הוא הרשומות, ותו לא.
- **התאוששות אוטומטית.** מכשיר שאיבד את הסימונים עדיין מסתנכרן נכון:
  ההשוואה היא בין העותק המקומי לענן, לא בין יומן פעולות למציאות.

### חמשת הכללים
1. **עותק מקומי מלא של כל הנתונים, כולל tombstones.** רשומה מחוקה **חייבת**
   להישמר עם `deleted=true` + חותמת. היעדר רשומה אצל צד אחד **אינו** מחיקה.
2. **קריאה מהעותק בעליית האפליקציה, לפני כל נגיעה ברשת.**
3. **כתיבה מקומית-תחילה.** הכתיבה נשמרת במכשיר ומוצגת מיד; הרשת היא עניין
   של הרקע. כשל כתיבה מקומית עוצר ברעש ולא מציג "נשמר".
4. **סנכרון = משיכה ← מיזוג ← דחיפת מה שמקומי-וחדש-יותר.** בסדר הזה: אי
   אפשר לדעת מה לדחוף בלי לראות קודם מה בענן, ודחיפה עיוורת מחזירה לחיים
   רשומות שנמחקו במכשיר אחר. **דוחפים רק לקטגוריה שנמשכה בהצלחה.**
5. **מיזוג ברמת רשומה לפי חותמת עדכון**, עם שובר-שוויון דטרמיניסטי (הענן
   מנצח בשוויון), כך ששני מכשירים שממזגים את אותם צדדים מגיעים לאותה תוצאה.

### ⚠️ הכלל שמונע אובדן עריכה — רשומה מסומנת ⏳ מנצחת במיזוג
בשלוש מהאפליקציות החותמת נקבעת **בצד השרת** בטריגר `BEFORE UPDATE`. המשמעות:
ברגע שמכשיר א' דוחף, השורה בענן מקבלת חותמת **מאוחרת יותר** מעריכה חדשה יותר
שיושבת אצל מכשיר ב' וטרם עלתה — ומכשיר ב' היה מאמץ את גרסת א', כלומר **מוחק
את העריכה שלו בשקט**. זה לא תרחיש תיאורטי; הוא נתפס בבדיקה.

לכן, בשני מקומות ולא באחד:
- **במיזוג** — רשומה מקומית מסומנת ⏳ מנצחת ללא תלות בחותמת.
- **בבחירת מה לדחוף** — רשומה מסומנת נחשבת תמיד "לדחיפה". בלי זה היא הייתה
  נשארת מסומנת לנצח ולעולם לא נדחפת.

**המחיר המודע:** מכשיר שהיה מנותק זמן רב ידרוס בעלייתו עריכה מרוחקת חדשה
יותר. זו העדפה מכוונת — הרשומה מסומנת ⏳ כל אותו זמן ומתריעה אחרי 24 שעות,
בעוד שעריכה שנמחקת בשקט אינה משאירה עקבות. אותה עדיפות כמו "היעדר רשומה
אינו מחיקה". **אין להסיר את הבדיקה הזו משני המקומות.**

### ⛔ אין תורים
`g_outbox` הוסר (gius, שלב 3) ולא ייבנה מחדש. `ys_offline_queue` בהנהלה נשאר
בינתיים כמנגנון פר-מפתח kv **בלבד** ואינו תור פעולות; אין להרחיב אותו.
**אין ליצור תור פעולות חדש בשום אפליקציה בארגון.**

### ⛔ מה לא נדחף לעולם
רשומה שהעותק המקומי שלה **חלקי** אינה ניתנת לדחיפת-מצב: הדחיפה הייתה
כותבת את מה שחסר כערך ריק. המקרה השכיח הוא **סיסמה**: היא מוסרת
מהמראה כדי שלא תשב בטקסט גלוי על הדיסק, ולכן אין מה לדחוף.
מקרה אחד קיים היום, מתועד במקומו — `g_users` ב-gius
(`strip: ['password']`). **שינוי סיסמה דורש רשת.** המקרה השני,
`sl_settings.admin_pass` ב-schar-limud, נסגר בסבב 35: השורה נמחקה
מהמסד ומהגיבויים ע"י המנהל, הרשימה `SL_NEVER_MIRROR_SETTINGS` רוקנה —
ומנגנון הסינון נשאר דרוך לסוד עתידי.

⚠️ **חריגה כזו היא ברמת השורה ולא ברמת הטבלה.** כל שאר השורות של
`sl_settings` נדחפות כרגיל (מ-`migrations/009`). וכשהסוד מוסר מהמראה —
**אסור ששער הכניסה ייפול חזרה לברירת המחדל.** בדיקה כמו
`p !== (SETTINGS['admin_pass'] || 'admin')` הופכת בדיוק ברגע ההסרה
לשער שנפתח לכל מי שמקליד «`admin`». ההשוואה חייבת להיות מול טביעה
נגזרת (PBKDF2) או מול הענן — **ולהיכשל סגור כשאין אף אחת מהן.**

### מזהים
כל רשומה חדשה מקבלת **מזהה שנוצר במכשיר** לפני שראתה שרת — uuid v4.
בטבלאות עם `SERIAL` (schar-limud) זהו `client_id` לצד ה-`id` של המסד, והכתיבה
היא `upsert` עליו. זה מה שהופך ניסיון חוזר אחרי תשובה שאבדה ברשת
לאידמפוטנטי, במקום ליצור שורה שנייה — בכסף, תשלום כפול.
<!-- SHARED:end -->

**כאן:** `MIRROR`/`slMerge`/`slDirtyRows`/`slPushTable`, מפתח מיזוג
`c:<client_id>` או `i:<id>`, ושני מסלולי כתיבה (`update` לפי `id` לרשומות
היסטוריות, `upsert` על `client_id` לרשומות שנוצרו במכשיר).

---

## ההגדרות והרשימות בשכבת האופליין (`migrations/009`)
`sl_settings` ו-`sl_lists` נכתבות מקומית-תחילה וממוזגות כמו טבלאות הליבה.
מה ש-009 נתנה: `updated_at` + טריגר (דורס ב-UPDATE בלבד; ב-INSERT חותמת
המכשיר נשמרת) · `client_id` + אינדקס ייחודי **מלא** · tombstones
ל-`sl_lists` (⚠️ `sl_settings` בלי tombstones בכוונה — זוגות מפתח/ערך
שאין להם נתיב מחיקה).
- ⛔ **אינדקס `client_id` מלא ולא חלקי (סבבים 12–13)** — אינדקס חלקי שובר
  את הסקת `ON CONFLICT` של PostgREST (`42P10`) ושמירת תשלום מפסיקה
  לעבוד. אין ליצור חלקי בשום מיגרציה עתידית.
- ⚠️ **מפתח המיזוג של `sl_settings` הוא `key`, לא `client_id`**
  (`slKeyOf` — `k:<key>`), וכך גם ה-`upsert` (`onConflict:'key'`): המפתח
  הוא טקסט ששני מכשירים מייצרים עצמאית, ומיפתוח לפי `client_id` היה יוצר
  שתי שורות לאותה הגדרה.
- ⛔ **סוד ב-`sl_settings` לעולם אינו יורד לדיסק** — הרשימה
  `SL_NEVER_MIRROR_SETTINGS` נאכפת בשלוש נקודות (סבב 24): משיכה
  (`slStripSecrets`), כתיבה מקומית (`slLocalWrite` מסרבת), ושער הדיסק
  (`slSanitizeRows` ב-`slMirrorSave`). מסבב 35 הרשימה **ריקה** — שורת
  `admin_pass` נמחקה מהמסד ומהגיבויים ע"י המנהל (17.8, אומת: אפס
  מופעים) — והמנגנון נשאר דרוך לסוד עתידי.
- **מחיקות עוברות לפי מפתח המיזוג** (`slKey`) ולא לפי `id` גולמי
  (סבב 13) — `LISTS` מחזיקה `key` לצד `id`, ואתרי ה-`onclick` מעבירים
  `esc(slKey(...))`.

---


## 🪟 מדיניות «מראה-חלון» לטבלאות התנועות — עיגון להמשך (תיעוד בלבד)
**הפרק הזה אינו מתאר קוד שנכתב.** הוא מעגן החלטה, כדי שסשן עתידי שיתבקש
«לחסוך מקום» יידע בדיוק מה מותר לו לעשות — ובשכר לימוד התשובה היא **כלום**.

### המצב היום — ואין לשנות אותו נקודתית
`sl_mirror_students` ו-`sl_mirror_transactions` **אינן מפונות לעולם**
במתכונת הנוכחית. הנימוקים כתובים בקוד עצמו, ליד `LS_CFG`, ולא הומצאו כאן:
- **תנועות הן כספים.** רשומה שפונתה מהדיסק אינה נראית אופליין, וחישוב חוב
  על נתון חסר גרוע מכל מכסה.
- **תלמידים הם אבות המפתח הזר של התנועות.** פינוי שלהם משאיר תנועות בלי
  שם מוצג.
- אימות מול הענן (סבב 14) **אינו משנה את זה**: הוא מוכיח שהרשומה בענן,
  ולא שמותר להסתיר אותה מהמסך.

### 🚫 הכלל המבצעי לסשן עתידי
**אף סשן אינו מוסיף פינוי לשתי הטבלאות האלה — גם לא פינוי «בטוח לכאורה».**
זה כולל: `tier1`/`tier2` חדשים, `idOf`/`verify` עליהן, אופק, «רק תנועות
של לפני שנתיים», «רק תלמידים לא-פעילים» וכל וריאציה אחרת. הבקשה «חסוך
מקום כאן» היא **בקשה לפתוח את סבב מראה-החלון** שלמטה, לא לאלתר טלאי.
פינוי חלקי מסוכן יותר מאי-פינוי: הוא מסתיר כסף בלי שאף שכבה יודעת על כך.

### התוכנית העתידית — דפוס «מראה-חלון» פר-שנת-לימודים
כשטבלאות התנועות יגדלו, הכיוון **אינו** פינוי לפי גיל אלא **חלון מוגדר**:
- **במכשיר:** כל הישויות החיות (תלמידים, הגדרות, רשימות) + **תנועות שנת
  הלימודים הנוכחית בלבד**.
- **בענן:** שנים סגורות במלואן. הן נשלפות לפי דרישה כשמסתכלים עליהן,
  ולא יושבות על הדיסק.
- **טקס סגירת-שנה:** מעבר מפורש ומתועד שמעביר שנה מ«נוכחית» ל«סגורה» —
  לא גלישה שקטה לפי תאריך.

### ⛔ זה סבב תכנון שלם — אסור לממש חלקית
החלון אינו שינוי ב-`LS_CFG` בלבד. **הוא חייב לכלול באותו סבב גם את
התאמת הדחיפה והמשיכה לגבולות החלון**, אחרת נוצר בדיוק המצב שכל שכבת
הסנכרון בנויה למנוע:
- **דחיפה** שאינה מודעת לחלון תקרא «אין לי את התנועה הזו» כ«היא נמחקה»
  ותמחק בענן שנה שלמה של כסף.
- **משיכה** שאינה מודעת לחלון תחזיר את השנים הסגורות לדיסק בסנכרון הבא,
  והחלון יהיה חסר משמעות — בדיוק הלקח של «אופק הפינוי» מסבב 11.
ולכן: או שהסבב מבוצע במלואו — מדיניות + דחיפה + משיכה + טקס הסגירה +
בדיקות — או שלא נוגעים.

### מתי לפתוח אותו
**הטריגר הוא המוקדם מבין השניים:**
1. **סוף שנת הלימודים הראשונה** שנצברה כאן במלואה, או
2. **חציית כמה מאות KB** בטבלאות התנועות המקומיות (נמדד בהגדרות ←
   «מידע טכני», פילוח לפי מפתח).

עד שאחד מהם מתקיים — הנתונים נשארים במלואם על המכשיר, וזו ההתנהגות הנכונה.

---

## אייקונים — הסט הירוק (סבב תאומים)
- **המקור לגזירות כאן הוא `icons/master-green-1024.png`** — המאסטר של
  הארגון (בריפו hanhala) שנצבע ירוק `#307535` באלפא-דיו; אין צורך לחזור
  לריפו ההוא בשביל אייקון חדש. `theme_color` = `#307535`.
- ⛔ **ב-manifest האייקונים המלאים הם `purpose:"any"` בלבד וה-maskable
  נפרד** (סבב תאומים) — «any maskable» על אייקון מלא נחתך בעיגול הלאנצ'ר.
- mipmap במעטפת `android/` — ירוק, launcher מלא על לבן + foreground על
  שקוף (לוגו ב-66%).

---



<!-- SHARED:start id="iron-rule-7-status-area" -->
## ⭐ כלל ברזל 7 — רכיב משותף = קוד זהה + מיקום זהה + חיווט חי (סבב 17, אוגוסט 2026)
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את ששת כללי
הברזל שלמעלה.

> **רכיב שמוכרז «משותף» חייב שלושה דברים, לא אחד: אותו קוד, באותו מקום
> במסך, ומחובר לקוד חי. תיעוד לבדו אינו אוכף אף אחד מהם — ולכן יש בדיקה
> אוטומטית שרצה עם שערי התחביר לפני כל דחיפה.**

### הבעיה שנמדדה
סבב 15 קיפל את מסכי האחסון לרכיב «מידע טכני ›» בארבע האפליקציות, והתיעוד
הכריז שהבלוק זהה בית-לבית. הבלוק אכן היה זהה — **וכל השאר לא**:

| | מסך «⏳ ממתין לסנכרון» | «מידע טכני ›» |
|---|---|---|
| schar-limud | **ראש** מסך ההגדרות | סוף המסך ✅ |
| yoman-avoda | **אמצע** מסך ההגדרות | סוף המסך ✅ |
| hanhala | **אמצע** מודול המערכת | **אמצע**, לפני ההרשאות |
| gius | כרטיס «☁️ מצב סנכרון» נפרד | נבנה בכל render() — **`techInfoMount` הייתה קוד מת** |

כלומר: קוד זהה, שלוש חוויות שונות, ובאחת מהן הרכיב לא היה מחובר בכלל.
בגיוס הייתה לזה גם תוצאה גלויה — מסך ההגדרות נבנה מחדש בכל `render()`,
העוגן חזר ריק, ו«מידע טכני» **נסגר בכל שמירה**.

### ההגדרה המחייבת — «אזור מצב»
בתחתית מסך (או מודול) ההגדרות של כל אפליקציה בארגון יושבים **תמיד, ובסדר
הזה, שני האלמנטים האחרונים** — חשופים, לא עטופים בכרטיס של האפליקציה:

1. **`<div id="sync-status-box">` — בלוק «☁️ סנכרון».** מציג את מסך ה-⏳
   הקיים (`pendStatusHTML`, **בלי שינוי בתוכנו**) ומתחתיו שורת מצב תמציתית:
   מצב חיבור, מועד הסנכרון המוצלח האחרון, וכפתור «🔄 סנכרן עכשיו».
2. **`<div id="tech-info-box">` — רכיב «מידע טכני ›».** האלמנט האחרון במסך.

שניהם מורכבים בקריאה אחת — **`statusAreaMount()`** — וזו נקודת החיווט
היחידה. מסך הגדרות שאינו קורא לה הוא מסך בלי אזור מצב, והבדיקה נכשלת.

### שלושת הבלוקים המשותפים
זהים בית-לבית בארבע האפליקציות, וכל שינוי בהם נעשה בארבעתן באותו סבב:

| בלוק | סמנים | חתימה (sha256, 16 תווים) |
|---|---|---|
| CSS אזור המצב | `/* ═══ אזור מצב — CSS משותף (סבב 15/17)` … `/* ═══ סוף CSS אזור המצב` | `aa10b52e9ce1157e` |
| «מידע טכני» | `/* ═══ "מידע טכני" — קיפול מסכי האחסון` … `/* ═══ סוף רכיב "מידע טכני"` | `83096d1117e813f0` |
| «☁️ סנכרון» | `/* ═══ אזור מצב — בלוק "☁️ סנכרון"` … `/* ═══ סוף בלוק "☁️ סנכרון"` | `65ff4f75bc4f6756` |

**ה-API:** `syncFmtTime` · `syncLineHTML` · `syncStatusHTML` ·
`syncStatusMount` · `syncNowFromStatus` · `statusAreaMount` ·
`techInfoHTML`/`techInfoToggle`/`techInfoMount`.

מעל הבלוק יושבת **`SYNC_CFG`** — הדבר היחיד שנבדל בין האפליקציות — עם שני
שדות בלבד: `lastSync()` (חותמת הסנכרון המוצלח האחרון) ו-`sync()` (מה
שהכפתור מפעיל, מחזיר Promise).

### ⚠️ שלוש הבהרות שאין לפספס
1. **הכפתור «סנכרן עכשיו» אינו מנגנון סנכרון.** הסנכרון האוטומטי ממשיך
   לרוץ ברקע בדיוק כמו קודם; זו רק דרך לבקש אותו עכשיו ולראות תשובה. לכן
   גם **אין בבלוק טוסט כשל משלו** — מסלול הסנכרון של כל אפליקציה כבר מדווח
   על כשל פעם אחת, וכפילות הייתה רעש.
2. **`lastSync()` אינו עֵד דחיפה ואינו משמש לפינוי.** הוא חותמת תצוגה,
   שנכתבת על כל שיחה מוצלחת עם הענן — כולל בדפדפן שרק קורא. לפינוי יש
   `_ysPushedAt`/`_tbPushedAt`/`syncedThrough` (סבבים 11 ו-14), **ואין
   לערבב ביניהם.** ⛔ אין להזין `LS_CFG.syncedThrough` מ-`lastSync()`.
3. **הבלוק אינו נשען על מחלקות הכרטיס של אף אפליקציה** — הן נבדלות ביניהן
   — אלא מרנדר חלונית משלו (`.sync-box`), וה-CSS יורש צבע וגופן. זה מה
   שמאפשר לו להישאר זהה בית-לבית.

### מצב הפתיחה של «מידע טכני» חי בזיכרון
`_techInfoOpen` (משתנה מודול, **לא** localStorage) שומר אם הרכיב פתוח.
בלעדיו, אפליקציה שבונה את מסך ההגדרות מחדש בכל רינדור מקבלת עוגן ריק,
ה-`details` הקודם כבר לא קיים, והרכיב נסגר בכל שמירה ובכל ציור מושהה של
⏳ (סבב 16). המצב בזיכרון בכוונה: זו העדפת תצוגה של הרגע, ולא נתון שראוי
לצרוך ממנו מכסת אחסון משותפת.

### ⛔ אין כפתורי פעולה באזור האחסון (סבב 15) — שקט הוא ברירת המחדל
מסך «💾 אחסון מקומי» קופל מלוח-מחוונים למידע טכני צנוע: המנהל צריך לדעת
שהכול מתנהל מאחורי הקלעים, בלי להתבלבל ממספרים, גרפים וכפתורים.
«🧹 פנה מקום עכשיו» · «↻ רענן» · «☁️ טען הכל מחדש מהענן» **אינם מוצגים** —
מוסתרים ב-CSS (`.tech-info .ls-status button{display:none}`), וה-HTML
שלהם עדיין נוצר ע"י `lsStatusHTML` שבמודול הקפוא. ⛔ אין לערוך את המודול
כדי להסירם (סבב 15) — עריכה שם שוברת את החתימות שהפרק הזה אוכף.
**המנגנון האוטומטי הוא הדרך היחידה לפינוי**, ו-`lsSweepNow`/`lsRestoreAll`
קיימות אך אינן נגישות מה-UI. האבחון עצמו — ניצולת, פילוחים, אזהרות האופק
והאימות ויומן הפינוי — מרונדר לתוך `#ls-status-box` שחי בתוך
`<details class="tech-info">`, ו-`techInfoToggle` קורא ל-`lsRenderStatus`
בפתיחה. **התוכן לא השתנה, רק המיקום.**
⚠️ **חריג יחיד ומכוון:** מעל `LS_CRIT_BYTES` (4MB) — מצב שהמנגנון
האוטומטי לא הצליח למנוע — `techInfoHTML` מציג שורת מצב עדינה
(`.ls-crit-line`) גם מחוץ ל«מידע טכני». זו רשת הביטחון היחידה שמוצגת
ביוזמת המערכת; ⛔ באנרי המודול עצמו (`#ls-alert`, התרעות `lsBootAlert`)
לא נגעו, ומסך «⏳ ממתין לסנכרון» אינו ממסכי האחסון.

### הודעת חזרת הסנכרון — נוסח אחד
**`MSG_SYNC_BACK = '☁️ הסנכרון חזר לפעול'`** בארבע האפליקציות, לצד
`MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN`. הוא החליף את «☁️ החיבור לנתונים
חזר» ב-schar-limud וב-gius, ונוסף ל-yoman שלא הייתה לה הודעת התאוששות
בכלל. ⛔ **אין להשתמש במחרוזת קשיחה** — הבדיקה נכשלת עליה.

### הבדיקה האוכפת
`tools/check-status-area.mjs` (ב-gius: `scripts/`) — **זהה בית-לבית
בארבעת הריפו פרט לבלוק `APP` שבראשו**, ורצה עם שערי התחביר לפני כל דחיפה.
היא נכשלת על שלושת סוגי הסטייה:
- **א. מיקום** — `sync-status-box` ו-`tech-info-box` אינם שני ה**ילדים
  הישירים** האחרונים של מיכל ההגדרות, בסדר הזה. ההשוואה היא על ילדים
  ישירים דווקא: עוגן שיושב בתוך כרטיס היה עובר בדיקה רופפת («שני המזהים
  מופיעים בסוף») גם כשמשהו אחר מוצג אחריו. ב-gius, שבונה את המסך כמחרוזת,
  הבדיקה היא על זנב `viewSettings()`.
- **ב. קוד** — אחד משלושת הבלוקים אינו תואם לחתימה שבטבלה למעלה.
- **ג. חיווט** — `statusAreaMount()` אינה נקראת בשום מקום מחוץ לבלוק
  המשותף, כלומר הרכיב קיים אך אינו מחובר. זו בדיוק התקלה של gius.
  בנוסף נכשלת על שריד `id="pend-status-box"` מחוץ לאזור המצב.

**שינוי מכוון בבלוק משותף** = עדכון הבלוק בארבע האפליקציות **ובארבעת
עותקי קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב.

### תיאום גרסאות
`sw.js`: hanhala **v33** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v23** · gius **gius-v9**.
<!-- SHARED:end -->

### היישום כאן (schar-limud)
- מסך «⏳ ממתין לסנכרון» ישב ב**ראש** מסך ההגדרות והועבר לאזור המצב;
  `<div id="sync-status-box"></div>` נוסף לפני `tech-info-box`, ושניהם
  ילדים ישירים של `settings-main`. `renderSettingsLists()` קורא
  `statusAreaMount()` במקום `pendRenderStatus()` + `techInfoMount()`.
- **`pendRenderStatus` הוסרה** (וכך גם העוגן `pend-status-box`) — תוכן מסך
  ה-⏳ עצמו לא נגע, הוא מוגש מתוך `#sync-status-box`.
- `SYNC_CFG`: `lastSync` = `_slLastPullOk` (נכתב רק כשכל ארבע הקטגוריות
  נמשכו בהצלחה), `sync` = `syncAll()`.
- הטוסט «☁️ החיבור לנתונים חזר» הוחלף ב-`MSG_SYNC_BACK`.

### היישום כאן — קיפול מסכי האחסון (מסבב 15)
כרטיס «💾 אחסון מקומי» הוסר מ-`settings-main`; `<div id="tech-info-box">`
נוסף בתחתית המסך, אחרי כרטיס הסעיפים; `renderSettingsLists()` קורא
`techInfoMount()` במקום `lsRenderStatus()`. (המסך עדיין מאחורי שער סיסמת
המנהל — כמו כל ההגדרות כאן.)

---

<!-- SHARED:start id="iron-rule-8-docs" -->
## ⭐ כלל ברזל 8 — פרוטוקול תיעוד קבוע (סבב 18, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שבעת כללי הברזל שלמעלה.

> **התיעוד עצמו הוא רכיב משותף, ולכן חלים עליו אותם תנאים שכלל ברזל 7
> קבע לקוד משותף: אותו נוסח, במקום קבוע, ומסומן במפורש. פרק שאינו מסומן
> «משותף» הוא פרטי לאפליקציה — ואין לגעת בו על סמך נוסח מריפו אחר.**

### הבעיה שנמדדה
ביקורת חיצונית סרקה את ארבעת קבצי ה-CLAUDE.md ומצאה עשרות פרקים שאמורים
להיות זהים. רובם אכן היו — אבל שום מנגנון לא אכף את זה, ולכן ארבעה נסחפו
בלי שאיש שם לב. באחד מהם הסחיפה הייתה **הפוכה**: פרק «בדיקה» של סבב 14
הועתק כלשונו לארבעת הריפו, כולל לשתי אפליקציות שבהן הבדיקות שהוא מתאר
מעולם לא רצו ולא יכלו לרוץ (אין בהן `verify` ואין `tier2`). תיעוד
שמועתק «כי כך כתוב אצל האחות» גרוע מתיעוד חסר: הוא נקרא כעדות.

### ההגדרה המחייבת — סימון משותף/פרטי
כל פרק שאמור להיות זהה בארבעת הריפו עטוף בסימוני HTML-comment:

```html
<!-- SHARED:start id="שם-מזהה-קצר" -->
…
<!-- SHARED:end -->
```

**הסימון הוא מקור האמת:**
- מה שבתוך הסימון — **חייב** להיות זהה בית-לבית בארבעת הריפו.
- מה שמחוצה לו — **פרטי לאפליקציה**, ואיש אינו מיישר אותו.

הסימון עוטף **רצף** של פרקים, לא בהכרח פרק בודד: פרק סבב שכולו משותף
פרט לזנב פר-אפליקציה (למשל שורת «**כאן:**» שבסוף «מזהים», או «מה נעשה
כאן») נחתך בדיוק שם — הרצף המשותף בפנים, הזנב בחוץ.

### שבעת הכללים
1. **כל סבב מוסיף פרק סבב במקום קבוע ובמבנה קבוע** — בסוף הקובץ, אחרי
   פרק הסבב הקודם, עם שלושת התת-פרקים ובסדר הזה: **מה נעשה** (משותף) ·
   **מה נעשה כאן** (פר-אפליקציה) · **אימותים** (פר-אפליקציה).
2. **כל סבב מקדם את שורת «עודכן לאחרונה»** שבראש הקובץ, בפורמט הקבוע
   `עודכן לאחרונה: סבב N · YYYY-MM-DD`. גם סבב שנגע רק בקובץ אחד מקדם
   אותה בקובץ שבו נגע.
3. **סבב שנוגע בקוד משותף חייב לעדכן את הפרק המשותף בארבעת הריפו באותו
   סבב** — לעולם לא באחד בלבד. פרק מסומן SHARED שמשתנה, משתנה בארבעתם,
   באותו שינוי בדיוק, ובאותו קומיט-סבב.
4. **⛔ אסור לסשן לשנות תוכן של פרק שאינו מסומן SHARED על סמך נוסח מריפו
   אחר.** זה בדיוק מה שייצר את באג «84 הטענות»: טקסט נכון באפליקציה אחת
   שהועתק לשלוש שבהן הוא שקרי. אם נראה שפרק פרטי «חסר» משהו שיש לאחות —
   זו אינדיקציה לבדוק את **המציאות באפליקציה הזו**, לא להעתיק.
5. **«בדיקה» ו«חתימת APK» הם פרקים פרטיים בהגדרה.** «בדיקה» מתאר מה
   באמת רץ באפליקציה הזו ולכן הוא נבדל בהכרח; «חתימת APK» מחזיק
   keystore, alias וטביעת SHA ייחודיים — **איחוד שלו יהיה שגוי ומסוכן**,
   ולכן הבדיקה נכשלת אם הוא סומן SHARED.
6. **סבב שכותב בדיקות שומר אותן כקובץ בריפו.** רתמה שרצה בסשן בלבד
   נעלמת ברגע שהסשן נסגר — הסשן הבא אינו יכול להריץ אותה, לא לפני שינוי
   ולא אחריו, ולכן היא אינה מגינה על דבר.
   **⛔ ובפרט: אין לתעד בדיקות כאלה כאילו הן קיימות.** «N טענות עברו»
   בפרק סבב, בלי קובץ שאפשר להריץ, נקרא כעדות שאינווריאנטה שמורה — בזמן
   שאין שום דבר ששומר עליה. זה בדיוק הכשל של סעיף 4, בכיוון אחר: תיעוד
   שמתאר מציאות שאיננה.
   ⚠️ הכלל חל על **בדיקות** — טענות שניתן להריץ מחדש. סריקה חד-פעמית או
   מדידה שנעשתה כדי להכריע החלטה אינן בדיקות; הן נרשמות כמדידה, ואין
   להציג אותן כשער.
7. **⛔ אין לזרוע פרטי כניסה — שם משתמש, סיסמה, מפתח, טוקן — בשום קובץ
   שנדחף לגיט.** לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ
   בדיקה. ארבעת הריפו **ציבוריים**, ומפתחות ה-anon של Supabase יושבים
   ב-`index.html` — כלומר סיסמה שנדחפה היא סיסמה שכל אחד יכול להשתמש בה
   מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותה.** הערך נשאר קריא בהיסטוריית ה-git
   לנצח, ומי שכבר מצא אותו מחזיק בו. הטיפול היחיד בסוד שנדחף הוא
   **להחליף אותו במסד**, ורק אחר כך לנקות את הקובץ.
   המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר איך — עם מצייני מקום
   ולא עם ערכים אמיתיים.

### הבדיקה האוכפת
`tools/check-docs.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא לה) —
**זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם שערי
התחביר לפני כל דחיפה. היא נכשלת על שבעה סוגי סטייה:
- **א. סימון** — `SHARED:start`/`SHARED:end` אינם מאוזנים, מקוננים, בלי
  `id`, עם `id` כפול, או שרשימת המזהים בקובץ אינה תואמת לרשימה הקנונית.
- **ב. תוכן** — תוכנו של בלוק SHARED אינו תואם לחתימת ה-sha256 הקנונית
  הרשומה בקובץ הבדיקה.
- **ג. שורת העדכון** — «עודכן לאחרונה» חסרה, אינה בראש הקובץ, או בפורמט
  שגוי.
- **ד. פרק פרטי שסומן** — «חתימת APK» או «בדיקה» נמצאים בתוך בלוק
  SHARED, בניגוד לסעיף 5.
- **ו. שלד שלושת הקבצים הנלווים** (סבב 39) — `README.md`, `CONTEXT.md`
  ו-`android/README.md` חייבים לשאת כותרות `##` נדרשות, בסדר יחסי קבוע.
  ⚠️ פרק **נוסף** בין הנדרשים אינו כישלון.
- **ז. תוכן הפסקאות המשותפות שבהם** (סבב 41) — ⛔ **שלד אינו תוכן**: פרק
  שאיבד את גופו עובר את סעיף ו, מפני שהכותרת עדיין שם. לכן הפסקאות שהן
  משותפות באמת מסומנות בשלושת הקבצים באותם סימוני `SHARED`, ונאכפות
  ב-sha256 מלא — ⛔ לא בהשוואת כותרות ולא בספירת פסקאות.
  ⚠️ **ומה שמחוץ לסימון נשאר פרטי בכוונה** — כותרת הריפו, «מסכים», טבלת
  הטבלאות, «מצב נוכחי» ואזהרת `CACHE_NAME` שנבדלת פר-אפליקציה. ⛔ אין
  להרחיב את הסימון לפסקה שאינה זהה בארבעתן **בפועל**.

- **ה. שורת עדכון שלא קודמה** (סבב 20) — `CLAUDE.md` שונה מול
  `origin/main` אבל שורת «עודכן לאחרונה» זהה לזו שב-`origin/main`.
  סעיפים א–ד בודקים את **צורת** השורה; זה בודק שהיא באמת **קודמה**.
  ⚠️ **הסעיף הזה מדלג ואינו מפיל** כשאין git, כשאין `origin/main`, או
  כש-`CLAUDE.md` אינו קיים שם (סבב 20).

סימונים שבתוך גדר קוד (```) אינם נספרים — אחרת הדוגמה שבפרק הזה עצמו
הייתה נקראת כבלוק אמיתי.

**שינוי מכוון בפרק משותף** = עדכון הפרק בארבעת הריפו **ובארבעת עותקי
קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב. אותו נוהל בדיוק שכלל
ברזל 7 קבע לבלוקי הקוד המשותפים.
<!-- SHARED:end -->

---

## כניסה ומשתמשים — טביעה בשני המצבים (סבבים 19–26, עודכן בסבב 40)
⛔ **אין להחזיר עמודת `deleted` ל-`sl_users` (סבב 37)** — ההשבתה היא
`active=false`, ועמודה שנייה לאותו מושג היא מקור אמת שני.

**בענן:** `sl_users` עם `pass_salt`+`pass_fp` (`migrations/010`)
ו-`role` (`migrations/011`); ⚠️ ולצידן `password`
בטקסט גלוי, **נכתבת ואינה נקראת לאימות**. **במכשיר:** מראת משתמשים
`sl_mirror_users` עם טביעות בלבד.

### ⭐ ההחלטה שהתהפכה — 2026-08-19, בהחלטת המנהל
עד לתאריך הזה `doLogin` שלף את השורה עם `.eq('password', p)` — ⛔ **כלומר
הסיסמה הגלויה שבענן הייתה המפתח שפותח את הכניסה**, ומפתח ה-anon שקורא
אותה יושב ב-`index.html` הציבורי. ⚠️ זו הייתה **בחירה תפעולית מתועדת**
(המנהל מנהל סיסמאות מלוח הבקרה של Supabase — אין כאן מסך ניהול משתמשים),
וכלל ברזל 9 מייחד את ההכרעה לו. **הוא הפך אותה ב-2026-08-19 (סבב 40).**
מעכשיו השליפה היא לפי `username` בלבד, וההכרעה ב-**`slVerifyOffline`** —
אותה פונקציה שאימתה אופליין מסבב 24 ושחוסמת משתמש מושבת מסבב 37.

### ⚠️⚠️ `SL_PLAINTEXT_BOOTSTRAP` — למה נשאר כאן מסלול אחד מול הטקסט הגלוי
הטביעה נוצרת כאן **רק בכניסה מקוונת מוצלחת** (`slEnsurePassFp`), ומשתמש
נוצר ב-`INSERT` **ידני** של המנהל — אין מסך ניהול משתמשים. ⛔ כלומר משתמש
חדש נולד **בלי טביעה**, ודרישת טביעה בכניסה הייתה נועלת אותו בחוץ לנצח:
הוא לעולם לא היה מגיע לכניסה המוצלחת שיוצרת אותה.
לכן משתמש **שאין לו טביעה — ורק הוא** — עובר אימות חד-פעמי מול העמודה
הגלויה, ומיד אחריו נוצרת לו טביעה. ⛔ **המסלול נסגר לו לתמיד באותו רגע**,
ואינו נגיש למי שכבר יש לו טביעה.
⚠️ **נמדד ב-2026-08-19:** המשתמש היחיד הקיים כבר מחזיק `pass_salt`
ו-`pass_fp` באורך 64 — הבוטסטרפ אינו פעיל לאיש היום, והוא קיים בשביל
המשתמש הבא. **הטריגר לסגירתו** כתוב ב-`migrations/015`: בניית מסלול
יצירת-משתמש שגוזר טביעה.
- **הטביעה:** PBKDF2-SHA256, 100,000 סיבובים, מלח אקראי פר-משתמש עם
  קידומת ההקשר `SL_PASS_CTX` — ה-origin משותף לארבע האפליקציות, ובלי
  הפרדת הקשר טבלה מחושבת מראש עבור אחת תקפה לכולן.
- **`slEnsurePassFp`** רצה בכניסה מקוונת מוצלחת בלבד — הרגע היחיד שבו יש
  ביד סיסמה נכונה. ⚠️ בכוונה אינה סורקת את שאר המשתמשים (אין כאן תפקיד
  «בעלים»); כל משתמש משלים טביעה בכניסתו המקוונת הראשונה. ⛔ **טביעה
  לא-תואמת + כשל גזירה ⇒ שני השדות מאופסים** (סבב 24) — עדיף בלי כניסה
  אופליין מאשר כניסה בסיסמה מבוטלת; אין-טביעה + כשל גזירה ⇒ דילוג שקט.
- **המראה — רשימת היתר `SL_USER_COLS`** (`id`,`username`,`pass_salt`,
  `pass_fp`,`role`), נאכפת בשלוש נקודות (משיכה, טעינה, שער הדיסק),
  וה-`select` עצמו מבקש רק אותן. ⛔ **המראה אינה חלק מ-`MIRROR` ואינה
  נדחפת לעולם** (סבב 24) — דחיפת-מצב שלה הייתה כותבת `password` ריק.
  המשיכה מווסתת ל-60 שניות; `doLogin` מריץ `slPullUsers(true)` אחרי
  כניסה מוצלחת (זה «נתיב העדכון» כאן — מטריצה שורה 4), ⛔ ואם ייבנה
  נתיב חלקי — דרך `slUserPub` (כלל ברזל 10 סעיף 5).
- **ארבע הודעות אופליין נבדלות** (`MSG_OFF_UNKNOWN` · `MSG_OFF_NO_FP` ·
  `MSG_OFF_NO_CRYPTO` · `MSG_BAD_LOGIN`); ⛔ אין לאחד (סבב 24). כשל רשת
  באמצע כניסה מקוונת נופל-חזרה לאימות אופליין. טבלה ריקה ⇒ `MSG_NO_USERS`
  והנחיה ליצירת המשתמש הראשון ידנית.
- ⛔ **אין אכיפת פורמט שש-ספרות באף מסלול כניסה (סבבים 19 ו-26)** — אכיפה
  שם נועלת בחוץ סיסמה קיימת ותקפה בלי מסלול שחזור; `au-pass` נושא
  `inputmode="numeric"`+`maxlength="6"` כרמז קלט בלבד. הפורמט נאכף במקום
  שבו סיסמאות נקבעות — ה-INSERT הידני שקובץ ההתקנה מדגים.

## הרשאות — `role`, במקום שער סיסמה (סבב 26)
שער הסיסמה הנפרד של מסך ההגדרות (`unlockSettings`, `slHashPass`,
`sl_admin_pass_h`, `changeAdminPass`) **הוסר כליל** — ההרשאה נגזרת
מ-`sl_users.role` (`'admin'` / `'user'`), עובדת אופליין (ה-`role` יורד עם
המראה), ונאכפת ב-`slSettingsAccess(u)`.
- ⚠️ שלושה מצבים — `'ok'` / `'denied'` / `'no-role'` — ⛔ אין לאחד את
  הודעות `denied` ו-`no-role` (סבב 26): «אין הרשאה» למנהל שחסרה לו עמודה
  שולח אותו לחפש באג במקום מיגרציה.
- ⚠️ **`role` נשמר גם בסשן** (`slSaveSession`) — בלעדיו עלייה בלי רשת
  נועלת את המנהל; המראה גוברת על הסשן כשהיא מכירה את המשתמש.
- ⛔ **אין להחזיר את המנגנון הישן או נפילה-חזרה ל-`'admin'`** (סבב 26) —
  `slPassOf` שנפלה ל-`'admin'` הייתה שער שנפתח לכל מקליד בהתקנה טרייה.
  `slDropLegacyPassHash()` מוחקת את השריד מהמכשיר ורושמת ליומן.

---

<!-- SHARED:start id="iron-rule-9-security-spread" -->
## ⭐ כלל ברזל 9 — שדרוג אבטחתי מתפשט (סבב 21, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שמונת כללי הברזל שלמעלה.

> **כשסבב מחליף מנגנון אבטחה באפליקציה אחת — טיפול בסודות, אימות, הצפנה,
> הרשאות — הוא חייב לרשום במפורש, בפרק הסבב, האם המנגנון החדש רלוונטי גם
> לשאר האפליקציות. רלוונטי ולא יושם ⇐ נרשם פער מתועד עם טריגר.
> שקט אינו תשובה.**

### הבעיה שנמדדה
סבב 13 העביר את schar-limud מהשוואת סיסמה גלויה ל**טביעת PBKDF2-SHA256**.
ההחלטה שם נכונה ומנומקת: מיגרציה 009 הורידה את `sl_settings` למראה המקומי,
ו-`admin_pass` בטקסט גלוי **על הדיסק** לא היה מתקבל על הדעת.

מה שלא נעשה: **לא נרשם באותו סבב אם המנגנון רלוונטי גם לשלוש האחיות.**
לא «כן והוחל», לא «לא, וזו הסיבה» — פשוט כלום. התוצאה היא ש-hanhala
ו-gius המשיכו שמונה סבבים עם מודל סיסמאות אחר לגמרי, **בלי שאיש החליט
על כך** — ההבדל התגלה רק בביקורת של סבב 21. תיעוד שותק אינו מתעד החלטה;
הוא מסתיר את היעדרה.

### שלוש התשובות המותרות — ורק שלוש
סבב שנגע במנגנון אבטחה חייב לרשום **אחת** מהן, במפורש:
1. **הוחל בכולן** — עם ציון מה השתנה בכל אחת.
2. **אינו רלוונטי לאחרות** — עם **הנימוק**, לא כהצהרה. «אין שם סוד על
   הדיסק» הוא נימוק; «לא נדרש» אינו.
3. **רלוונטי, ולא יושם** — ואז **פער מתועד עם טריגר**: מה חסר, מה הסיכון,
   ו**מה האירוע שמחייב לפתוח את הסבב** (הדוגמה
   ההיסטורית: פער «הוספת משתמשים ל-gius» — נרשם בסבב 22 ונסגר בסבב 23).

⛔ **מה שאסור הוא הרביעית — לא לכתוב כלום.**

### ⚠️ הכלל דורש **רישום**, לא יישור — ואינו גובר על כלל ברזל 8 סעיף 4
הרישום נעשה **בפרק הפרטי של כל אפליקציה, לפי המציאות שלה**. כלל ברזל 9
**אינו** רישיון להעתיק נוסח מריפו אחר, ו**אינו** מחייב שהמנגנונים יהיו
זהים: אפליקציות שונות חיות במודלי איום שונים, ואחידות מנגנון אינה מטרה
בפני עצמה. שני הכללים אומרים יחד דבר אחד: **מה שנבדל — נבדל בכוונה
ובכתב.**

### ⛔ הלקח נרשם בארבעתן — גם כשהמנגנון רלוונטי רק לאחת (סבב 26)
**לקח אבטחתי שנלמד באפליקציה אחת נרשם כהערת ⛔ בארבע האפליקציות**, ליד
המקום בקוד שבו הוא עלול להישבר — ולא רק באפליקציה שבה הוא נלמד.

⚠️ **וזו אינה כפילות של שלוש התשובות שלמעלה.** הרישום שהן מחייבות הוא
**החלטה**: «הוחל» / «אינו רלוונטי, וזה הנימוק» / «פער עם טריגר», והוא
יושב בפרק הפרטי של כל אפליקציה. ההערה בקוד עונה על שאלה אחרת לגמרי —
**מה אסור לעשות כאן בעתיד.** אפליקציה שהמנגנון «אינו רלוונטי» לה עדיין
חשופה ל**כשל** שהוליד אותו: סשן עתידי יוסיף בתום לב בדיוק את הדבר
שהוסר במקום אחר, מפני שאצלו הוא מעולם לא היה — ולכן גם מעולם לא נכתב
אצלו שאסור.

> **הערה שיושבת במקום אחד אינה מגינה על הארגון.** היא מגינה על הקובץ
> שהיא יושבת בו, ועל שום קובץ אחר.

**הדוגמה החיה** היא הסבב שהוליד את הסעיף: שער הסיסמה הנפרד של מסך
ההגדרות הוסר מ-schar-limud (סבב 26), ובשלוש האחיות שער כזה לא היה
מעולם — כלומר לפי שלוש התשובות, «אינו רלוונטי». ובכל זאת ההערה נוספה
**בארבעתן**, בראש אזור ההגדרות: לכל אחת מהן יש מסך הגדרות, וכל אחת מהן
היא מקום שבו אפשר להמציא את השער הזה מחדש.

### ⛔ החלטת מודל אבטחה היא של המנהל, לא של הסשן
מנגנון שנבחר במפורש ע"י המנהל — כולל בחירה **לא** להקשיח — אינו «חוב
טכני» שסשן עתידי מוסמך לסגור מיוזמתו. סבב שמוצא פער מתועד **מדווח עליו
ומציע**; ההגירה עצמה מחייבת החלטה מפורשת. ר' הדוגמה החיה בפרקי הסיסמאות
של hanhala ושל gius: שם ההחלטה על טקסט גלוי היא **בחירה תפעולית מודעת**,
והמרתה ל-PBKDF2 בלי אישור הייתה **שוברת יכולת ניהול שהמנהל בחר בה בכוונה**.
<!-- SHARED:end -->

**הרישום הנדרש כאן (schar-limud):** מנגנון הטביעה נולד כאן (סבב 13, על
`admin_pass`) והורחב למשתמשים (סבב 24). ⭐ **ובסבב 40 הוא חדל להיות יחיד:**
המנהל הפך ב-2026-08-19 את ההחלטה על הטקסט הגלוי, ובשלוש האפליקציות שיש בהן
משתמשים האימות — מקוון ואופליין — הוא מול הטביעה. ⚠️ **ומה שנשאר כאן פרטי
הוא הבוטסטרפ:** אין כאן מסך ניהול משתמשים, ולכן משתמש חדש נולד בלי טביעה
ונדרש מסלול חד-פעמי מולה — ר' «כניסה ומשתמשים» למעלה. ⛔ אין להעתיק אותו
לאחיות: אצלן הטביעה נוצרת ביצירת המשתמש.

---

<!-- SHARED:start id="iron-rule-10-users" -->
## ⭐ כלל ברזל 10 — מודל המשתמשים וההרשאות בארגון (סבב 26, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את תשעת כללי הברזל שלמעלה.

> **אפליקציה בארגון שיש בה משתמשים נבנית לפי שמונת הסעיפים שלמטה, מהיום
> הראשון.** הדפוס הזה לא תוכנן מראש — הוא התגבש בסבבים 13–26, סעיף אחרי
> סעיף, כל אחד מהם אחרי כשל שנמדד בפועל. הניסוח כאן נועד לדבר אחד:
> שהאפליקציה **הבאה** לא תעבור את אותה סדרה מחדש.

### ⚠️ למה זה כלל ולא פרק תיעוד
מי שבונה כאן אפליקציה חדשה **מעתיק ריפו קיים** — הוא לוקח את המודול
המשותף, את `LS_CFG`, את אזור המצב ואת מבנה `index.html`, ומתחיל לעבוד.
הוא **אינו** קורא שלושים פרקי סבבים היסטוריים כדי לגלות למה `role` הוא
בלי `DEFAULT` או למה הסיסמה אינה יורדת לדיסק. **דפוס שחי רק בפרקי
היסטוריה אינו עובר בהעתקה** — הוא נשאר מאחור, והאפליקציה החדשה נולדת עם
בדיוק הכשלים שכבר שילמנו עליהם. כלל ברזל כן עובר, מפני שהוא נקרא לפני
שכותבים ולא אחרי שנשבר.

### שמונת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם, ואין צורך לחפש.

1. **טבלת משתמשים: `role TEXT NOT NULL`, ⛔ בלי `DEFAULT`.**
   *(נלמד בסבב 26 והושלם בהשלמה שלו.)* יצירת משתמש בלי תפקיד **נכשלת
   במסד**, וזו ההתנהגות הרצויה: **תפקיד הוא החלטה, ולא ערך שנופל
   מאליו.** ברירת מחדל ש**מעניקה** הרשאה היא הכשל החמור מבין השניים,
   אבל גם ברירת מחדל שנכשלת סגור סוטה מהדפוס.
   ⚠️ **הערכים עצמם אינם משותפים ואין ליישר אותם** — כל אפליקציה ומודל
   ההרשאות שלה, בטבלה נפרדת ולעיתים בפרויקט Supabase נפרד.

2. **⛔ אין שער סיסמה נפרד מעל מסך ההגדרות.** *(נלמד בסבב 26.)* ההרשאה
   נגזרת **מהמשתמש המחובר בלבד**. סיסמה שנייה לאותו אדם אינה שכבת
   אבטחה נוספת — היא מנגנון שני עם מסך משלו, מסלול שינוי משלו ותחזוקה
   משלו, ובהתקנה טרייה היא נוטה ליפול חזרה לערך ברירת מחדל ולהיפתח לכל
   מי שמנחש אותו. ⚠️ הכלל חל גם על אפליקציה שאין בה משתמשים כלל: שם שער
   כזה אינו «יישור לאחיות» אלא **שכבת הרשאה שלמה יש מאין**.

3. **⛔ אין לזרוע משתמש, סיסמה או תפקיד בשום קובץ שנדחף לגיט.**
   *(נלמד בסבב 21 בהנהלה, ובסבב 24 ובהשלמתו ב-schar-limud וב-gius.)*
   לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ בדיקה. הריפו
   **ציבורי**, ומפתחות ה-anon יושבים ב-`index.html` — כלומר סוד שנדחף
   הוא סוד שכל אחד יכול להשתמש בו מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותו:** הערך נשאר קריא בהיסטוריית ה-git
   לנצח. הטיפול היחיד בסוד שנדחף הוא **להחליף אותו במסד**, ורק אחר כך
   לנקות את הקובץ. המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר
   איך — עם מצייני מקום ולא עם ערכים.

4. **האימות הוא מול טביעה — ⛔ ולא מול סוד שמור, לא במכשיר ולא בענן.**
   *(נלמד בסבב 13 ב-schar-limud, הורחב בסבבים 22–24, והושלם בסבב 40.)*
   מה שיורד למכשיר הוא **`pass_salt` + `pass_fp` בלבד**: PBKDF2-SHA256,
   **100,000 סיבובים**, **מלח אקראי פר-משתמש**, ועם **קידומת הקשר
   פר-אפליקציה** בתוך המלח — ה-origin משותף לארבע האפליקציות, ובלי
   הפרדת הקשר טבלה מחושבת מראש עבור אחת מהן תקפה לכולן.
   ⛔ **וגם הכניסה המקוונת מכריעה מול אותה טביעה** (סבב 40) — דרך אותה
   פונקציה בדיוק. ⚠️ **הרקע, וזה מה שהופך את הסעיף למחייב:** עד
   2026-08-19 שלוש האפליקציות שיש בהן משתמשים שלפו את שורת המשתמש
   **לפי הסיסמה** (`.eq('password', …)` וכדומה), כלומר הערך הגלוי בענן
   היה **המפתח שפותח את הכניסה** — ומפתח ה-anon שקורא אותו יושב
   ב-`index.html` הציבורי.
   ⚠️ **הטקסט הגלוי היה החלטה תפעולית מתועדת** (המנהל רצה לראות ולנהל
   סיסמאות), ⭐ **והמנהל ביטל אותה ב-2026-08-19.** ⛔ אין לקרוא את
   הפרקים ההיסטוריים שמתארים אותה כהוראה בתוקף, ⛔ ואין להחזיר השוואה
   מול עמודת סיסמה בשום מסלול.
   ⚠️ **והעמודה עצמה יורדת בשני צעדים ולא באחד** — היא `NOT NULL`, ולכן
   קוד שיפסיק לכתוב אליה לפני שהמיגרציה רצה יפיל כל יצירת משתמש: דגל
   נתיב-חזרה ⇐ הרצת מיגרציה ⇐ כיבוי הדגל ⇐ ובסבב נפרד מחיקת העמודה
   (סבב 30).
   ⛔ **וסיסמה שישבה בטקסט גלוי בענן נחשבת חשופה לצמיתות** — מחיקת
   העמודה אינה מבטלת את מה שכבר ניתן היה לקרוא; הטיפול היחיד הוא
   **החלפת הסיסמאות**.
   ⛔ **כישלון גזירה מאפס את שני השדות ולא מדלג על הכתיבה** — טביעה
   ישנה ששורדת שינוי סיסמה ממשיכה לפתוח את הכניסה עם הסיסמה **הקודמת**,
   לנצח.

5. **כל נתיבי הכתיבה למראת המשתמשים עוברים דרך פונקציית סינון אחת.**
   *(נלמד בסבב 23 ב-gius, וחזר בסבב 26 שם.)* **רשימת היתר** ולא רשימת
   איסור — רשימת היתר נשארת נכונה גם כשתיווסף לטבלה עמודה רגישה חדשה.
   ⛔ **ואין נתיב שני שעוקף אותה.** זו הנקודה שנשברה בפועל יותר מפעם
   אחת: מסנן שיושב במשיכה בלבד, וכתיבה מקומית שכותבת את הסוד לדיסק
   מסביבו. כל נתיב חדש — משיכה, כתיבה מקומית, עדכון חלקי, מיגרציה,
   ייבוא — חייב לעבור דרך אותה פונקציה, ורצוי שגם שער הדיסק יסנן שוב
   כשכבה שנייה.

6. **כניסה אופליין מרובת-משתמשים היא דרישה, לא תוספת.**
   *(נלמד בסבב 22 בהנהלה, בסבב 23 ב-gius ובסבב 24 ב-schar-limud.)*
   **כל משתמש נכנס בכל מכשיר, גם בלי רשת** — מול הטביעה שבסעיף 4, ולא
   מול הענן. מטמון שמחזיק רק את המשתמש האחרון שנכנס **שובר את הדרישה**:
   מי שקיבל מכשיר שמישהו אחר עבד בו נעול בחוץ.
   ⚠️ **וההודעות נבדלות זו מזו.** «אין לך במכשיר במה לאמת» אינו «סיסמה
   שגויה»: ⛔ הודעה מאוחדת היא כשל שקט שמוביל את המשתמש להקליד שוב ושוב
   סיסמה נכונה לחלוטין, במקום לומר לו את הדבר היחיד שיפתור — חיבור אחד.

7. **קובץ התקנה שמוכרז «אידמפוטנטי» חייב שורת התכנסות לכל שינוי מבני —
   ולא רק `create table if not exists`.** *(נלמד בסבב 27.)*
   `create table if not exists` **מדלג על טבלה קיימת, ועל כל מה
   שבתוכה**: עמודה שנוספה אחרי ההתקנה הראשונה, אינדקס, טריגר, פוליסה,
   אילוץ, או `drop default`. כלומר התקנה קיימת שמריצה את הקובץ מחדש
   מקבלת «רץ בהצלחה» — **ונשארת בלי השינוי**, בשקט.
   לכן לכל שינוי מבני שנעשה מאז ההתקנה הראשונה יושבת בקובץ **גם** שורת
   התכנסות: `add column if not exists` · `create index if not exists` ·
   `create or replace` לטריגר · `drop policy if exists` + `create policy` ·
   `alter column … drop default` · `revoke`+`grant` מפורשים.
   ⛔ **ואין לגעת בנתונים — מבנה בלבד.** ה-`UPDATE` היחיד שמותר הוא
   מילוי עמודה שזה עתה נוספה, ורק ב-`WHERE <col> IS NULL`.
   ⚠️ **הסעיף יושב דווקא כאן** מפני שקובץ ההתקנה הוא המקום שבו נולדים
   טבלת המשתמשים, ה-`role` והטביעה — כלומר בדיוק שלושת הסעיפים
   הראשונים שלמעלה. התקנה שהריצה את הקובץ מחדש ולא קיבלה את
   `pass_salt`/`pass_fp` או את `role` היא התקנה **בלי כניסה אופליין
   ובלי הרשאות**, בלי שאיש יידע.

8. **⛔ האיסור על זריעת פרטי כניסה חל על כל סוגי הקבצים — לא רק על
   מיגרציות.** *(נלמד בסבב 27.)* SQL · HTML · Markdown · README ·
   קובץ בדיקה · פיקסטורה · כל קובץ שנדחף לגיט. ⛔ **וגם ערך «לדוגמה»
   אסור** — מציין מקום בלבד, לעולם לא סיסמה אמיתית ולא סיסמה גנרית
   שמישהו עלול להקליד.
   ⚠️ **הרקע, וזה מה שהופך את הסעיף לנחוץ:** סעיף 3 כבר אסר את זה
   מסבב 21 — ובכל זאת סוד שרד בהנהלה עד סבב 27, ב-`setup-db.html`,
   מפני שהחיפוש שאישר את «אין בריפו פרטי כניסה» נעשה **בקבצי SQL
   ובקוד בלבד**. כלי עזר ב-HTML לא נסרק, והצהרת סבב 24 נכתבה על סמך
   סריקה חלקית. ⛔ **סריקה שמוגבלת לסוג קובץ אחד אינה ראיה** —
   היא ראיה לסוג הקובץ שנסרק, ותו לא.

9. **`anon` ו-`authenticated` מקבלים `SELECT`, `INSERT`, `UPDATE` — ותו
   לא.** *(נלמד בסבב 29.)* ⛔ **אין להעניק להם `DELETE` או `TRUNCATE`.**
   מחיקה בארגון היא **תמיד סימון** — `deleted=true` + חותמת, או tombstone
   בתוך הערך — ו**לעולם לא הסרת שורה** (כלל ברזל 6 סעיף 1). כלומר ההרשאה
   מיותרת בהגדרה, ובתמורה היא פותחת למי שמחזיק את המפתח הגלוי
   שב-`index.html` דרך למחוק שורות ואף לרוקן טבלה שלמה.
   ⚠️ **והכלל חל על שני התפקידים גם כשאין משתמשי Auth:** `authenticated`
   קיים בכל פרויקט Supabase ומחזיק את אותן הרשאות בירושה, ולכן פתיחת
   signup או Auth בעתיד הייתה פוערת את ההגנה **בשקט**. ⛔ `service_role`
   אינו בכלל הזה — הוא תפקיד השרת ואינו נגיש מהדפדפן.
   ⚠️ **וזה אינו מתקיים מאליו:** פרויקט Supabase סטנדרטי מגיע עם
   `alter default privileges … grant all on tables to anon, authenticated`,
   ולכן **כל טבלה נולדת עם `DELETE` ו-`TRUNCATE`**. ⛔ **`GRANT` הוא
   אדיטיבי ואינו מסיר דבר** — נדרש `REVOKE` מפורש, וכל מיגרציה שמוסיפה
   טבלה חייבת אחד משלה.
   ⛔ **וטבלאות יומן הן החריג, בכיוון ההפוך:** `sync_log` ו-`kv_backup`
   מקבלות `INSERT` ו-`SELECT` בלבד — **בלי `UPDATE` ובלי `DELETE`, לשני
   התפקידים כאחד** — כדי שלא ניתן יהיה לזייף רישום קיים ביומן ראיות ולא
   להעלים אותו. ⚠️ אין «ליישר» אותן לשאר הטבלאות בשם האחידות: הסט הצר
   שם **הוא** ההגנה, ו-`UPDATE` נחוץ בשאר הטבלאות רק מפני שהכתיבה שם
   היא upsert.
   ⛔ **ובדיקת הרשאות סורקת את שני התפקידים, תמיד** *(נלמד בהשלמת סבב
   29)* — **הרשאה שהוחלה על תפקיד אחד בלבד היא הגנה חלקית.** זה נמדד
   כאן בפועל: שתי טבלאות היומן החזיקו `INSERT, SELECT` ל-`anon` ואת
   הסט **המלא** ל-`authenticated`, ולכן ההגנה שתועדה כקיימת התקיימה מול
   מפתח אחד בלבד. ⚠️ סריקה שכיסתה תפקיד אחד היא ראיה לתפקיד שנסרק,
   ותו לא — בדיוק כמו סעיף 8 לגבי סוגי קבצים.
<!-- SHARED:end -->

**כאן:** `sl_users.role` — `text NOT NULL` בלי `DEFAULT`, ערכים
`'admin'`/`'user'` (נמדד מול המסד, סבב 26 השלמה); ⛔ אין ליישר את הערכים
לאחיות. schar היא המקור לסעיפים 1, 2 ו-4 של הכלל. הערת ההגנה על אזור
ההגדרות יושבת מעל `renderSettingsPanel`.

---

<!-- SHARED:start id="capability-matrix" -->
## ⭐ מטריצת היכולות של ארבע האפליקציות (סבב 27, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> **מה שנבדל בין האפליקציות — נבדל בכתב, בטבלה אחת.** סבבים 17–26 חזרו
> ומצאו שוב ושוב הבדלי יכולת שאיש לא החליט עליהם ואיש לא רשם אותם: אחת
> עובדת אופליין והשנייה לא, לאחת כניסה מרובת-משתמשים ולשנייה מטמון של
> משתמש אחד, לאחת קובץ התקנה ולשנייה שדרוגים בלי מקור אמת. בכל אחד
> מהמקרים ה**גילוי** עלה סבב שלם. התשובה לשאלה «מה ההבדל בין
> האפליקציות» צריכה להיות **כתובה**, לא משהו שמחפשים בקוד.

⚠️ **הטבלה נמדדה מהקוד ולא הוצהרה** — כל תא אומת מול `index.html`,
`sw.js`, `migrations/`, `signing/` ו-`android/` של האפליקציה שלו.
⛔ **ומאותה סיבה אין לעדכן אותה «לפי ההיגיון»:** סבב שמשנה יכולת מודד
מחדש את התא שהוא שינה ומעדכן אותו **בארבעת הקבצים באותו סבב** (כלל
ברזל 8 סעיף 3).

### שלושת הערכים — ומה ההבדל ביניהם
- **✅** — היכולת קיימת ופעילה.
- **❌** — היכולת **חסרה** באפליקציה שהיא כן רלוונטית לה. כל ❌ נושא
  **נימוק**, ואם הוא פער — גם **טריגר**: האירוע שמחייב לפתוח סבב.
- **לא רלוונטי** — אין לאפליקציה הזו את המושג שהיכולת מדברת עליו, ולכן
  אין מה לממש. ⛔ זה **אינו** «❌ מנומס»: ❌ אומר «חסר»; «לא רלוונטי»
  אומר «אין כאן שאלה כזו».

| # | יכולת | yoman-avoda | hanhala-ruchanit | schar-limud | gius |
|---|---|---|---|---|---|
| 1 | כניסה אופליין | לא רלוונטי | ✅ | ✅ | ✅ |
| 2 | עריכת נתונים אופליין | ✅ | ✅ | ✅ | ✅ |
| 3 | מודל משתמשים | אין | רב-משתמשים | רב-משתמשים | רב-משתמשים |
| 4 | נתיב עדכון חלקי למראת המשתמשים | לא רלוונטי | ✅ | ❌ | ✅ |
| 5 | סוד במכשיר | אין | טביעה | טביעה | טביעה |
| 6 | פינוי אוטומטי | ✅ | ✅ | ❌ | ✅ |
| 7 | אימות פינוי מול הענן | ✅ | ✅ | לא רלוונטי | ❌ |
| 8 | שיתוף קבצים | ✅ | ❌ | ❌ | ❌ |
| 9 | מעטפת APK (WebView) | ✅ | ✅ | ✅ | ✅ |
| 10 | מפתח חתימה קבוע בריפו | ✅ | ✅ | ✅ | ✅ |
| 11 | מקור אמת יחיד לסכימה | ✅ | ✅ | ✅ | ✅ |
| 12 | קובץ התקנה מלא | ✅ | ✅ | ✅ | ✅ |
| 13 | גיבוי יומי אוטומטי | ✅ | ✅ | ✅ | ✅ |
| 14 | יומן פעולות | ✅ | ✅ | ✅ | ✅ |
| 15 | נתונים בטבלאות מובנות | ✅ | ❌ | ✅ | ✅ |
| 16 | שער תקינות JS | ✅ | ✅ | ✅ | ✅ |
| 17 | חלון חם במכשיר | ✅ | ❌ | ✅ | ✅ |
| 18 | שחזור מקומי מהענן | ✅ | ✅ | ✅ | ✅ |
| 19 | מסך שינוי סיסמה עצמי | לא רלוונטי | ✅ | ❌ | ✅ |
| 20 | מטמון-CDN מראש עם ריפוי עצמי | ✅ | ✅ | ✅ | ✅ |
| 21 | גיבוי יומי מטבלאות מובנות | ✅ | ❌ | ✅ | ✅ |
| 22 | פינוי גיבויים אוטומטי במסד | ✅ | ✅ | ✅ | ✅ |
| 23 | מנוע מיזוג עם הגנת ⏳ | ✅ | ✅ | ✅ | ✅ |
| 24 | חסימת משתמש מושבת בכניסה אופליין | לא רלוונטי | ✅ | ✅ | ✅ |
| 25 | מודול מזהי רשומות | ✅ | ✅ | ✅ | ✅ |
| 26 | מזהה מכשיר | ✅ | ✅ | ✅ | ✅ |
| 27 | ליבת מיזוג משותפת | ✅ | ✅ | ✅ | ✅ |
| 28 | מעטפת WebView חתומה | ✅ | ✅ | ✅ | ✅ |
| 29 | אימות מול טביעה בענן | לא רלוונטי | ✅ | ✅ | ✅ |
| 30 | בניית APK אחידה עם שער חתימה | ✅ | ✅ | ✅ | ✅ |
| 31 | מודול ה-service worker | ✅ | ✅ | ✅ | ✅ |
| 32 | ניסיון חוזר בתור הסנכרון | ✅ | ✅ | ✅ | ✅ |
| 33 | מנגנון משיכה אחיד | ✅ | ✅ | ✅ | ✅ |
| 34 | נעילת חוסר-פעילות | לא רלוונטי | ✅ | ✅ | ✅ |
| 35 | מודל הסשן — בזיכרון בלבד | לא רלוונטי | ✅ | ✅ | ✅ |
| 36 | בדיקת עדכון תקופתית ל-service worker | ✅ | ✅ | ✅ | ✅ |
| 37 | מסך התקנה עם המתנה אוטומטית | לא רלוונטי | ✅ | ✅ | לא רלוונטי |
| 38 | רענון תקופתי של מונה הממתינים | ✅ | ✅ | ✅ | ✅ |
| 39 | פסק זמן אחיד לקריאות רשת | ✅ | ✅ | ✅ | ✅ |
| 40 | מאזיני מצב רשת | ✅ | ✅ | ✅ | ✅ |
| 41 | גריעת tombstones לפי גיל | ✅ | ❌ | ❌ | ❌ |
| 42 | אוטו-אפדייט מ-raw.githubusercontent | ✅ | ❌ | ❌ | ❌ |

### הנימוקים — שורה אחר שורה
⚠️ **הנימוקים עברו ל-`tools/_capability-notes.md` בסבב 49** — שורה אחר
שורה, **כלשונם ובלי לגרוע דבר**, כדי להחזיר את `CLAUDE.md` אל תוך תקציב
התיעוד של כלל ברזל 18. ⛔ **הטבלה עצמה נשארה כאן** מפני שהשערים קוראים
ממנה (`check-capabilities` · `test_round37_matrix`), והנימוקים אינם
נקראים ע"י אף שער — כלומר המעבר אינו משנה שום אכיפה.
⛔ **סבב שמשנה תא במטריצה מעדכן גם את הנימוק שלו שם**, בארבעת הריפו
ובאותו סבב (כלל ברזל 8 סעיף 3) — ⛔ נימוק שנשאר מאחור הוא בדיוק
«מטריצה שאינה תואמת לקוד» שכלל ברזל 12 אוסר.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-11-comments" -->
## ⭐ כלל ברזל 11 — תקן ההערות בקוד (סבב 28, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את עשרת כללי הברזל שלמעלה.

> **הערה על אותו סוג דבר נראית אותו דבר בארבע האפליקציות.** כלל ברזל 7
> דרש עד היום זהות **תוכן** — הבלוקים המשותפים זהים בית-לבית. מעכשיו
> נדרשת גם זהות **צורה** באזורים הפרטיים: כותרת בלוק אחת, שלושה סימנים,
> תבנית איסור אחת ומילון מונחים אחד — כדי ששתי אפליקציות זו לצד זו
> ייקראו כשני עותקים של אותו ספר, ולא כשני פרויקטים של שני אנשים.

### הבעיה שנמדדה
סבב 27 הוסיף תיעוד פנימי ל-gius, ומדד אגב כך שהאחיות נבדלות זו מזו
**לא באחוזים אלא בכיסוי ובצורה**: אזורים לוגיים שלמים בלי שום כותרת,
הערות ⛔ שאומרות «אל תעשה» בלי לומר **מתי זה נלמד ולמה**, סימנים
שהומצאו לצורך שורה אחת, ואותו מושג בשלושה כתיבים.
⛔ **והנזק אינו אסתטי: הערת איסור בלי סבב ובלי סיבה היא הזמנה להסיר
אותה.** הסשן הבא קורא «אל תעשה X», אינו מוצא הצדקה, ומסיק שזו קפריזה —
ומחזיר בדיוק את הבאג שהאיסור בא למנוע.

### ששת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם.

1. **כותרת בלוק לכל אזור לוגי, בפורמט אחד ויחיד.**
   *(נלמד בסבב 27 ב-gius, והורחב לארבעתן בסבב 28.)* המסגרת היא **בדיוק
   74 תווי `═`**, שורת השם היא שלושה רווחים ואחריהם שם האזור (עד 78
   תווים), ואחריה מסגרת נוספת — גוף אופציונלי — ומסגרת סוגרת:

   ```
   /* ══…(74)══
      <שם האזור>
      ══…(74)══
      [גוף אופציונלי — נימוקים, ⛔, ⚠️]
      ══…(74)══ */
   ```

   ⚠️ **הרוחב הקבוע אינו קישוט:** הוא מה שהופך «אותו דבר» למדיד. מסגרת
   ברוחב אחר נראית לעין כמעט זהה, ולכן היא נסחפת בשקט — בדיוק הסחיפה
   שכלל ברזל 7 נבנה נגדה.

2. **שלושה סימנים בלבד — ⛔ איסור · ⚠️ אזהרה · ⭐ החלטה מרכזית.**
   *(נלמד בסבב 28.)* ⛔ **ואין להמציא סימנים נוספים.** סימן רביעי אינו
   מוסיף מידע — הוא מוריד את המשקל של השלושה: קורא שרואה חמישה סמלים
   שונים מפסיק להתייחס לאף אחד מהם כאל עצירה.
   ⚠️ הכלל חל על **פתיחת שורת הערה**. ✅/❌/⏳ בתוך משפט, או בתוך מחרוזת
   שמוצגת למשתמש, אינם סימון — הם טקסט.

3. **כל ⛔ נושא את הסבב שבו נלמד ואת הסיבה** — בתבנית
   **«⛔ אל תעשה X (סבב N) — הסיבה»**. *(נלמד בסבב 28.)* שני החלקים
   נדרשים **יחד**: הסבב הוא הדרך למצוא את הרקע המלא בלי לחפש, והסיבה
   היא מה שמונע מהסשן הבא לבטל את האיסור בתום לב.
   ⚠️ איסור שקדם למספור הסבבים נרשם כ«(נרשם בסבב N)» — האמת היא שהוא
   **תועד** אז, לא שנולד אז, ואין לכתוב מספר סבב שלא היה.

4. **טרמינולוגיה אחידה — אותו מונח עברי לאותו מושג בארבעתן.**
   *(נלמד בסבב 28.)* **מראה** (לא «מירור») · **טביעה** (לא «טביעת
   אצבע») · **אופק הפינוי** · **שער** · **פינוי** · **דחיפת-מצב**
   (עם מקף) · **מטמון** (לא «קאש») · **אופליין** · **tombstone**
   באנגלית. ⚠️ שני כתיבים לאותו מושג הופכים כל חיפוש טקסטואלי בין
   הריפו ל**חלקי** — וחיפוש חלקי הוא בדיוק איך שנשמט נתיב כתיבה שני
   (כלל ברזל 10 סעיף 5).

5. **כל החלטה ארכיטקטונית שנראית שרירותית מנומקת במקומה.**
   *(נלמד בסבב 27 ב-gius.)* קבוע שנראה שרירותי (רצפת גיל, תקרה,
   השהיה), טבלה שמוחרגת מרשימה, סדר שתי שורות שאסור להפוך — הנימוק
   יושב **ליד הקוד**, ולא רק ב-CLAUDE.md. ⚠️ מי שעורך את הקוד אינו
   קורא שלושים פרקי סבבים לפני שהוא נוגע בשורה.

6. **⛔ הבלוקים המשותפים והמודולים הקפואים אינם באזור התחולה.**
   *(נלמד בסבב 28.)* חמשת הבלוקים של סבבים 11–17 זהים בית-לבית
   ונשמרים ע"י `check-status-area.mjs`; עריכת הערה בתוכם מחייבת שינוי
   בארבעת הריפו **ובחתימות**. כלומר תקן שהיה חל עליהם היה **מייצר
   בעצמו** את הסחיפה שהוא בא למנוע. התקן חל על **הקוד הפרטי בלבד**.

### הבדיקה האוכפת
`tools/check-comments.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא
לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם
שערי התחביר לפני כל דחיפה. היא נכשלת על ארבעה סוגי סטייה:
- **א. כותרת בלוק** — כותרת שצורתה אינה תקנית (רוחב המסגרת, שורת השם),
  או **רצף של יותר מ-400 שורות קוד פרטי לא-ריקות בלי כותרת**.
  ⚠️ הכיסוי נמדד כמרווח בין כותרות ולא כ«כותרת לכל פונקציה»: אזור לוגי
  הוא רעיון, לא הצהרה. והמרווח נספר ב**שורות קוד פרטי לא-ריקות** ולא
  במספרי שורות — בין שני אזורים יושבים HTML ו-CSS, וספירה גולמית הייתה
  מדווחת «1700 שורות בלי כותרת» על אזור שאין בו קוד כלל.
- **ב. ⛔ בלי סבב או בלי סיבה** — בלוק הערה שיש בו ⛔ חייב לשאת
  `סבב <N>` **וגם** מפריד « — » שאחריו הנימוק.
- **ג. סימן שאינו מהרשימה** — שורת הערה שנפתחת בסמל שאינו ⛔/⚠️/⭐.
- **ד. טרמינולוגיה** — מונח מהרשימה בכתיב שאינו התקני.

⚠️ **הבדיקה קוראת הערות בלבד, דרך טוקניזציה של JS** (מחרוזות, תבניות
ו-regex). ⛔ **אין להחליף אותה בביטוי רגולרי על `//` ועל פתיחת בלוק:**
כל URL מכיל `//`, ולכן מחרוזת הייתה נקראת כהערה — `'✅ נשמר'` היה נספר
כסימן אסור, וביטוי רגולרי שמכיל כוכבית היה פותח בלוק שלא נסגר.

### ⛔ מה התקן אינו
**אינו יעד אחוזים.** ⛔ אין להשוות יחסי הערות בין האפליקציות, ואין
להמציא הערות כדי להגיע למספר. הדרישה היא **כיסוי**: כל אזור לוגי, כל
החלטה ארכיטקטונית וכל באג שתוקן — מכוסים, באותה צורה, בארבעתן.
⚠️ ואחוז גבוה אינו ראיה לכיסוי: קובץ יכול להיות מלא בהערות שחוזרות על
שם הפונקציה, ולא לענות על אף אחת מארבע השאלות שהתקן שואל.

<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-12-capabilities" -->
## ⭐ כלל ברזל 12 — יכולת משותפת נמדדת בשלושה ממדים (סבב 30 — השלמה, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את אחד-עשר כללי הברזל שלמעלה.

> **יכולת שמוכרזת «משותפת» מוגדרת בשלושה ממדים, לא באחד: ליבה זהה
> בית-לבית · **נקודת הפעלה זהה** · רישום תואם במטריצת היכולות.
> ⛔ שני מימושים שונים לאותה יכולת הם פער — גם כששניהם עובדים היום.
> הפער מתגלה רק כשאחד מהם נשבר, וזה בדיוק מאוחר מדי.**

### הבעיה שנמדדה
סבב 30 חילץ את הגיבוי היומי לבלוק משותף אחד, זהה בית-לבית בארבעתן —
כלומר קיים את כלל ברזל 7 במלואו. ובכל זאת התברר שהגיבוי של
hanhala-ruchanit **הפסיק לרוץ ב-2026-07-20** ואיש לא ידע:

| | היכן הופעל הגיבוי | התוצאה |
|---|---|---|
| schar-limud | בכניסה למערכת (`enterApp`) | ✅ רץ בכל יום שבו מישהו נכנס |
| hanhala-ruchanit | **מתוך מסלול הדחיפה** (`ysPushToCloud`) | 🔴 25 יום בלי גיבוי |

ההבדל אינו באג בקוד — **הקוד עצמו היה תקין בשתיהן.** ההבדל הוא היכן
הוא מופעל: בהנהלה הגיבוי רץ רק כשמישהו דחף אחד מארבעת המפתחות שהוא
גיבה. בתקופה סטטית — חופשה, שבוע בלי עריכות — אין דחיפה, אין גיבוי,
ואין שום סימן. ⛔ **מנגנון בטיחות שתלוי בפעילות רגילה אינו מנגנון
בטיחות:** הוא נעצר בדיוק בתקופה שבה תקלה עוברת בלי שאיש שם לב.

⚠️ **וזה נמדד ולא הונח:** `sync_log` מראה **444 פעולות ב-30 יום ו-47
בשבוע** באותה תקופה — האפליקציה הייתה בשימוש מלא. תשע מפתחות נוספים לא
גובו **38 יום**. כלומר לא «אף אחד לא נכנס», אלא «אף אחד לא דחף את
המפתחות הנכונים».

### שלושת הממדים
1. **ליבה זהה בית-לבית** — מה שכלל ברזל 7 כבר דורש, ומה
   ש-`check-status-area.mjs` כבר אוכף.
2. **⭐ נקודת הפעלה זהה.** לכל יכולת משותפת יש **תפקיד** קבוע שממנו היא
   מופעלת, זהה בארבעתן, גם כששם הפונקציה נבדל:
   - **יכולות עלייה** (`lsBoot` · `pendBoot` · `bkBoot`) — **פונקציית
     העלייה של האפליקציה**, זו שכבר קוראת לשתי האחרות.
   - **יכולות מסך** (`statusAreaMount` · `bkStatusMount`) — פונקציית
     מסך ההגדרות.
   ⛔ **ואין נקודת הפעלה שנייה.** קריאה נוספת לאותה יכולת ממקום אחר היא
   בדיוק הסטייה הזו בהתהוותה, גם אם היא «רק ליתר ביטחון».
3. **רישום תואם במטריצה** — יכולת שמסומנת ✅ קיימת בקוד, ומה שקיים בקוד
   מסומן ✅. ⛔ מטריצה שאינה תואמת לקוד גרועה מהיעדר מטריצה: היא נקראת
   כעדות.

### ⛔ מעטפת דקה אינה «נוחות» — היא נקודת הפעלה שנייה
כשיכולת נקראת דרך פונקציה מקומית שרק עוטפת אותה (`ysMaybeDailyBackup`,
`slMaybeDailyBackup`), **כל מקום בקוד יכול לקרוא לה** — והכלל שקובע
נקודת הפעלה אחת הופך להמלצה. לכן המעטפות הוסרו, והמודול חושף **קריאה
אחת** שקוד האפליקציה רשאי לעשות. הפונקציה הפנימית נשארת חשופה לבדיקות
ולמודול עצמו, ו-`check-capabilities.mjs` נכשל על כל קריאה אליה מקוד
האפליקציה.

### ⚠️ התרעה על מנגנון שקט שנעצר — `BK_STALE_DAYS`
המדד השני שהיה חסר: **גם אחרי שהגיבוי מופעל נכון, אין דרך לדעת שהוא
באמת רץ.** לכן «מידע טכני ›» מציג «גובה לאחרונה», ומעל
**`BK_STALE_DAYS` = 3 ימים** נוספת שורת התרעה.
⚠️ **הסף הוא שלושה ימים ולא יום אחד** — הדגל היומי אומר שכשל בודד הוא
רעש לגיטימי (מכשיר אופליין, סוף שבוע), ואילו שלושה ימים הם **דפוס**.
עם הסף הזה, ההשבתה בת 25 הימים הייתה מתגלה ביום הרביעי.
⛔ **ולא התראה קופצת** — סבב 15 קבע ששקט הוא ברירת המחדל באזור המצב,
וההתרעה יושבת במקום שבו מסתכלים כשבודקים.

### הבדיקה האוכפת
`tools/check-capabilities.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs`
קורא לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה
עם שערי התחביר לפני כל דחיפה. היא נכשלת על שלושה סוגי סטייה:
- **א. ליבה** — הבלוק המשותף של היכולת אינו תואם לחתימה הקנונית.
- **ב. נקודת הפעלה** — פונקציית החיווט אינה נקראת מתוך הפונקציה
  הקנונית, נקראת **גם** ממקום אחר, או שפונקציה אסורה (`bkMaybeDaily`)
  נקראת מקוד האפליקציה.
- **ג. מטריצה** — יכולת שמסומנת ✅ אינה קיימת בקוד, או להפך.

⚠️ **הסריקה נעשית על קוד בלבד**, אחרי טוקניזציה שמנקה הערות ומחרוזות.
⛔ אין להחליף אותה בחיפוש טקסטואלי: שם הפונקציה מופיע גם ברשימת ה-API
שבהערת המודול, וכל אזכור בתיעוד היה נספר כקריאה.

### ⛔ מנגנון פיצוי נבדק מחדש כשהמבנה שהוליד אותו תוקן (סבב 32)
**מנגנון שנולד כדי לפצות על מבנה או על מגבלה — נבדק מחדש כשהמבנה מתוקן,
ומוסר אם אינו נחוץ עוד.** ⛔ מנגנון פיצוי ששרד את הסיבה לו הוא **חוב טכני
שנראה כמו ארכיטקטורה**: הוא עבר ביקורת פעם אחת, הוא מתועד, והוא נראה
כהחלטה — ולכן איש אינו שואל עליו שוב.

⚠️ **וזה ממד רביעי ולא חזרה על השלושה שלמעלה.** הם שואלים «האם היכולת
מיושמת אותו דבר בארבעתן»; זה שואל **«האם היא עדיין נחוצה בכלל»**. יכולת
יכולה להיות זהה בית-לבית, מופעלת מאותה נקודה, ורשומה נכון במטריצה — ועדיין
לפצות על בעיה שנפתרה לפני שני סבבים.

**שלוש הדוגמאות שהולידו את הסעיף**, וכולן נמדדו:
- **סבב 24** — `SETUP_SQL_FALLBACK` ב-schar-limud: עותק מוטבע של הסכימה
  שנועד לכסות על כך שאין קובץ התקנה. משנוצר הקובץ, העותק נשאר — מקור אמת
  שני שמתיישן בשקט.
- **סבב 30** — הגיבוי היומי הופעל ממסלול הדחיפה, פיצוי על כך שלא הייתה
  נקודת עלייה משותפת. משנוצרה `bkBoot`, החיווט הישן היה נשאר ומשבית את
  הגיבוי 25 יום.
- **סבב 32** — שתי הדוגמאות החיות: **הארכוב ביומן** (פיצוי על כך שכל
  היומן נמשך כערך `kv` יחיד — בוטל כשהטבלאות המובנות אפשרו שליפה
  מסוננת), ו**הסכימה המוטבעת בהנהלה** (אותו דפוס של סבב 24, ריפו אחר).

⚠️ **הבדיקה היא שאלה, לא סריקה אוטומטית** — ולכן היא נשאלת בכל סבב שמשנה
מבנה: *מה נבנה כדי לפצות על המבנה הזה, והאם הוא עדיין נחוץ?* ⛔ והתשובה
נרשמת גם כשהיא «כן, ומהסיבה הזו» — שקט כאן נקרא כ«לא נשאל».
⚠️ **וההסרה אינה מיידית:** המנגנון הישן נשאר כנתיב חזרה, עם **טריגר כתוב**
לכיבויו ולמחיקתו — בדיוק כפי שסבב 30 קבע לכתיבה הכפולה.

**שינוי מכוון ביכולת משותפת** = עדכון בארבע האפליקציות **ובארבעת עותקי
קובץ הבדיקה** (החתימות ונקודות ההפעלה כתובות בתוכו), באותו סבב.
<!-- SHARED:end -->

### היישום כאן (schar-limud)
**`slBoot()` היא פונקציית העלייה** (מטפל ה-`DOMContentLoaded` קיבל שם —
בלי שם אין נקודת הפעלה שהבודק יכול לאכוף): `lsBoot()` · `pendBoot()` ·
`bkBoot()` · `slDropLegacyPassHash()` יושבות בה. ⛔ `slMaybeDailyBackup`
הוסרה (סבב 30 השלמה) — גם מעטפת דקה היא נקודת הפעלה שנייה בפוטנציה.

---

<!-- SHARED:start id="backup-module" -->
## ⭐ מודול הגיבוי היומי ויומן הפעולות (סבב 30)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

הגיבוי היומי ויומן הפעולות הם בלוק קוד משותף, זהה בית-לבית בארבע
האפליקציות, בין `/* ═══ גיבוי יומי ויומן פעולות` לבין
`סוף מודול הגיבוי היומי`. מעליו יושבת `BK_CFG` — הדבר היחיד שנבדל.
**ה-API:** `bkBoot` · `bkMaybeDaily` · `bkLastAt`/`bkStale` ·
`bkStatusHTML`/`bkStatusMount` · `logAction`/`logFlush` · `bkToday`/`bkSig`.

- ⛔ **נקודת ההפעלה היחידה היא `bkBoot()` מפונקציית העלייה** (כלל ברזל 12) —
  קריאה ל-`bkMaybeDaily` מקוד האפליקציה אסורה, והבודק נכשל עליה.
  `logFlush` היא החריג המותר: ריקון תור חוזר, מותר על חזרת רשת או כניסה
  מוצלחת.
- **הליבה:** נעילת ריצה, דגל-יומי שנכתב **אחרי** ההצלחה, ובדיקת `error`
  מפורשת על ה-`select` ועל ה-`insert`. ⛔ אין להחזיר כתיבת-דגל-מראש
  (סבב 6) — גיבוי שנכשל היה מדלג בשקט על יממה שלמה.
- ⭐ **גיבוי דיפרנציאלי** — ערך שלא השתנה מאז הגיבוי האחרון אינו נכתב שוב,
  לפי חתימה מקומית (אורך + FNV-1a) שאינה נוגעת ברשת. ⚠️ זה תנאי קיום ולא
  קיצור דרך: כתיבה יומית עיוורת של הערך הגדול בארגון (~1.2MB) הייתה
  מוסיפה ~36MB בחודש. ⛔ ובספק — כותבים: חתימה חסרה או מכשיר חדש מביאים
  לכתיבה מלאה.
- ⭐ **מדיניות שמירה — עותק יומי נשמר 30 יום** (`BK_RETENTION_DAYS`,
  השלמת סבב 35): אחרי גיבוי מוצלח נגרעים מ-`kv_backup` עותקים ישנים
  מזה, לפי **רשימת-היתר של מפתחות** שנגזרת מ-`BK_CFG.sources()` —
  ⛔ לא לפי קידומת (השלמת סבב 35) — קידומת ריקה (הנהלה) הייתה תופסת גם
  מפתחות של אפליקציה אחרת. נכשלת סגור ואינה מפילה את הגיבוי; `retention`
  נרשם ליומן רק כשנמחק משהו בפועל.
- ⭐ **הפינוי עצמו רץ במסד, לא בלקוח** (סבב 35ג) — משימת `pg_cron` יומית
  שמוחקת עותקים בני 30+ יום לפי **רשימת-היתר מפורשת של מפתחות**, בדיוק
  אותה רשימה מבנית שב-`BK_CFG.sources()`. ⚠️ **שתי השכבות מכוונות ואינן
  כפילות:** ל-`kv_backup` יש `insert`+`select` בלבד לשני התפקידים (סבב 29,
  אומת מול שני הפרויקטים) ו⛔ אין לפתוח לה `delete` ל-`anon` — כלומר
  **המסד מפנה, והלקוח לא יכול ולא צריך**. `_bkRetention` שבמודול נשארת
  שכבה שנכשלת-סגור: מנסה, נדחית, ומדלגת בשקט בלי להפיל את הגיבוי.
  ⛔ אין להסיר אותה מהקוד (סבב 35ג) — היא מה שיפעל אם תוענק הרשאה
  סקופית, והיא מה שהבדיקות אוכפות. ⭐ **שתי המיגרציות הורצו ואומתו
  ב-2026-08-18** (סבב 36): `hanhala-ruchanit/migrations/004_backup_retention_cron.sql`
  (הפרויקט המשותף — חלה גם על yoman ועל schar) ו-`gius/migrations/0005_backup_retention_cron.sql`.
  המשימה `bk_retention_daily` פעילה בשני הפרויקטים בתזמון `'0 3 * * *'`,
  והריצה הראשונה במשותף פינתה 37 שורות ונרשמה ב-`sync_log`.
- ⛔ **סינון סודות** (השלמת סבב 35) — מפתח או שדה שברשימת
  `BK_CFG.secrets` אינו נכתב לגיבוי לעולם; הרשימה פרמטר פר-אפליקציה
  (בשכר: `SL_NEVER_MIRROR_SETTINGS`, שרוקנה — המנגנון דרוך גם כשהיא
  ריקה). הלקח: `admin_pass` שרד בגיבויים אחרי שנמחק מהמקור, ונוקה
  ידנית (17.8).
- ⛔ **טבלה שיש בה סוד מוחרגת מהגיבוי או נשלפת בעמודות מפורשות** (סבב 30) —
  `kv_backup` קריאה ל-`anon`, וגיבוי גורף של טבלת משתמשים היה מעתיק
  סיסמאות למקום שני.
- **היעד:** `kv_backup`, והיומן ל-`sync_log` — שתיהן `INSERT`+`SELECT`
  בלבד לשני התפקידים (כלל ברזל 10 סעיף 9). ב-gius הן חיות בפרויקט הנפרד
  שלה (`migrations/0004`), בסכימה זהה בית-לבית. ⚠️ רשימת האירועים שנרשמת
  ליומן היא פר-אפליקציה ואינה מיושרת — המשותף הוא המנגנון.
- **«גובה לאחרונה» והתרעת `BK_STALE_DAYS` = 3** מוצגים ב«מידע טכני ›»
  (כלל ברזל 12) — מנגנון שקט שנעצר נראה בדיוק כמו מנגנון שקט שעובד, ולכן
  חייבת להיות דרך לראות שהוא באמת רץ.
- ⛔ **גיבוי חד-פעמי לפני-פעולה** (קידומות `PRE_SYNC_UNIFY_`,
  `PRE_ROUND3B_`, `ORPHAN_`, `pre-delete-…`) **אינו נמחק אוטומטית
  לעולם** (סבב 30) — אינו מצטבר, והוא מחוץ לרשימת-ההיתר של מדיניות
  השמירה מעצם בנייתה, וכך גם מחוץ לרשימת-ההיתר של משימת ה-`pg_cron`.
  ⭐ הכלל (השלמת סבב 35): **באחריות יוצרו למחקו כשהפער שהוא מגן עליו
  נסגר** — לא באחריות המודול.
  ⚠️ **עובדה, ולא הנחה (סבב 35ג): 57 השורות שהיו בפרויקט המשותף תחת
  הקידומות האלה נמחקו ב-2026-08-18 ואינן ניתנות לשחזור** — הפרויקט על
  מסלול Free, שאינו כולל גיבויי פרויקט. מי שיחפש אותן לא ימצא אותן.
  ⛔ האיסור העקרוני נשאר בתוקף לכל גיבוי `PRE_*` **עתידי**.
<!-- SHARED:end -->

### היישום כאן (schar-limud)
⛔ **`sl_users` אינה ברשימת הגיבוי** (סבב 30) — היא מחזיקה `password`
בטקסט גלוי ואת הטביעות, ו-`kv_backup` קריאה ל-`anon`; שחזור משתמשים נעשה
ידנית ע"י המנהל, כמתואר בקובץ ההתקנה.

---

<!-- SHARED:start id="iron-rule-13-shared-scope" -->
## ⭐ כלל ברזל 13 — סבב שנוגע ב-SHARED נפתח עם גישה לארבעת הריפו (סבב 32ב, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שנים-עשר כללי הברזל שלמעלה.

> ⛔ **סבב שנוגע בבלוק SHARED נפתח מלכתחילה עם גישה לארבעת הריפו.**
> אי אפשר לצרף ריפו לסשן קיים, ולכן סשן שאין לו גישה לארבעתם **אינו
> נוגע בבלוקי SHARED** — הוא רושם את הצורך וסוגר.

### הבעיה שנמדדה
בסבב 32 השתנו שני בלוקי SHARED — `capability-matrix`
ו-`iron-rule-12-capabilities` — **בשניים מתוך ארבעת הריפו**: לסשן לא
הייתה גישה ל-schar-limud ול-gius, וניסיון לצרף אותם באמצע העבודה נחסם.
התוצאה היא בדיוק המצב שכלל ברזל 8 סעיף 3 אוסר — פרק «משותף» שאינו זהה
בארבעתם — ⚠️ ובצורתו המסוכנת ביותר: `check-docs.mjs` של שני הריפו שלא
עודכנו **המשיך לעבור** אצלם, מפני שהחתימות הישנות שלהם תאמו לתוכן הישן
שלהם. **החתימות הישנות הסתירו את הסטייה** — השער שנבנה לתפוס סחיפה בין
הריפו אישר אותה, כי הוא מודד כל ריפו מול העותק המקומי של הרשימה
הקנונית, ולא בין הריפו זה לזה.

### ⛔ ההשלכה המבצעית — לפני פתיחת הסבב, לא באמצעו
- סבב שמתכנן לגעת בבלוק SHARED — או שעלול לגעת בו: שינוי יכולת, כלל
  ברזל חדש, עדכון תא במטריצה — **נפתח מלכתחילה עם ארבעת הריפו בהיקף.**
- סשן שמגלה באמצע העבודה שנדרש שינוי SHARED ואין לו גישה לארבעתם —
  ⛔ **אינו משנה את הבלוק בחלק מהריפו.** הוא רושם את הצורך בפרק הסבב
  (מה צריך להשתנות, ולמה) וסוגר; השינוי עצמו נעשה בסבב שנפתח נכון.
  ⚠️ עדיף פער **גלוי** — בלוק שטרם עודכן באף ריפו — על פער **שקט** שהשער
  מאשר בשניים מארבעה.
- סבב 32 בחר בדיעבד בדרך השנייה — עדכן בשניים ורשם טריגר מפורט עם שתי
  החתימות — וזה מה שאיפשר לסבב 32ב לסגור את הפער בהעתקה בית-לבית.
  ⛔ אבל זו נסיבה מקילה, לא דפוס: הטריגר ישב בפרק פרטי של שני ריפו,
  ושום שער לא אכף אותו.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-14-org-wide" -->
## ⭐ כלל ברזל 14 — קיים באחת = קיים בארבעתן, או חריגה מנומקת (סבב 33, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שלושה-עשר כללי הברזל שלמעלה.

> ⛔ **יכולת, שער או מודול שקיים באפליקציה אחת — או שהוא קיים בארבעתן, או
> שהוא רשום כחריגה מנומקת** (במטריצת היכולות, או ברשימת-ההיתר של
> `check-structure.mjs`). **«קיים רק באחת, בשקט» אינו מצב חוקי.**

### הבעיה שנמדדה
`check-js.mjs` — השער שמחלץ את ה-JS המוטבע ומריץ עליו `node --check` — נכתב
ב-gius בסבב 11, **וחי שם לבדו עשרה סבבים**. בשלוש האחיות אותה עבודה בדיוק
נעשתה בהעתקה ידנית של שבע פקודות מתוך פרק «לפני כל push»: אותן בדיקות, בלי
שום דבר שמוודא שכולן רצו. ⛔ **ואיש לא החליט על ההבדל הזה** — לא «הוחל
בכולן», לא «אינו רלוונטי, וזה הנימוק», ולא «פער עם טריגר». הוא פשוט היה שם.

⚠️ **וזה לא היה יחיד.** אותה סריקה מצאה שרידים שישבו בשורש של כל ריפו בלי
שאיש החליט עליהם — שלושה קובצי SQL שתוכנם מכוסה במלואו ב-`migrations/`,
סקריפט בנייה שאף workflow אינו קורא לו, ו-`.well-known/assetlinks.json`
משתי אפליקציות שכבר אינן TWA. כל אחד מהם היה «נכון בזמנו», וכל אחד מהם
שרד את הסיבה שלו.

### ⛔ למה זה כלל נפרד ולא כלל ברזל 12
כלל ברזל 12 שואל שלוש שאלות על יכולת שכבר **מוכרזת** משותפת: ליבה זהה,
נקודת הפעלה זהה, רישום תואם. ⚠️ **הוא אינו שואל דבר על יכולת שמעולם לא
הוכרזה** — ובדיוק שם `check-js` שרד: הוא לא היה «משותף שנסחף», הוא היה
**פרטי שאיש לא שאל עליו**. כלל 14 סוגר את החור הזה: קיום באחת הוא בעצמו
השאלה, גם לפני שמישהו קרא לזה יכולת משותפת.

### שלוש התשובות המותרות — ורק שלוש
לכל רכיב שקיים באפליקציה אחת:
1. **הוחל בארבעתן** — עם ציון מה נכנס לכל אחת.
2. **חריגה מנומקת** — נרשמת **במקום שהשער קורא**: תא «לא רלוונטי»/❌ עם
   נימוק במטריצת היכולות, או שורה ברשימת-ההיתר של `check-structure.mjs`
   שנושאת את הסיבה. ⛔ נימוק ב-CLAUDE.md לבדו אינו נחשב — הוא אינו נאכף.
3. **פער מתועד עם טריגר** — מה חסר, ומה האירוע שמחייב לפתוח את הסבב.

⛔ **מה שאסור הוא הרביעית — קיום שקט, בלי אף אחת מהשלוש.**

### ⛔ פער שנסגר בפעולת מנהל — מי מוחק את שורתו
הכלל הרגיל הוא שהסבב שסוגר פער מוחק את שורתו **באותו קומיט**. ⚠️ הוא
אינו מכסה את המקרה השכיח כאן: פער שנסגר בפעולת **מנהל** — הרצת מיגרציה,
מחיקת מפתח מהמסד, החלפת סוד. הסשן אינו יכול למחוק שורה שבזמנו עדיין
פתוחה, והמנהל אינו עורך קבצים — ולכן השורה שורדת את הסיבה שלה, ונקראת
כמציאות.

⛔ **שורת פער שנסגרה בפעולת מנהל נמחקת בסבב הראשון שנוגע בקובץ, כסעיף
אחרון, ⛔ ובלי לפתוח סבב ייעודי** (נלמד בסבב 39). ⚠️ המחיקה מותנית
במדידה — ר' כלל ברזל 15 — ⛔ ולא בזיכרון של מי שביקש אותה.

### ⚠️ «אחיד» אינו «זהה» — והשער אוכף את הראשון
`check-js.mjs` זהה בית-לבית בארבעתן פרט לבלוק `APP`, אבל **תוכן `APP.rules`
נבדל בכוונה**: הוא נמדד מ-`sw.js` ומ-`index.html` של כל אפליקציה, וכל כלל
נושא את שם האפליקציה שלו. ⛔ **כלל שנכתב על מה שאינו קיים הוא
הצהרה ולא מדידה** — בדיוק הכשל שכלל ברזל 8 סעיף 4 אוסר, בציר אחר.
⚠️ **ומסבב 42ג יש כאן דוגמה חיה גם לכיוון ההפוך:** «ל-schar אין דף
אופליין» היה **נכון ונמדד** במשך תשעה סבבים, והליבה המשותפת ביטלה אותו —
כלומר נימוק של חריגה נבדק מחדש כשהמבנה שהוליד אותו השתנה (כלל ברזל 12,
הממד הרביעי). ⛔ נימוק שאיש אינו בודק מחדש הוא חריגה ששרדה את הסיבה לה.
כך גם המבנה: הסט הקנוני זהה, והחריגות (`setup-db.html` · `gen-icons.mjs` ·
`tools/fixtures`) יושבות ברשימת-היתר **עם נימוק לכל שורה**. ⚠️ `.nojekyll`
היה ברשימה הזו עד סבב 39 ועלה לסט הקנוני — חריגה שקיימת בשתיים מארבע היא
אסימטריה שלא הוכרעה, לא «חריגה מנומקת».

### הבדיקה האוכפת
`tools/check-structure.mjs` — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP`
שבראשה**, ורצה מתוך `check-js.mjs` לפני כל דחיפה. היא נכשלת על שלושה סוגי
סטייה:
- **א. תיקיות** — סט התיקיות בשורש אינו הסט הקנוני (`.github` · `android` ·
  `icons` · `migrations` · `signing` · `tools`). תיקייה חסרה **או עודפת**
  מפילה את השער.
- **ב. קובצי שורש** — קובץ שאינו ברשימה הסגורה המשותפת (`CLAUDE.md` ·
  `CONTEXT.md` · `README.md` · `index.html` · `manifest.json` · `sw.js`)
  ואינו ברשימת-ההיתר הפר-אפליקציתית.
- **ג. `tools/`** — שבעת הבודקים המשותפים חייבים להתקיים; כל קובץ אחר חייב
  להיות `test_round*` או חריגה מנומקת.
- **ד. `icons/` ו-`design/`** (סבב 50) — `icons/` מחזיקה **בדיוק** את ששת
  הנכסים הקנוניים (`apple-touch-icon` · `favicon-16` · `favicon-32` ·
  `icon-192` · `icon-512` · `icon-maskable-512`): חסר מפיל, ⛔ **ועודף
  מפיל גם כששמו תמים**. `design/` מחזיקה **קובץ מאסטר אחד** בשם
  `icon-master.svg` או `icon-master.png`, ⛔ ואף קובץ מוגש (`index.html`,
  `sw.js`) אינו מפנה אליה — הוא נכס עיצוב במגה-בייט, ולא משהו שהדפדפן
  מוריד. ⛔ **הסט נאכף, לא התמונה** — גודל, צבע ותוכן פיקסלים נגזרים
  מהעיצוב הייחודי של כל אפליקציה ואינם ניתנים ליישור.

⚠️ **ורשימת-היתר שהתיישנה מפילה גם היא:** חריגה רשומה שאינה קיימת בפועל
נכשלת. ⛔ אחרת הרשימה הייתה הופכת בעצמה לשריד — בדיוק סוג הדבר שהסבב הזה
בא לסלק.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-15-gaps-verified" -->
## ⭐ כלל ברזל 15 — פרק «פערים פתוחים» נאכף בשער (סבב 39, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את ארבעה-עשר כללי הברזל שלמעלה.

> ⛔ **כל שורת פער נושאת תג שאומר איך יודעים שהיא עדיין פתוחה.** תנאי
> שניתן למדוד מהריפו — השער בודק אותו ו**נופל כשהוא כבר אינו מתקיים**.
> תנאי שאינו ניתן למדידה — נרשם ככזה, **בכתב ועם נימוק**. ⛔ שורה בלי תג
> אינה מצב חוקי.

### הבעיה שנמדדה
פרק «פערים פתוחים» היה הפרק היחיד בקובץ ש**שום שער לא קרא**. התוצאה
נמדדה בסבב 39 עצמו: שורות תיארו פערים שנסגרו — מפתח `kv` שנמחק
מהמסד, מיגרציה שהורצה מחדש, ומסלול `rpc` שמעולם לא היה קיים — והן
נקראו כמציאות עד שמישהו בדק ידנית. ⚠️ **וזה בדיוק ההפך מכשל התיעוד
הרגיל:** לא «חסר תיעוד» אלא **תיעוד שממשיך לתאר עולם שהשתנה**, בפרק
שכל תפקידו לומר מה עוד פתוח.

### תבנית התג — ⛔ אחת, ובסוף השורה
כל פריט ברמה העליונה בפרק «פערים פתוחים» מסתיים באחד משני אלה:

- **`[מדיד: <תנאי>]`** — התנאי שמתקיים **כל עוד הפער פתוח**.
- **`[לא-מדיד: <נימוק>]`** — ⛔ עם נימוק אמיתי, לא «לא ניתן».

⚠️ **התג נדרש מפני שפרוזה אינה ניתנת לגזירה אמינה.** ⛔ ואין לנחש מתוך
הטקסט (סבב 39) — ניחוש היה מייצר שער שנופל על ניסוח ועובר על מציאות,
כלומר גרוע משער שאינו קיים.

**ארבעת התנאים המדידים, וזה הסט כולו:**

| תנאי | הפער פתוח כל עוד |
|---|---|
| `code:<מחרוזת>` | המחרוזת **מופיעה** ב-`index.html` |
| `no-code:<מחרוזת>` | המחרוזת **אינה מופיעה** ב-`index.html` |
| `file:<נתיב>` | הקובץ **קיים** |
| `no-file:<נתיב>` | הקובץ **אינו קיים** |

### שלושת הכללים
1. **⛔ תנאי מדיד שחדל להתקיים מפיל את השער.** זו כל הנקודה: הפער נסגר,
   והשורה חייבת להימחק (כלל ברזל 14, «פער שנסגר בפעולת מנהל»).
2. **⛔ הבודק מדווח ואינו מוחק.** מחיקה אוטומטית של תיעוד היא בדיוק סוג
   הפעולה שאין ממנה חזרה בלי לקרוא git, והיא גם עוקפת את שיקול הדעת
   שקובע **מה** לכתוב במקום. השער אומר «השורה הזו נסגרה» ועוצר.
3. **⛔ `לא-מדיד` אינו פטור שקט.** הוא רישום מפורש שנקרא בעין, ומספרו
   נספר ומוצג. ⚠️ שורה שסומנה `לא-מדיד` רק כדי לעקוף את השער היא בדיוק
   הכשל שכלל ברזל 8 סעיף 4 אוסר, בציר אחר.

### ⭐ שני חישוקים על העוגן — ⛔ בלי שניהם השער נכשל בכיוון המסוכן
המנהל מדד: שינוי **רווחים בלבד** באותה שורה — `X = true` → `X  =  true` —
הפיל את השער עם «⭐ הפער נסגר», בזמן שהקוד **לא השתנה**. ⛔ זה כשל בכיוון
המסוכן: הודעה שמכריזה שפער נסגר היא הודעה שמזמינה מחיקת שורת תיעוד.

1. **⚠️ נרמול רווחים לפני ההשוואה.** רצף רווחים, טאבים ושורות חדשות מתקפל
   לרווח יחיד **בשני הצדדים**. ⛔ שמות, מרכאות ותוכן **אינם** מנורמלים —
   נרמול שלהם היה הופך את העוגן לשער שעובר על קוד אחר לגמרי.
2. **⭐ עוגן בסיס לכל תג `code:` — שם הסמל לבדו.** לא נמצא העוגן המלא,
   ⛔ **והסמל עצמו אינו קיים** ⇒ **«⚠️ העוגן נרקב — דורש בדיקה ידנית»**,
   ⛔ **ולעולם לא «הפער נסגר»** (סבב 39): סמל שנעלם — שינוי שם, מחיקה,
   ריפקטור — אינו ראיה שהפער נסגר אלא שהעוגן הפסיק להצביע על משהו,
   וההכרעה חוזרת לבן-אדם. נמצא הסמל ולא נמצא העוגן המלא ⇒ הפער אכן נסגר,
   וההודעה אומרת גם שעוגן הבסיס כן נמצא.
   ⚠️ מילות מפתח מדולגות בגזירת הסמל — הסמל ב-`function migrateOutbox`
   הוא `migrateOutbox`.

### הבדיקה האוכפת
`tools/check-gaps.mjs` — **זהה בית-לבית בארבעת הריפו** (אין בה בלוק `APP`;
היא קוראת את `CLAUDE.md` שלצידה), ורצה מתוך `check-js.mjs` לפני כל דחיפה.
היא נכשלת על ארבעה סוגי סטייה:
- **א. תג חסר** — פריט ברמה העליונה בלי `[מדיד:…]` ובלי `[לא-מדיד:…]`.
- **ב. תנאי שאינו מוכר** — `מדיד` עם תנאי שאינו מארבעת הסוגים שבטבלה.
- **ג. ⚠️ העוגן נרקב** — הסמל שבתג `code:` אינו קיים ב-`index.html`.
- **ד. ⭐ פער שנסגר** — התנאי המדיד כבר אינו מתקיים, והסמל כן קיים.

⚠️ **הפרק כולו אופציונלי** — ריפו בלי «פערים פתוחים» עובר. ⛔ אבל פרק
שקיים נבדק במלואו.
<!-- SHARED:end -->

**כאן:** `APP.rules` של `tools/check-js.mjs` נמדדו מהקבצים של האפליקציה
הזו — מסבב 35 יש כלל `mode:'cors'` וכלל נעילת ה-CDN (הרשימה נוספה
ל-`sw.js` באותו סבב); אין כלל דף-אופליין, כי אין כאן דף אופליין.
⭐ אין אף חריגה ברשימות `check-structure.mjs` — הריפו היחיד מהארבעה שכך.

---

<!-- SHARED:start id="sw-strategies" -->
## ⭐ מודול ה-service worker — המדידה והאיחוד (סבבים 40 ו-42ג)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> ⛔ **`sw.js` הוא הקובץ המסוכן ביותר אחרי `index.html`, ויש לו ליבה
> משותפת אחת** — הוא קובע מה המשתמש רואה אופליין. ⛔ **קודם כול מדידה,
> ורק אחר כך הכרעה**, וכל סטייה בין הארבע היא **ידית ב-`SW_CFG`**
> ולא קוד שנבדל.

### ⭐ המצב מסבב 42ג — ליבה אחת, פרמטרים מדודים
`sw.js` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ מודול ה-service worker — מודול משותף (סבב 42ג)` לבין
`/* ═══════════════ סוף מודול ה-service worker`. **הוא זהה בית-לבית
בארבעתן** (294 שורות, חתימה `568ecbd986b69a14`), נאכף
ב-`check-capabilities.mjs`, ורשום במטריצה (שורה 31). מעליו יושבת
**`SW_CFG`** — הדבר היחיד שנבדל — לצד `CACHE_NAME`, `CORE`,
`CDN_ASSETS` ו-`SW_OFFLINE_HTML`.

**האסטרטגיה המאוחדת:** רשת-קודם עם נפילה-חזרה מדורגת — רשת ⇐ מטמון ⇐
(בניווט) הקליפה ⇐ דף אופליין; ובתת-משאב ⇐ **שגיאת רשת אמיתית**, ⛔ לעולם
לא HTML. ⛔ **ושמירה רק של תשובה שאומתה** — `ok && 200 && !opaque`.

**ידיות `SW_CFG` — ⛔ כל אחת מהן היא התנהגות שנמדדה בסבב 40 ונשמרה,
ולא ברירת מחדל שנפלה מאליה** (בדיוק כמו ידיות המדיניות של `mergeCore`,
סבב 38):

| ידית | yoman | hanhala | schar | gius |
|---|---|---|---|---|
| `prefix` | `yoman-avoda-` | `hanhala-ruchanit-` | `schar-limud-` | `gius-` |
| `skipHosts` | `raw.githubusercontent.com` | — | — | — |
| `cdnHosts` | — | — | — | `cdn.jsdelivr.net` |
| `scoped` | false | false | false | **true** |
| `navFallback` | request | request | request | **shell** |
| `navIgnoreSearch` | false | **true** | false | false |
| `subStrategy` | network-first | network-first | network-first | **cache-first** |
| `subMiss` | error | error | error | **504** |
| `offlineStatus` | 503 | 503 | 503 | **200** |
| `skipWaiting` | true | true | true | **false** |

⛔ **ארבע הידיות שסבב 40 סימן כסטיות מכוונות — לא יושרו** (סבב 42ג):
- **`skipWaiting` ב-gius** — נעדר בכוונה: הדף מציג «🔄 גרסה חדשה זמינה»
  והמשתמש מחליט מתי לעדכן. ⛔ אין ליישר בלי החלטת מנהל — הוא משנה מתי
  גרסה חדשה נכנסת לתוקף.
- **דילוג `raw.githubusercontent` ביומן** — רק ליומן יש אוטו-אפדייט
  שמושך משם, וכלל שנכתב על מה שאינו קיים הוא הצהרה ולא מדידה.
- **`navIgnoreSearch` בהנהלה** — ⛔ **ורק בניווט**: שימוש כללי בו גרם
  שם לכניסה בזהות זרה, מפני שפילטרי PostgREST יושבים ב-query.
- **`scoped`+`navFallback:'shell'` ב-gius** — יורשי `SHELL_PATHS`.
  ⚠️ **נמדד: אין ניתוב-לקוח באף אחת מארבע האפליקציות**, ⛔ אך אין למחוק
  על סמך המדידה הזו לבדה (כלל ברזל 16).

⭐ **ושלוש התנהגויות דווקא כן אוחדו, מפני שאינן נצפות ואינן פרמטר:**
שמירת הקליפה רק מנתיב-קליפה · נפילה לקליפה גם על 404 בניווט · ומחיקת
מטמון ישן רק אחרי שאומת שהקליפה קיימת בחדש. שלושתן היו בחלק מהארבע
בלבד, ומעכשיו בארבעתן.

### ⛔ שלוש התנהגויות שהליבה אוכפת — כל אחת נולדה מליקוי שנמדד
1. **⛔ `respondWith` אינו מקבל `undefined` לעולם (סבב 42ג)** —
   `caches.match` מחזירה `undefined` כשאין התאמה, ו-`respondWith` על
   Promise כזה זורק `TypeError`: כל בקשה שנכשלה ברשת ולא הייתה במטמון
   נכשלת **פעמיים**. ⛔ הליבה מחזירה תמיד תשובה תקפה.
2. **⛔ `swStore` שומרת אך ורק תשובה שאומתה — `ok && 200 && !opaque`
   (סבב 42ג)** — בלי הבדיקה, תשובת 404 של GitHub Pages נכנסת למטמון תחת
   מפתח הבקשה ומוגשת ממנו אופליין.
3. **⛔ תת-משאב חסר מקבל שגיאת רשת אמיתית ולעולם לא HTML (סבב 42ג)** —
   דף אופליין שמוזרק לתוך תג `script` הוא שגיאת תחביר בדף, לא הודעה
   למשתמש. דף האופליין הוא למסלול הניווט בלבד.

⭐ **וזה נמדד ולא הוצהר:** `tools/test_round42_sw.mjs` (זהה בית-לבית
×4 פרט לבלוק `APP`) מריץ את ה-`sw.js` **האמיתי** ברתמת `vm` ומשווה
ארבעה-עשר תרחישים מול קו בסיס. ⛔ `defectCount` חייב להישאר **0**
בארבעתן.
<!-- SHARED:end -->

<!-- SHARED:start id="pull-module" -->
## ⭐ מנגנון המשיכה — המדידה והאיחוד (סבב 51)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> ⛔ **הפולינג קורא שורת חותמת אחת ומושך את הנתונים רק כשהיא התקדמה.**
> ⛔ **וכל מסלול שכותב נתונים לענן מקדם את החותמת** — אחרת השינוי נשאר
> בלתי-נראה למכשירים האחרים לנצח.

### הבעיה שנמדדה
⚠️ **סבב 46ב מדד אסימטריה שאיש לא החליט עליה:** yoman ו-hanhala כבר קראו
שורת חותמת אחת כל שלוש שניות ומשכו מלא רק כשהיא התקדמה; schar משכה
**ארבע טבלאות ב-`select('*')` בכל תקתוק**, ו-gius משכה משיכה מלאה כל
עשרים שניות. ⛔ ושתי האחרונות גם הריצו **דחיפה תקופתית עיוורת** —
`setInterval` שדחף בלי שום ראיה שיש מה לדחוף.
⚠️ **וזו עלות שגדלה עם הנתונים:** ברוב התקתוקים אין שום שינוי בענן, ולכן
כמעט כל בייט שנמשך שם נמשך לחינם.

### הליבה — בלוק משותף אחד
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ מנגנון המשיכה — מודול משותף (סבב 51)` לבין
`/* ═══════════════ סוף מנגנון המשיכה`. **הוא זהה בית-לבית בארבעתן**
(108 שורות, חתימה `a5733e59b1f5e02b`), נאכף ב-`check-capabilities.mjs`,
ורשום במטריצה (שורה 33). מעליו יושבת **`PL_CFG`** — הדבר היחיד שנבדל.
**ה-API:** `plBoot`/`plStop` · `plTick` · `plTouch` · `plNum` · `plBusy` ·
`plState`.

| ידית | yoman | hanhala | schar | gius |
|---|---|---|---|---|
| `every` | 3000 | 3000 | 3000 | **5000** |
| שורת החותמת | `tb_last_changed` | `ys_last_changed` | `sl_last_changed` | `g_last_changed` |
| היכן היא יושבת | `KV_TABLE` פר-מוסד | `kv` | `sl_settings` | `g_config` |
| `active()` | נבחר מוסד | תמיד | משתמש מחובר | משתמש מחובר |
| `pull()` | `tbPullFromCloud` | `ysPullFromCloud` | `syncAll` | `syncNow` |

⚠️ **שתי החותמות החדשות יושבות בטבלת מפתח/ערך קיימת**, ⛔ ולכן הסבב הזה
**אינו מצריך מיגרציה** (סבב 51). ⛔ ובשתיהן השורה **מוחרגת מהמראה**
(`slStripMeta` · `stripRows`) — שורת מטא אינה נתון, ודחיפת-מצב שלה הייתה
מחזירה לענן חותמת ישנה שיושבת במכשיר הזה ומשתיקה בכך שינוי אמיתי.

### ⛔ ארבע ההתנהגויות שהליבה אוכפת
1. **⛔ כשל בקריאת החותמת אינו ראיה לכלום (סבב 51)** — אין רשת, שגיאה או
   זריקה ⇒ לא מושכים, ולא מקדמים את «מה שכבר נראה». משיכה או דילוג על
   סמך כשל הם הסקה ולא ראיה (כלל ברזל 6).
2. **⛔ אין חותמת בענן ⇒ נפילה-חזרה מווסתת (סבב 51)** — `PL_FALLBACK_MS`
   = 60 שניות. שער שנשען על שורה שאינה קיימת הוא שער שמפסיק לסנכרן בשקט,
   וזה בדיוק מה שקורה בהתקנה טרייה.
3. **⛔ אין ריצה כפולה (סבב 51)** — `_plBusy`. בלעדיו רשת איטית הייתה
   ערמת משיכות מלאות זו על זו.
4. **⛔ `plTouch` מקדם את «מה שכבר נראה» לפני שהוא כותב (סבב 51)** —
   אחרת המכשיר הכותב מושך בחזרה את מה שזה עתה דחף.

### ⛔ אין יותר פולינג שדוחף — בשום אפליקציה
הדחיפה היא **מונעת-אירוע**: קריאה ישירה אחרי כל שמירה מקומית (או דיליי
קצר של 400/500 מ״ש), ריקון על `online`, ומודול הניסיון החוזר של סבב 44 —
שנדרך מנקודת המשפך היחידה, רץ רק כשהתור אינו ריק, ונסוג אקספוננציאלית.
⛔ שלושת אלה מכסים את מה שהפולינג התקופתי כיסה, בלי העלות הקבועה.
⚠️ **ולכן `APP.gated` ב-`test_round44_retry.mjs` הוא `false` בארבעתן** —
`rtyGate()` היא ידית לאפליקציה שהפולינג שלה דוחף, ואין כזו יותר.

### הבדיקה האוכפת
`tools/test_round51_pull.mjs` — **זהה בית-לבית בארבעת הריפו פרט לבלוק
`APP` שבראשו**, ורץ מתוך `check-js.mjs` לפני כל דחיפה. הוא מריץ את הליבה
**האמיתית** ברתמת `vm`, עם `PL_CFG` מזויף ושעון מזויף, ואוכף בנוסף שתי
טענות סטטיות: ⛔ כל משפך כתיבה מקדם את החותמת, ⛔ ואין `setInterval`
מחוץ למודול שקורא לפונקציית הסנכרון של האפליקציה. עם **חמש מוטציות** על
הליבה, מוטציה סטטית שמחזירה פולינג תקופתי, ומוטציית-נגד על החתימה.
⛔ המוטציות אינן נכתבות לעץ (הלקח של סבב 42ג).
<!-- SHARED:end -->

<!-- SHARED:start id="lock-module" -->
## ⭐ נעילת חוסר-פעילות — המדידה והאיחוד (סבב 52)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> ⛔ **משתמש שהתחבר ואינו נוגע במסך מקבל אחרי ארבע דקות חלון אזהרה,
> ואחרי חמש הוא מוצא החוצה** — אותם ערכים, אותה התנהגות ואותה הודעה
> בשלוש האפליקציות שיש בהן כניסה.

### הבעיה שנמדדה
⚠️ **המנהל דיווח (2026-08-25) שהנהלה מוציאה אותו אחרי כמה דקות ושכר
וגיוס כמעט לעולם לא, והמדידה מצאה שני מנגנונים נפרדים ולא אחד:**

| | נעילת חוסר-פעילות | חלון אזהרה מקדים | סשן נשמר במכשיר |
|---|---|---|---|
| yoman | לא רלוונטי — אין כניסה | — | — |
| hanhala | ✅ 5 דקות | ✅ ב-4 דקות | ❌ אין — כל טעינה דורשת כניסה |
| schar | ✅ 5 דקות | ❌ טוסט בלבד | ✅ בלי תפוגה |
| gius | ❌ אין | ❌ | ✅ בלי תפוגה |

⛔ **שלושה מימושים לאותה יכולת, ואיש לא החליט על ההבדל** — ⚠️ ובעיקר:
היכולת מעולם לא נכנסה למטריצה, ולכן **שום שער לא חיפש אותה**. זו בדיוק
צורת הכשל שכלל ברזל 14 אוסר, בשכבה שאיש לא סרק.

### ⛔ ומה ש**אינו** קיים — ונמדד
⛔ **אין תפוגת סשן באף אחת מארבע האפליקציות.** הערך `30 * 60 * 1000`
שנמצא בהנהלה ובשכר הוא מרווח `checkForUpdate` (בדיקת גרסה), ולא סשן.
⚠️ ההבדל האמיתי הוא **עצם שמירת הסשן**: הנהלה אינה שומרת אותו כלל
(`AUTH.user` חי בזיכרון בלבד), ושכר וגיוס משחזרים אותו מ-localStorage
ללא תפוגה, עד יציאה או תשובה סמכותית שהמשתמש הושבת.
⛔ **החלטה על תפוגה מוחלטת היא החלטת מודל אבטחה של המנהל** (כלל ברזל 9),
⛔ ולכן סבב 52 לא המציא אחת. ⚠️ מה שכן — הנעילה המשותפת חוסמת חמש דקות
של חוסר פעילות, כלומר היא **מחמירה** משמעותית מכל תפוגה בת 30 או 60
דקות שנשקלה.

### הליבה — בלוק משותף אחד
`index.html` של שלוש האפליקציות שיש בהן כניסה מכיל את אותו בלוק בדיוק,
בין `/* ═══ נעילת חוסר-פעילות — מודול משותף (סבב 52)` לבין
`/* ═══════════════ סוף מודול נעילת חוסר-הפעילות`. **הוא זהה בית-לבית
בשלושתן** (133 שורות, חתימה `faf3168aa73eeb97`), נאכף
ב-`check-capabilities.mjs`, ורשום במטריצה (שורה 34). מעליו יושבת
**`LK_CFG`** — הדבר היחיד שנבדל.
**ה-API:** `lkBoot` · `lkReset` · `lkStop` · `lkShown` · `lkState`.

| ידית | yoman | hanhala | schar | gius |
|---|---|---|---|---|
| `LK_LOCK_MS` | לא רלוונטי | 5 דקות | 5 דקות | 5 דקות |
| `LK_WARN_MS` | לא רלוונטי | 4 דקות | 4 דקות | 4 דקות |
| `active()` | — | `AUTH.user` | `CUR_USER` | `state.user` |
| `lock()` | — | `doLogout` | `doLogout` | `logout` |
| נקודת העלייה | — | `loadDash` | `slBoot` | `start` |
| דריכה אחרי כניסה | — | `_doLoginInner` | `enterApp` | `boot` |

### ⛔ ארבע ההתנהגויות שהליבה אוכפת
1. **⛔ הערכים אינם ברירת מחדל אלא מדידה (סבב 52)** — חמש וארבע הדקות
   נמדדו בהנהלה, שם המנגנון חי מאז ההקמה. ⛔ אין לשנות אותם באפליקציה
   אחת בלבד; `test_round52_lock.mjs` נופל על כך גם בערך וגם בחתימה.
2. **⛔ חלון האזהרה נבנה בליבה ולא במסמך (סבב 52)** — עד כאן הוא היה
   סימון HTML+CSS פר-אפליקציה, כלומר רכיב שנבדל בשקט. הליבה בונה אותו
   בעצמה, בדיוק כמו סרגל ה-⏳.
3. **⛔ `lkStop()` אינו נשען על `lkReset()` (סבב 52)** — שם הדריכה מחדש
   מותנית ב-`LK_CFG.active()`, ומסלול יציאה שמנקה את המשתמש **אחרי**
   הקריאה היה נשאר עם טיימר דרוך שנועל את מסך הכניסה.
4. **⛔ אין הודעה פר-אפליקציה (סבב 52)** — הטוסט «🔒 ננעל עקב חוסר
   פעילות» שהיה בשכר הוסר: חלון האזהרה הוא ההודעה, ותוספת מקומית הייתה
   מחזירה בדיוק את ההפרש שהסבב סגר.

### ⚠️ הנעילה אינה תפוגת סשן
היא נמדדת **מהפעולה האחרונה** ולא מרגע הכניסה, ולכן משתמש שעובד ברציפות
אינו מוצא החוצה באמצע עבודה. ⛔ אין להמיר אותה לתפוגה מוחלטת בלי החלטת
מנהל (כלל ברזל 9).

### הבדיקה האוכפת
`tools/test_round52_lock.mjs` — **זהה בית-לבית בארבעת הריפו פרט לבלוק
`APP` שבראשו**, ורץ מתוך `check-js.mjs` לפני כל דחיפה. הוא מריץ את הליבה
**האמיתית** ברתמת `vm` עם DOM מזויף ושעון מזויף (שישה תרחישים), ואוכף
בנוסף את הערכים עצמם, את החיווט, ⛔ ואת היעדרו של מנגנון נעילה **פרטי**
לצד המשותף. עם **חמש מוטציות**, מוטציית ערך ומוטציית-נגד.
⚠️ **ובאפליקציה שהמנגנון אינו רלוונטי לה** (`APP.present = false`) הוא
אוכף את ההיפך: ⛔ שהבלוק אינו שם, ו⛔ שאין במקומו מנגנון מאולתר.
⛔ המוטציות אינן נכתבות לעץ (הלקח של סבב 42ג).
<!-- SHARED:end -->

<!-- SHARED:start id="sess-module" -->
## ⭐ מודל הסשן — המדידה וההכרעה (סבב 53)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> ⛔ **המשתמש המחובר חי בזיכרון בלבד, ואינו יורד לדיסק לעולם.** כל טעינה
> מחדש של הדף דורשת כניסה, ⛔ ואין סשן שנשמר במכשיר בשום אפליקציה.

### הבעיה שנמדדה
⚠️ **סבב 52 מדד את ההבדל שהמנהל חש בו — והוא לא היה זה שהוצהר.** לא
«תוקף סשן של 30 מול 60 דקות»: ⛔ תפוגה כזו לא הייתה קיימת באף אחת
מהארבע, והערך `30 * 60 * 1000` שנמצא בקוד הוא מרווח `checkForUpdate`.
מה שכן נבדל הוא **עצם השמירה**: הנהלה החזיקה את המשתמש ב-`AUTH.user`
בזיכרון בלבד, ואילו schar ו-gius שמרו `sl_session` ו-`gius.session`
ב-localStorage **בלי תפוגה** — כלומר מי שנכנס פעם אחת נשאר מחובר לנצח
על אותו מכשיר. ⚠️ ולא זו בלבד: הסשן שמר גם `role`, כלומר הוריד לדיסק
את **ההרשאה** עצמה.

⭐ **המנהל הכריע ב-2026-08-26 ליישר לדפוס ההנהלה** (כלל ברזל 9 — החלטת
מודל אבטחה היא שלו ולא של הסשן). ⚠️ **וזו הכרעה שמשנה התנהגות ביודעין:**
אחרי הפריסה כל משתמש נדרש להתחבר מחדש, וגם רענון דף מחזיר למסך הכניסה.

### הליבה — בלוק משותף אחד
`index.html` של שלוש האפליקציות שיש בהן כניסה מכיל את אותו בלוק בדיוק,
בין `/* ═══ מודל הסשן — מודול משותף (סבב 53)` לבין
`/* ═══════════════ סוף מודול הסשן`. **הוא זהה בית-לבית בשלושתן**
(76 שורות, חתימה `1977a56060bd1796`), נאכף ב-`check-capabilities.mjs`,
ורשום במטריצה (שורה 35). מעליו יושבת **`SESS_CFG`** — הדבר היחיד שנבדל.
**ה-API:** `sessBoot` · `sessSet`/`sessGet`/`sessClear` · `sessActive` ·
`sessState`.

| ידית | yoman | hanhala | schar | gius |
|---|---|---|---|---|
| המודול קיים | **לא רלוונטי** | ✅ | ✅ | ✅ |
| `SESS_CFG.legacy` | — | `[]` | `['sl_session']` | `['gius.session']` |
| המשתנה שנפתח אליו | — | `AUTH.user` | `sessGet()` | `state.user` |

⚠️ **ביומן אין מסך כניסה ואין משתמש מחובר**, ולכן אין מה להחזיק — לא
בזיכרון ולא בדיסק. ⛔ זו «אין כאן שאלה כזו» ולא «חסר», והחריגה מוצהרת
ב-`APP.skipCaps` של שני הבודקים.

### ⛔ ארבע ההתנהגויות שהליבה אוכפת
1. **⛔ המשתמש אינו יורד לדיסק (סבב 53)** — `_sessUser` הוא משתנה מודול,
   ⛔ ואין בליבה שום כתיבה ל-localStorage מלבד המחיקה שבסעיף 4. סשן שנשמר
   בלי תפוגה הוא כניסה קבועה על מכשיר משותף.
2. **⛔ נקודת גישה אחת (סבב 53)** — `sessSet`/`sessGet`. ⚠️ שתי האפליקציות
   ששמרו את המשתמש בשדה רגיל (`state.user`, `AUTH.user`) פותחות אליו
   מעכשיו **מאפיין גישה** (`get`/`set`), כך שכל 73 אתרי הקריאה נשארו
   כמות שהם והערך עבר מקום. ⛔ אין להחזיר שם שדה רגיל — שדה שכל אחד יכול
   להציב בו ערך הוא בדיוק המסלול שדרכו הסשן חזר לדיסק.
3. **⛔ הכניסה האופליין אינה נשענת על המודול (סבב 53)** — היא מוכרעת מול
   `pass_fp` שבמראת המשתמשים (כלל ברזל 10 סעיף 4), ולכן הסרת שמירת הסשן
   אינה פוגעת בה: מי שנכנס בלי רשת ממשיך להיכנס בלי רשת.
4. **⛔ `sessBoot()` מנקה את שרידי הסשן מהמכשיר (סבב 53)** — מפתח שנכתב
   בגרסה קודמת אינו נעלם מאליו והוא נשאר קריא בדפדפן לנצח. הניקוי
   אידמפוטנטי, נכשל-סגור באחסון חסום, ונרשם ביומן הפינוי רק כשנמחק משהו
   בפועל. ⛔ אין למחוק שם מ-`SESS_CFG.legacy` גם אחרי שהמנגנון הישן ירד
   מהקוד — המפתח עצמו עדיין על הדיסק של כל מכשיר שכבר נכנסו בו.

### ⚠️ וזו אינה כפילות של נעילת חוסר-הפעילות (סבב 52)
הנעילה נמדדת **מהפעולה האחרונה** ומוציאה החוצה משתמש ששכח את המסך פתוח;
⛔ היא אינה עוזרת למי שסגר את הלשונית וחזר אליה מחר, מפני שהטיימר מת
יחד עם הדף. המודול הזה סוגר בדיוק את החור ההוא. ⛔ שתי השורות במטריצה
(34 ו-35) הן שתי יכולות ולא אחת.

### הבדיקה האוכפת
`tools/test_round53_session.mjs` — **זהה בית-לבית בארבעת הריפו פרט לבלוק
`APP` שבראשו**, ורץ מתוך `check-js.mjs` לפני כל דחיפה. הוא מריץ את הליבה
**האמיתית** ברתמת `vm` עם localStorage מזויף, ואוכף בנוסף שתי טענות
סטטיות: ⛔ אין בקוד קבוע ששמו `SESSION_KEY`, ⛔ ואין כתיבה של המשתמש
המחובר ל-localStorage. עם **חמש מוטציות** ומוטציית-נגד על החתימה.
⚠️ **ובאפליקציה שהמודול אינו רלוונטי לה** (`APP.present = false`) הוא
אוכף את ההיפך: ⛔ שהבלוק אינו שם, ו⛔ שאין במקומה שמירת סשן מאולתרת.
⛔ המוטציות אינן נכתבות לעץ (הלקח של סבב 42ג).
<!-- SHARED:end -->


---

<!-- SHARED:start id="iron-rule-16-remnant" -->
## ⭐ כלל ברזל 16 — הנחת השריד (סבב 39, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את חמישה-עשר כללי הברזל שלמעלה.

> ⛔ **הוראת מחיקה שנימוקה «שריד», «מיותר» או «לא בשימוש» אינה מבוצעת לפני
> מדידה שמוכיחה שאין לו קורא.** נמצא קורא — הסשן **עוצר ומדווח**,
> ⛔ **ואינו מוחק, גם אם ההוראה הייתה מפורשת.**

### הבעיה שנמדדה
שלושה ממצאים בסבב 39 זוהו כ«שרידים» **לפי המראה** — קובץ שנראה יתום, מנגנון
שנראה מת, כלי שנראה כמו שארית של תקופה שהסתיימה. ⚠️ **ובשלושתם המדידה
הפריכה את ההנחה, ומחיקה הייתה שוברת אפליקציה:**
- **`android/…/res/xml/file_paths.xml` ב-gius** — ל-gius **אין** גשר שיתוף,
  ולכן הקובץ נראה כשארית של הדפוס שהועתק מיומן. ⚠️ מדידת סבב 39: ה-`AndroidManifest`
  שלה **הכריז `FileProvider` שמצביע עליו**, ולכן מחיקה הייתה קריסה בזמן ריצה.
  ⭐ **וסבב 41 הרחיב את ההיקף והגיע למסקנה ההפוכה:** לא הקובץ היה יתום אלא
  **ההכרזה עצמה** — אפס `getUriForFile`, אפס `androidx` ב-`MainActivity`, ואפס
  `navigator.share` ואפס `type="file"` בדף. שלושת הרכיבים (ההכרזה · הקובץ ·
  `androidx.core`) הוסרו **באותו קומיט**, מפני שהסרת אחד מהם לבדו שוברת את
  ה-APK בהתקנה.
  ⚠️ **וזה הלקח החזק של הכלל ולא חריגה ממנו:** מדידה תקפה **להיקף שנסרק
  ולרגע שבו נעשתה**. מי שמצא קורא עוצר ומדווח; מי שמרחיב את ההיקף — מודד שוב,
  ורושם את שני הממצאים.
- **`migrateOutbox()` ב-gius** — התור היוצא הוסר בסבב 12, והפונקציה שממירה
  אותו נראית כקוד שאיבד את מושאו. ⚠️ המדידה: היא ההגירה החד-פעמית שמצילה תור
  ישן במכשיר שטרם עבר. מחיקה = אובדן שקט של פעולות שממתינות במכשיר בשטח.
- **`_bkRetention` במודול הגיבוי** — הפינוי רץ מאז סבב 35ג **במסד** ב-`pg_cron`,
  ולכן השכבה שבלקוח נראית מיותרת. ⚠️ המדידה: היא שכבה **שנכשלת-סגור** שהבדיקות
  אוכפות, והיא מה שיפעל אם תוענק הרשאה סקופית.

⚠️ **המשותף לשלושתם: «נראה כמו שריד» הוא תיאור של המראה, לא ממצא.** מה
שהופך רכיב לשריד הוא **היעדר קורא** — וזה נתון שנמדד, לא נתון שמנחשים.

### ⛔ שלושת התנאים לפני כל מחיקה
1. **חיפוש קוראים בכל ארבעת הריפו** — לא בקובץ אחד ולא בסוג קובץ אחד
   (הלקח של כלל ברזל 10 סעיף 8: סריקה שמוגבלת לסוג קובץ אחד היא ראיה
   לסוג שנסרק, ותו לא). כולל: קוד · workflow · סקריפט · manifest אנדרואיד ·
   תיעוד · קובצי בדיקה.
2. **בדיקה שהרכיב אינו נתיב-חזרה או שכבת נפילה-חזרה** — «אין לו קוראים
   היום» אינו «אינו נחוץ»: מנגנון שנכשל-סגור, הגירה חד-פעמית ונתיב חזרה
   מכובה הם בדיוק רכיבים שאין להם קוראים **עד הרגע שבו הם נחוצים**.
3. **רישום המדידה בפרק הסבב** — מה נסרק, ומה נמצא. ⛔ «נמחק כשריד» בלי
   המדידה שלצידו הוא בדיוק הסוג של תיעוד שכלל ברזל 8 סעיף 6 אוסר.

### ⚠️ מה הכלל **אינו**
⛔ **אינו איסור על מחיקה.** באותו סבב עצמו נמחק `signing/sign-apk.bat`
**אחרי** שנמדד שאין לו אף קורא בארבעת הריפו — וזו בדיוק הדרך: המדידה היא
שהתירה את המחיקה, ולא המראה. הכלל אוסר דבר אחד — **מחיקה על סמך הנחה**.

⚠️ **והוא חל גם על הוראה מפורשת של המנהל.** מנהל שכותב «מחק את X, זה שריד»
מתאר את מה שהוא רואה; הסשן הוא זה שיכול למדוד. נמצא קורא ⇒ הסשן **מדווח
ומציע**, והמחיקה חוזרת להחלטה — בדיוק כמו כלל ברזל 9 («החלטת מודל אבטחה
היא של המנהל, לא של הסשן»), בכיוון ההפוך.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-17-touch-scan" -->
## ⭐ כלל ברזל 17 — סריקת האזור שנגעו בו (סבב 42ב, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שישה-עשר כללי הברזל שלמעלה.

> ⛔ **כל סבב שנוגע באזור בקוד — בונה, משפץ, מוסיף או מתקן — סורק את האזור
> שנגע בו בשלושה ממדים לפני שהוא מסיים.** ⛔ הסריקה אינה רשות.

### שלושת הממדים
1. **באג שקט.** מה באזור הזה יכול להיכשל בלי להרים שום סימן — **מפתחות
   סימון** שנכתבים בצורה אחת ונקראים באחרת · **מזהים** שנוצרים במכשיר או
   משמשים כמפתח מיזוג · **תאריכים** וגבולות חודש ושנה · ו**מסלולים שרצים
   פעם בשנה**, שאיש אינו רואה אותם נשברים.
2. **האם משהו כאן היה אמור להיות משותף.** רכיב שנבנה או תוקן כאן ויש לו
   מקבילה — או שראוי שתהיה לו — באפליקציה אחרת. ⛔ ואם כן: **הוא עולה מיד
   לרמת הארגון ואינו נשאר מקומי** — בלוק משותף, חתימה, ורישום במטריצה
   (כללי ברזל 12 ו-14).
3. **האם התיעוד באזור תואם למציאות.** הערת ⛔ שמתארת מנגנון שכבר אינו
   קיים · שורת פער שנסגרה · תיאור תפעולי בהווה של קובץ שנמחק.

### ⚠️ ממצא רוחבי עוצר את הסבב
⚠️ **ממצא שנוגע לכל האפליקציות — עוצר את הסבב ומדווח, ⛔ ואינו מטופל
מקומית.** תיקון מקומי לממצא רוחבי הוא בדיוק «קיים רק באחת, בשקט» שכלל
ברזל 14 אוסר, והוא גרוע מאי-תיקון: הוא סוגר את הפער באחת ומשאיר את השלוש
פתוחות **בלי שאיש יידע שהן נותרו**.

### הבעיה שנמדדה
⚠️ **שלושת הבאגים החמורים של סבבים 36–37 נמצאו בסריקת-הזדמנות ולא בביקורת
גורפת** — כלומר במקרה, מפני שסבב אחר עבר באותו אזור:
- **באג ה-`arc:`** — מפתח סימון ⏳ שנכתב בצורה אחת ונקרא בשלוש נקודות
  בצורה אחרת, ולכן **אף אחת מהן לא ראתה אותו**: עריכה בארכיון לא נחשבה
  «לדחיפה», ושער הפינוי היה רשאי לפנות סנאפשוט שהעריכה שלו טרם עלתה.
- **ארבעת הסדרים הכפולים** — שני סדרים חיים לאותו שם ולאותו יום, שנוצרו
  בחור שבין פתיחת הסדר לסימון הראשון.
- **סימון ה-⏳ על הרשומה השגויה** — הסימון נגזר מהאיבר האחרון במערך
  **אחרי** שמוין אלפביתית, ולא מהרשומה שנוספה.

⛔ **שלושתם היו יושבים שם עד היום** אלמלא סבב שנגע באזור והסתכל סביבו. זה
מה שהכלל הזה הופך מהזדמנות לחובה.

### ⛔ מה הכלל אינו
⛔ **אינו ביקורת גורפת ואינו סבב סריקה.** הוא חל על **האזור שנגעו בו**,
ולכן עלותו קבועה ומצטברת עם העבודה עצמה. ⚠️ סבב שסורק את הקובץ כולו «כי
ממילא נגעתי» אינו מקיים את הכלל אלא מחליף אותו במשהו אחר — וזה בדיוק סוג
הדבר שנדחה שוב ושוב מחוסר היקף.
<!-- SHARED:end -->


---
<!-- SHARED:start id="iron-rule-18-doc-budget" -->
## ⭐ כלל ברזל 18 — תקציב תיעוד (סבב 49, הודק בסבב 50)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שבעה-עשר כללי הברזל שלמעלה.

> ⛔ **`CLAUDE.md` נשמר בחלון של שני סבבים, מתחת לתקרה של 3,000 שורות,
> ועם חלק פרטי-קבוע שאינו עולה על 900.** סבב שלישי נכנס — פרק הסבב
> הראשון נמחק **באותו קומיט**; סבב שחוצה תקרה גוזם **באותו קומיט**.
> ⛔ שלושת התנאים נאכפים ב-`check-docs.mjs`.

### הבעיה שנמדדה
**תשעה סשנים נחנקו בשבוע אחד** — ⚠️ והסיבה נמדדה ולא הונחה: `CLAUDE.md`
תפח מ-**1,782** שורות (אחרי הגיזום של סבב 34) ל-**4,447**, ⛔ מפני שכל
סבב מוסיף פרק ואף סבב אינו מוחק אחד. הגיזום של סבבים 48א–48ג הוריד את
ארבעת הקבצים ל-3,554–3,964 — ⛔ **ובלי כלל שאוכף את הגבול הוא היה מתבטל
תוך שנים-עשר סבבים**, בדיוק כפי שהגיזום של סבב 34 התבטל.

⭐ **וסבב 50 הידק את החלון משישה סבבים לשניים, בהחלטת המנהל.** ⚠️ הנימוק
הוא חלוקת תפקידים ולא קיצוץ לשמו: **git מחזיק את ההיסטוריה, ו-`CLAUDE.md`
מחזיק ⛔ הוראות לעתיד בלבד.** פרק סבב הוא תיאור של מה שנעשה **אז** — כלומר
היסטוריה — וכל לקח שראוי להישמר ממנו כבר עלה לכלל ברזל, לפרק מודול או
להערה בקוד, לפי הסעיף שלמטה. ⛔ פרק שלישי ששרד הוא היסטוריה שמשלמים עליה
מס הקשר בכל סשן.

⚠️ **וזה אינו כשל תיעוד אלא כשל תפעולי.** קובץ שנקרא בתחילת כל סשן הוא
מס קבוע על **כל** עבודה שנעשית בריפו, וכשהוא חוצה את גבול ההקשר הסשן
נחנק לפני שהוא נוגע בקוד. ⛔ תיעוד שאיש אינו מצליח לקרוא עד סופו אינו
מגן על דבר — הוא בדיוק כמו בדיקה שלא רצה.

### שלושת התנאים
1. **⛔ חלון של שני סבבים.** רק פרקי שני הסבבים האחרונים נשמרים בקובץ.
   פרק סבב הוא כותרת `## ` שנפתחת ב«סבב N» (עם או בלי ⭐), והוא **נגמר
   בכותרת `## ` הבאה** — ⛔ ולא בשורת המפריד ולא ב«אימותים».
2. **⛔ תקרה קשיחה של 3,000 שורות**, מדידה גולמית של `wc -l`. ⛔ בלי
   יוצאים מן הכלל: פרקים משותפים, טבלאות וכללי ברזל נספרים בה כמו כל
   שורה אחרת — אחרת התקרה הופכת להמלצה.
3. **⛔ תקרת 900 שורות על החלק הפרטי-הקבוע** (סבב 50) — מה שאינו בלוק
   `SHARED` ואינו פרק סבב. ⚠️ **ושתי התקרות אינן כפילות:** שתיים
   הראשונות מודדות את הקובץ **כולו**, ולכן תפיחה של החלק הפרטי נבלעת
   בהן ואינה נראית עד שהיא חוצה את 3,000. ⛔ והחלק הפרטי הוא בדיוק מה
   ששום ריפו אחר אינו מיישר — כלומר מה שאיש אינו מודד מלבד השער הזה.

### ⚠️ מה עולה לפני שהוא נמחק
**לקח שראוי להישמר — ⛔ עולה לכלל ברזל, לפרק המודול המשותף, או להערה
במקום הרלוונטי בקוד, לפני המחיקה** (סבב 49). ⛔ ואין למחוק פרק שהלקח שלו
לא הועבר: git זוכר את ההיסטוריה, אבל **git אינו מקום שקוראים בו** — וזו
בדיוק הסיבה שסבב 34 זיקק את הלקחים לכללי הברזל במקום למחוק אותם סתם.
⚠️ **וגיזום אינו ביקורת:** הוא חל על **הפרקים הישנים ביותר ולפי הסדר**,
⛔ ולא על «מה שנראה פחות חשוב» — בחירה לפי תוכן היא בדיוק שיקול הדעת
שהחלון בא להחליף.

### ⛔ שני סוגי תוכן שאינם מתועדים ב-`CLAUDE.md` כלל (סבב 50)
⚠️ שניהם **מקור אמת שני** — תיעוד שמתאר מצב שנקבע במקום אחר, ולכן נסחף
בשקט ברגע שהמצב משתנה בלי שאיש עורך את הקובץ.

**א. ⛔ טבלת מצב המיגרציות אינה מתועדת כאן.** מצב ההרצה נגזר
מ-`supabase_migrations.schema_migrations` שבמסד — ⛔ וזה מקור האמת היחיד.
⚠️ **נמדד בסבב 45: הטבלה פיגרה בסבב שלם בארבעת הריפו**, כלומר היא נקראה
כמציאות בזמן שהמסד כבר אמר אחרת. ⛔ ומה שכן נשאר בקובץ הוא **הוראה
לעתיד** בלבד: איך מריצים, מה אסור לכתוב במיגרציה חדשה, ופער פתוח שממתין
להרצה — שיושב בפרק «פערים פתוחים» עם תג, ולא בטבלת מצב.

**ב. ⛔ טבלה היסטורית בתוך פרק פעיל אינה נשמרת.** פרק פעיל מתאר את
**ההווה בלבד**; נוסח «כך זה היה לפני האיחוד» ⛔ אינו נשמר לצידו, גם
כשהוא מסומן «היסטוריה». ⚠️ git זוכר את המדידה, והפרק שקורא אותה כבר
מחזיק את ההכרעה שנגזרה ממנה.
⚠️ **והגבול מדוד ולא נתון לפרשנות:** מה שיורד הוא **צילום מצב מלא** של
עולם שכבר אינו קיים (הידועה: טבלת ארבעת מימושי ה-`sw.js` שבתוך
`sw-strategies`, שנמחקה בסבב 50). ⛔ מה ש**נשאר** הוא הכשל שנמדד
והוליד את הכלל — שתי-שלוש שורות שבלעדיהן ההוראה נקראת כקפריזה
(כלל ברזל 11 סעיף 3).

### ⛔ שלושה דברים שהכלל אינו מתיר
- ⛔ **אינו מתיר למחוק בלוק `SHARED`** בלי לעדכן את הרשימה הקנונית
  בארבעת עותקי `check-docs.mjs`, באותו סבב (כלל ברזל 8 סעיף 3).
- ⛔ **אינו מתיר למחוק פרק ששער קורא ממנו טקסט** — מטריצת היכולות
  (`check-capabilities` · `test_round37_matrix`), «פערים פתוחים»
  (`check-gaps`), והפסקאות ש-`test_round35c_cron` דורש.
- ⛔ **אינו חל על שלושת קובצי ה-md הנלווים** — להם יש שלד ופסקאות
  משותפות משלהם, בסעיפים ו–ז של `check-docs`.

### הבדיקה האוכפת
`tools/check-docs.mjs`, סעיף **ט** — זהה בית-לבית בארבעת הריפו, ורץ מתוך
`check-js.mjs` לפני כל דחיפה. הוא נכשל על שלושה סוגי סטייה:
- **א. יותר משני פרקי סבבים** — ומדפיס את שמות הפרקים שיש למחוק.
- **ב. יותר מ-3,000 שורות** — ומדפיס בכמה חרגה.
- **ג. חלק פרטי-קבוע מעל 900 שורות** — ומדפיס בכמה חרג.
⚠️ **ושלושתם מודעים לגדרות קוד** — ⛔ כותרת `## סבב` או סמן `SHARED`
שבתוך דוגמה בתיעוד אינם אזור אמיתי, וספירה גולמית הייתה נופלת על טקסט.

⚠️ **ושתי הטענות נמדדות על הקובץ עצמו ולא על הצהרה:**
`tools/test_round49_budget.mjs` מריץ את `check-docs` **האמיתי** על עותק
בתיקייה זמנית, עם **מוטציה לכל תנאי** — פרק סבב שלישי ⛔ מפיל · 3,001
שורות ⛔ מפילות · ו-901 שורות פרטיות ⛔ מפילות. ⭐ **ולצידה מוטציית-נגד:**
אותה כמות שורות **בתוך פרק סבב** ⛔ **אינה** מפילה — כלומר המדידה מבחינה
בין השניים ואינה סופרת גולמית. ⛔ המוטציות אינן נכתבות לעץ (הלקח של סבב 42ג).
<!-- SHARED:end -->
---

<!-- SHARED:start id="iron-rule-19-read-discipline" -->
## ⭐ כלל ברזל 19 — משמעת קריאה (סבב 49, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שמונה-עשר כללי הברזל שלמעלה.

> ⛔ **סשן קורא את מה שהוא צריך, ולא את מה שנוח לפתוח.**
> ⚠️ **הכלל הזה אינו ניתן לאכיפה מכנית** — אין שער שיכול למדוד מה סשן
> קרא — ולכן הוא יושב בתיעוד כדי שכל סשן יקרא אותו **בתחילת עבודתו**,
> לפני שהוא פותח את הקובץ הראשון.

### חמשת הסעיפים
1. **⛔ לעולם לא לקרוא את `index.html` במלואו** (סבב 49) — הוא הקובץ
   הגדול בריפו, וקריאה מלאה שלו לבדה מכלה את ההקשר לפני שהעבודה
   מתחילה. חיפוש ממוקד בלבד: `grep -n` לאיתור, ואז
   `sed -n '<from>,<to>p'` לקטע שנמצא.
2. **⛔ לא לקרוא קובץ שלם כשחיפוש מספיק** (סבב 49) — גם קובץ קטן.
   השאלה היא תמיד «איזו שורה אני צריך», ולא «מה יש בקובץ».
3. **⛔ לא לשפוך תוכן קבצים לצ'אט** (סבב 49) — פלט של בדיקה, דיף ארוך
   או רשימת ממצאים נכתבים **לקובץ**, והדיווח הוא **מונה** ושורת סיכום.
   ⚠️ פלט שנשפך לצ'אט נשאר בהקשר עד סוף הסשן, גם אחרי שכבר נעשה בו
   שימוש — כלומר מחירו משולם שוב בכל צעד.
4. **⚠️ קומיט ודחיפה אחרי כל סעיף** — ⛔ ולא בסוף הסבב (סבב 49). סשן
   שנחנק באמצע מאבד את כל מה שלא נדחף, וסעיף שנדחף הוא סעיף שאינו צריך
   להיעשות שוב.
5. **⚠️ סבב שדורש מדידה על ארבעת הריפו — ⛔ מפוצל לריפו אחד בכל סשן**
   (סבב 49). ⚠️ **וזה אינו סותר את כלל ברזל 13:** שם מדובר ב**היקף
   הגישה** — סבב שנוגע ב-SHARED נפתח מלכתחילה עם ארבעת הריפו — וכאן
   ב**נפח העבודה** בתוך סשן יחיד. ⛔ סבב SHARED שמצריך מדידה כבדה נפתח
   עם ארבעתם **ומחלק את המדידה בין סשנים**.

### ⚠️ למה זה כלל ברזל ולא המלצה
כללי הברזל 1–18 נולדו מכשלים שנמדדו **בקוד**; ⭐ **הכלל הזה נולד מכשל
שנמדד בסשנים עצמם** — תשעה סשנים שנחנקו בשבוע אחד. ⛔ וסשן שנחנק אינו
מותיר שום סימן בריפו: אין שער שנופל, אין בדיקה אדומה, ואין קומיט. הוא
פשוט אינו מסיים — ⚠️ וזו בדיוק צורת הכשל השקט שכלל ברזל 17 מחפש, בשכבה
שמעל הקוד.
<!-- SHARED:end -->
---


## רתמות הבדיקה ההיסטוריות — מצב מקובל, ⛔ לא פער

סבבים מוקדמים הריצו את הרתמות שלהם **בסשן בלבד** ולא שמרו אותן כקובץ;
מסבב 30 כל סבב שומר קובץ בדיקה בריפו (כלל ברזל 8 סעיף 6). ⛔ **הפער הזה
אינו נסגר בדיעבד, וזו החלטה ולא הזנחה** (סבב 39):

- ⚠️ **רתמה שתיכתב היום מול הקוד הנוכחי מקבעת את ההתנהגות הנוכחית** — כולל
  באג שטרם התגלה. היא תיראה כמו רשת ביטחון, ותפעל בפועל כאישור: כל תיקון
  עתידי של אותו באג ייראה כרגרסיה ויפיל את השער.
- ⚠️ **הלקחים עצמם לא אבדו** — הם זוקקו בסבב 34 לכללי הברזל ולפרקי
  המצב-הנוכחי, ושם הם נאכפים על **כל** קוד חדש ולא רק על האזור שנבדק.
- ⛔ **ולכן אין לפתוח סבב «שחזור בדיקות» יזום** — החלטת המנהל. מה שכן חל:
  סבב שנוגע באזור כזה **משחזר את רתמת האזור באותו סבב**, מול השינוי שהוא
  עושה ולא מול הקוד ההיסטורי.

**כאן:** רתמות סבבים 6–13 רצו בסשן ולא נשמרו; מה שכן בריפו —
`test_round24_offline_login.mjs`, `test_round26_roles.mjs` ו-`test_round30_stage_a.mjs`.

## פערים פתוחים (schar-limud)
- **⭐ החלון החם פעיל — הטריגר שנותר הוא נפח בלבד** (עודכן בסבב 39).
  ⚠️ **הניסוח הקודם כאן («אין ולא יהיה פינוי») נקרא כאילו היכולת חסרה, וזה
  אינו נכון:** `HW_CFG.enabled = true`, הספק `sl_mirror_transactions` מחווט
  ל-`acadYearOf`, ו-`hwSweep` רץ **בכל עלייה**. פשוט אין עדיין מה לפנות —
  נמדד ב-2026-08-19: **89 תנועות · ~13KB**, כולן בשנת הלימודים הנוכחית,
  כלומר כולן בתוך החלון.
  ⛔ ומה ש**כן** נשאר סגור לתמיד הוא הפינוי מבוסס-ה-tier של `LS_CFG`
  (`tier1`/`tier2` ריקים — מטריצה שורה 6) — תנועות הן כספים, ר' הנימוק
  שם. ⛔ אין לפתוח אותו.
  **הטריגר לסבב «מראה-חלון» המלא** (טקס סגירת-שנה + התאמת דחיפה ומשיכה):
  שנת לימודים ראשונה שנצברה במלואה, או חציית כמה מאות KB.
  [לא-מדיד: נפח המראה במכשירי הצוות]
- **⛔ העמודה הגלויה `sl_users.password` עדיין במסד, ואיתה הבוטסטרפ**
  (סבב 40) — האימות אינו נשען עליה, אבל היא `NOT NULL` ועדיין נכתבת,
  ו-`SL_PLAINTEXT_BOOTSTRAP` הוא המסלול היחיד שעדיין **קורא** אותה.
  ⛔ **וכאן צעד ב של `migrations/015` חסום ולא רק מוער:** מחיקת העמודה
  לפני שיש מסלול יצירת-משתמש שגוזר טביעה תנעל בחוץ כל משתמש עתידי.
  **הטריגר:** בניית מסלול כזה (מסך ניהול, או פונקציית מסד) ← כיבוי
  הבוטסטרפ ← צעד א ← צעד ב.
  [מדיד: code:SL_PLAINTEXT_BOOTSTRAP = true]

## סבב 52 (2026-08-25) — יישור שכבת הכניסה ותיקון מונה הממתינים

⚠️ **סבב תיקון-באג ואיחוד אבטחה.** ⛔ לא הורצה שום מיגרציה, ⛔ ולא נדרשה
אחת. ⛔ `versionCode` לא קודם: שכבת האנדרואיד לא נגעה.

### מה נעשה
- **⭐⭐ הבאג שהמנהל דיווח עליו — סיבה אחת לשני הסימפטומים.** פעולה
  שנרשמה אופליין סונכרנה בפועל כשהרשת חזרה, ⛔ אבל שורת «פעולות ממתינות
  לסנכרון» נשארה על המסך עד שהמשתמש ביצע פעולה נוספת, ובמקביל
  «✅ נשמר בענן» חזר שוב ושוב. ⚠️ **נמדד:** מסלול הריקון האוטומטי ביומן
  (`tbSyncPushNow` — מודול הניסיון החוזר, אירוע `online`, והדיליי הקצר
  אחרי שמירה) דחף בהצלחה **ולא הוריד את סימוני ה-⏳**. מכאן `pendCount()`
  נשאר > 0 (המונה מצייר את האמת הלוגית, והיא לא השתנתה — ⛔ כלומר זו
  **אינה** בעיית ציור), ו-`_rtyPending()` נשאר אמת, ולכן הניסיון החוזר
  ירה שוב, הצליח שוב, והשמיע את הטוסט שוב.
- **⚠️ ובשלוש האחרות הבאג לא היה, וזה נמדד ולא הונח:** הנהלה מאשרת
  ב-`pendConfirmKv` מתוך `ysFlushQueue`, שכר ב-`slPushTable`
  (`pendClear`/`pendFailed`) וגיוס ב-`pushTable` וב-`pendReconcile`.
  ⛔ הבדיקה החדשה אינה מחפשת שם אחד אלא **גיעוּת**: ממסלול הריקון חייבת
  להיות דרך קריאה לאחת מפונקציות האישור של המודול.
- **⭐⭐ נעילת חוסר-פעילות הפכה לבלוק משותף** (133 שורות, חתימה
  `faf3168aa73eeb97`) בשלוש האפליקציות שיש בהן כניסה. הפרק המלא —
  «נעילת חוסר-פעילות — המדידה והאיחוד» — הוא בלוק `SHARED` חדש
  (`lock-module`), ושורה **34** נוספה למטריצה.
- **⛔ ומה שנמדד ולא הוצהר:** לא «המנגנון קיים בהנהלה בלבד» — לשכר היה
  **אותו ערך בדיוק (5 דקות) בלי חלון האזהרה**, ולגיוס לא היה כלום.
  ⛔ שלושה מימושים לאותה יכולת, ⚠️ ובעיקר: היכולת מעולם לא נכנסה
  למטריצה, ולכן **שום שער לא חיפש אותה**.
- **⛔ תפוגת סשן — אינה קיימת באף אחת מהארבע, ולכן לא יושרה** (סבב 52).
  ⚠️ הערך `30 * 60 * 1000` שנמצא בהנהלה ובשכר הוא מרווח `checkForUpdate`
  ולא סשן. ⭐ ההבדל האמיתי הוא **עצם שמירת הסשן**: הנהלה אינה שומרת אותו
  כלל, ושכר וגיוס משחזרים אותו מ-localStorage בלי תפוגה — וזה נרשם
  כשורה **35** במטריצה. ⛔ הכרעה על תפוגה מוחלטת היא החלטת מודל אבטחה של
  המנהל (כלל ברזל 9), ולכן הסבב **מדווח ומציע** ואינו ממציא אחת; הפער
  רשום עם טריגר בשכר ובגיוס.
- **שתי בדיקות חדשות**, זהות בית-לבית בארבעתן פרט לבלוק `APP`, ורצות
  מתוך השער: `tools/test_round52_pendflush.mjs` (חמש טענות התנהגות
  ברתמת `vm`, ארבע מוטציות, מוטציה סטטית שמנטרלת את סגור-הקריאות של
  מסלול הריקון, ומוטציית-נגד) ו-`tools/test_round52_lock.mjs` (שש טענות
  ברתמת `vm` עם DOM ושעון מזויפים, חמש מוטציות, מוטציית ערך
  ומוטציית-נגד). ⛔ המוטציות אינן נכתבות לעץ (הלקח של סבב 42ג).
- **⛔ מדידה בלבד — סריקת שכבת הכניסה** (כלל ברזל 17, ⛔ אף שורת קוד לא
  השתנתה בעקבותיה): `NET_TIMEOUT_MS` = 8,000 בארבעתן · PBKDF2 עם 100,000
  סיבובים בשלוש שיש בהן משתמשים · ⛔ אין תפוגה לטביעה באף אחת · אימות
  מחדש של הסשן מתנתק **רק על תשובה סמכותית** בשלושתן. ⚠️ שלוש
  אסימטריות שנמצאו ולא טופלו: רענון מראת המשתמשים מווסת ל-60 שניות בשכר
  בלבד (בהנהלה הוא בכניסה, בגיוס במשיכה) · `setInterval(checkForUpdate,
  30 דקות)` קיים בשלוש ו**נעדר בגיוס** (שם העדכון מגיע מבאנר ה-SW) ·
  ו-`startSetupPoll` קיים בשכר בלבד (מסך ההגדרה הראשונית).
- **⛔ מדידה בלבד — סריקת יכולות תשתיתיות שאינן במטריצה** (⛔ לא תוקן
  דבר): נסרקו ארבע הקטגוריות שהוגדרו. **מה שכן במטריצה:** ⏳, ניסיון
  חוזר, משיכה, גיבוי, פינוי, נעילה. **מה שאינו, וכולו מיושר בפועל:**
  `setInterval(pendRender, 60000)` ×4 · `NET_TIMEOUT_MS` ×4 · מאזיני
  `online`/`offline` של המודולים ×4. **מה שאינו ואינו מיושר:** שלוש
  האסימטריות שלמעלה, ובנוסף `TOMBSTONE_TTL_MS` (90 יום) ו-`UPDATE_INTERVAL_MS`
  (שעה) שקיימים ביומן בלבד — שניהם מנומקים במקומם.
- **⛔ מדידה בלבד — סריקת קובצי הבדיקה** (⛔ לא יושר ולא נמחק דבר):
  **27 קובצי `test_round*` משותפים לארבעתם** ו-13 פרטיים. אחרי נרמול
  (הסרת `APP`, שם הריפו, רווחים) — ⭐ **23 זהים בית-לבית**; ארבעה
  נבדלים, ⚠️ **ואף אחד מהם אינו הפרש לוגיקה**: ב-`test_round35_hotwin`
  וב-`test_round37_merge_pending` ההפרש הוא **אורך קו ה-`─` בהערת
  המפריד של `APP`** בלבד, וב-`test_round35c_cron` וב-`test_round37_ids`
  אלה הערות פר-אפליקציה שצמודות ל-`APP` ומנמקות את ערכיו.
  ⚠️ **זמני ריצה נמדדו:** השער כולו רץ **24.7–27.5 שניות** בארבעת הריפו
  (ולא מעל שתי דקות), ⛔ ושני קבצים אחראים ל-~75% מהזמן —
  `test_round33_structure` (12.6–14.2 שנ') ו-`test_round37_matrix`
  (5.4–6.6 שנ'), שניהם מריצים את הבודקים האמיתיים על עותקים מוטבים.
  ⚠️ **ארבעת השמות הגנריים בדקו תוכן חי:** `test_round22` (632 מ״ש) ·
  `test_round23` (366) · `test_round24` (625) · `test_round26`
  (170–296) — כולם שומרים על כניסה אופליין, על סוד שאינו נוגע בדיסק ועל
  הרשאות לפי תפקיד. ⛔ ההכרעה על שינוי שם או איחוד — של המנהל.

### סריקת האזור שנגעו בו (כלל ברזל 17)
1. **באג שקט.** ⭐ **זה נושא הסבב עצמו** — מסלול ריקון שמצליח ואינו
   מוריד את הסימון הוא כשל שקט מושלם: הנתון בענן, המשתמש רואה אזהרה,
   והמנגנון שאמור לתקן מייצר את הרעש בעצמו. ⚠️ **וממצא שני, קטן יותר:**
   אותו מסלול ביומן גם לא עדכן את `_tbPushedAt`, כלומר עֵד הפינוי פיגר
   אחרי דחיפה שהצליחה. ⛔ הכיוון בטוח (פינוי שמדלג), אבל האסימטריה מול
   `saveEntries` הייתה שקטה — ונסגרה באותו קומיט.
2. **מה היה אמור להיות משותף.** ⭐ נעילת חוסר-הפעילות — ר' למעלה.
   ⚠️ ובנוסף: מנגנון הנעילה נבנה בליבה עם חלון האזהרה שלו, ⛔ ולא נשען
   על סימון HTML פר-אפליקציה כפי שהיה בהנהלה — שלוש גרסאות של אותו
   רכיב הן בדיוק הסחיפה שכלל ברזל 7 נבנה נגדה.
3. **התיעוד באזור.** ⚠️ **מקום אחד תיאר מציאות שלא הייתה**: ההנחה
   שקיימת «תפוגת סשן» של 30 מול 60 דקות. ⛔ היא נבדקה מול הקוד ונמצאה
   לא-קיימת, והרישום המדויק נכנס לשורה 35 ולפרק המשותף.
   ⚠️ **ושורת פער אחת לא נסגרה** — נבדק בארבעתן; נוספה שורה חדשה
   בשכר ובגיוס.

### מה נעשה כאן (schar-limud)
- ⭐ **כאן היה ההפרש שאיש לא החליט עליו:** `SL_LOCK_MS = 5*60*1000`
  ו-`slResetLock()` היו **אותו ערך בדיוק** כמו בהנהלה — ⛔ בלי חלון
  האזהרה, ועם טוסט «🔒 ננעל עקב חוסר פעילות» במקומו. שניהם הוסרו לטובת
  הליבה המשותפת; ⛔ הטוסט לא הוחזר כידית ב-`LK_CFG` (סבב 52) — הודעה
  פר-אפליקציה היא בדיוק ההפרש שהסבב סגר.
- `lkBoot()` מ-`slBoot()`, `lkReset()` מ-`enterApp()`, `lkStop()`
  מ-`doLogout()`.
- ⛔ **מונה ה-⏳ כאן היה תקין** — `slPushTable` מורידה סימון פר-שורה,
  ו-`pendSyncScan` סורקת אחרי משיכה מלאה שהצליחה.
- ⚠️ **שורה 35 היא ✅ כאן, ובלי תפוגה** — `sl_session` מוחזר בעלייה עד
  יציאה או תשובה סמכותית. הפער נרשם עם טריגר.
- `CACHE_NAME` v41→v42, ותא `sweep-scope` שברתמת קו-הבסיס.
- **גיזום:** פרק סבב 46ב נמחק (הלקחים נשלפו ל-`tools/_prune-lessons.md`),
  ⚠️ **ובנוסף נגזמו שמונה שורות פרטיות** — סיפור התקלה שבפרק `client_id`
  קופל לארבע שורות שמחזיקות את הכשל שנמדד, ⛔ בלי צילום המצב המלא
  (כלל ברזל 18). ⚠️ החלק הפרטי-הקבוע כאן היה **898/900**, ולכן שורת
  הפער החדשה חייבה גיזום **באותו קומיט**.

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- `test_round52_lock.mjs` — 26 טענות, כולל טענה 8 («אין שריד של מנגנון
  נעילה פרטי») שהייתה נכשלת לפני הסרת `SL_LOCK_MS`.
- `test_round52_pendflush.mjs` — טענה 3 עוברת דרך `slPushTable`.
- `test_round42_sw.mjs` — `defectCount` 0 (תא `sweep-scope` ל-v42).
- 22 בלוקי ה-SHARED הושוו ב-sha256 בין ארבעת הריפו — **זהים בית-לבית**.
- ⛔ לא הורצה שום מיגרציה.

## סבב 53 (2026-08-26) — תפוגת סשן וסגירת ממצאי הסריקות

⚠️ **סבב אבטחה ואחידות.** ⛔ לא הורצה שום מיגרציה ולא נדרשה אחת,
⛔ ו-`versionCode` לא קודם. ⚠️ `CACHE_NAME` קודם בשלוש שהקוד הרץ שלהן
השתנה (הנהלה · שכר · גיוס); ⛔ ביומן לא — `index.html` ו-`sw.js` לא נגעו.

### מה נעשה
- **⭐⭐ המשתמש המחובר חי מעכשיו בזיכרון בלבד, בהכרעת המנהל.** הפרק המלא —
  «מודל הסשן — המדידה וההכרעה» — הוא בלוק `SHARED` חדש (`sess-module`),
  והליבה (76 שורות, `1977a56060bd1796`) זהה בית-לבית בשלוש שיש בהן כניסה.
  ⚠️ **וזו הכרעה שמשנה התנהגות ביודעין:** אחרי הפריסה כל משתמש נדרש
  להתחבר מחדש, וגם רענון דף מחזיר למסך הכניסה. ⛔ הכניסה האופליין
  **נבדקה לפני ההסרה ולא הונחה** — היא מוכרעת מול `pass_fp` שבמראה.
  ⛔ ו-`sessBoot()` מנקה את השריד: `sl_session` ו-`gius.session` נשארו על
  הדיסק של כל מכשיר שכבר נכנסו בו, עם `id`, `username` ו-`role`.
- **⭐ שורה 35 במטריצה התהפכה** — מ«סשן נשמר במכשיר» (סבב 52) ל«מודל
  הסשן — בזיכרון בלבד». ⛔ ושורת הפער של סבב 52 נמחקה: הטריגר התממש.
- **⭐⭐ שלוש האסימטריות משכבת הכניסה נמדדו והוכרעו** (סעיף 5 של סבב 52):
  - **ויסות משיכת מראת המשתמשים** — `SL_USERS_PULL_MS` היה **בשכר בלבד**,
    ⚠️ ונולד כשה-`syncAll` רץ כל שלוש שניות **בלי תנאי**; מסבב 51 הוא רץ
    רק כשהחותמת התקדמה. ⛔ כלומר פיצוי ששרד את המבנה שהוליד אותו (כלל
    ברזל 12, הממד הרביעי) — הוסר, ⛔ ושומר הריצה הכפולה נשאר.
  - **בדיקת עדכון תקופתית ל-service worker** — הייתה בשלוש **ונעדרה
    ב-gius**, ולכן לשונית פתוחה שם לא למדה לעולם שיצאה גרסה חדשה. ⭐ הוחלה,
    ⛔ בלי לגעת בהכרעה שאין שם `skipWaiting`. שורה **36**.
  - **`startSetupPoll`** — היה **בשכר בלבד**; ⚠️ ל-yoman ול-gius אין מסך
    התקנה כלל («אין כאן שאלה כזו»). ⭐ ההמתנה האוטומטית הוחלה בהנהלה,
    ⚠️ והכפתור הידני נשאר. שורה **37**.
- **⭐ חמש שורות תשתית נכנסו למטריצה** (סעיף 6 של סבב 52) — יכולות שהיו
  בקוד **ושום שער לא חיפש אותן**: רענון מונה הממתינים (38), פסק הזמן
  לקריאות רשת (39) ומאזיני מצב הרשת (40) — ✅ בארבעתן, ⛔ וה-probe נועל
  את **הערך** ולא רק את הקיום. לצידן שתי שורות שקיימות **ביומן בלבד**:
  גריעת tombstones לפי גיל (41) והאוטו-אפדייט מ-raw (42) — ❌ בשלוש
  האחרות, ⛔ ואין ליישר; הנימוקים ב-`tools/_capability-notes.md`.
- **`tools/test_round53_session.mjs`** — בדיקה חדשה, **זהה בית-לבית
  בארבעתן** פרט לבלוק `APP`. היא מריצה את הליבה **האמיתית** ברתמת `vm`
  עם localStorage מזויף (תשעה תרחישים), ואוכפת שתי טענות סטטיות: ⛔ אין
  קבוע `SESSION_KEY`, ⛔ ואין כתיבה של המשתמש המחובר ל-localStorage.
  **חמש מוטציות** ומוטציית-נגד; ⚠️ וביומן היא אוכפת את ההיפך.
- **⭐ קובצי הבדיקה המשותפים יושרו** (סעיף 7 של סבב 52). ⚠️ **והמדידה
  כאן שונה מהדיווח:** 35 קבצים משותפים, ⛔ ושניים בלבד נבדלו — בשניהם
  (`test_round35_hotwin` · `test_round35c_cron`) הסיבה היא ש**חסר בהם
  סמן `סוף APP`**, כלומר לאזור הפר-אפליקציתי לא היה גבול מוצהר כלל.
  הסמן נוסף, ⭐ ומאז **35/35 זהים בית-לבית מחוץ ל-`APP`**. ⭐ וארבעת
  הגנריים קיבלו שם נושא, בדפוס `test_round<N>_<נושא>.mjs`.
- **⛔ מדידה בלבד — זמן השער** (סעיף 7ג): 41ש׳ בהנהלה, 36 בגיוס,
  ⚠️ **ו-75% משני קבצים**: `test_round33_structure` (17–20ש׳) מריץ את
  **check-js השלם** בעותק כבקרה חיובית — כלומר החבילה רצה **פעמיים**;
  ו-`test_round37_matrix` (10ש׳) משכפל את הריפו כולו לכל שורת מטריצה,
  ועכשיו יש 40. ⚠️ שתי הדרכים לצמצם — שכפול חלקי, ובקרה שאינה מריצה את
  השער כולו — ⛔ משנות שער, ⛔ ולכן סבב נפרד בהחלטת המנהל.

### סריקת האזור שנגעו בו (כלל ברזל 17)
1. **באג שקט.** ⭐ **נמצא אחד, ונסגר באותו קומיט:** מסך ההתקנה של schar
   הציג הוראת `INSERT` עם **סיסמת «דוגמה» גנרית בת שש ספרות** — ערך
   שמישהו עלול להקליד, ובדיוק מה שכלל ברזל 10 סעיף 8 אוסר. ⚠️ הוא שרד
   מפני שקובץ ההתקנה עצמו כבר תוקן למציין מקום והמסך לא — **שני מקורות
   לאותה הוראה**. ⚠️ ומה שנבדק ולא נמצא בו פגם: אין מפתחות סימון חדשים,
   אין מזהי מיזוג, ואין מסלול שרץ פעם בשנה.
2. **מה היה אמור להיות משותף.** ⭐ זה **נושא הסבב עצמו** — שלושה מודלים
   לאותה שאלה («האם המשתמש המחובר יורד לדיסק»), ⛔ בלי שאיש החליט על
   ההבדל. הליבה, החתימה, שורת המטריצה והשער נבנו יחד בשלושתן.
3. **התיעוד באזור.** ⚠️ **שלושה מקומות תיארו מציאות שהסבב ביטל** — פרק
   «כניסה אופליין — `sl_session`» בשכר, ושתי הפניות ל-`revalidateSession`
   בגיוס; ⛔ כולם עודכנו באותו קומיט. ⭐ **ושורת פער אחת נסגרה ונמחקה** —
   «אין תפוגת סשן מוחלטת, וההכרעה למנהל» (סבב 52), בשכר ובגיוס.
   ⚠️ **ומדידה תפעולית:** ההפרש «שורות הקובץ פחות החלק הפרטי» חייב
   להישאר מתחת ל-2,096 — תוכן משותף חדש הוא תקציב שמשלמים בארבעתן יחד.

### מה נעשה כאן (schar-limud)
- ⭐⭐ **כאן היה עיקר השינוי:** `sl_session` נכתב ב-localStorage ושוחזר
  בעלייה **בלי תפוגה**. הוסרו `SL_SESSION_KEY`, `slSaveSession`,
  `slReadSession`, `slClearSession` ו-`slRevalidateSession`, ⛔ ומסלול
  השחזור ירד מ-`slBoot()`; `slResolveUser` **נשארה** (מיזוג מול המראה).
  **`CUR_USER` הוסר** ו-15 אתרי הקריאה עברו ל-`sessGet()`/`sessSet()`,
  ו-`slWhoName()` נוספה לשדות `created_by`/`deleted_by`.
- **`SL_USERS_PULL_MS` הוסר**; ⛔ שומר הריצה `_slUsersPulling` נשאר.
  ⭐ סיסמת ה«דוגמה» במסך ההתקנה הוחלפה במציין מקום, ⭐ ופרק «חתימה» נגזם.
- `CACHE_NAME` v42→v43 ותא `sweep-scope`. **גיזום:** פרק סבב 51 נמחק.

### אימותים
- `node tools/check-js.mjs` עבר מלא; `test_round53_session.mjs` — 22
  טענות; `test_round24_offline_login.mjs` — אין סשן בדיסק.
- `test_round37_matrix.mjs` — כל 40 השורות שאינן מוחרגות נבדקו במוטציה;
  23 בלוקי ה-SHARED הושוו ב-sha256 — **זהים בית-לבית**. ⛔ אין מיגרציה.
