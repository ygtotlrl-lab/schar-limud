-- ============================================================
-- שכר לימוד — הקובץ הזה אינו מכיל סכימה יותר.
-- ============================================================
--
-- מקור האמת היחיד לסכימה הוא:
--
--     migrations/000_initial_schema.sql
--
-- הקובץ הזה החזיק עותק שני של הסכימה, והוא הפך מיושן: חסרו בו
-- deleted / deleted_at / deleted_by ו-created_by, כך שהתקנה טרייה
-- לפיו לא יכלה לשמור או למחוק תשלום. עותק שאי אפשר לסמוך עליו גרוע
-- מהיעדר עותק — לכן הוא הוסר במקום להתעדכן.
--
-- ------------------------------------------------------------
-- התקנה חדשה:
--   הרץ את migrations/000_initial_schema.sql ב-SQL Editor.
--   https://supabase.com/dashboard/project/kxbtskqobynewvnckaaz/sql/new
--   (זה כל מה שצריך — 000 כולל כבר את 001 ו-002.)
--
-- התקנה קיימת שנוצרה לפני יולי 2026:
--   הרץ את 000 (אידמפוטנטי, לא יפגע בנתונים קיימים), או לחלופין רק את
--   migrations/001_sl_transactions_soft_delete.sql
--   migrations/002_sl_students_soft_delete.sql
--
-- מתוך האפליקציה:
--   מסך "הגדרה ראשונית" מושך את migrations/000_initial_schema.sql
--   ומציג אותו להעתקה — אין צורך לפתוח קבצים ידנית.
-- ------------------------------------------------------------
